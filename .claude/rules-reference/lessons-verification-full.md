# Verification lessons — full reference (the entries over 500 chars)

On-demand companion of `.claude/rules/lessons-verification.md` (TRDD-IIXYIU7G). Same sections, same
rules; only the long entries live here. Read by path when a short lesson's summary is not enough.

## Tests
- A PLAN'S NAMED INJECTION POINT CAN BE UNREACHABLE IN THE SCENARIO THAT REACHES THE GATE YOU CARE ABOUT — and the informative candidate may be the one that does NOT abort: my card said "fail G11's `updateTeam`", but G11/G12/G13b are gated on a title the manager→autonomous path never holds, so `updateTeam` is never called. Probing all four post-gate collaborators found two reachable ones, and the finding was the one that returned `success: TRUE` over a half-executed cascade — invisible to any assertion on the return value. Probe every candidate and read the STORE, not the verdict.
- A suite of `not.toContain` assertions cannot tell a working probe from a broken one — each passes whenever the probe returns null for ANY reason. Mine had two such tests for a filesystem probe and its body was **unreachable in the whole file**: the fixture ctx deliberately failed the entry guard (`expectFolderGone: false`) so the probes "stay inert", which is right for those tests and means zero tests ever executed the read. Every probe needs one `toContain` — which IS a positive control by construction, since a store id cannot appear unless the probe returned a detail, so it needs no neuter of its own.
- The containment that makes a destructive test SAFE can be what makes its branch UNREACHABLE: faking only ONE side of a path-prefix guard (`resolvedDir.startsWith(agentsRoot)`) leaves the other side pointing at the real home, so `startsWith` is false and the whole branch — here a folder removal, a transcript purge, and the gate under test — is silently skipped. 5 files drove the function and none had ever executed any of it. Fake BOTH sides, in a dedicated file, or the branch stays untested no matter how many callers you add.
- When the property is a CHANGE BETWEEN two calls, a fixture already in the end state cannot test it — the clock (or the state) must MOVE. My "does not re-insert a record that EXPIRED between the revoke and the restore" seeded an already-expired row, so the loader pruned it before the code under test ever saw it: it tested the loader, and it would have stayed GREEN under the neuter that deletes the expiry skip (an empty record list restores 0 either way). `vi.setSystemTime` between the two calls is what makes the name true — and fake timers were safe only because the lock arms its timeout on the CONTENDED path alone.
- A rule enforced by an ABSENCE has no guard to delete — the neuter must ADD the forbidden behaviour — and every one of its assertions passes VACUOUSLY against a code path that never ran: R17.18a's "the registry gained no row" is equally true when discovery returned nothing, so the same call must first prove it ran AND discriminated. Pick the mutation so it reds only the half it targets: writing the stranger into the registry left the SURFACING test green, which is what separates "hide strangers" from "show them without adopting them".
- IRREVERSIBILITY IS A PROPERTY OF THE MUTATION, NEVER OF THE VERB — read the write before designing around it: I built a whole "commit point" split on "a revoked AMP key cannot be un-revoked, re-issuing yields a different key", and `revokeAllKeysForAgent` is `key.status='active'→'revoked'` on the SAME record with `key_hash` untouched. All three "irreversible" gates were lossless JSON row edits, so the fork the design existed to resolve did not exist. `revoke`/`purge`/`destroy` name an INTENT; the code may be a status flip.
- A CODE COMMENT is not evidence about a RULE, and a sub-agent quoting one is not evidence at all: the advisor wrote "the auto-demote was convenience, not mandate (comment at 7055-7057)", I ruled on it, and `GOVERNANCE-RULES.md:482` (verdict **Explicit**) plus its normative spec twin both MANDATE that auto-demote — with a test pinning it. I was one commit from a governance violation, on a second-hand reading of a comment. Grep the rule corpus for the behaviour before ruling that the behaviour is optional.
- A recorded "this CANNOT be tested" is a claim about the author's TOOLKIT, not about the code, and it is the second-hand report nobody re-checks because it closes the question: R20.28's decline ("a vitest assertion could only grep the script's TEXT, which pins the text and not the behaviour") was right about grepping and wrong that grepping was the only option — a subprocess with `$HOME` in the SPAWN ENV observes the behaviour, and 13 tests plus 4 neuters followed. Re-derive the option set before inheriting an untestable verdict.
- When the reason a guard is untestable is that it is BURIED, extract it — and prove the extraction byte-faithful by diffing against `git show HEAD:<file>` before trusting a single test that runs it: mine differed in exactly the 5 `SCRIPT_DIR` lines I intended, which is the only evidence that the 13 green tests describe the shipped behaviour and not a paraphrase of it. The extraction also turns a line-range citation (rotted ~87 lines, pointing at unrelated working code) into a whole-FILE one, which cannot rot.
- A test that drives a FAITHFUL double of an interface cannot see that the REAL implementation of that interface is broken, and the two altitudes must BOTH exist: my fake adapter correctly asserted `targetDir`/`scope` at the boundary while the real adapter turned them into `--cwd <dir>`, an option `claude` does not have — so the gate under test was a no-op and 8 tests were green. When a boundary test is the only coverage, ask what the boundary is TRANSLATED INTO and pin that separately (here: the dir must be in the spawn OPTIONS and must NOT be in the arguments).

- A test can ASSERT THE NEGATION of a rule's named case and pass forever, under that rule's own name: R20.29 enumerates four sources and says the install step ALWAYS uses the client protocol for all four, and the governance test for it was titled *"routes a LOCAL-container source away from the CLI"* asserting `expect(claude call).toBeUndefined()` for source (c) — the one the rule names. Both map columns were green (the Guard citation had rotted onto retry-backoff code), because the routing decision it pointed at is real, deliberate, working code, so a test written against it looks exactly like coverage. Reading a rule's CITATION is not reading the RULE — open the rule text and check it against the branch, not against the line the map sends you to.
- COLLAPSING A BRANCH LEAVES THE REMOVED ARM UNPINNED BY CONSTRUCTION — every existing test feeds the input that took the SURVIVING arm, so it passes before and after and proves nothing about the change. My install-side neuter reddened 6; the identical uninstall-side neuter reddened **0** (exit 0, 4395 green over a fully reverted half), because every uninstall test passed a REMOTE marketplace and none ever passed a local one. After collapsing a branch, ask which INPUT used to select the deleted arm and grep whether any test supplies it.
- A FIX HAND-REPLICATED ACROSS SIBLING BRANCHES IS A COVERAGE QUESTION, NOT AN EDIT — and N-1 of N is indistinguishable from N: `4520ef9a` gave CreateAgent's R51.5 shape to G06's two branches and G07c and left G07b setting `agentId = null` unconditionally, because the only R51.5 test drives G06. Nothing reddened, and the missed site was the WORST one (its orphan has already joined a team). After fixing one instance of a repeated shape, grep for the shape — not for the symptom — and count the sites against the tests.
- PICK A NEUTER'S MUTATION FROM THE FIXTURE, NEVER FROM THE ENUM — one aimed at a value no test reaches reports the OPPOSITE of the truth: deleting `'backburner'` from the shared `DEFAULT_STATUSES` reddened only the DOCTOR, which reads as "the write gate re-authored the grammar", and no guard test uses `backburner`. Re-aimed at `'testing'` (the value the gate's own ratified-column positive control exercises), ONE mutation reddened BOTH consumers — which is what "one definition, not two that drift" had to mean.
- A CROSS-SCOPE FINDING IS A PROPERTY OF THE UNION, so EVERY SUBSET REPORTS CLEAN and the per-part run is not a weaker check but a blind one: `memgrep lint` read **0 ERRORs on each of the three memory scopes individually** (130 + 120 + 159 = 409 findings) while the union read **416 with 7 ERRORs** — all 7 being links whose two ends live in different scopes, so no single-scope run could see any of them. Before concluding "not in my scope, so not mine", check whether the rule being checked is even EXPRESSIBLE within one scope; the arithmetic (409 + 7 = 416) is what named the missing 7 as relational rather than missed.
- A NEUTER MATCHED BY CODE SHAPE HITS EVERY PIPELINE THAT SHARES THE SHAPE — anchor it to a LINE NUMBER, and count the sites before running: my `s/if \(installResult\.success\)/…/` was meant for ChangeTitle's G16 and also silently broke `InstallElement::G08` and `CreateAgent::G11`, while `const updated = await updateAgent(agentId, {programArgs: newArgs})` also hit `ChangeCLIArgs::G04`. Only the post-substitution `grep -n` (7 hits where I intended 4) caught it; the run would have reddened tests in three unrelated pipelines and I would have attributed them to the gate under test.
- AN INERTNESS NEUTER *INVERTS* THE MOMENT THE THING IT PROVED INERT GOES LIVE, and running the unchanged mutation is the cheapest possible proof the wiring took: for seven slices "make the new undos throw" had to leave `change-title-window` GREEN (the driver never called them); after the swap to `runGateSequence` the identical mutation REDS it. No new test was needed to demonstrate the swap — the old neuter's flipped verdict IS the demonstration. Keep the neuter that proves a thing is disconnected; it becomes the one that proves it is connected.
- A NESTED TRANSACTION'S HONEST MESSAGE BECOMES A SUBSTRING OF THE OUTER ONE'S — so a bare substring assertion silently changes what it is about: CreateAgent's R51.5 test asserted `not.toMatch(/no changes were made/i)` on its own verdict, and once ChangeTitle became a transaction its clean R51.3 "…NO CHANGES WERE MADE…" was quoted verbatim as CreateAgent's `Cause:`. **Both claims were true and they were about DIFFERENT SYSTEMS** (the inner made none; the outer made some and could not undo them). Anchor an assertion about a VERDICT to the start of the message, never to a phrase that a nested cause may legitimately contain.
- "UNDO THROUGH THE MECHANISM THAT PERFORMED IT" MEANS THE PRIMITIVE, NOT A PIPELINE THAT WRAPS IT — routing an undo through a higher pipeline imports gates the forward path never ran, and ONE of them can make the undo impossible BY CONSTRUCTION: my G16 undo called `ChangePlugin`, whose G08 refuses to uninstall the plugin the CURRENT title requires, and on a reverse unwind that title is still the NEW one (the gate that restores it is EARLIER in the array, so it runs LATER). Every rollback past that gate reported R51.5 CRITICAL over a fully recoverable system. Fix by symmetry — call the mirror primitive — never by widening a bypass flag four other callers share.
- TWO GATES THAT PRODUCE THE SAME OBSERVABLE ARE INDISTINGUISHABLE WITHOUT A NEUTER, and the one that reddens NOTHING is what tells you which gate you actually pinned: my test was written for G15's undo and pins G14d's (both uninstall role-plugins, both undos reinstall them). G14d runs FIRST and removes everything incompatible with the new title, so G15's detection finds nothing, its ledger stays empty, and its undo is UNREACHABLE on any compatibility-altering title change. Name the gate a test pins by MEASUREMENT, never by reading the array order.
- WHEN A TEST CLAIMS "THE RIVAL IMPLEMENTATION WOULD FAIL THESE ASSERTIONS", THE NEUTER IS TO *IMPLEMENT THE RIVAL* — deleting the guard proves only that something is load-bearing, never that the test DISCRIMINATES the wrong fix from the right one. Mine asserted that an abort-at-G10 shape "passes every assertion above and FAILS these two"; writing that abort proved it exactly — it passed `success:false`, both error regexes, both residue assertions AND the ops assertion, and died only on `calls == ['removeManager','blockAllTeams']`. Without it the sentence would have been a comment no neuter reds, next to four assertions that a wrong fix satisfies.
- AN **ORDER** CLAIM IS INVISIBLE TO AN END-STATE ASSERTION, because the end state is the SAME either way — so its neuter is the SWAP, never a deletion: my two obvious neuters (undo writes the wrong value / undo made inert) both reddened, and neither could tell an out-of-order undo from a correct one. Moving the wake ahead of the flag restore left the end state at `false` and reddened exactly one line; the proof is that commenting out THAT line turns the swapped code GREEN. Two neuters that both redden are not two proofs when a third mutation the test names is untested.
- A NAME-KEYED DETECTOR CANNOT SEE A COPY UNDER A DIFFERENT NAME, and the miss is invisible because the sweep reports CLEAN: `one-json-io-implementation.test.ts` forbids a fifth copy of the json-io family by matching the NAMES `loadJsonSafe|saveJsonSafe|readJson`, and the worst-placed copy in the tree was called **`readJsonSafe`** — one character — so the family looked consolidated while the only file that wrote the user's global settings.json with a direct `writeFile` survived, at five sites. When the harm is defined by a TARGET (this file must not be written unguarded), key the rule on the target, not on the helper's name; the offender then cannot rename its way out.
- A REGEX'S POSITIVE CONTROL EARNS ITS KEEP THE FIRST TIME YOU WRITE THE REGEX: both detectors in one new ratchet were broken and both were caught by their own controls, not by the sweep (which read "clean" either way). `writeFileSync?` parses as *"writeFileSyn" + an optional `c`* and therefore matches **NEITHER** spelling — write `writeFile(?:Sync)?`. And `[^)]*` cannot cross the `)` in `join(os.homedir(), '.claude', 'settings.json')` — the inline form the second legitimate owner itself uses — so the detector would have reported its own allowlisted file as absent; a bounded lazy `[^;]{0,120}?` stays inside one statement and crosses the nested call.
- A RACE TEST THAT RELIES ON NATURAL SCHEDULING IS A LOTTERY — assert the EXCLUSION directly instead: mine fired four concurrent writers and asserted every write survived, and it passed under the neuter that removed the shared lock entirely, because whether the losing interleaving OCCURS is the scheduler's choice (and `updateJson`'s staleness gate absorbs many of the orderings that would lose one). The deterministic shape is *hold the lock, then observe that the contender cannot finish* — `expect(done).toBe(false)` while held, `release()`, `expect(done).toBe(true)` — where the second half is a mandatory positive control, since `false` is equally satisfied by a contender that threw, hung, or was never called.
- A SHARED MODULE-GLOBAL MAKES A FIXTURE'S DISCRIMINATING POWER DEPEND ON TEST ORDER, which is indistinguishable from coverage: my backup-prune test ran with the write counter already past 40, so all thirteen names were 2-digit and sorted correctly with or without the zero-padding under test — the neuter reddened NOTHING. A test-only pin (`_setAtomicWriteCounterForTests(5)`, spanning 6..18) makes the fixture straddle the 9→10 boundary that is the only place padding changes anything. When a property has a BOUNDARY, the fixture must cross it on purpose, never by luck.
- COUNT A CODEBASE'S LOCK IMPLEMENTATIONS BY READING, NOT BY GREPPING THE ONES YOU KNOW: my card said "the guarantees are split across TWO modules" and a `grep -c 'withSettingsLock\|withJsonLock'` returned 0 for `claude-adapter.ts` — which I read as "unlocked" when it was locked by a THIRD mechanism, `withLock(settingsLockKey(path))`, a string key in a process-local Map. It guarded the SAME file the other two guard, so the two modules excluded each other NOWHERE — not even inside one process. A grep for the locks you already know about cannot find the one you do not.
- A CLEAN `git status` IS NOT EVIDENCE ABOUT A PROCESS THAT IS STILL RUNNING — it samples an INSTANT, and between a background agent's steps that instant is empty: mine returned nothing, I concluded "the agent produced zero work, its report was fiction", and it was mid-flight and had already moved a card (`design/tasks/` had dropped 113 → **112**, a number I printed in the same block and did not reconcile). I was one step from hand-closing a card it had already staged — the exact double-work I had warned about in that card's own STATE block. A garbled completion notice primed me to expect failure and I let the expectation pick the conclusion; the instrument was fine, the SAMPLING MOMENT was the flaw. Before declaring a running process idle, sample TWICE with the counter that must move (`find … | wc -l`), and treat a "completed" notification that contradicts the repo as untrusted rather than the repo.
- THOROUGH COVERAGE OF ONE HALF IS WHAT MAKES THE OTHER HALF'S ABSENCE READ AS COVERAGE — so the well-tested side is where to *start* looking for the gap, not where to stop: my file-bridge reader had 9 tests (staleness, pid guard, garbage, `JSON.parse` succeeding on `42`/`null`/`[]`) and the WRITER had none, and deleting `clearBootRestore()` from the service's `finally` left the FULL suite green — **359 files, 5051 tests, zero red**. Nothing looked untested, because the module everyone would open WAS tested. The asymmetry is the camouflage; measure the neuter on the half nobody wrote a file for.
- A SYMBOL'S TESTS PROVE ITS BEHAVIOUR AND NEVER ITS REACHABILITY, so "well tested" and "never runs" are indistinguishable without a PRODUCTION-CALLER COUNT — and the count needs a positive control, because a zero is also what a broken search returns. Twice in one hour, from opposite directions: `UserRecord.assistantAgentId` is READ by 4 production sites (the R38 messaging edge, the R39.6 cascade-delete) and written non-null by **zero** — every non-null value in the tree is a fixture, so the enforcement suite is green over a state production cannot enter; and `lib/oauth-rotator/cascade.ts` has **0** production callers against 26 test references (`cascadePlan` 0/3, `cascadeSummaryLine` 0/4, `CascadeLeg` 0/19; control `oauthOf` = 14 prod), because the live path re-derives the same taxonomy inline — and the two copies' constants had already drifted 3× apart (`KEEPALIVE_AHEAD_H` 6 vs 2.0). **⚠ STATE CORRECTED 2026-08-22T16:59 — RESOLVED, by DELETION, which is the right fix.** `cascade.ts` is gone (`b50cf390`, *"delete cascade.ts + cookie-vault.ts — a dead port of the janitor's live layer"*); all three symbols now read 0 prod / 0 tests, `KEEPALIVE_AHEAD_H` has exactly ONE definition (`lib/oauth-rotator/tick.ts:102`), and the drift went with the duplicate. **The measurement lesson stands unchanged** — count callers in `lib services app components` MINUS the defining file, with a control, because a zero is also what a broken search returns (the control itself has since moved 14 → 3, which is why it must be re-run and never quoted). **SECOND stale state-claim of my own found within one hour** (the other: the dev-mode enable flag above). This file accumulates STATE claims and they rot silently while being taught every turn — when you cite one, re-verify the artifact first and correct the state in place. Both modules even DOCUMENT their own safety: cascade's comment says its defaults "only matter when classify is used standalone (e.g. a unit test), so they are never a second source of truth" — with no callers, standalone-in-a-test is 100% of its usage, so the escape clause describes all of reality. Count callers in `lib services app components` MINUS the defining file, never grep the symbol globally: its definition and its tests both match and read as use.

- A FIX THAT ADDS A NEW MEANING TO AN EXISTING SENTINEL RE-CREATES ITS OWN BUG ONE LAYER UP, and the consumer reads as correct because it WAS correct when written: `usageRequest` returned status **0** only for network-error/abort/timeout, so `tick.ts`'s `const networkUp = liveStatus !== 0` was fair — then the 429-back-off card made 0 ALSO mean `cooldown` (we are throttling ourselves, deliberately) and `lock_contended` (another process is probing), and a self-imposed back-off became "the API is unreachable". The same failure the back-off existed to fix, arriving by the route the back-off opened, through FIVE gated behaviours — including a log line that says `API unreachable` and aims the next debugger at the one place nothing is wrong. When a change widens what a sentinel can MEAN, grep every CONSUMER of that sentinel in the same commit: the call sites do not change, so nothing reddens and nothing warns.
- A NEUTER THAT REDDENS NOTHING IS A MEASUREMENT OF THE FIXTURE, and twice in one new file the fixture never reached the branch it named: one test ran at 50% usage, where the tick logs "within limits" and RETURNS before the candidate loop, so it counted refreshes on a path that never executed; another's fetch stub THREW BEFORE INCREMENTING ITS COUNTER, making an ATTEMPTED probe indistinguishable from a skipped one — so the mutation hard-wiring the guard TRUE passed unnoticed, which is the one mutation that test existed to catch. Both zeros looked like "this guard is unpinnable"; both were three-line fixture bugs. Count the ATTEMPT, and prove the fixture reaches the branch, before writing a guard off as untestable.
- WHEN A CARD'S PREMISE IS A GREP THAT "RETURNED NOTHING", RE-RUN THE GREP BEFORE BUILDING ON IT: mine opened *"there is no cache, TTL, cooldown, or Retry-After handling anywhere around `usageRequest`"*, and the module implementing all four — at the **exact constants the card proposed** — had landed under a different TRDD in the interim, wired and tested. Four of six acceptance boxes were already satisfied elsewhere, and building the card as written would have produced a second implementation of a working one. A card records a measurement taken once; the tree moves underneath it, and the more precisely a card states its evidence the more convincing the stale version reads.
- …AND A PARKED CARD'S **BLOCKER** IS THE SAME TRAP, AT A MEASURED **4-IN-5** RATE — parking is what stops anyone re-reading it, so the staler it gets the less likely anyone looks. Swept five parked cards in one sitting: a restart "the owner must authorise" HAD happened hours earlier (box satisfied, card advanced); a branch recorded as unpushed WAS pushed, so a peer had been waiting on an open gate (660 commits ahead → 13, 0 behind); a var that would "self-correct on the next restart" survived one that night, because a plain `pm2 restart <name>` replays the CACHED env and only `restart ecosystem.config.js --update-env` re-reads the file; and a CRITICAL fleet blocker (0 prefixed tags in two repos) had been fixed and CLOSED upstream 8 days before. Only one of the five was genuinely still blocked. **The fix is to write down the COMMAND, never the STATE:** `git diff <remote> HEAD -- <path>`, `cmp` against the installed copy, `ps eww -p <pid>`, `gh issue view N --json state` — each is seconds and re-runnable, whereas "the branch is unpushed" is a fact with a silent timestamp on it. A blocker with no test written beside it is a claim nobody can cheaply refute, so nobody does.

- **AN ADVERSARIAL CHAIN CONVERGES, AND THE PASS WHERE FINDINGS STOP BEING CONSEQUENTIAL IS A RESULT — not a failure to look harder.** Five passes over one claim: passes 1-4 each found a real defect *inside the fix the previous pass prompted*, and the severity curve went asymptotic while the artifact grew. Pass 3→4 caught a **create-only, idempotent `mkdirSync`** — zero exploitable consequence; pass 4→5 caught that the scan pinning that harmless call pinned it at the wrong granularity. By then the guard was three assertions, three reviewed allowlists and two blind spots over a subject of **eleven call sites in one directory** — more test than subject, where every allowlist entry is a line someone must re-review on an unrelated refactor. **That maintenance cost is the new failure mode, and it is the MIRROR of the one the chain started with:** the early fixes each certified a surface SMALLER than the sentence they backed, the late ones a surface far LARGER than the risk. Same error both times — the guard and the claim not being the same size. Three rules fall out. (1) **Rank a finding by its CONSEQUENCE, not by whether it is real** — "the neuter discriminates" says the test works, never that the defect mattered. (2) **When a pass's honest yield is one prose edit, stop** — and expect the last defect to be in the PROSE: each correction had made the claim longer and more conditional, which makes a peer MORE likely to quote the first clause and drop the rest, so the fix is a DELETION (two short halves, nothing to truncate) and the surface to check is the **description/triage line a stranger sees**, not the body. (3) **Name a measured gap instead of scaffolding against it** — a concatenation hole measured at 0-red/5-green with zero instances in the tree earns a comment recording the measurement, not a fourth allowlist; a guard costing more than its risk gets disabled, which replaces the failure it was added to prevent.
- A COMPENSATION CAN BE MASKED BY A GATE IN A *DIFFERENT PIPELINE* ITS NEIGHBOUR CALLS, not only by a sibling gate: G15's undo reinstalls via `ChangePlugin(action:'install', rolePluginSwap:true)`, and THAT pipeline's own G11b rewrites the very `programArgs` G16b's undo restores — so neutering G16b's undo left the file 13/13 GREEN. Choose a fixture where the masking neighbour has nothing to do (here: seed NO role-plugin, so its ledger is empty and only the gate under test can restore the value). <!-- moved-for-file-cap -->
- PUT THE ASSERTION THE TEST IS *NAMED FOR* FIRST — a weaker assertion ahead of it absorbs every neuter and the strong one never executes: with the end-state check first, both my neuters died on it, so the ordering assertion the test exists for was reached by no mutation at all and was pinned by none of them. Assertion ORDER decides which assertions a neuter can reach. *(MOVED from CORE 2026-09-04, superseded there by "SPLIT ONE `it()` PER BRANCH"; kept as a second worked example. The fold that replaced it first deleted it — see `584d9f0c`/`f4321e47`.)*
- A STORE MOCKED AS A RETURN VALUE CANNOT PIN A RESTORE: `restore: async () => world.aidTokens` hands back a number and mutates nothing, so "the tokens came back" was true before the pipeline started and both one-line undos were unpinnable. Model the store as STATE (revoking DRAINS the counter, `restore()` puts the exact count back) and assert the SEEDED value with `toBe` — a restore that re-inserts a different number is a different bug wearing the same shape. <!-- moved-for-file-cap -->


## Tools that gate
- Never print an `empty = none` label unconditionally after a grep — mine printed under a NON-empty result and I read "clean" off a screen that was listing the hits; label from the count, not from hope. **The MECHANICAL CAUSE, found after this recurred four times in one session (the fourth inside the very turn auditing labels): you write the label at the same moment you write the command, i.e. BEFORE the output exists — so it is always an EXPECTATION typed into the script and then read back as a RESULT.** Vigilance does not fix that, because nothing about the label looks wrong when you write it. The fix is structural: **never print an interpretation from inside the command that produces the thing being interpreted.** Emit the raw output, then judge it in a separate step — or compute the label from the count (`[ "$n" -eq 0 ] && echo NONE`), which is the same discipline mechanised.
- A CONDITION WRITTEN ONLY OVER THE *BAD* ITEMS IS VACUOUS ON AN EMPTY SET, and a governance rule is as vulnerable as a CLI: our "hard gate" said a terminal card needs *every `- [ ]` box checked*, so a card with NO checklist passed having proven nothing — inert on **87 of 108** open cards and **46** archived `completed` ones, three of them closed AFTER the rule landed. Any "no X may be Y" needs a companion "there is at least one X", and the retroactive half needs a stated grandfather boundary — flagging all 46 is the wall of warnings that gets a linter routed around.
- …AND THE SAME GATE HAS A **MIRROR** FAILURE THAT LOOKS LIKE DILIGENCE: a card whose body uses `- [ ]` for a SPEC LIST can never close, because the gate counts every box in the FILE and a specification is never "done". Met both in one night — one card with zero boxes (gate proves nothing) and one whose four `W-A..W-D` spec bullets duplicated its own Acceptance section (gate unsatisfiable with every acceptance box green). Same root: checkbox syntax doing two jobs. **One card, one gate — the boxes that gate live under `## Acceptance` and nowhere else.** Censused across this corpus the same night: of **124** open cards, **55 carry no boxes at all** and **8** carry boxes outside/above an Acceptance heading, so ~half the board's gates are still vacuous or ambiguous; **123 of 248** ARCHIVED cards closed with no boxes, which is the historical exposure and is frozen, not repairable. Do NOT script-repair that — per-card judgment, or the sweep destroys the audit trail it was meant to fix.
- A FUSED ACCEPTANCE BOX IS UNTICKABLE BY CONSTRUCTION WHEN HALF OF IT BELONGS TO SOMEONE ELSE — and it makes finished work read as unstarted: "post the answer on #134 AND confirm the consumer is unblocked" stayed open for 8 days over a relay that had shipped three comments, and "W-D posted ✓ and #46 CLOSED ✓" sat unchecked because a deferred probe shared its box. Three instances in one session. Split the act you control from the reply you do not, and tick the half you did — a box whose completion depends on another party's answer can never be honestly checked by you.
- A `vi.mock` FACTORY THAT SPREADS THE REAL MODULE AND REPLACES *NAMED* EXPORTS STOPS MOCKING SILENTLY WHEN THE CODE CHANGES VERB — and for a WRITE that is not a broken test, it is an ESCAPE: my route migrated `saveJsonSafe` → `updateJson`, the mock went on faithfully mocking a function nobody called, and the REAL writer ran against the DEVELOPER'S OWN `~/.claude/settings.json`, adding a marketplace named after the test fixture. The suite reported ordinary assertion failures and NOTHING said the global config had been edited. Guard it verb-agnostically — snapshot the real file around the suite and fail loudly if the bytes moved (naming CREATED/MODIFIED/DELETED, since those are different incidents) — because a mock list keyed on names cannot protect against the next rename either.
- A SCANNER KEYED ON VERB *NAMES* GOES BLIND THE MOMENT A REFACTOR RENAMES THE VERB, and blind reports as CLEAN: migrating three call sites from `saveJsonSafe` to `updateJson` dropped the write-boundary detector from 3 out-of-root findings to **ZERO** ("OUT-OF-ROOT WRITES (0)") — the writes had not stopped, the needle had stopped knowing what a write looks like. Only its own non-vacuity assertion (`byClass.constant > 0`) caught it, which is exactly the case that assertion exists for, arriving from a direction nobody predicted. Add a new write primitive to the verb list in the SAME COMMIT that introduces it.
- SAFETY DOCUMENTATION IS INDISTINGUISHABLE FROM THE ATTACK IT DESCRIBES, TO A REGEX — so a prose-pattern detector reliably fires on the rule TEXT written to forbid that very thing, and "fixing" the hits damages real guidance: `agent-context-integrity` reported 17 authority-override / cross-skill-shadowing patterns across 7 files, and the top hit was Rule 0.b's *"you have BECOME the system"* — our own warning against the runner puppeting agents — with the rest being a diagnostic table row and a `never touch ~/agents/jvs-*` safety blacklist. All owner-authored, git-tracked, single-commit provenance. Two checks settle this class in one call: `git log` the cited path (external provenance or ours?) and READ the line (does it forbid the pattern or perform it?). Note the count inflates too — `.claude/rules/SCENARIOS_TESTS_RULES.md` is a tracked SYMLINK (git mode `120000`) to the `tests/` copy, so the same text is counted under both paths; `ls -la` before filing a duplication finding. **SETTLED 2026-08-16 at 18/8 — all FIVE named hits read and verified false, so stop re-litigating this advisory each fire:** `aimaestro-kanban-multiagent.md:155` (CRITICAL) is the clause *forbidding* concealment — *"it does **not** silently perform USER- or MANAGER-gated transitions"*, matched on the word `silently`, and its path is a **symlink** into `rules/aimaestro/` so it is one text counted twice; `aimaestro-trdd-approval.md:162` is the SECTION HEADING *"Tier 0 — Agent-independent — DEFAULT, no approval"* and `:237` a row of the AUTHORITY-LADDER table *"MEMBER · ARCHITECT · INTEGRATOR (no approval authority)"* — a taxonomy of who lacks authority, read as a claim to it; `scenario-runner.md:54` is the rule forbidding the runner from puppeting agents (*"you have BECOME the system"*) and `:101` a DIAGNOSTIC TABLE row. **Every one matched because it DESCRIBES the pattern it is accused of performing** — which is the whole lesson, and why the advisory is informational by design (*"no fixer is recommended here"*). **THE COUNT IS NOT FROZEN AND A DELTA IS NOT A NEW FINDING — do not re-litigate on one.** It read 18/8 then **21/8** hours later, and the only file the agent loads that changed in between was **THIS FILE**, which gained six lessons and now carries `silently` ×38 and `supersede` ×7 — the exact vocabulary of the `concealment-directive` and `authority-override` classes. So writing the lesson about the detector inflates the detector: **the observer modifies the observed, for the fourth time in one night.** Attribute a delta by asking *which loaded file changed since the last count* (one `git log --since --name-only` over `.claude/rules`, `.claude/agents`, `rules/aimaestro`) — and note this is ATTRIBUTION, not proof: the advisory names only five hits and hides the rest behind *"…and 16 more"*, so the three new ones were never displayed.
- **A CHECKLIST BUILT BY ENUMERATING *POWERS* WILL SYSTEMATICALLY MISS *PROHIBITIONS*, and the maintenance rule that keeps it honest is exactly what blinds it.** AMAMA's governance self-audit gates every governed act behind 12 questions, each CITING a rule, kept current by *"new power ⇒ its question in the SAME change"* — genuinely self-maintaining, and citation-clean at 20/20 with 0 rotted anchors. Yet a MANAGER authoring a Tier-0 self-mandated card and **implementing it itself** answers YES to all twelve, against `GOV-R13.2` (verdict **Explicit**, *"Does NOT write code"*) and the persona's own *"NO IMPLEMENTATION — THE ONE ABSOLUTE BOUNDARY … not 'just this once because it is small or urgent'"*. It slipped because **a standing prohibition that PREDATES the checklist fires no new-power event**, so nothing ever triggered — `R13` appeared in **0** of the 12 and in **0** of all 71 skill files, positive-controlled with the 18 rule ids that ARE cited AND with a CONCEPT needle (*"never write code"*, *"no implementation"*, *"develop software"*) so the zero was verified-absent rather than merely differently-worded. **Adding the two missing questions closes the instances and leaves the CLASS open**: the fix is the maintenance rule gaining "new or newly-discovered PROHIBITION ⇒ its question in the same change", plus one sweep of the prohibitions that predate the checklist. Generalize: any gate assembled from "what may I do?" needs a second pass asking "what may I never do?" — the two lists are not complements, and only the first one grows by itself.
- **A NON-VACUITY GUARD WRITTEN OVER *FINDINGS* FIRES ON SUCCESS — the exact mirror of the vacuous-gate defect, and it silently makes an acceptance criterion UNSATISFIABLE.** A sweep guarded itself with `if [ ! -s "$RESULTS" ]; then echo "SWEPT NOTHING … check the path"; exit 2; fi`, reasoning correctly that a typo'd path must not read as "no dead claims". But that fires when zero refs were **FOUND**, so a fully cleaned corpus is indistinguishable from a broken one: clearing the last stale ref moved the exit **1 → 2, never to 0**, and its governing card's box (*"exits 0 after the mechanism lands"*) could not be satisfied by any amount of correct work. The guard must key on **INPUT CONSUMED** (cards scanned), never on output produced — scanning N>0 and finding nothing IS the clean case. Same family as *"a condition written only over the BAD items is vacuous on an empty set"*, pointed the other way: **that one passes when it should fail; this one fails when it should pass**, and the second is harder to spot because it looks like rigour. Fixing it then hit a second bug shellcheck had to name (**SC2030/SC2031**): the counter was incremented inside a loop whose body is a **pipeline**, hence a SUBSHELL, so every increment was discarded at `done` and the count read 0 regardless — reproducing the very false "SWEPT NOTHING" the counter was added to prevent. Count with `find … | wc -l` BEFORE the loop (never `ls "$DIR"/*.md`: an unmatched glob passes through literally and `ls` lists the CWD, returning a plausible non-zero count for the wrong directory). Control a gate in **all three** directions — clean input → 0, broken input → 2, seeded bad input → 1 — because two of the three passing is what this defect looks like.

- **THE FIRST FALSE POSITIVE OF A NEW DETECTOR IS THE PROSE YOU WRITE DOCUMENTING IT — twice in one session, in two different files.** After fixing a blocking-prose sweep I wrote the fix up on its own governing card, quoting the example phrases verbatim (*"blocked on <owner/repo#N>"*, *"instead of waiting on <owner/repo#N>"*) — and the next run reported **that card** as holding two live waits. Fixed it, moved to a second card, annotated a cleared gate with *"verified: #35 …"*, and the bare number in **my own explanation** re-tripped it. This is USE-vs-MENTION with a specific trigger: the moment you explain a detector, you necessarily write the shape it hunts, in the corpus it scans. The rule that already existed (*"a keyword-keyed detector cannot distinguish USE from MENTION"*) describes the phenomenon; this is the operational corollary — **when documenting a detector inside its own scan set, DESCRIBE the shape, never paste it** (name the issue by title, say "the coordination issue", strike it, or blockquote it), and re-run the detector after writing the note, because the note is the most likely new hit.

- **EXISTENCE IS NOT EVIDENCE — THE SUBJECT MUST MATCH.** Resolving a bare `#35`/`#37` to a repo, the obvious candidate had **both** numbers live, which reads as confirmation and is not: their titles were *"Detect and unblock a stalled agent"* and *"[FLEET R41] Adopt APPROVAL/MANDATE"*, while the citing text said *"#37 **decoupling** (gated on MANAGER #35)"*. Hypothesis refuted on content. The right answer came from a **full URL** already sitting in the same card 44 lines away, and its title (*"Coordinate: MANAGER ↔ ai-maestro"*) matched the claim. In any numbering-per-repo namespace (GitHub issues, PRs, ticket ids) a number resolves nearly everywhere, so **"it exists there" is worth nothing; only "its subject is what the citation claims" is evidence.** Corollary from the same pass: when your hand grep and the tool disagree, run the TOOL'S OWN pipeline to locate the hit — my `[^0-9a-zA-Z/]#35` returned **0** on a file where `grep 'MANAGER #35'` returned **1** (ordinary space, no explanation found), and replicating the sweep's extraction found the site immediately.

- **`git add` FAILS THE *WHOLE* PATHSPEC ON ONE MISSING PATH, AND `2>/dev/null` TURNS THAT INTO A RENAME-ONLY COMMIT.** After a `git mv` I ran `git add -- <new-path> <old-path> 2>/dev/null`; the old path no longer existed, so the add staged **nothing**, and because `git mv` had already staged the rename, the commit succeeded looking normal — `1 file changed, 0 insertions(+), 0 deletions(-)` — under a message describing five content edits. HEAD then held a card in `design/archived/` still reading `column: todo`, a ZONE-MISMATCH in the committed tree, while the `trdd:doctor` run that reported `ZONE-MISMATCH 0` was reading the **working** tree, which was correct the whole time. Three guards, each cheap: never suppress stderr on a command you depend on and **gate on `$?`**; after any `git mv`, check `git diff --cached --numstat` shows non-zero insertions before committing; and when the claim is about a COMMIT, verify with `git show HEAD:<path>`, never the tree. *A verified working tree is not a verified commit* — already in this file, and it recurred through a door that rule did not name.

- **A DETECTOR KEYED ON A STRUCTURED FIELD IS BLIND TO EVERY RECORD THAT NEVER POPULATES THAT FIELD — and the records that skip it are systematically the oldest and largest ones, so the blind spot is worst exactly where the stakes are highest.** `trdd:extrefs` reads the `external-refs:` FRONTMATTER field and reported a confident **"45 open cards, 70 refs, 14 repos, 0 unresolved"**. Measured across the open zones: **168 cards, 111 with the field, 57 without — and 27 of those 57 cite an issue in body prose.** One of the 27 was `TRDD-903B7A20`, the board's largest campaign card, whose sole blocker `ai-maestro#132` sat in prose, **CLOSED for 8 days**, still gating a descoped gate that its own re-entry condition had already reversed. The tool ran clean over it every time and could not have done otherwise. **This is the mirror of the verb-rename blind spot** (a needle keyed on a NAME goes blind at a rename): here the needle is keyed on a SCHEMA, and goes blind at every record that predates or ignores it — which no rename ever announces either. Two things make it invisible: the number it reports is *true* of the population it scanned, and a field-keyed scan looks more rigorous than a text scan, so nobody asks what the field's coverage is. **The fix is not to scan prose** — a bare `#35` matches ordinary text and filing off it manufactures false positives (already hit on this board) — **it is to COUNT the unscanned population and PRINT it**, so a clean run says "clean of what I can see" rather than "clean". Before trusting any field-keyed report, measure `records WITHOUT the field` as a first-class number; it is the floor on what the tool cannot answer.
- **A DESCOPE THAT NAMES ITS OWN RE-ENTRY CONDITION IS A SELF-CANCELLING PARK THAT STILL NEEDS A WATCHER — and it reads as more responsible than a plain park, which is why nobody watches it.** G4 was descoped with a correct, evidenced measurement (*"zero callers, the wiring is UNBUILT, so this gate cannot honestly gate"*) and it explicitly wrote its own reversal: *"re-enters as its own card when a visualizer actually wires the CLI."* The visualizer wired it, shipped it in two releases with its own test suite, recorded itself as *"the FIRST caller"* — and the gate stayed descoped, because a condition stated in prose fires for nobody. Fifth instance of the stale-blocker class already recorded here at 4-in-5; the new wrinkle is that **naming the reversal condition creates a false sense that the reversal is handled.** Same fix as the parent lesson, sharpened: write the re-entry condition as a RUNNABLE COMMAND in the box (`gh issue view 132 --json state`, `grep -rl PANEL_CLI <plugin>`), never as a sentence — a sentence has a silent timestamp, a command re-answers itself.


## Lenient readers and the writes built on them
- A LENIENT READER'S READ SIDE IS VACUOUS AND ITS WRITE SIDE IS DESTRUCTIVE — count the WRITES, not just the reads: my card was filed for verifications reading *unreadable* as *absent*, and **21 of 21** `saveJsonSafe` calls turned out to be read-modify-write fed by that same reader, 4 of them on the user's own `~/.claude/settings.json`, each REPLACING the file with a minimal object built from `{}`. The worst site logged `writing safeguard` while truncating it — an ops line that reads like a REPAIR.
- THE PROJECT'S OWN RATIFIED ANSWER BEATS YOUR REASONING *AND* THE ADVISOR'S — grep for a sibling that already solved it BEFORE designing: `lib/claude-settings-enforcer.ts` had ruled exactly this ("Corrupt JSON — NEVER overwrite … would destroy whatever the user actually has") with a test pinning it; the ruling had simply never been extended to the family. It also caught what neither of us had: `JSON.parse` SUCCEEDS for `[]`/`null`/`42`/`"str"`, so a `data: Record<string, unknown>` return type is a LIE and the caller writes the result of mutating an array.
- THE SAME FUNCTION IS OFTEN BOTH AN OPERATION AND A COMPENSATION, AND A VERDICT CORRECT FOR ONE IS CATASTROPHIC FOR THE OTHER — ask WHO CALLS IT before wiring a check to fail: I made `ChangePlugin`'s G11 fail on a genuine mismatch, and `ChangeMarketplace`'s R51 rollback *reinstalls plugins through it*, so a restored system was reported as `THE SYSTEM IS IN AN INVALID STATE … manual repair is required`. One hour after arguing PG01 must not do exactly this. The axis was per-CALLER, not per-action, and no amount of reading the gate would have shown it — the rollback TEST did.

## Verifying a fix
- **TWO MEASUREMENTS TAKEN IN THE SAME WRONG ENVIRONMENT AGREE WITH EACH OTHER AND PROVE NOTHING — and the agreement is what makes you stop.** Three tests failed; I separated mine from pre-existing by reverting my change and re-running at HEAD (correct), then reproduced from a SHELL against a `mktemp` fixture instead of trusting the assertion (correct), and filed a card asserting a broken CLI stderr contract. Both arms ran on **Node v26.5.0** against a `package.json` whose `engines` caps at **`<26`**, in a repo whose CLAUDE.md opens by calling Node 22 *"a hard ABI constraint, not a preference"* and prints the exact prefix. Re-run through `bash scripts/with-node.sh`: the contract holds, and the file goes **3 failed → 1 failed** (the cluster 3/117 → 1/119). **Two of the three "pre-existing failures" were manufactured by my own shell**, and the card I filed on them named a defect that does not exist. The tell was in output I had already printed and skimmed — `node -v`. **The structural gap: yarn enforces `engines` before a SCRIPT, so `yarn test` is protected, while `npx vitest run <file>` — what anyone reaches for to run ONE file — bypasses it entirely and fails in a way that reads as a product bug.** Run the pinned toolchain explicitly whenever the result will be written down; rigour inside a wrong harness is indistinguishable from rigour. (TRDD-W636KQBN, which is the retitled corpse of that card.)
- `git checkout -- <file>` CANNOT TELL YOUR FIX FROM YOUR SABOTAGE: reverting a neuter discarded the still-UNCOMMITTED fix underneath it, silently, and the next run's green was green about the ORIGINAL code. **Commit the work BEFORE neutering it** (I had done exactly that for the previous card and skipped it for this one), or revert the neuter with a targeted edit. The complement of the held-lock lesson above: there the safety net is unavailable, here it is available and eats the thing you were protecting.
- NAME EVERY FAILING FILE INDIVIDUALLY BEFORE CALLING ANY OF THEM A KNOWN FLAKE — a label attaches to the SET and hides whatever else is in it: three files failed under full-suite load and all three were being carried as "pillar-graph-cli timeouts". Two were genuine 5s timeouts; the third was a real concurrency bug, and its assertion (`expected 420 to be 292`) was not the count it resembled but **octal file modes, 0o644 vs 0o444** — the defect caught in the act. "Passes in isolation" is the SIGNATURE of a concurrency defect, not evidence that there is none.
- THE CHEAP DISCRIMINATOR IS THE FAILURE **MODE**, NOT THE FILE SET — read it BEFORE re-running anything: a timeout says "did not finish", an assertion says "finished and was WRONG", and only the second can be a regression. Two full runs minutes apart gave 3 red files each with only ONE in common (4 distinct files, 1 overlap) — an unstable set is contention's signature, since a real regression fails the SAME test every time. All 5 failures across both runs read `Test timed out in 5000ms`, ZERO assertions, and every affected file spawned a subprocess or drove a full pipeline harness. Two `sed` calls settled it, beating the per-file isolation re-runs I had already spent on the previous set. The trap in the other direction: `grep -c FAIL` on such a log counts the word FAILED inside the negative-path tests' own expected OUTPUT — mine matched a dozen lines of deliberately-injected failures before reaching the summary. Parse vitest's `Failed Tests` block, never a substring.
- WHEN TWO RULES CONTRADICT, CHECK WHICH ONE IS STILL **SHIPPED** BEFORE RULING ON THE DISAGREEMENT — a superseded predecessor that nobody deleted reads exactly like a peer authority, and both load into every session at once: a peer Claude filed a careful 4-question governance issue over "the archival protocol contradicts the terminal-freeze", and the archival protocol it cited lives in an ORPHAN (`trdd-approval-tiers.md`, mode 644, mtime 07-22, **no** janitor header) whose successor is this repo's DEP overlay, while the freeze lives in the canonical file (mode 600, mtime 07-31, header present) that already answered all four questions verbatim. The provenance was three cheap facts — header, mode, mtime — cross-checked against the shipping component's own manifest of what it ships. Rule the PROVENANCE first; the "conflict" often evaporates. **⚠ REFUTED (2026-08-20) — the "orphan" classification above was itself wrong, from the same failure this whole file exists to catalogue: a signature was treated as diagnostic without ever measuring its base rate.** Census of `~/.claude/rules/`: 41 files, only 7 carry the janitor header, and mode splits 9×600 / 27×644 / 5×664 — so **32 of 41 (78%)** share the "644-or-664 + no header" signature this lesson called a mark of orphanhood. Live counter-examples sharing that exact signature: `manager-approval-defaults.md` (644, headerless, mtime **2026-08-20**, content read first-hand the same day), `decide-on-facts.md`, `never_use_sed.md`, `how-to-fix-issues-of-other-projects.md` — and the mtime leaks the other way too, since `janitor-heartbeat-protocol.md` is 600 WITH no header. Re-measured, `trdd-approval-tiers.md` itself now carries mtime **Aug 8** (not 07-22, i.e. edited AFTER this lesson was written) — it is not dead, it is a live, user-scope rule that still teaches the `approval-tier: N` field its own project retired on 2026-07-10 (`rules/aimaestro/aimaestro-trdd-approval.md:286`, "`min-approval-requirement:` supersedes `approval-tier: N`"). That makes the original finding WORSE, not resolved: the file needs a CORRECTION, not a dismissal as orphaned. **The transferable rule: a signature only discriminates if you have measured how common it is in the population, and then actively hunted for a counter-example that shares it and is provably live** — a specific, well-formed, confident-sounding claim (three cheap facts, cross-checked!) is exactly what makes a wrong classification credible to the next reader, including the one who wrote it. **The method that actually settles "which version is current" for a governed field: the newest artifact still WRITING that field beats any rule text as evidence** — `grep -l "^<field>:" design/**/*.md` sorted by `created:` (in this repo: 79 cards still carry `approval-tier`, newest 2026-07-29; 334 carry `min-approval-requirement`, newest today) — because rule prose can rot in place while the corpus it governs keeps moving. **AND THE AMENDMENT ABOVE COMMITTED ITS OWN DEFECT ON ITS FIRST DRAFT: it carried the corrected CONCLUSION and the base-rate number but NO command to re-derive them** — a lesson saying "count the population" that the next reader cannot recount is another measurement recorded as a property, caught by the same peer who refuted the original. So a durable note carrying a measured fact MUST carry its DATE **and** the command that reproduces it; here that is `find ~/.claude/rules -maxdepth 1 -name '*.md' -type f | wc -l`, `grep -l 'ai-maestro-janitor rule' ~/.claude/rules/*.md | wc -l`, and `find ~/.claude/rules -maxdepth 1 -name '*.md' -type f -exec stat -c '%a' {} \; | sort | uniq -c` (2026-08-20: 41 · 7 · 9×600/27×644/5×664). A measurement with neither a date nor a repro reads as a property, and that is the whole failure — the stale mtime in the original claim WAS its own disproof, sitting in the same sentence, unread for weeks, because a recorded fact does not invite re-checking the way an open question does.
- A VERIFICATION WHOSE RESULT YOU CANNOT GATE ON IS NOT A VERIFICATION — never put the check and the irreversible act in the SAME command block: I ran `test -f <flag> && echo PRESENT || echo absent` in the same block as the `git commit` that asserted the flag was absent, so the commit sealed the claim before I could read the output — which said **PRESENT**. I did check. I just could not act on it, which is indistinguishable from not checking, except that it *feels* rigorous. Worse than skipping the check: the transcript shows diligence next to a false claim. Put the probe in its own turn and branch on it, or write the claim only after the probe's output is in front of you.
- INFERRING A SUBSYSTEM'S **RUN** STATE FROM THE CARD THAT BUILT IT IS A CATEGORY ERROR: a card records what its author BUILT and freezes at the moment they wrote it, while ACTIVATION is a separate act by someone else that no card observes — mine said "WORKING but INERT … nothing runs against the real credentials", and the USER had armed the flag six days earlier, so the "gated infra" I was describing was a live credential writer beating every 60 s. Check the runtime gate itself (the flag AND the thing it produces — here the tick's own stamp, proving it beats rather than merely being enabled).

## Concurrent writers over shared derived state
- A REPAIR THAT MUST UNPROTECT A RESOURCE TO REPAIR IT OPENS THE VERY WINDOW IT EXISTS TO CLOSE — the moment it can run concurrently with ITSELF: the invariants watchdog restores read-only (0444) rule files, and `writeProtected` must `chmod 0o644` first (a `mode` option applies only at CREATE). `setInterval` with no re-entrancy guard let a second sweep read a PARTIALLY-written rule, conclude "bytes differ", and chmod it writable — a protected file was transiently writable BECAUSE the guardian was guarding it. Ask of any self-healing loop: can two runs overlap, and does the repair pass through a weaker state than the one it enforces?
- A `.finally` THAT NULLS A **SHARED** HANDLE IS CLEARED BY WHICHEVER RUN FINISHES FIRST, NOT BY ITS OWNER: `void inFlightSweep.finally(() => { inFlightSweep = null })` meant sweep A finishing wiped sweep **B**'s handle, so `stop()` returned `Promise.resolve()` while B was still writing — reintroducing, in the overlap case, the exact bug the `inFlightSweep` handle had been added to fix. Clear by identity (`if (shared === mine)`), and treat "we already fixed that" as scoped to the concurrency the fix assumed.
- **A PERSISTENT RESOURCE WHOSE SHAPE IS DERIVED FROM THE CALLER'S AMBIENT CONTEXT OSCILLATES BY CONSTRUCTION — and it is WORSE than two payloads disagreeing, because making the payloads agree cannot fix it.** The branch-protection baseline on 9 public repos took `pull_request` from `require_pull_request_for()` → `in_ai_maestro_agent_env()`, which reads `AIMAESTRO_AGENT`/`AID_AUTH` **from the calling process**, and `required_status_checks` from `detect_required_status_checks()`, which reads `.github/workflows` **from the caller's CWD**. So the hub applier (inside a harness agent) wrote `pull_request` and the janitor applier (standalone) removed it — **neither buggy on its own terms**, one predicate honestly answering two callers, and the same applier will flip against ITSELF depending on where it is invoked. Measured: 6 repos carried the hub's 01:50 shape, 3 the janitor's; **the split was by WRITER, not by repo.** Two things make it invisible: **each writer's post-condition is true of what it SENT**, and a conformance checker that re-derives the expected payload IN ITS OWN CONTEXT agrees with whichever writer shares that context and flags the other — so the verdict depends on where the CHECK runs too. No single-process test can see it; the divergence exists only ACROSS processes. Ask of any persistent resource: is its shape a property of the RESOURCE, or of the moment someone wrote it? (`TRDD-SX5FPMG0`; the discriminating evidence was a repo that HAD a checkout and a workflow file and still got no checks rule — the two repos with no checkout at all were consistent with the story but could not discriminate it.)


## Measuring at scale
- An "undocumented" marker from a plugin whose stub AUTO-ROLLS to the newest cached version is VERSION SKEW, not a missing spec: my session's rules were injected at SessionStart from 0.60.1 while the firing dispatcher had rolled to 0.64.1, whose OWN shipped rule documents the marker (3 mentions) and even tests it. I treated a documented marker as unknown for 3 turns and was one command from filing a false issue against a user-owned repo. Diff the newest cached version's shipped rule against the installed copy BEFORE concluding anything is missing — mine was byte-identical to an older version, i.e. stale, not broken.
- Never TYPE a timestamp — read the clock, then PASTE what it returned: I wrote plausible `updated:` values into 3 TRDDs (03:12, 03:22, 03:52 → one 43 min in the FUTURE) while the commits landed at 03:00-03:19, so the board's sort key was fiction in every one. Nothing rejects a future date, and a card that looks freshest is read first. Recurred twice more the same day WITH the lesson already written and `date` already run in the same turn — reading the clock is not the hard part, *transcribing instead of adjusting* is. `date +%Y-%m-%dT%H:%M:%S%z`, copy the exact string.


## Mocked modules
- A STUB PRESENT ONLY SO A DESTRUCTURE WILL NOT THROW IS DECORATIVE, and it silently disarms every hook keyed on its sibling: my harness exported both `revokeTokensX` and `revokeTokensXCompensable` but only the count-only one called `step()`, so when the pipeline switched forms the parity assertions saw NO revocation and — worse — the `after` hook anchored on the old name never fired, leaving three post-condition tests passing over a pipeline nothing had perturbed. A hook keyed on a call that no longer happens is indistinguishable from a gate that found no drift. Wire EVERY variant into the observation ledger, under its OWN name, so which one the code calls is itself pinnable.
- A TIMED-OUT CALL DOES NOT STOP — IT STAYS PENDING AND EATS THE **NEXT** TEST'S `…Once`, so the failure is reported against the wrong test with a status that reads like a regression: my route import costs 0.8s warm and 3.3-5.0s cold, so vitest's 5s default was a coin flip; the timed-out `post()` then consumed the next test's `mockRejectedValueOnce` and that test saw a **200 where it asserted 409**. I was one step from "fixing" a 200 that did not exist — the isolated re-run showed the true signature (`expected 500 to be 409`) and vindicated the neuter. Isolate before believing a failure's identity, and raise the timeout at the file when the subject is a heavy import.

## All-in-one functions (R50/R51)
- A COMPENSATION FUSED TO ONE ABORT SITE IS INVISIBLE TO EVERY OTHER ONE, and it reads as coverage because it is real, careful code: DeleteTeam's 45-line reverse restore lived inside `if (revertFailures.length)` and so ran ONLY for a per-agent failure, while G04's `return result` reported "registry delete failed" over a team whose every member had already been un-enrolled, demoted, plugin-stripped and hibernated. Code can only roll back the failure it is written for — the runner's value is not the undo but DECIDING WHEN IT RUNS.
- A COMPENSATION THAT RETURNS EARLY DOES NOT THROW, so a runner's `<gate>: reverted` line proves only that the undo did not FAIL — an empty one earns it too. My neuter (`if (true) return`) left all 10 tests green, and the second assertion was vacuous in a different way: `not.toContain(result.agentId)` compared against NULL, because a clean rollback is exactly the case that nulls agentId, and nothing contains null. Assert on the STORE the compensation moves (here the `updateTeam` call sequence: the id in on the join, out on the undo), never on the runner's log line or on a field the failure path has just cleared.
- AN `undo` AFTER THE LAST GATE THAT CAN ABORT IS UNREACHABLE, and unreachable compensation is worse than none because it READS as a guarantee: a compensation runs only when a LATER gate fails, and CreateAgent's G08-G12 are all WARN-and-continue with nothing fallible after them — so ~150 lines of careful undos for the tmux session, the keypair and the SHARED AMP index row could never execute, while the next reader would believe that index row was covered. Put them outside the sequence and write the trigger that would make them reachable ("if any of these is ever made fatal it MUST move in"). Find that boundary by asking which gate can still abort, not by wrapping everything that mutates.
- REVERSE-ORDER UNWINDING ASSUMES THE COMPENSATIONS COMMUTE IN LIFO — when two mutations have a forward order AND a reverse order that is NOT its mirror, they are ONE gate, not two: ChangeTitle Gate 9 requires membership for a team title and REFUSES a standalone title while still a member, so join→title reverses as leave→demote — the same order again, not its mirror. As two gates, LIFO demotes first, Gate 9 refuses, and EVERY rollback reports R51.5 "INVALID STATE" about a fully recoverable system. Read the nested pipeline's constraints BEFORE choosing the gate boundaries; the neuter that proves it is "swap the two halves inside the undo".
- A COMPENSATION CAN BE MASKED BY ANOTHER GATE'S, so its test passes with it DELETED and only the neuter says so: my "G04b restores the orchestratorId" passed with G04b's undo gone, because the title restore in the NEXT undo runs ChangeTitle, whose Gate 13b re-sets that same field. A redundant compensation is not wrong — but it is unpinnable on the happy path, so the test must drive the branch where the masking gate does NOT run (here: make the forward title write FAIL, so `titleChanged` stays false and nothing else can restore the slot).
- THE LAST GATE THAT CAN ABORT HAS A LATENT UNDO BY CONSTRUCTION, so no neuter can red it and calling it covered is a lie: write-ahead registration makes a failing gate's own compensation reachable only if `run` completes its mutation and THEN throws, and G05's single `saveJsonSafe` has nothing after it — so its flag is either set with the write durable or never set with nothing to restore. Neutering it reddened 0 of 4 tests. It stays (the runner requires it, and the partial-work contract makes it correct); what a test CAN pin is that its FAILURE unwinds the gates before it. Name the latency instead of counting it.
- AFTER FUSING TWO GATES TWICE, THE REFLEX IS TO FUSE, so apply the LIFO test in BOTH directions and expect the opposite answer sometimes: in one pipeline G03+G04 fused (one `marketplace add` restores both registration and cache, so a split unwind would restore the cache *before* the re-add that re-creates it) while G02b+G03 correctly stayed SPLIT (a reinstall needs the marketplace registered, and reverse order runs the re-add first — dependency and unwind agree). Same test, opposite verdicts, one function.
- A MUTATION IS NOT A WINDOW JUST BECAUSE IT IS A MUTATION — "does abortable work follow the write?" is necessary and NOT sufficient, because it finds windows a rule then FORBIDS you to close: 3 of `InstallElement`'s pre-EXE mutations passed that test and failed the second one — `.claude/` is a guaranteed agent invariant (deleting it fights the watchdog that re-creates it), the marketplace registration is SHARED (deregistering breaks every other agent), and the converted plugin dir is protected by R20.31 verdict **Explicit** (*"NEVER DELETES … the user's responsibility"*). Retrofitting by op count would have written a compensation that deletes a user-owned source folder and called it R51 compliance. Ask BOTH: is there abortable work after it, AND is reversing it legal and harmless?
- COPYING A SIBLING MODULE'S RATIONALE CAN SHIP A FALSE PREMISE UNDER A CORRECT DECISION, which is the hardest kind to catch because the code is right and only the WHY is wrong: I justified restoring an expired portfolio token with *"`loadPortfolio` prunes nothing"* — it prunes, and the module's own header says so. The DECISION survived (the two stores prune by different means: one REMOVES rows and persists it, the other only DERIVES a status in memory), but nothing in the tests could have caught the sentence, and the next reader inherits it as fact. When you write "unlike X, this one …", open X AND this one and state the actual difference; a comment is the one artifact no neuter reds.
- A REPAIR THAT FAILS LEAVES THE PRE-CALL STATE, WHICH IS EXACTLY WHAT R51 ASKS FOR — so a post-gate that only ever turns OFF an already-wrong flag is not a window and must not be gated: I recorded `InstallElement`'s two USER-scope writes as "two scopes disagreeing about what is installed" when PG03/PG07 only DISABLE a duplicate that pre-dated the call. Gating them would revert a good install to avoid a duplicate that already existed. Read what the write DOES to the store before classifying it as a mutation that needs an undo.
- A MESSAGE THAT HARDCODES A FACT THE CODE NEVER ESTABLISHED IS AN APOLOGY FOR A MISSING COMPENSATION — and a test asserting it is propped up by the gap: G07b's invalid-state message named `joined team <id>` whether or not the join had succeeded, and the rollback never left the team, so the string stood in for the undo nobody wrote. Once G07 recorded the join and its undo reversed it, the honest message names a team ONLY when one is still occupied, and the assertion had to go. Interpolating a value you did not read back is the tell.
- MAKING A HALF-WRAPPED CASCADE *ABORT* IS THE OBVIOUS FIX AND CAN BE A SECURITY REGRESSION, because the abort strands whatever an EARLIER gate already wrote: ChangeTitle's G10 swallowed a `blockAllTeams` failure and returned success, and "un-wrap it so it aborts" — which my own card had recorded as the fix's shape — skips the later revocation gates, leaving a demoted MANAGER holding AID tokens that EMBED `manager`, with the retry short-circuiting at Gate 6 (`titleUnchanged` ⇒ `success:true`) before revocation is ever reached. Ask what the abort SKIPS and whether a RETRY can still reach it; when it cannot, withhold the VERDICT at the terminal and let the alignment gates run. Abort becomes correct only once the earlier write is itself compensable.
- THE CTX A GATE RUNNER PASSES IS AN UNDO LEDGER, NOT A REIFICATION OF THE FUNCTION'S LOCALS — and mis-sizing it does not merely bloat the edit, it DEFERS the work: my card said "reify ~10 threaded locals", a later measurement inflated that to *117 declarations, ~12-15 carriers*, and on that estimate a whole session declined to start. The landed sibling's ctx has **4 fields, every one something `run` records so `undo` can reverse it**, with the adapters, plans and dirs closed over LEXICALLY — so its content is decided by the undos and there is nothing to reify before them. Read a sibling's ctx before sizing the edit.
- A WINDOW STARTS AT THE FIRST MUTATION WHOSE REVERSAL IS LEGAL AND HARMLESS, and that is not only a safety question — it decides whether the restructure is EXPRESSIBLE AT ALL: opening at ChangeTitle's literal first mutation (a `program` self-heal whose undo would restore the corrupt empty value) would have enclosed two gates that early-return SUCCESS, which `runGateSequence` cannot represent — it gives a gate return-or-throw and no third outcome. The conversion dies mid-edit on a shape the plan never mentioned.
- CONVERTING `return` TO `throw` INSIDE AN EXISTING `try` HANDS YOUR ABORTS TO ITS `catch`: G14's two drift checks sat inside the try that guards its disk read, harmless as returns, and as throws would have been swallowed and re-reported under the generic "verification failed" — losing the specific message. Narrow the try to the I/O it was written for, and keep a sentinel error class so a deliberate abort stays distinguishable from a bug (else every unexpected exception is silently reclassified as a clean refusal).
- A DEFENCE-IN-DEPTH ABORT IS UNPINNABLE BY DESIGN, so its neuter reddening NOTHING is the MEASUREMENT, not a broken test — but take the measurement rather than trusting the comment: G15's R9.13 denial says "Gate 3 should already have rejected this path", and neutering it to a no-op left the full suite at 310/4437/2. What the missing test means is that the conversion is safe only by an equivalence nothing checks — `return result` after setting `result.error` and `throw new GateAbort(msg)` agree ONLY because the loop's catch does `result.error = err.message; return result` on the SAME `result` object. Say which invariant each unpinned conversion rests on; the day the driver builds a fresh result, every unreachable abort changes silently.
- THE GUARD THAT EXISTS *BECAUSE* A FALSE SUCCESS SHIPPED IS THE ONE MOST LIKELY TO HAVE NO TEST — the incident is treated as the evidence, so nobody writes one: ChangeTitle's G22 was promoted from a silent WARN after three separate scenarios found callers claiming success while `governanceTitle` stayed null on disk, and disabling BOTH of its drift aborts leaves the full suite green (310/4437/2, zero red). A retrofit is the moment you learn this, because the neuter is already in your hand — and the gap PRE-DATES the change, so it is a finding to file, not an EHT to hang on the card that found it.
- MOVING AN ASSIGNMENT INTO A CLOSURE DELETES TYPE NARROWING AT EVERY *OTHER* CLOSURE, and the error surfaces far from the edit: hoisting G15's `targetPluginName` above the array and assigning it inside the gate's `run` made TS drop the guard's non-null proof inside a later `() => adapter.install(...)` thunk — 130 lines away, in code the slice never touched. Bind the guarded value to a `const` (a `!` silences the checker and stops protecting the call site the day the guard changes), and expect one such site per `let` you move.
- WHEN TWO COMPENSATIONS OVERLAP ON ONE FIELD, a test that drives only the path where the outer one runs cannot see the inner one: G04's undo restores the PRE-G03 team snapshot, whose `agentIds` already holds every agent, so it silently also restores membership and the LIFO neuter left that test green. Not a bug — but name the overlap, or the next edit deletes the half no test can miss. <!-- moved-for-file-cap -->
- THE BOUNDARY IS PER BRANCH, NOT PER FUNCTION — wrapping a whole pipeline to look thorough manufactures fake guarantees: ChangeMarketplace's `remove` had a four-store window while `add`/`update` had one mutating gate each with nothing abortable after, and `update` has no honest undo at all (you cannot un-pull a marketplace). Wrapping all three would have invented two unreachable undos to buy one real one — and the runner's refuse-to-start check exists precisely to reject the third. <!-- moved-for-file-cap -->
- MEASURE EVERY CANDIDATE'S WINDOW BEFORE PICKING BY SIZE — the op count is not the risk: 5 of 5 remaining sub-100-op pipelines turned out to be ONE mutating gate with nothing abortable after (`ChangeMCP`, `ChangeLSP`, `ChangeHook`, `ChangeMetadata`, `ChangeCLIArgs`), so retrofitting any of them moves the conformance ratchet and buys zero safety. Five `awk`-scoped greps settled it; picking by gate count would have spent a session on the wrong one. <!-- moved-for-file-cap -->


## Reading a running system
- BEFORE WAITING ON EVIDENCE FROM A SYSTEM, PROVE THE CODE YOU ARE WAITING ON IS THE CODE THAT IS RUNNING — otherwise every observation is about the OLD code and you will attribute it to some other actor, which leaves the wait with no end condition. I spent three measurement rounds waiting on "a Claude Code boot" to explain hourly registry writes; the writer was MY OWN undeployed lane (`.next` built 07:08:18 and pm2 started 07:08:27, both BEFORE the first fix commit at 07:26:25 — and `services/*.ts` is BUNDLED, so a restart alone never loads it). I had even correctly ruled out "this is a boot" in writing, then never asked *who else it could be*: **ruling out one cause is not identifying one.** The three tells were all non-temporal and each decisive alone — the new FIELD absent from the state file, the state file's rows still carrying the OLD shape, and the cadence still matching the OLD interval.
- SNAPSHOTTING `ps` TO A FILE DOES **NOT** REMOVE THE SELF-MATCH — it only removes the *grep's*. The snapshot is taken BY a shell whose own argv already contains the search string (the whole pipeline is in the `zsh -c` command line), so that shell appears in its own snapshot and the count comes back +1. Mine read `2` for a single live process and I nearly reported two refreshes. The snapshot fixes the narrow case and the general defence is still the bracket trick (`[c]laude plugin marketplace update`) or matching a column the argv cannot contain — and ALWAYS eyeball the matched LINES, not just the count, since a self-match is instantly recognisable as your own command and a count is not.
- ONE MODULE CAN HAVE TWO LIVE COPIES WITH DIFFERENT DEPLOY MECHANICS, and verifying the wrong one reads as proof: `services/auto-update-service.ts` is BUNDLED into `.next` for the API routes, and ALSO imported by `server.mjs` as `await import('./services/auto-update-service.ts')` — the TypeScript SOURCE, transpiled at runtime — for the scheduler. So the scheduler half goes live on `pm2 restart` ALONE and the route half needs a build, and my `.next` grep proved only the half that was not running the lane. **Ask WHO LOADS the code you changed before choosing what to grep.** Two zeros were the tell and I nearly read past them: a function NAME (minified away) and a pre-existing control string that lives only on the scheduler path, hence is never in `.next` at all. For a runtime-imported module the honest proof is RUNTIME BEHAVIOUR — here, `ps` showing one argless `claude plugin marketplace update` at exactly boot+settle, which proved the catch-up fired AND that the new single-call shape replaced the per-name loop.
- A BARE `setInterval` FIRES FIRST AT boot+INTERVAL, so a process restarted more often than its interval NEVER runs that chore — and RAISING a cadence multiplies the starvation window (1 h → 3 h tripled mine, three hours after I had "fixed" the lane). Compute the first fire from a PERSISTED last-run stamp, not from process uptime; persistence is also what makes it safe under a restart LOOP, since the first catch-up stamps the field and every reboot inside the interval then computes "not overdue". Two fail-safe directions worth pinning explicitly: an unparseable stamp must count as never-run (failing the other way starves the chore forever on one bad write), and a FUTURE stamp must count as fresh (a backwards clock must not cause a storm).
- AN ASYNC EFFECT'S ABSENCE IS NOT EVIDENCE UNTIL YOU HAVE WAITED LONGER THAN THE EFFECT TAKES: my 5-minute waiter reported "the catch-up wrote nothing", and the catch-up had fired on time — its refresh was still in flight (900 s timeout, and the previous sweep took minutes), and the state file is written only on completion. "No output yet" and "it never ran" are the same observation; separate them by looking for the WORK IN PROGRESS (a `ps` snapshot, an open handle, a partial artifact), not by waiting a bit more.
- **`atime` PROVES A FILE WAS *READ*, NEVER THAT IT WAS *EXECUTED* — and one binary's launch touches its siblings.** Asked who opened headed "Chrome for Testing" windows, I found `chromium-1208/…/MacOS/Google Chrome for Testing` with atime `16:53:57` and `~/.dev-browser/browsers/default/chromium-profile` written **the same second**, and called that a headed launch. It was a **headless** one: `--headless` runs the separate `chrome-headless-shell` binary, and Playwright *stats/validates its browser registry* on launch, touching the headed sibling without running it. The running processes said so plainly the whole time — 4 × `chrome-headless-shell` on that very `--user-data-dir=…/browsers/default/…` — and I read past them because the same-second coincidence felt like proof. Everything downstream inherited it: I named a project publicly, **twice**, off a name correlation (`am2svg_*.png` ↔ ANIME2SVG) that was real and load-bearing for nothing, since **a screenshot is byte-identical headed or headless**. The refutation was one grep of that project's source: `spawnSync(BIN, ['--headless'])`. **A headed browser is distinguishable from a headless one ONLY by its argv** — no timestamp, no artifact, no profile path can carry that bit, so catch it live (`ps -eo pid,ppid,command > file` first, then grep the file) or say you do not know.
- **COUNTING A STRING IN A TRANSCRIPT IS NOT COUNTING INVOCATIONS, and a project that DEVELOPS the tool is the worst place to try** — its file is *made* of the tool's name. I reported "304 `dev-browser` references, only 34 `--headless` ⇒ ~270 headed runs"; the true count of Bash calls containing `dev-browser` in that transcript was **0**, all day. The hits were source being edited (`dev-browser/daemon.mjs` ×44), error strings, and the tool's own `--help` text — which is also why its 51 `--headed` hits were a usage line, not a flag anyone passed. Extract the *tool-call* (`"name":"Bash"` + the command string), and **positive-control the extractor on another corpus**: my first control returned 0 and I nearly read that as "genuinely none" when it equally meant "my regex is broken" (it was validated only when it found real calls in a different project).
- **AN UNBOUNDED SWEEP IS NOT A VALIDATED SWEEP — VALIDITY IS THE DEFINED POPULATION, and "fix the cap" can make the number WORSE:** the integrator's true count was ~19; my capped `find` reported 19 (wrong in principle, right by ACCIDENT — the cap happened to select exactly the shippable tier), and my "corrected" unbounded runs published 44 then 39, because a working tree contains DEAD COPIES WITH REAL FRONTMATTER (`.trashcan/`, `*_dev/` backups — no parser can tell them from live) and **OTHER CHECKOUTS OF ITSELF** (`.claude/worktrees/<n>/` duplicated all 20 skills at a pre-fix commit). Three false-positive classes, three different defenses: fenced doc examples → parse the delimited frontmatter block, never line-grep; dead copies → population rules, stated beside the count; self-duplication → exclude worktree dirs or dedupe by repo-relative path. A peer then EXECUTED against one of my wrong numbers ("pin all 19") and was right only because the error cancelled — publish a count without its population sentence and it becomes someone's build order.
- **TWO WRONG INSTRUMENTS AGREEING IS THE MOST CONVINCING NUMBER IN THE ROOM:** the COS "independently confirmed" my truncated 19 by counting 20 files — and BOTH were accidentally right about the shippable tree while my "verified" corrections (which I defended with a depth histogram as PROOF) were wrong twice. The histogram proved the find was unbounded; it said nothing about what the extra tiers WERE. When a correction contradicts a number two parties measured, decode every tier of the delta into named artifacts before publishing — the 20/5/20 histogram decoded in one `sed | awk` into worktree/backup/real, and I ran that decode only on the FOURTH pass.

- **`stat` DOES NOT DEREFERENCE A SYMLINK BY DEFAULT, so a liveness check on `tasks/<id>.output` measures the LINK — whose size and mtime are FIXED AT CREATION and can never move however alive the worker is.** I stat'd a background agent's output file at 9 minutes, read `150 bytes, mtime 19:55:41`, matched it against my own recorded "a stub that never grew is decisive ⇒ kill" heuristic, and announced the frozen signature. The agent was mid-flight and returned 3 minutes later having made **62 tool calls**; `stat -L` on the same path reads **1,044,276 bytes**, and 150 is exactly the length of the target path (151 chars). **Both signals I used were frozen BY CONSTRUCTION** — the check could not have returned anything else for a healthy agent, a dead one, or an empty file, so it was not a weak discriminator but a non-discriminator, and its output happened to spell the answer I was primed for. One `ls -la` (the `l` in `lrwxr-xr-x`) settles it, and `stat -L` / `readlink` gives the real numbers. The general form is the rule this file already states and I did not apply to my own instrument: **a zero — or any never-changing value — is not a result until a positive control proves the instrument can see something you KNOW is there.** Here the positive control is free: a worker doing real work must have a MEGABYTE-scale transcript, so a reading in bytes is prima facie the wrong file. Cost: I told the user twice it was frozen and was one heartbeat from killing a worker that was 62 tool calls deep.

- **A BRIEF THAT DEMANDS A DECISION MUST NAME WHAT ALREADY DECIDED IT — otherwise the worker cannot detect that it is REVERSING one, and the contradiction is structurally certain rather than merely likely.** I dispatched a worker with *"STATE A CONVENTION as a decision (not a question)"* and no instruction to read the thread's existing decisions. It enumerated the family correctly, found a real collision, and ruled — **reversing a hub decision recorded on that same issue the day before**, unmarked. Both comments then stood on ai-maestro#148 with equal authority, so a future reader grepping the issue would hit the older one first and execute the dead plan. The worker did nothing wrong; **the brief made the failure inevitable.** Note the asymmetry that hides it: asking for a decision feels like delegating judgement, so you supply the *criteria* and forget to supply the *prior art* — and a decision brief is exactly where prior art is load-bearing, because reversing on better evidence is legitimate and reversing WITHOUT MARKING is not. Caught by the peer whose issue it was, not by me. Fix in the brief (*"read the thread's existing decisions; if you reverse one, mark it SUPERSEDED by date and quote its acceptance line"*), never by re-litigating the ruling.
- **A KEYWORD-KEYED DETECTOR CANNOT DISTINGUISH *USE* FROM *MENTION*, so the documentation ABOUT a hazard ranks highest precisely because it says the hazard's name most often — and the perverse consequence is that a detector produces its WORST signal-to-noise exactly where someone did the work.** The better the warning, the more it looks like a violation. FIVE independent instances across FOUR systems now — three mine in one session, two the peer's, and the fifth found independently before either of us hit this: my sweep's top hit was **my own warning box added minutes earlier**; the `agent-context-integrity` advisory's top hit was the clause FORBIDDING concealment; a peer's `.md` file with **zero code and zero shell verbs** NIT-BLOCKED A PUBLISH because the English words *"function ("* matched a case-insensitive `Function\s*\(\s*["'`]` pattern meant for the JS constructor — two wasted publish passes before anyone read the pattern instead of trusting the finding; and the janitor's own `repair_defect` matching `## Applies to` ANYWHERE in a file, so a lesson merely QUOTING that heading falsely flagged a correctly-tiered page — a detector in a fourth system, hitting the identical shape. **THE FIX IS MECHANICAL, NOT VIGILANCE: rank by USE-SHAPE, not token presence** — require the executable context rather than the bare name. **Measured on the corpus that produced this lesson: token-shape 46 hits / 0 real → use-shape 0 hits, with a positive control confirming the use-shape needle still catches a genuine bare-name call.** 46 false positives eliminated without losing detection. **TIGHTENING AND CONTROL MUST LAND IN THE SAME CHANGE** — 0 hits with no known-positive is indistinguishable from a blind needle, which is this lesson's own failure mode one layer down.

  **⚠ TWO CORRECTIONS TO THE PARAGRAPH ABOVE, both found by the peer and then measured here.**
  (a) **`0` IS NOT PROOF OF ABSENCE — use-shape cannot see a call CONSTRUCTED AT RUNTIME.** Verified: `cmd="aimaestro-trdd"; $cmd read …`, `CLI=aimaestro-message` and `"$CLI" send …` are ALL missed, while the direct call is caught. No static needle of either shape catches that class, so the honest reading of the 46→0 result is *"zero static call sites"*, never *"zero call sites"*.
  (b) **THIS LESSON'S OWN PROSE OVER-CLAIMED ITS OWN INSTRUMENT.** It originally listed the use-shapes as *"command position, `$(...)`, a pipe or `&&`, **an assignment**"* — and the regex I actually measured has **no assignment branch**: it returns **0** on `CLI=aimaestro-trdd`. So the lesson described a needle strictly stronger than the one the 46→0 figure came from, and the next reader would have built the assignment branch believing it was already validated. Describing an instrument in prose the instrument does not implement is the same defect as a comment claiming a test that does not exist — worse here, because the surrounding paragraph is *about* instrument validity. Corollary from the same exchange, on why two honest counts disagreed: a suffix-anchored needle (`aimaestro-[a-z-]+\.sh`) and a bare-stem needle answer **different questions**, and the bare-stem one matches inside longer names — neither is wrong, so reconcile the QUESTIONS before reconciling the numbers.
- **A NEEDLE KEYED ON A TOKEN FLAGS THE DOCUMENTATION *AGAINST* THAT TOKEN, AND FLAGS ANY NAME THAT MERELY CONTAINS IT — three times in one session, and the top hit was always the fix.** Sweeping 13 repos for bare-name CLI invocations (`aimaestro-trdd` where `aimaestro-trdd.sh` is required), the needle returned **46 hits and 0 real violations**: 38 were **Docker image names** (`docker build -t aimaestro-agent`) and a JSON `"family"` field; 4 were a skill's own NAME, `maintainer-aimaestro-trdd`, which contains the needle as a substring, plus a reports dir named after it; 1 was a `printf` **log prefix**; 1 was shorthand for three scripts sharing a suffix (`aimaestro-governance/teams/hook.sh`); and the single highest-ranked hit was **my own warning box**, added minutes earlier, stating that `aimaestro-trdd` is command-not-found. Same family as the `agent-context-integrity` advisory flagging safety rules, and the mirror of the `readJsonSafe`-vs-`saveJsonSafe` one-character miss: there a name that DIFFERED by one character escaped the needle, here names that merely CONTAIN it are caught by it. **Before reporting a count to the repo that owns the code, read every hit in its source line** — I was one message from telling a peer they had 5 violations in a tree they had just measured clean at 58/58, and they would have been right. A positive control proves the needle can SEE; it says nothing about whether what it sees is what you mean.
- **THE DEPLOYED CLI DRIFTS IN *BOTH* DIRECTIONS AND ANNOUNCES NEITHER, so a recorded capability measurement rots toward BOTH false-absent and false-present.** A peer's skill asserted `verify` was NOT deployed, from a measurement of **330 lines / 7 verbs** taken 2026-07-16; re-measured today the installed `aimaestro-trdd.sh` is **627 lines / 9 verbs including `verify` and `create`**. The skill was therefore telling agents to SKIP a verb that works — the mirror image of the bug it was written to prevent, and strictly worse than the original, because a missing verb fails loudly while a skipped-but-working one is never attempted. They kept BOTH measurements as a dated pair rather than overwriting the old one, which is the right shape: the lesson is not "the number was wrong" but "the number has a silent timestamp and the surface moves in both directions". Pair with the standing rule that a fix landing and the code RUNNING are different claims — this is that rule pointed at capability rather than at deployment.

- **THE IDENTITY WAS IN THE STREAM AND MY OWN FORMAT STRING THREW IT AWAY — then I reported it as never captured.** Investigating a `[system-daemon-runaway]` alert on a pid, I sampled with `ps -o rss=,etime=,command= -p <pid> | awk '{printf "%.0f MB etime=%s\n", $1/1024, $2}'`. I **requested `command=`**; the format prints only `$1` and `$2`, so all four samples carried the argv and discarded it before I could read it. The process then exited and I wrote *"I never captured its argv, identity unestablished"* — **false about the measurement, true only about what survived my formatter.** This is not the usual shape (a proxy read *in place of* the thing); it is worse and harder to see: **the thing was captured and dropped in favour of the proxy**, so the transcript shows me asking the right question and answering a smaller one. Identity of a one-shot event is unrecoverable by construction — and note HOW that was established, because my first answer was right for the wrong reason: I grepped the janitor's data dir for the **pid**, got nothing, and called it unrecoverable. The ledger writer (`findings_ledger.py`, 462 lines) truncates messages to `MAX_MSG_CHARS = 120` and contains **no pid field** — but note how nearly I got that right by luck too: `grep -ncE 'pid|comm'` returned **1**, I did not READ the match, and reported "no pid field at all" as though 1 meant something. Reading it: line 80 is a COMMENT, and the "match" is `comm` inside the word **"commit"**. So the true count of pid/comm fields is **zero**, my conclusion held, and my stated evidence (a count of 1) contradicted it. **A count is not an identification — read the match, especially when the count is small enough to read.** Substring-inside-a-longer-word is the same trap as every other name-keyed needle in this file. (Caveat still standing: I assumed `findings_ledger.py` is the writer the runaway detector uses; if it writes elsewhere, "unrecoverable" is too strong.) **And a pid is the most perishable possible needle** — "not found by this needle" is what I measured, "not recorded" is what I asserted. **When sampling a process you may not get to sample twice, print the WHOLE line first and format second.** Two collateral traps in the same investigation: (a) **`log` is a zsh BUILTIN, so macOS's `log show` never runs under zsh** — it returns `(eval):log:2: too many arguments` and my "no jetsam kill logged" was a broken instrument, not a negative result; only `/usr/bin/log` works, and its positive control (60131 lines) is what made the subsequent empty result mean anything. (Full entry in **Shell** below — and note I first wrote it as "a shell function on this machine" until `type log` gave the real mechanism and the much wider blast radius.) (b) **A `memory_pressure` reading taken AFTER the process died measures the host without it** — 76% free says nothing about pressure *during* the climb, which is the question the alarm asked; same structure as a post-mortem mtime.
- **A SERIES THAT TERMINATES EARLY IS NOT A COMPLETED SERIES, and "absence from the process table" is not "exited cleanly."** My 6-sample loop produced 4 (RSS 4584→5557 MB over 25 s) because the subject vanished; I read that as *"self-resolved, no action"* — picking the benign member of a set that also contains OOM-kill and heap-abort, which have the opposite operational meaning (**a process that OOMs will do it again next run**). The reviewer's counter-hypothesis was checkable and specific (~39 MB/s extrapolates into a `--max-old-space-size=6144` ceiling within ~1% at the observed death time). Tested properly it was **NOT supported** — no `DiagnosticReports` crash file, and no jetsam/OOM entry under a positive-controlled `/usr/bin/log`. **But "not supported" is NOT "ruled out", and the distinction is load-bearing here: neither instrument would necessarily carry the event either way** — a V8 heap abort on its own `--max-old-space-size` ceiling does not reliably write a crash report, and jetsam logs only when the KERNEL kills for system pressure, which is a different event entirely. **(Those two platform behaviours are RECALLED, not measured here — flagged because this file's own premise is that recalled facts rot, and the `ps %cpu` entry below is the long cautionary tale about exactly that. The conclusion does not depend on them: the general form is "say whether your instrument could see the alternative at all", which holds however those two specifics turn out.)** So I failed to find evidence in two places where it might not appear. The honest close is **"unidentified, gone, cause unestablished, may recur"** — materially different from "self-resolved", and also from "OOM excluded". State which member of the set you measured, say whether your instrument could even see the others, and never let absence choose for you.


## Second-hand reports (sub-agents, prior sessions, TRDD verdicts, audit findings)
- A negative control that "fails" can mean a MISUSED instrument, not a broken one — and I wrote the wrong conclusion into a commit message before reading `--help`. `memgrep find` returned the same 3 notes for a nonsense term, so I published "that invocation does not filter"; in fact a **bare term is OPTIONAL (it RANKS)** and `+TERM` is the mandatory operator, and `--only-notes` searches `[^N]` lessons rather than atom keywords — doubly the wrong call. Telling the next session their working tool is broken is worse than not testing at all. Read the query grammar, then A/B: pre-fix file → empty, post-fix → hit, `+nonsense` → empty on both is what actually settles it.
- GREPPING *ASSIGNMENTS* TO A FIELD ANSWERS NOTHING WHEN ITS **INITIALIZER** IS THE VALUE YOU ARE ASKING ABOUT — and the sentence you write can be literally true and completely wrong: I measured "`result.success` is assigned exactly twice, both times to `true`" and filed a card titled *"ChangePlugin never reports failure"*. `result` is initialized `success: false`, so the two `= true` are the EXCEPTIONS (a no-op path and the terminal success) and every gate failure returns the default. Refuted three ways an hour later — the initializer, the exit trace, and **12 existing tests already asserting `success === false`**. Read the DECLARATION before counting the writes, and when the question is "can this return X?", grep the CALLERS and the TESTS for X first: they answer it in one command.
- READ THE PRESCRIBED INSTRUMENT'S OWN CONTRACT before queueing work on it — a report can prescribe a remedy that cannot exist: the drift line said "fix via `/janitor-memory-update` (never hand-edit)", that skill forbids multi-page reorg and delegates to an agent whose SIX chores and the transaction CLI's FOUR ops all exclude a cross-scope promotion, and two of those chores say so outright ("re-homing is SURFACED, not done"; "promotion is a deliberate human act"). "The other system will do it" was waiting forever, and it had me filing a self-reproach for doing the one thing only I could do.
- **A SECOND-HAND REPORT'S PROPOSED FIX MAY ALREADY BE SHIPPED — and a REAL symptom is what makes the wrong diagnosis credible.** A repair agent's report said the janitor's `_restart_decision` fail-safes `None → restart` and therefore SIGTERMs every keepalive-launched daemon, and asked for authorization to file its 2-line guard upstream. Refuted three ways in ten minutes: the guard is present in **all 11 cached versions** (`global_state.py::daemon_needs_restart`, 2.4.1:1811, 2.3.0:1599), placed one level ABOVE `_restart_decision` and carrying a comment describing that exact loop; so `_restart_decision` is never reached for a keepalive daemon; and `launchctl list` shows **no janitor job at all**, so no keepalive-launched daemon exists on this machine to be killed. The crash loop WAS real (6 spawns in 398 s, dated from `daemon.spawn-history`) — but it was cross-session contention, four distinct `[s:…]` session identities cycling one machine-wide singleton, resolved two hours later by a daemon that has since run 10.5 h **logging the very server-yield line the report blamed**. The real symptom is what stops you checking: it makes the report feel confirmed. Would have been the SECOND wrong cross-repo filing in one session (cf. #60/#218). Before asking another repo to ADD a guard, grep every shipped version to prove it is ABSENT.
- "NEVER REACHED" IS A CALLER-CENSUS CLAIM, and finding one guarded caller does not make it — the same wrong diagnosis then came back a THIRD time as a filed issue (janitor#219), armored with a "reproduced as a pure function" repro that proves the function's isolated behavior and nothing about reachability (calling it directly BYPASSES the guard under dispute). My own refutation had silently assumed `daemon_needs_restart` was the only caller; the census (`grep -rn _restart_decision` over both cached versions' whole scripts trees) closed it: one caller, guard line precedes the call line, no external callers. A pure-function repro attached to a reachability claim is the tell to run the census, not to concede.


## Claims about the codebase
- A VARIABLE NAME PLUS A `sed` WINDOW IS NOT A FUNCTION'S CONTRACT, and the inference it licenses is the sentence that persuades: `catch (deleteError)` around a registry delete read to me as a rollback "firing when the system is already broken" — the enclosing function was `transferAgent` and the site was its `mode === 'move'` branch, i.e. the SUCCESS path of a user action. The bypass was real; the severity story was fabricated, and I had committed it to a card a future session plans against. Read the enclosing declaration, not the window. The reviewer named two readings that fit the same window with OPPOSITE severities (an import rollback vs a transfer-out's own removal) and gave one settling command: grep the enclosing range for the function declaration and for `DeleteAgent`. It resolved in one call — `transferAgent` at :1134, `DeleteAgent` absent from the whole file.
- A RULE IN THE PROJECT'S OWN CONSTITUTION CAN BE FALSE IN BOTH DIRECTIONS, and then it punishes correct work: CLAUDE.md said the console gate covered "this op + MAESTRO login ONLY, never other routes" while MAESTRO login was **never** gated and **four** other callers already existed — and it contradicted itself two sentences later under "Still open". A sub-agent adding a legitimate fifth caller correctly reported it was overriding an explicit in-repo warning; the warning had been wrong since the third caller landed. Before ruling a change a transgression, verify the rule it transgresses (`git grep -l <symbol> <rev>` at the revision BEFORE the change). And never keep a second copy of a census beside the code — the copy is what drifts.
- A LITERAL NUL BYTE IN A SOURCE FILE MAKES GIT TREAT IT AS BINARY, and the whole file silently stops being reviewable: a test written with a raw NUL instead of `\x00` committed as `Bin 0 -> 10159 bytes, 0 insertions(+), 0 deletions(-)`, so every future `git diff`/`git log -p`/review of it would show NOTHING — for the file that was the evidence for an injection guard. The tell is `Bin` where a line count belongs in `git show --stat`. Note two instruments lied on the way: `iconv -f UTF-8 -t UTF-8` reported INVALID (a NUL is valid UTF-8) and `grep -c` for the byte found none; python's byte count and git's own `Bin` verdict were the two that agreed.
- THE SAME NUL ALSO MAKES `grep` REPORT **NO MATCH** FOR THE WHOLE FILE — a false ABSENCE, which is worse than the `Bin` diff because nothing looks wrong: `grep -c "neuter" .claude/rules/lessons-verification.md` printed nothing for a word occurring **65** times, `--text` printed 45 lines, and I concluded the string was absent and nearly went looking for why. A sweep then found **four** tracked files carrying one, including `scripts/trdd-doctor.mjs` (a governance tool) and the lesson ABOVE, so the warning was itself unsearchable. And the two symptoms do not co-occur: git sniffs only the first 8000 bytes, so the three ~8 KB code files diffed as `Bin` while the 136 KB markdown (NUL at byte 103074) diffed fine — same defect, opposite symptom, decided by a byte OFFSET. Nobody typed any of them: **writing the escape through a file-write tool can materialize it as the byte**, non-deterministically per occurrence (in one write the `String.raw` one stayed text and a plain string literal became a byte), so "escape it more carefully" is not a followable rule. Build a NUL from a NUMERIC literal (`String.fromCharCode(0)`, `Buffer.from([0])`) and VERIFY BY BYTE COUNT — it is invisible in every terminal and editor that would otherwise show it to you. Guard: `tests/governance/no-nul-bytes-in-tracked-text.test.ts` (whose own first draft planted three, and was caught by itself).
- A COMMAND ON PATH WITH NO ARG PARSING HANGS FOREVER ON THE FIRST THING ANYONE DOES TO IT: a statusline wrapper deliberately has no options (every argument is the inner command) and reads stdin unconditionally, which is correct under Claude Code and a trap on PATH — `--help` blocked for **8 minutes** with no output. Its 352-line test file covered byte-identical relay, exit codes, cancellation and temp-file leaks but never "invoked without piped stdin", because every test necessarily supplies input; the bug lived in the one shape the harness could not produce. Guard with `[ -t 0 ]` (false under any pipe, so the production path is untouched), and prove the guard on a REAL pty — `spawnSync` always hands the child a pipe, so without one the branch is pinned by nothing.
- A CAVEAT IS NOT A CHECK, and writing one is often what STOPS you checking: I published "the dispatcher never writes the sidecar" to a public tracker under a self-caveat saying it was inferred from a sub-agent's one-word "absent" — the write is at `memory-maintenance.py:416`, atomic, before the marker, with a test pinning it, and the check was the two commands I ran an hour later. Hedging FEELS like rigor and functions as permission to publish. Before any outward-facing write, a factual claim is CHECKED or REMOVED; hedge only what genuinely cannot be checked (a dead agent's cwd), never what one grep would settle.
- WHEN A SHARED HELPER EMITS THE SAME SIGNAL AS THE SUBJECT, IT INFLATES EVERY COUNT — discriminate by which one OWNS the state, never by name: `gate0Auth` pushes a real `G00:` line into its CALLER's `ops` array, so by gate-ops alone it read as a pipeline, and it would have sat in the violation list permanently as a thing nobody can fix (there is no pipeline there to wrap). A pipeline DECLARES `const ops = []`; a helper takes it as a parameter. That test dropped it and nothing else — a name blacklist would have needed updating forever.

- A RETRY LOOP'S EXIT CONDITION DECIDES WHAT A NEW PER-CALL FIELD CAN MEAN AT THAT SITE — read the exit before theorising about the site: I filed that a `mismatch` inside a candidate-key retry chain *"plausibly means: this key shape did not take, try the next"*, and the loop advances ONLY on `success === false`, so a verdict riding on `success: true` returns at the first candidate and the next one is never reached. Five lines of control flow refuted it. And the REAL reason not to copy the pattern was a third thing neither option named — a sibling handler's stale-cleanup retry, gated on `!ok`, that already REPAIRS the exact condition the mismatch reports, so a 409 there would report a fault the route knows how to fix.
- "SHOULD THIS GATE ABORT?" PRESUPPOSES A WINDOW — locate the transaction's CLOSE before answering, because the answer is often that there is nothing to abort into: I carried abort-vs-report as the card's largest open question, and the pipeline's only `runGateSequence` **closes 86 lines and several gates BEFORE** the gate in question, with three of five actions never entering it at all. So the real question was *"should the window be widened across five actions?"* — a different and far larger one. One grep for `if (!txn.ok)` reframed a deferred design debate into a measured fact.

- A HELD `.git/index.lock` TAKES YOUR SAFETY NET, NOT JUST YOUR COMMIT — and the safety net is what makes a NEUTER safe: `git stash` and `git checkout -- <file>` both fail on the lock, so any TEMPORARY source mutation becomes unprotected, and a turn that dies mid-way (rate limit, compaction, a heartbeat boundary) leaves it sitting in a file that is already modified, with no clean way to tell neuter from work. Under a held lock: read-only measurement only, and defer every neuter until the lock clears. Notice this BEFORE reaching for the mutation — "I'll just edit it back" is true only for the turns that finish.

- A PROCESS LIST IS NOT EVIDENCE ABOUT *THIS* REPO UNTIL YOU CHECK EACH PROCESS'S **cwd** — `ps` shows the whole machine, and a plausible-looking culprit can be working somewhere else entirely: I blamed four live `git difftool` sessions for a `.git/index.lock` and told the user so four times over 1h44m; `lsof -a -p <pid> -d cwd` showed **three of the four were in OTHER repositories** and could never have touched it. The lock was 0 bytes with mtime equal to my OWN interrupted `git commit`, and `lsof` on it showed no holder at every probe — the right answer was available in the first five minutes. **The tell was in output I had already printed** (one session had `src/exactTokens.ts` open, a path that does not exist in this repo) and I read past it, because I was scanning for confirmation of the story rather than for the one field that discriminates. When a shared-name resource looks contended, identify the CONTENDER before believing the contention.

- A FIXTURE THAT MODELS *EXISTENCE* BUT NOT *CONTENT* IS STILL A NO-OP WRITE, and the second half is the one read-back verifications need: the fs overlay I inherited tracked which paths exist — its own comment saying it was added because "a mocked write is a no-op, so no post-install assertion could ever discriminate" — and never what they CONTAIN, so 13 tests still asserted an install "succeeded" with the plugin absent from the settings file it had just written. Fixing it took THREE layers, each found by the next red rather than by reading: record the body on write and consult it on read; MOVE the body on `rename` (an atomic `saveJsonSafe` writes `<path>.tmp.N` first, so content must travel or the target lands existing-but-empty); and model the SUBPROCESS's own effect — a user-scope install is performed BY the CLI, so nothing ever calls `writeFile` on that path and no amount of fs modelling reaches it.
- WHEN AN ACCURATE MOCK BREAKS A PASSING TEST, THE TEST WAS PINNING THE MOCK'S GAP — not the code: making the CLI mock actually remove the settings entry (which the real one does) broke a test asserting that a `writeFile` HAPPENED, because that write was the service's *fallback*, reachable only while the CLI mock did nothing. Split it: assert the OUTCOME for the happy path, and give the fallback its own test driving the case it exists for (the CLI THROWS). The neuter then reds the fallback test and only it — proving a safeguard block that nothing had pinned before.
- THE FIRST FILE THAT *ANSWERS* YOUR QUESTION IS NOT THE FILE THAT *OWNS* IT, and a number taken from it is a guess wearing a measurement's clothes — ask WHICH SOURCE IS AUTHORITATIVE before quoting any count. Three files, three upward corrections, one session, same question ("how many plugins would deleting this loop strand?"): `settings.json::enabledPlugins` said **19**; `~/.claude/plugins/known_marketplaces.json` (270 entries vs settings' 252) proved the marketplaces were not *absent* but flagged `autoUpdate:false`/unset, so I had also had the MECHANISM wrong; `~/.claude/plugins/installed_plugins.json` (91 entries) showed `enabledPlugins` was the wrong DENOMINATOR entirely — the loop updates INSTALLED plugins, the live fire did 78 updates (tracking 91, not 35), and the real answer is **52**, on two marketplaces my earlier passes never even named. Nothing contradicted anything (enabled ⊆ installed); each source was internally consistent and each was a *subset view*, which is precisely why every intermediate number looked verified. **The tell is structural, not textual:** I never asked "is this the authority?", only "does this answer me?" — and a subset always answers, quietly and low. Corollary that bit in the same hour: I wrote *"X is the ONLY key holding marketplaces"*, true of the file I had open and false of the system, one `ls` away from the second registry. When a count will be acted on, enumerate the candidate sources FIRST (`ls` the config dir, not just the file you know), state which one owns the fact, and re-derive from that one.
- "ZERO FAILURES" IS ALWAYS SCOPED TO WHAT THE LOG RECORDS, so quote the scope or the phrase overstates itself — mine read *"zero failures across all 200 trail rows"*, which was true and meant *zero failures of the thing we log*: the rows are per-PLUGIN updates plus ONE aggregate marketplace row derived from a single process exit code, and **10 marketplaces had silently not refreshed in 11-155 days** while the lane printed `Refreshed every registered marketplace`. A per-item failure inside a one-invocation batch cannot move that batch's exit status, so an aggregate success claim over N items is evidence about 1 item, not N. Before reporting a clean run, name the population the counter actually covers — and treat any message asserting *every* from one exit code as the defect (TRDD-FXPV7L4D).

- CHANGING A TUNED CONSTANT OBLIGES YOU TO FIND EVERYONE WHO KNOWS THE OLD VALUE — the edit is one line and the blast radius is every party that derives from it: I moved an absorbed chore's cadence 3h→4h on a USER directive, and a DIFFERENT REPO's watchdog judges that same chore stale past **180 m**, so a HEALTHY run trips a `claimed-chore-stale` alert. I had spent the same session removing three messages that asserted the opposite of the truth (`tick-stalled`, `all-maxed`, "Refreshed *every* registered marketplace") and was about to ship a fourth of my own making — caught ONLY because the detector fired at me mid-session, not by any review. Two tests pinned the constant locally and both reddened, which is exactly why they exist; **no test on either side could see the cross-repo consumer.** So: grep the value AND its semantic twins across every consumer you ship to, and prefer DERIVING a peer's bound from something you publish over letting one cadence live in two codebases.
- **…AND THEN VERIFY THE MECHANISM BEFORE YOU REPORT THE NUMBER TO ITS OWNER — a remembered constant that reproduces the RIGHT number can still be the WRONG story, and the wrong story sends the fix to the wrong line.** I wrote the lesson above claiming the peer *hardcoded* 180 m. It does not. The bound is `max(3 × cadence, cadence + 600)`, where `cadence` is read from **that repo's own chore roster**, which lists this chore at 3600 s — so 3 × 3600 = 10800 s = 180 m, numerically exact and mechanistically fiction. There is no duplicated constant anywhere; the peer derives, which is precisely what my own lesson prescribed. Getting this right changed BOTH halves of the report. **Severity:** the ask is not "bump 180 to 240" — our 4 h cadence now EXCEEDS the 3 h threshold outright, so a healthy server reads as wedged for the last hour of EVERY cycle, permanently, where at 3 h it sat exactly on the boundary and fired only on jitter (my edit converted a latent defect into a deterministic one). **Owner:** the real defect is that a chore CLAIMED by another executor has its threshold derived from the NON-EXECUTOR's cadence — a bug that recurs for every chore any server claims at its own rate. And the function carried a soundness proof: *"the roster is test-asserted against `daemon.py` name-for-name and default-for-default, so this can never describe a cadence the daemon does not use"* — **true, and about the wrong party**, since this detector runs only on chores the daemon is NOT executing. A guarantee about the wrong party reads exactly like a guarantee.

- **A COMMENT STATING AN ERROR'S *SCOPE* READS AS AN *EXEMPTION* GRANTED TO THE EXCLUDED CASE — and "this branch is skipped for X" is not "X evades this check" until you ask whether the branch could ever have fired for X at all.** I asserted three severity positions for one finding in three commits, each from reading one more branch, and **all three were wrong**. The cheap discriminator I never used is the **STATUS CODE**: `:885` returns **500 `internal_error`**, which is a *should-be-impossible* handler, not an authorization gate — the caller had already passed the 401 upstream — and its guarded value is null for that caller **by construction**, so without the skip *every* such request would 500. **The skip is what makes the feature work.** Its comment (*"a non-mesh sender that is neither a known agent nor a known user is an error"*) states the error's SCOPE, and I read it as a carve-out handed to mesh. The second site was worse: `if (senderAgent?.governanceTitle)` is falsy for **any** title-less sender, so there was no case-specific bypass to find — I had generalised a class from one member, the same defect this file already records for API families. **Two questions before calling a skipped branch a bypass: what does it RETURN (401/403 = a gate; 500 = an invariant), and could it EVER have fired for this caller?** And enumerate the class first — `grep -n '<the flag>' <file>` lists every site where the caller's status changes behaviour, which is one command and forecloses all three wrong positions.
- A HAND-MAINTAINED NAME LIST IS NOT AN INVENTORY, and a count taken off one is not a measurement: TRDD-DQ6XN2VP's "26 pipelines, 21 remaining" (its STATE said 19) counted 7 one-line DELEGATORS as pipelines and omitted a real one that was already done — measured from the AST, the answer was **19 and 14**, a third smaller. A list is a snapshot of what someone believed once; DISCOVER the set from the code, and the number cannot go stale between sessions. *(Relocated from CORE 2026-09-05 for the file cap — a completed survey's result, mostly one incident's own identifiers, which is the lowest-recurrence class the budget rule names.)*


## Shell
- **`log` IS A zsh BUILTIN, SO macOS's `log show` NEVER RUNS UNDER zsh — AND ITS FAILURE LOOKS LIKE A CLEAN NEGATIVE.** `log show --last 5m --predicate '…'` returns `(eval):log:2: too many arguments` and **0 lines**, which reads exactly like "the unified log contains no such event". Use **`/usr/bin/log`**, and positive-control it (`--predicate 'eventMessage CONTAINS "a"'` returned 60131 lines) before believing any empty result. **This is not a quirk of one machine — MEASURED: `zsh -f -c 'type log; whence -v log'` still answers `log is a shell builtin`, and `-f` skips EVERY startup file**, so it is baseline zsh rather than something this box's dotfiles or a `zmodload` injected. Since zsh is the default macOS shell, expect it in essentially every session here. **Exact bound, VERSION-QUALIFIED, because widening past the evidence is this entry's own repeated failure — the scope sentence was rewritten three times while the measurement stayed the same size, and the word MEASURED ended up in front of a claim the measurement did not cover.** `-f` rules out startup files; it does not rule out the BUILD. What is actually established: `zsh --version` → **`zsh 5.9 (arm64-apple-darwin25.0)`**, and in that dotfile-free shell `whence -w log` → **`log: builtin`**. **`log` is an AUTOLOADABLE builtin from the standard `zsh/watch` module — NOT compiled in**: `zmodload -aL | grep -iw log` → **`zmodload -ab zsh/watch log`**. Corroborated from the primary source, and note WHICH source: **`zshmodules(1)`** — *"the zsh/watch module makes available one builtin command: **log** — List all users currently logged in who are affected by the current setting of the watch parameter"* — while `log` is **absent from all 2356 lines of `zshbuiltins(1)`**. That split is itself the confirmation: a module builtin is documented with its module, so the man page it lives in independently corroborates `-ab`. It takes no operands, which is exactly why `log show …` dies with "too many arguments". (A reviewer cited `zshbuiltins(1)` from recall and quoted the description verbatim — right text, wrong page, and the wrong page was the one that would have contradicted the `-ab` finding.) Honest scope: **every zsh that registers `zsh/watch`'s autoloads, which is the stock configuration** — so in practice every zsh you will meet, though it is a default rather than a compile-time certainty.
>
> **THE FOURTH SCOPE CORRECTION TO THIS ENTRY IS THE LESSON, AND IT WAS PRODUCED BY A CHECK A REVIEWER TOLD ME TO SKIP.** I had written "COMPILED IN, not supplied by an autoloaded module", inferring it from `whence -w` saying `builtin` AND `zmodload -L` listing no module. **`zmodload -L` lists LOADED modules; `zmodload -aL` lists AUTOLOADABLE declarations, and they answer different questions** — an `-ab` stub reports as a builtin while its module is still unloaded, so the first listing alone is a guaranteed false negative for exactly this case. The reviewer named that alternative, judged it "does not obtain here", and told me not to spend a cycle; I ran the one command anyway and it refuted us both. **An inference from two agreeing instruments is still an inference when a third instrument is the one that discriminates** — and "the conclusion is right, only the evidence is short" is a prediction, not a finding, however confidently a second party endorses it. Operationally it does not matter: **assume `log` is shadowed under zsh and use `/usr/bin/log`**, which costs nothing on a build that lacks it. A version-qualified claim is falsifiable in the useful direction; a universal one gets disproved by the first reader on a different build, who then discards the true half of the entry with it. **The scope took two corrections and the second one nearly shipped unmeasured.** I first wrote "a shell function on this machine" (wrong mechanism); `type log` gave `builtin`, and I then widened the claim to "every zsh everywhere" on the strength of ONE shell with this user's dotfiles loaded — a real alternative, since zsh's builtin set is not fixed. The `-f` run is what actually earned it. **A correction that widens scope needs evidence that widens too; mine got more confident with each revision while the measurement stayed the same size.** Also: **`command -v log` prints a bare `log` and does NOT reveal it** — `type`/`whence` is the probe that works. Every session that greps the unified log for a kill, a crash, or a service event and reads emptiness as evidence draws a wrong conclusion silently. Recorded here as well as in the runaway-investigation lesson above, because *here* is where someone hunting a shell trap will look.

- **A CHECK BUILT FROM THE EDIT YOU JUST MADE CAN ONLY SEE THAT EDIT — so it answers "did my change land?" while you read it as "is the file clean?"** Scrubbing a credential from a rules doc, I removed the Rule 12 example, ran `grep -c "and click Confirm"` → **0**, and reported the file clean; the card said **2 occurrences** and I wrote into its STATE block that the card was off by one. The card was right. The survivor was the WORSE of the two — the step-format table's `Action` row mandating *"Never write \"enter password\" — write `enter password <literal>`"*, i.e. the instruction that PRODUCES the exposure, in the table an author consults for every step. A needle derived from the passage you edited is structurally incapable of finding a second instance elsewhere; the instrument has to be built from the THING SOUGHT, over the whole population. For a secret that means extracting the token from the pre-fix blob **by SHAPE** and counting occurrences across every tracked file — never reading or printing it — which is both sounder and safer than the grep it replaces. Note the failure also inverted a correction: I "fixed" a true record to match a false measurement, so the wrong number would have outlived the wrong check.
- **STOPPING AT THE FIRST `✗` IN A LONG STATE BLOCK, THEN SCOPING THE RE-CHECK TO THE WRONG POPULATION, PRODUCES A CONFIDENT FALSE "STILL BLOCKED".** A card recorded *"✗ NO Emasoft template repo exists"*; I ran `gh repo list Emasoft --json isTemplate` → **0** and published *"the blocker is REAL and still standing 25 days later"* in a box AND a commit message. Nine paragraphs further down, the SAME card records the USER resolving it that same day with a **third-party** template (`fannijako/repo_template` — PUBLIC, `isTemplate: true`, wired at 4 sites by a named commit). So the resolution named a different OWNER, and a search of `Emasoft` could only ever return 0 — a number that reads exactly like a standing blocker and cannot distinguish *absent* from *not in the set I searched*. Two independent guards, both cheap: **read the whole STATE block before acting on any one entry in it** (they are append-only, so the newest fact is at the BOTTOM and the ✗ you find first is usually the oldest), and **derive the search population from the resolution, never from the problem statement**. This is the mirror of the stale-blocker pattern: there, a recorded blocker had cleared and nobody re-read it; here, it had cleared, the card SAID so, and I re-measured the wrong thing anyway.
- **A NEGATIVE CLAIM NEEDS AN UNBOUNDED INSTRUMENT — never `head`/`-m`/`limit` a search whose result will support "X does not exist."** `find app/api -ipath '*governance*' -name route.ts | head -6` printed six paths, `manager/` was the eighth, and I published *"there is no `app/api/governance/manager/route.ts`"* to another repo as fact. The truncation IS the finding and it is invisible: a capped list and an exhaustive one are byte-identical up to the cap. The tell is structural, not textual — I chose the cap, so the absence I "found" was one I imposed. Before writing *doesn't exist / absent / gone*, re-run the command with no cap and a `wc -l`. (Same family as `ls dir/*.md | wc -l` and `$?`-after-a-pipeline: an instrument whose failure mode is a plausible number.)
- **A NEUTER MEASURED WHILE ANOTHER PROCESS EDITS THE SAME FILE MEASURES NOTHING — AND IT FAILS BY REDDENING *EXTRA* TESTS, WHICH READS AS A RICHER RESULT, NOT A BROKEN ONE.** A delegated agent's neuter reddened 2 where it predicted 1; the second was my concurrent revert landing underneath its run, and the honest move it made was to DISCARD the measurement rather than report it — caught by `git hash-object` vs `git rev-parse HEAD:<path>`, never by reading the neuter's output, which was internally consistent and wrong. This is the concurrent twin of the `sed -i ''` case (a neuter that applies nothing): there the file did not change when it should have, here it changed when it should not, and BOTH present as a plausible test result. So when anything else may touch the tree — a background agent, a watcher, a formatter — bracket every neuter with a blob-hash check, and treat "reddened more than predicted" as suspect rather than as a bonus finding.
- **THE COREUTILS ON THIS MACHINE'S PATH ARE GNU, NOT BSD — DESPITE `platform: darwin`** — so every macOS idiom in your fingers is the WRONG one here, and the failures are silent in different ways per tool: `sed -i ''` takes `''` as an empty FILENAME (edits nothing, exit 0 ⇒ three neuters "measured" that never applied), while `date -j -f FMT` dies on `invalid option -- 'j'` (⇒ a `|| echo "$NOW"` fallback fabricated a whole staleness table reading **19/19 at exactly 0 days**, when the truth was 10 cards ≥7 days and two at 38-39). Hit TWICE in one session on two different tools before I generalised it, so generalise it now: **do not reach for a BSD flag at all** — `perl -pi -e` for in-place edits, `python3` for date arithmetic, both identical everywhere. And when a fallback exists, a uniform column of identical values is the tell that the fallback fired, never a result.
- BACKTICKS IN A DOUBLE-QUOTED `git commit -m` ARE COMMAND SUBSTITUTION, AND THE SHELL DELETES THE WORDS: my message read *"a row that is `updated`, or `skipped` carrying a pid, or `failed` whose detail…"* and SHIPPED as *"a row that is , or  carrying a pid, or  whose detail…"* — the three load-bearing values gone, replaced by the empty output of three not-found commands, while `zsh: command not found: updated` scrolled past as if it were noise from something else. A commit message is write-once documentation, so the corruption is permanent unless caught in the same turn. Never put prose containing a backtick (or `$`, or `!`) in a double-quoted `-m`; write it to a file and use `-F`. Same family as the `sed`-that-matched-nothing: the shell reports ITS own success, not whether your intent survived.
- A field can have a spelling your anchor misses: `status:` / `column:` / `**Status:**` are three spellings of one concept, and two greps agreeing a field is "absent" proved nothing — the value was on line 19 of every file, in bold body prose. I told the USER a real finding was fabricated, TWICE, then found the same premise in my OWN linter where it was `autofixable` and would have DELETED a legitimate field. Enumerate the spellings before declaring absence, and never key a destructive rule on a field NAME when the defect is a VALUE.
- An in-process `$HOME` swap cannot test a module that resolves paths at MODULE LOAD: `lib/agent-registry.ts` fixes `AIMAESTRO_DIR = getStateDir()` at import, so my zero-writes neuter passed GREEN while the write landed in the developer's REAL `~/.aimaestro` and my assertion checked the fake one. The property was only observable in a SUBPROCESS, where the whole module graph loads under the fake home. When a guard's target is a path constant, ask WHEN that constant is computed before choosing the test's altitude.
- AN UNKNOWN CLI OPTION CAN FAIL OPEN — `claude … --cwd X` prints `error: unknown option '--cwd'` and **exits 0**, so `promisify(execFile)` RESOLVES and the caller reports success having run nothing. A wrapper's `try/catch` around a subprocess proves the PROCESS ran, never that the COMMAND did. Probe an undocumented flag against a NONEXISTENT target (nothing can mutate) and read WHICH error returns: mine printed only the option error, while the same command without the flag reached the real code path and complained the plugin was missing — that difference is the whole proof.
- A SUBSTRING COUNT CANNOT COMPARE TWO VERSIONS OF A FILE — the same token appears in both while meaning OPPOSITE things, and the count reads as AGREEMENT: `grep -c 'Use --id'` returned **2 for both** the deployed and the repo `amp-helper.sh`, and the deployed copy PRINTS that string as advice (then lists every live agent's uuid, pickable) while the repo copy's two hits sit inside the sentence FORBIDDING it — 98 changed lines apart. I nearly published "the safety fix IS deployed" off that 2-vs-2. `cmp`/`diff` is the only instrument that answers "same or not"; a grep answers "is this token present", which is a different question that happens to return a number.
- A DEPLOYMENT CENSUS IS A SNAPSHOT AND GOES STALE LIKE ANY OTHER — mine was recorded as `56 identical / 24 stale / 6 never-deployed` and re-measured the same week as **55 / 25 / 7**, so a file had also moved identical→stale under it. "Is the CLI current?" is answerable ONLY per file, never per repo, and never from a recorded tally: re-run the byte compare. Corollary that bit twice: a commit landing and the code RUNNING are different claims (`#53` was complete in the repo and absent from `PATH`; the groups CLI shipped as "the last unbuilt one" and was never deployed) — so mark work done only against the surface the caller actually invokes.
- WHEN THE OLD CODE AND THE NEW CODE FAIL WITH THE **SAME EXIT STATUS**, A STATUS-ONLY ASSERTION PINS NOTHING — and it looks like a passing test for the fix: moving `check_api_running` behind verb recognition changed *which* error an unknown verb gets, not *that* it errors, because the gate already exited 1. So `expect(exit).toBe(1)` was green before and after, and the honest assertion is that the server is **NOT BLAMED** (`stderr` says `Unknown command`, with **zero** mentions of the API). Whenever a fix reroutes a failure rather than removing it, assert the *cause*, never the *code* — a 401 cannot discriminate "the gate refused" from "the command is wrong".
- **`jq 'select(…)'` USED AS A SHELL PREDICATE EXITS 0 WHEN IT SELECTS *NOTHING*** — an empty selection is a successful filter, not a false one, so `jq 'select(X)' && queue "$item"` queues EVERY item and the filter reads as if it worked. Mine was meant to queue only rulesets carrying a `pull_request` rule; it queued all 10, so a fix shaped for PR rulesets (bypass ⇒ admin) hit three `baseline-tag-protect` rulesets whose ratified bypass is **NOBODY** — silently WEAKENING three public repos while the apply output printed `bad=0 rules-ok` for every row, because those assertions were true of the payload I sent and said nothing about whether that payload belonged there. Use `jq -e` (exit 1 on null/false/empty), or test the extracted VALUE (`[ "$(jq '[…]|length')" != 0 ]`). And the reason it was caught at all: the verification compared each ruleset against **its own pre-change backup**, not against the intent — a post-condition read from the same idea that produced the write cannot see a write aimed at the wrong target. Diff before-vs-after per object, and flag `0 -> non-zero` on any field whose correct value may be empty.
- **A FIX BUILT FROM A NAME LIST IS A SNAPSHOT; THE HAZARD IS A PATTERN — and the list-shaped fix reads CLEAN forever afterwards**, because the instrument can never see what it was not told about. A 39-site handle sweep here and the downstream repo's own residual grep BOTH searched a list of known names, and a pattern-based guard then found **six accounts that are LIVE on GitHub** (verified `gh api users/<name>`) which neither sweep could have found. Two of them were sibling-file duplicates discovered *after* a manual pass reported done. Same family as a detector keyed on a symbol NAME going blind after a rename — and the trap has a mirror on the repair side: a "safe placeholder" chosen by intuition is a live account you have not checked yet (two obvious-looking replacements were real Organizations), so **verify every replacement returns 404 before shipping it**.
- **GREP FINDS TOKENS; ONLY READING FINDS ADVICE.** After every literal handle was removed from a doc, the line `# Or direct mention in issue body` remained — an INSTRUCTION to re-create the hazard, invisible to every sweep aimed at the hazard's literal form, and guaranteed to regenerate it the first time someone followed it. The complement of the same measurement: scoping the guard BY POSITION (text that actually becomes a `--body` argument) cut 68 apparent sites to **12 real** ones, because the other 56 were prose ABOUT handles. A guard that cannot tell a hazard from a discussion of the hazard gets disabled for crying wolf, and one that only matches literals gets quietly re-violated by its own documentation.
- **CHECKED-IN TOOLING THAT NOTHING INVOKES IS WORSE THAN ABSENT, and a green suite is not evidence it ran** — only that the WIRED checks did. A validator in-tree reported `valid:false` for **100% of its inputs** (it required v1 `type:`/`status:` against a corpus that is entirely v2 `task-type:`/`column:`) while the suite passed 12/12, because it was never wired into the runner. Anyone who ran it once concluded the tool was broken and stopped, so the checking it existed to do had not happened for as long as the drift had. Fixing only the schema leaves it just as unrun: **wire it in, in the same change.**
- **A CLOSED ISSUE IS NOT A PASSING GATE**, and a version pin duplicated across N sites is N chances to half-fix. An upstream fix landed in v5.5.0 while the consuming repo still pinned v3.1.0, so "closed" and "green" were unrelated facts; the bump had to touch **7 sites together** (publish.py ×5, ci.yml, release.yml) or CI would disagree with the local gate for a reason nobody finds. Related, from the same session and the same day: **a card can cite the wrong rule clause and survive unchallenged** — it asserted R22.2 for 8 days while the detector fires on R22.4. Inherited prose, never measured.
- **`ps` `%CPU` IS *NOT* A LIFETIME AVERAGE ON macOS — READ THE MAN PAGE BEFORE BELIEVING ANY CLAIM ABOUT A TOOL'S SEMANTICS.** `man ps`: *"a decaying average over up to a minute of previous (real) time."* Measured three ways after six sessions had spent a day repeating the opposite: (1) it does not equal `time/etime` — pid 607 read **99.0 vs 0.05%**, ~1,800× apart; (2) it MATCHES actual consumption computed from cpu_time deltas over a known wall interval — 99.7/94.8/100.2 reported vs **~103% actual**, and 73.2/79.4 vs ~81%, and 31.2/37.0 vs ~40%; (3) it DECAYS — the same pid fell **100.2 → 5.0** in minutes when its burst ended, which a 13-day denominator forbids. **The inversion is the real finding: if `ps %cpu` is a ~1-minute average and `top -l 2` is a ~1-second delta, then 167% and 2.7% are BOTH true of one bursty process, so the alarms everyone dismissed may have been CORRECT — a defect in the REVIEW, not in the detector.** Three collateral traps: `ps -o time` prints `MM:SS.ss` until it reaches hours, so splitting on `:` as `HH:MM:SS` inflates it 60× (mine read 560 s as 33,612 s); one row out of three happened to agree (`641`, 33.8 vs 31.2), which is exactly the coincidence that makes a small sample look confirmatory; and a doc that vindicates a process on a false mechanism is worse than no doc, because it stops the next reader checking.
- **AN AGREEING PEER LOOKS EXACTLY LIKE A VERIFICATION, and a second opinion is most dangerous precisely when it AGREES with what you already wanted to do.** Measured twice in one afternoon. A peer stated a doubt correctly *before* executing — *"`complete` is not literally in the archive-eligible list, I will not mass-mv 14 cards on a spelling judgement"* — I overrode it with a confident wrong ruling supported by no evidence, and 8 cards were mis-archived; the rule was one grep away the whole time (archive eligibility is a function of BOTH `column:` AND `release-via:`, never `column:` alone). Separately, a mechanism I invented and never tested was cited back to me as established by two of the six sessions I had told. **An approval that contradicts a stated doubt must RAISE the evidence bar, not replace it** — and when a claim comes back to you as consensus, check whether every holder learned it from the same untested source.
- **A ZERO IS NOT A RESULT UNTIL A POSITIVE CONTROL PROVES THE INSTRUMENT CAN FIND SOMETHING YOU *KNOW* IS THERE** — and the instrument's SCOPE, not its logic, is what silently makes it wrong: too narrow or too wide, it reports CLEAN from inside the gap. One session hit **three false cleans in a single audit, all its own, none of which felt uncertain while it was happening**: a `cmp` sweep aimed at an unversioned cache path reported ALL 16 scripts missing from the install (the cache is version-nested); `--help` exiting 0 for all 16 read as "documented flags work" when six ignore argv entirely and print NOTHING (an exit code cannot distinguish *help printed* from *argument never read*); and a collision scan at `-maxdepth 4` returned zero because plugin skills live at depth 6 — exposed only because its control, a skill KNOWN to be installed, also failed to resolve. A fourth failed the opposite way, a path extractor matching English prose containing a slash ("design/plan exists"), which would have filed 8 phantom broken references had pass 2 trusted the list instead of reading each hit in its source line. **Both directions are the same defect** and the sibling traps are the same family: `find -maxdepth 3` where macOS `$TMPDIR` is depth 4, and a character class excluding the quote GNU `sed -i` actually contains. Confident, uniform, and false is the normal presentation.
- **THE UNIT YOU KEEP FOR YOURSELF IS THE ONE THAT SHIPS UNTESTED, BECAUSE YOU WRITE THE DELEGATED BRIEFS PROPERLY AND YOUR OWN FROM MEMORY** — and it will be the security core, since that is exactly what you keep. Fanning out 5 units, I self-assigned the crypto core (it was the contract the other four compiled against) and wrote its acceptance as `tsc --noEmit` alone; the four I delegated each got a test + a named neuter, because writing a brief for someone else forces you to state the check. Result: the one unit with no suite was `lib/dev-mode-token.ts`, and neutering its enable flag reddened **0 of 14** — the flag being the ENTIRE justification for refusing to build a security-weakening env var, so the refusal was decorative and a disabled host would have honoured a token anyway. **⚠ STATE CORRECTED 2026-08-22T16:50 — the DEFECT IS CLOSED; the lesson is not.** Re-measured through `scripts/dev/neuter` on the same gate (`lib/dev-mode-token.ts`, `if (!rec || rec.enabled !== true) return false` → `if (!rec) return false`, 1 ins / 1 del, restore verified by blob hash): **2 red / 23 green**, pinned by *"refuses a correct token while disabled, and honours it again when re-enabled"* plus one coupling test, across four suites that did not exist when this was written. **Read the shape of that correction, because this file is loaded every turn: a lesson stating a defect's STATE acquires a silent timestamp and eventually teaches something false — re-verify the cited artifact before relaying it as present fact, and correct the state in place rather than deleting the lesson.** **A type-check cannot see a missing branch**, and it is the acceptance you reach for when nobody is going to read the brief. So: write your OWN unit's row in the ledger to the same standard as the ones you hand out — a command and a neuter, never `tsc` — and treat "I'll keep the tricky one" as the signal to raise the bar, not lower it. Corollary measured the same hour, from the other side: **my brief for unit 5 omitted `tsc`, so the worker correctly reported "syntax clean, 2/2 green" over a test file carrying 2 TS errors that broke the whole-tree gate.** Same defect, both directions — the check you do not write down is the one nobody runs and nobody can report missing.
- **A WORKER THAT COMPUTES FOR AN HOUR WITHOUT CHECKPOINTING ITS REPORT FILE LOSES EVERYTHING ON A KILL — so the contract must say WRITE THE FILE EARLY AND APPEND.** Three workers went ~1h with no report and no answer to a status ping; killed, **two had done real work that died with them** (one had completed a full `cmp` sweep, another had cleared several scripts), and the axes had to be re-run from scratch. A stalled worker that has been appending still leaves usable evidence, and the partial file also distinguishes *stalled* from *found nothing* — which is otherwise the same silence. Pair it with the standing rule that "NOT VERIFIED" and "VERIFIED ABSENT" are different tokens: both exist so that a worker stopping is never mistaken for a worker concluding.
- **AN AUDIT PASS RETURNS "CONFIRMED" FOR *CONFIRMATIONS THAT SOMETHING HOLDS*, AND THAT INFLATES EVERY DEFECT COUNT IT TOUCHES** — measured twice in one hour on two independent plugins: one axis reported `confirmed=9` where the session's own first-hand count was **3 defects** (six CONFORMANT results had been counted as findings), and another reported `confirmed=6` where **all six were confirmations that a guard is correct**. A count that mixes *defects found* with *invariants verified* is not a defect count, and it reads as one. The contract fix is at the BRIEF, not at the reader: pass 1 returns **DEFECT CANDIDATES ONLY** — an observation of correct behaviour is not a candidate, and a refuter handed one can only ever "confirm" it, which is how a clean subsystem acquires a finding count.
- **A WRONG MECHANISM THAT YIELDS A RIGHT-LOOKING ANSWER MEETS NO RESISTANCE, AND THAT IS *WHY* IT SPREADS** — the outcome cannot falsify it, so every downstream check passes and each reader adds confidence instead of scrutiny. My invented `ps %cpu` mechanism reached six sessions and survived a retraction: one of them then *designed a fix on top of it* ("the derivative of the lifetime average proves current < average") and forwarded that fix as an improvement — an argument valid only for a lifetime denominator, which under the real ~1-minute decaying average still lands on the same practical conclusion (the burst ended). Right answer, wrong physics, no friction anywhere. Its own retraction named the root cause better than I had: *"I never opened `man ps`, which was one command away, before building on it."* **It does not stop at prose: measured the same day, my invented mechanism had become a FILED CARD in another repo (titled after it), an IMPLEMENTED code change (`daemon_runaway.py:209`, `window = "a lifetime average, not a live sample"`), a TICKED acceptance box, a PASSING TEST pinning the string, and three shipped releases — so it is now emitted on every heartbeat to every session on the machine, teaching the false mechanism while telling each recipient the number is meaningless.** The test is the worst rung: a fabricated claim with a green guard means the next person to correct it breaks a test and looks wrong. And the card's PREMISE was wrong, not just its wording, so ticking its box settled nothing. **The tell is not a wrong result — there isn't one — it is that nobody ever consulted the primary source.** Before building anything on a claim about a tool's semantics, read that tool's own documentation once; and when a claim comes back to you as consensus, ask whether every holder learned it from the same untested source. The collateral finding is the one nobody had reached: if 146% and 39.6% are BOTH true of one bursty process over different windows, the alarms six sessions dismissed may have been CORRECT at fire time — so a tally of "3 alarms, 3 false positives" silently assumes the very mechanism under dispute, and the defect may be in the REVIEW rather than the detector.
- **A SOUND ARGUMENT RESTING ON AN UNVERIFIED QUANTITY IS INDISTINGUISHABLE FROM ANALYSIS UNTIL SOMEONE MEASURES THE NUMBER** — and stating the rule does not install the reflex. Having just retracted an invented `ps %cpu` mechanism, I published a second false claim about the SAME detector within the hour: *"it corroborates by re-reading the same window; two checks seconds apart cannot corroborate anything BY CONSTRUCTION."* Valid reasoning, invented premise — `dispatch.py:479` sets the interval to **600 seconds** (its own comment says "10-min cadence"), so against a ~1-minute window the two fires read NON-OVERLAPPING samples and the streak gate works exactly as designed. `man ps` was one command away for the first error; the cadence was one grep away for the second, **and I made the second while writing another session a message about consulting primary sources.** Two recipients were already carrying it onward. The generalizable form, from the peer who refuted it: before shipping an argument, list the quantities it rests on and confirm you MEASURED each one rather than assumed it — the argument's internal validity is not evidence about its premises, and being burned by this an hour ago is not either.
- **SORTING A SWEEP BY PATTERN SHAPE INSTEAD OF BY WHAT THE FILE *DOES* PUTS A WRITER IN THE DOCUMENTATION BUCKET** — and the two failure modes are OPPOSITE, so one pattern can never find both. Hunting a stale ruleset value I split the corpus into "EXECUTABLE (asserts on a bypass-actor count)" and "PROSE (the literal value)", concluded **"one executable site in the entire fleet"**, and told a peer so. Wrong: a PAYLOAD (`"bypass_actors": []` inside a dict that is POSTed) contains no assertion, so it landed in the prose bucket — while being wired through `RulesetPlan(action="UPDATE")` to an `apply_ruleset()` that PUTs. **An enforcer REJECTS correct state (loud, recoverable); a writer DESTROYS it (silent).** The one I missed was the writer, and it was the only tool in the fleet still able to move those rulesets, because the other implementation's own gate could not write at all — so the single thing that COULD act, acted wrongly. Grep for the assertion AND for the value, then classify each hit by tracing it to a write, never by the shape that matched. Committed on the very sweep whose stated purpose was to separate acting from documenting.
- **A WIRED, PASSING CHECK WHOSE PREDICATE DOES NOT COVER THE SHIPPED SHAPE IS A PRODUCT BUG, NOT ONLY AN AUDIT BUG.** Two findings in one repo turned out to be the same defect at different layers: a `@handle` guard passing 6/6 while the shipped template evades it twice (`@{{OLD_AGENT}}` fails the regex because `{` is not alphanumeric; `@old-agent` sits in a ```bash fence the guard skips *as an example* — while that block builds the `--body` argument actually posted), and a dedup doing `if url in line` so `…/issues/5` matches `…/issues/50` and the board-sync silently writes nothing. The auditing rule "a zero is not a result until a positive control finds something known-present" is therefore a CODE-REVIEW rule: apply it to every guard in the product, and note that the worker's guessed CAUSE was wrong in both cases — a fix aimed at it would have left both evasions live with the test still green.
- **A NOTE THAT RECORDS A *POLICY* AS A FACT ACQUIRES A SILENT EXPIRY DATE IT CANNOT SHOW — and in LOCAL scope it then OUTRANKS the correct note.** A memory page asserted *"the ratified baseline enforces `required_linear_history`, `bypass_actors: []`"*; the USER abolished the first on 2026-08-08 and the second on 2026-08-13, and the page went on prescribing `--squash` to satisfy a rule that no longer existed — permanently collapsing granular history to pay a cost nobody was charging. **The USER-scope corpus already held the correct rule**, so a correct memory existed and the stale LOCAL copy would have won the recall by precedence. Measured across a fleet the same day, the same abolished pair was stale in **five** prose sites — a machine-global rule file, a library's own module docstring 200 lines above the code contradicting it, and three separate memory corpora — and it was found only because one peer repaired its own copy and suggested a sweep. **Record the COMMAND that re-derives a policy (`gh api repos/<o>/<r>/rulesets`), never the policy's current value**; and when you find one stale copy of a rule, sweep for the others, because prose stale in one place is stale in the places nobody thought to look.
- **A RULING THAT LANDED IN CODE CAN BE LIVE ON NOTHING, AND THE ODD-REPO-OUT IS NOT A POSITIVE CONTROL.** A USER Tier-3 ruling ("the owner must be able to rewrite history") was implemented in the executable SSOT and shipped in **eleven consecutive releases** — and measured against the GitHub API three days later, **8 of 9 repos still carried the pre-ruling `bypass_actors: []`**, i.e. the lock the ruling abolished, while all 9 carried an approval count the same ruling had set to 0. Nothing was broken; the applier had simply never re-run. Three traps, all live at once: (1) **the module's own docstring contradicted its own code, 200 lines above it** (`"nobody bypasses"` over a payload granting admin bypass), and I read the docstring first — only the spec's explicit *"code beats this prose on any disagreement"* kept me reading; (2) **the machine-global IND rule still stated the pre-ruling shape**, so every session on the box reads the wrong baseline as current, and an agent "restoring the ratified baseline as-is" (a Tier-0 EXEMPT act) would have RE-IMPOSED the lock from that text; (3) **the single repo that differed looked like proof the code applies when it runs, and was not** — it carried the new bypass AND the old approval count, so the two rulesets had drifted independently and nothing measured carried the full current payload. I wrote that wrong inference down before checking the second field. **A closed ruling, a merged commit, an applier's own success line and a green suite are all silent about the deployed surface; only reading the surface is evidence.** And when exactly one member of a population differs, check a SECOND field before promoting it to a control.
- **STATE IS AUTHORITATIVE FOR INTENT; CODE AND GIT ARE AUTHORITATIVE FOR STATUS.** Measured across five repos in one day: **113+ terminal cards, ZERO with an unearned column, ~13 carrying stale authoring-time prose** ("Current state: PLANNED" with an unapplied NEXT ACTION, under `column: complete`). A prose-only audit reported 9 phantom failures in the first repo and would have re-columned correct work in three more. The trap is not carelessness — *"STATE overrides frontmatter"* is a CORRECT-SOUNDING rule applied to an UNMAINTAINED field, and a faithful agent given it cannot reach the right answer from prose alone. So the guidance must name the ARTIFACT TO CHECK (`implementation-commits:` → `git show <sha>` → does the file it claims to have produced exist?), never merely a precedence order.
- **READING THE CODE IS NECESSARY AND NOT SUFFICIENT — A MECHANISM DERIVED FROM CODE YOU READ IS STILL AN INDUCTION UNTIL A CASE IT FORBIDS IS TESTED.** Went 0 for 2 on one bug in twenty minutes, both explanations read from real source, each killed by ONE measurement. (1) *"`add-lesson` refuses because `add-atom` placed the atom past the footer boundary"* — I had read the boundary in a doc comment attached to a **DIFFERENT FUNCTION** (`add-atom`'s insertion point) and applied it to the lookup; the lookup walks the whole page and has no such boundary. Killed by retrying with the atom above the boundary. (2) *"then the marker line must not parse"* — killed by `cat -A` showing it byte-identical in shape to a working sibling. **A comment 200 lines from the code you are explaining is about the code IT sits on.** The third fact, which arrived only when the peer stopped inducing, narrowed it further than either story: the same verb with the same id form SUCCEEDED on another page the same session — so verb and id form are exonerated and the fault is specific to that atom/page. **Their rule is the one to keep: at two falsified passes, STOP and hand over measurements. "These two plausible explanations are wrong, here is the reproduction" is worth more to the owner than either guess.** Same family as the positive-control lesson one layer up: reading confirms an explanation on the examples in front of you, exactly as positive examples do.

- **ONE OUTWARD SIGNATURE CAN COVER THREE STATES WITH OPPOSITE CORRECT RESPONSES — and the shorthand you broadcast is where the distinction dies.** I measured a stalled subagent (transcript mtime frozen 2h45m, a queued "write your file early" never consumed), generalised it to *"poll the mtime, treat a frozen one as dead"*, and sent that to seven sessions. A peer refuted it inside the hour with one comparison: their two workers that produced **NOTHING** carried **266 KB / 201 KB** of transcript against **73 KB / 158 KB** for two that produced complete reports — so quiet-with-no-artifact spans **FROZEN** (mtime flat, file stuck at its ~170-byte stub ⇒ kill, costs nothing), **LIVE-BUT-SILENT** (transcript GROWING ⇒ **killing destroys real findings**), and **ABSENT** (file never existed while `ListAgents` said `running` for 3h ⇒ kill, not a slow start). **The discriminator is MOVEMENT, not quietness**, and my phrasing collapsed exactly that. Two caveats decide whether the check works at all: **poll BEFORE deciding, never as a post-mortem** — a kill appends a termination record and stamps mtime to kill time, after which frozen and busy are indistinguishable and only SIZE separates them; and **a stub that never grew is decisive, not ambiguous** — a worker mid-tool-call has already written its EARLIER calls, and one session that hedged this as *"could be one long tool call"* lost 45 minutes with three dead workers holding three axes. Duration is not a signal either (five workers ran 3-30 min and all returned). Two further inversions from the same incident: a **queued relay is a LIVENESS TEST, never a mitigation** — it lands iff the worker is still taking tool rounds (measured: 8 unconsumed across three sessions vs 1 consumed by a worker that then ran 30 min and returned) — so sending one *reads* as handling the problem and converts *"I should check whether these are alive"* into *"I have handled it"*; and the **re-dispatch's ~60× is NOT a speedup** (same model, same task — the first three never did the work at all), so recording it as performance invites someone to optimise the rule away. The strongest form of the write-early rule is not about liveness at all: a worker that wrote its COMPLETE report at 17:11 then froze 3h without returning was consumed **without ever needing the worker back** — **the FILE is the deliverable; a return value is a single point of failure that fails silently.** And the largest finding was none of these: an atom already in USER memory carried the write-early half, four sessions re-derived it independently in one evening at ~6 worker-hours, and recall fired for none of them — **a defect in RECALL, not in knowledge**, which is invisible precisely because each session's rediscovery looks like competence.

- **A PHRASE-GREP FINDS ONLY THE WORDING YOU ALREADY KNOW — sweep for the CONCEPT, or a partial fix reports itself complete.** Retracting a false claim, the owning session grepped the exact string `"lifetime average"`, fixed what matched, and reported done; the same claim survived three lines away as *"decaying average over the process's LIFETIME"* — **`decaying` right, `over the LIFETIME` wrong, and the pair reads MORE authoritative than the original**, which is the worst outcome a partial fix can have. Re-swept as an IDEA (`lifetime|burst that (already )?ended|burned earlier`, filtered to CPU context) it immediately produced a **third** site nobody had: a module docstring and a test comment, both also carrying the corollary *"a burst that already ENDED still reads high"* — the exact opposite of measured behaviour. Third instance of one family in one evening (a name-keyed collision scan that found a DESCRIPTION defect by luck; a verb-name-keyed write detector that went blind at a rename; this). **And the fix's reach is a separate claim from the fix**: they reported the pinning tests already corrected — true of their repo, and the INSTALLED 3.3.11 still carried all three assertions plus all three prose sites, so every heartbeat on the machine kept emitting the retracted mechanism. *Fixed* and *deployed* are two measurements, and the auto-rolling stub resolves to the newest CACHED version, not the newest commit.

- **A NEGATIVE ASSERTION MUST BE RE-POINTED WHEN THE POSITIVE WORDING CHANGES, OR IT DECAYS INTO A TAUTOLOGY THAT KEEPS PASSING.** Retracting a false claim across 7 sites, exactly one of them was `assert "lifetime average" not in line  # RSS is instantaneous; no window caveat` — a **TRUE** negative guarding against the caveat leaking into a branch that must not have one. A uniform sweep-and-replace over all 7 would have left `assert "<the new wording>" not in line`: **still green, now meaningless**, because that branch was never going to contain it either way. The owner instead RE-POINTED it at the new string, so it still fails if the caveat leaks back. **7 occurrences, 6 defects** — and a raw string count silently promotes the correct line into the fix list, which is the mirror of the same evening's inflation (an audit worker counting *"this invariant holds"* as a confirmed finding). Two needles failed in OPPOSITE directions on this one question within ten minutes: a case-sensitive grep missed `LIFETIME` entirely (recall), and widening to a bare `-i lifetime` matched dozens of unrelated security-pattern strings (precision) — only scoping to the three files that OWN the claim produced a meaningful count. As everywhere else tonight, the reassuring signal and the dead one are identical.

- **A CUMULATIVE AVERAGE IS WRONG IN *BOTH* DIRECTIONS, AND THE UNDER-REPORTING DIRECTION IS THE DANGEROUS ONE — it produces SILENCE, not noise.** The same pid, one hour apart, measured by the same instrument (two `ps -o time=,etime=` snapshots differenced over a known wall interval): **96.0% cumulative vs 6.4% interval** (it had STOPPED — the over-report everyone argues about) and then **99.3% cumulative vs ~191% interval** (~1.9 cores live — an UNDER-report by half, cross-checked by `top -l 2`'s second sample at 149.3%). All evening the stated failure mode had been *"a burst that already ENDED still reads high"*; nobody had noticed the mirror, in which a detector keyed on the cumulative figure reports ~99% for a process doing ~191%. **An alarm that cries wolf trains dismissal; an alarm that stays quiet during a real runaway trains nothing, because there is nothing to observe** — which is why "it over-reports" is the *reassuring* half of the bug. Two corollaries: a cumulative average can only RISE while current > average, so its DIRECTION is informative even when its level is not; and the fix's argument is stronger stated as *"unreliable in both directions"* than as *"noisy"*, because noisy invites a threshold tweak. Also measured in passing and unflagged by three sessions arguing about CPU: the same alarm says "RAM/CPU runaway" and `top` marked the RSS **growing** (`1798M+`, 2.07 GB at 3 h 10 m) — everyone had been auditing the half named first.

- **A CAVEAT IS NOT A SUBSTITUTE FOR THE SECOND SAMPLE — it only makes an unfounded claim feel responsible.** Having spent the evening establishing that one reading of a fluctuating quantity licenses nothing, I flagged a possible memory leak off a **single** `top` sample showing `1798M+`, wrote the correct caveat (*"one sample of the trend, not a series, so I am naming it rather than concluding it"*), and surfaced it to two sessions anyway. A peer took the trend: **six samples over 75 s, net -117 MB**, oscillating 2220-2337 MB at 36% of a 6144 MB heap cap — **GC sawtooth, a healthy heap, refuted.** `top`'s `+` growth marker samples one point on a sawtooth and cannot establish a direction. **Stating a reflex does not install it**: third demonstration in one evening (a peer's case-sensitive grep while verifying a report about grep blind spots; another's degenerate agreement-test; mine here), and hedging is the form the failure takes in someone who *has* learned the rule — the hedge discharges the feeling of rigour without paying for it. If the claim is not worth a second sample, it is not worth sending. **And the transmission half, supplied by the peer who received it: repeating a hedged claim STRENGTHENS it, because the repetition adds a CITATION that the hedge does not cancel** — they copied my flagged claim onto a durable card *while faithfully repeating my caveat*, and the claim thereby acquired a second source. That is precisely the mechanism of the false `%cpu` claim the same card exists to correct: **a plausible claim, honestly hedged, repeated until the hedge fell off.** Both halves ran in one evening, one layer apart, and the correcting party reproduced the failure inside the correction. So the discipline is not "hedge harder" but: take the series, or say nothing — and when relaying someone else's hedged claim, relay the *measurement gap*, not the claim plus its caveat.
- **⚠ THE LESSON BELOW IS WRONG AND I RE-COMMITTED THE EXACT ERROR THIS FILE ALREADY RECORDS — read this correction first, then the struck text as an artifact.** `ps %cpu` is **NOT** cumulative and **NOT** `time/etime`. Refuted in ONE ROW, no timing needed: if `%cpu` *were* the lifetime ratio the two columns must be equal in the same row, and on the live process they read **`%cpu` = 159.9** against **`time/etime` = 101.08%** — **58.8 points apart**. A peer measured the same thing dynamically: reported `%cpu` swinging 90.8 → 122.2 → 95.5 while the lifetime ratio moved 100.32 → 100.34 → 100.49, i.e. **±30 points against 0.17** in 40 s; nothing saturated can swing 30 points in 20 s. `man ps` says what it is — **a decaying average over up to a minute** — and **this file already said so, in my own words, as the correction to my own earlier fabrication of the very same claim.** I then adopted a peer's "saturated" reframing, built an elaborate correct-looking arithmetic on it, propagated it to two sessions (one emphasized it on a durable card as *"the strongest single line"*), and wrote it here as a NEW lesson — **with the refutation sitting in this same file, injected into every one of my turns.** The arithmetic was never wrong; it was arithmetic about `time/etime` **relabelled** as being about `%cpu`. **What makes this the dangerous class: the practical advice is IDENTICAL under both models** (interval-difference two `ps -o time=` snapshots), so nothing downstream contradicts it — and it silently INVERTS how a LOW reading is read. Under the false model a `%cpu` of 90.8 means *"pinned near its mean, tells you nothing"*; under the truth it means *the process genuinely dropped to ~0.9 cores in that minute* — a real signal discarded. A wrong mechanism that yields the right action meets no resistance and is therefore the one that spreads. **Retracting a claim does not immunise you against re-adopting it under a new name; only re-checking the premise does.** ~~The struck text:~~
- ~~**A CUMULATIVE `%cpu` MEASURES AGE, NOT LOAD — quantify the saturation before arguing about the threshold.**~~ *(TRUE of `time/etime`, which IS pinned by a large denominator; FALSE of `%cpu`, which is a ~1-minute decaying average. The numbers below are correct arithmetic about the WRONG quantity.)* Both failure directions (still high after a burst ends; still low while one runs) come from ONE property: the figure is CPU-time over the process's whole elapsed life, so past the first minutes the denominator is too large for any excursion to move it. Computed on a real 3.2-hour process (T=11510 s, C=100.0%) against a measured live rate of 286%: a **1-minute** burst moves the cumulative **+0.96 pp**, 10 minutes reaches 109.2%, a full **hour** only 144.3% — sensitivity **0.97 pp/min, decaying as `1/elapsed`**. **The same 1-minute burst on a 5-minute-old process reads 131.0%.** Identical behaviour, 131% or 100.96%, decided ONLY by uptime — which is why no threshold can fix it. Two consequences worth more than the arithmetic: *"do the lifetime ratio and the reported `%cpu` AGREE?"* is a **degenerate** burst-vs-sustained test, because a saturated ratio agrees with any high reading by construction; and **any replacement gate must be validated against a process that is live-hot but historically QUIET**, since a suite exercising only the cooled case passes a gate that is blind in the direction producing silence.

- **BEFORE REASONING ABOUT A DETECTOR'S METRIC, GREP WHAT THE CODE ACTUALLY READS — four sessions argued for hours about a quantity the detector never consults.** `daemon_runaway.parse_ps_rows` reads **`ps -axo pid,ppid,rss,%cpu,comm`**: no `time`, no `etime`, verified in the installed copy. So an elaborate "saturation" analysis built on `time/etime` was not merely arithmetic about the wrong quantity — it was arithmetic about **a number the codebase does not consult**, used to derive a "consequence for the fix" of a detector it cannot describe. One grep, never run, for hours, by four parties. **And the refutation was already written down BY BOTH OF US, with worked numbers, in files we each load:** my own lessons file carried *"pid 607 read 99.0 vs 0.05%, ~1,800× apart"* — the exact disproof — while I re-adopted the claim; their card proved `%cpu ≠ time/etime` in one section and reasoned as though they were equal **twenty lines later, same author, same file**. This is a sharper failure than "the corpus knew and recall never fired": the refutation was not merely *available*, it was **self-authored, adjacent, and specific**, and it still did not fire. Closing instrument note in the same spirit: my confirming `grep -c "etime"` returned **1**, and the hit was the substring inside *"li**fetime**"* — a needle matching the very false claim it was sent to disprove. Ask what the code reads; then ask what your needle matched.

- **A RETRACTION THAT STOPS AT THE CODE IS FINISHED ONLY FOR WHOEVER OPENS THAT FILE — enumerate the SURFACES before sweeping, and give the sweep a second pass.** One false mechanism reached **five** surfaces: the code, a module docstring, the tests, a USER-scope **memory page whose FILENAME was the claim** (`ps-cpu-is-a-lifetime-average.md`, `status: valid`, ranked **#1** for the very symptom that brings a reader there — and an atomize chore was still *growing* it hours after three independent disproofs), and the memory's **backup mirror**, which SessionStart restores from after a data-dir loss (measured: mirror 2156 B @16:29 vs canonical 6851 B @21:28, corrected text present in canonical, **absent** in the mirror — so a restore would resurrect the retracted claim *looking canonical*, with no sign it had been superseded). Six sessions tracked the claim through messages and code; **nobody looked in MEMORY**, which is precisely the surface built to answer the question later, unprompted, to someone who never saw the incident. **And the sweep needs a second pass:** a peer swept USER scope, correctly corrected the headline page, and classified three others as *"lessons ABOUT the error, no action"* — one of them asserted the mechanism as a **PREMISE** and gave detector-design guidance built on it, with its own supporting evidence already retracted. A false premise and a lesson about the error are **indistinguishable from the description line**; only the body separates them. Two operational notes from doing the correction: `add-lesson --supersedes` then `edit` the body (never delete — the old fact becomes its own dated guardrail), and **a correction unreachable by symptom is a correction nobody will meet** — the tool warned that two keywords shared no word with the page `description:`, and before extending it the symptom returned the page **not at all**; after, rank 1. Test the recall, do not assume it.

- **A SEARCH RESULT THAT DESCRIBES THE METHOD YOU ARE CURRENTLY EXECUTING IS ADVICE, NOT DATA — stop and re-run under it before trusting the output.** A peer's corpus sweep returned, among its own results, an atom keyworded `I_swept_for_the_wrong_string_and_missed_the_same_claim_reworded`. They read that line, classified it *"correct usage, no action"*, and moved on — **while that sweep had already committed exactly that error**, leaving a false-premise atom live. The corpus emitted the antidote *in the output of the very command that needed it*, and it was filed as a result. Their needle was `"lifetime average"`; the text said *"An instrument that averages over a lifetime…"* — same vocabulary, reordered, **so the needle could not match** (verified against the pre-correction text preserved verbatim in the `SUPERSEDED BODY`: `"lifetime average"` **absent**, `"averages over a lifetime"` and `"whole lifetime"` present). **My own sweep used the same needle and missed it identically**; I found it only because a *different instrument* — `memgrep recall`, ranking on `desc:` — surfaced the HYPHENATED `LIFETIME-AVERAGE`. Not a better needle: a different one. Hence the sweep needs **a second pass, under different phrasing, by someone who did not design the first needle** — and it cannot be done from the listing at all, because in triage a false PREMISE and a lesson ABOUT the error read identically on the description line; only the body separates them.
- **AFTER CORRECTING A TEXT YOU CAN NO LONGER USE IT TO TEST WHETHER A PRE-CORRECTION NEEDLE WOULD HAVE MATCHED — the observer modified the observed.** Checking the peer's diagnosis, my first measurement grepped the page *as it now stands* and found `"lifetime average"` **twice** — which would have let me tell them, with a number, that their explanation was wrong. **Both hits were text my own `add-lesson`/`edit` had just inserted.** The only valid instrument was the `SUPERSEDED BODY` that `--supersedes` embeds verbatim — a property that exists for the audit trail and turned out to be load-bearing for something else entirely. Same family as measuring a working tree instead of the commit, and as re-running a stale census: **name the moment your measurement belongs to**, and when you have edited the subject, measure the preserved copy.

- **NAME THE NOUN: when two quantities in a conversation share a loose label, a correct measurement can be re-conflated by the SENTENCE THAT SUMMARISES IT.** Having disproved that `ps %cpu` is cumulative, and having correctly labelled a fresh pair of numbers (`time/etime` **243%** at 16 min vs **99-100%** at 3 h — a clean demonstration that the *cumulative ratio* saturates as its denominator grows), I summarised it as *"the **reported figure** measures age as much as load"*. **True of `time/etime`; FALSE of `ps %cpu`**, which tracks a ~1-minute window and was measured swinging **64.6 → 194.4 in 30 seconds**. Both get called "reported" in the same discussion, one saturates and one does not — **and that exact conflation is what produced the original error I had already retracted twice.** The data were right, the labels on the data were right, and the one-line takeaway re-introduced the bug: a summary is where precision is cheapest to lose and hardest to notice, because nobody re-checks a sentence that merely restates numbers they just verified. Caught by a peer, not by me. **Pin the noun every time — "the cumulative ratio", "the ~1-minute average" — never "the reported figure".**

- **A CENSUS THAT CLASSIFIES BY "NO SAME-NAMED FILE TO BYTE-COMPARE" IS STRUCTURALLY BLIND TO SYMLINKS — a symlink has no bytes of its own, so it lands in the UNOWNED bucket BY CONSTRUCTION and inflates the defect count.** Mine reported **8 unowned executables**; `aimaestro-agent-bash` is an 18-byte symlink to `aimaestro-agent.sh` and belongs in the *launcher* bucket beside nine others — it was the SECOND link to a target whose first link the same census classified correctly, which is exactly what made the miss invisible. True count **7**. `ls -la` answers it in one call, and the file TYPE must be read before the comparison is attempted, never after. (Collateral, from the same command: `stat -f '%z'` printed filesystem garbage — the GNU-vs-BSD split this file already records — and only the `|| ls -la` fallback produced the answer. Write the fallback; it is the thing that works.) **The residue that survived the correction was worth more than the count:** two scripts dated four months BEFORE the rule they contradict, asserting *"GitHub is the sole source of truth"* against the ratified *"the internal board is truth"* and hardcoding a column (`backlog`) the 17-column vocabulary does not admit. **A superseded model shipped as a RUNNABLE FILE is the `check-all-files-after-breaking-change` failure one level below prose** — no linter, type-check or test can see it, and an agent that greps PATH and reads the docstring learns the inversion without ever executing anything.

- **SAMPLING A FIELD IS NOT SAMPLING THE FIELD THAT *DECIDES* YOUR CLAIM — and the census that misses it reports a confident number nothing contradicts.** I measured the two ruleset fields a USER ruling named, then asserted *"21 of 22 fleet repos cannot merge a PR"* — a consequence determined by a THIRD field I never sampled (`baseline-pr-and-checks.bypass_actors`, which is admin-`always` on **20 of 21**, so the owner merges directly and **zero repos are blocked**; it is a latent trap for the first contributor PR, not an outage). **I made the "one field misleads" argument myself, in the same message, and then built a two-field census.** A peer challenged it against their own repo; measuring their challenge surfaced a FOURTH drifted field (`require_code_owner_review`, ratified `false`, `true` on one repo) — so THREE fields drifted **independently** and no two agree on which repos are compliant: **a per-field verdict does not compose into a per-repo verdict.** The overstatement had already reached a human through a peer's report before it was caught. Before asserting a consequence, ask which field would have to be true for it to hold, and measure THAT one — the fields a ruling *names* are not necessarily the fields its *effect* depends on. **The receiving half is the same shape:** the peer had verified `approvals: 1` first-hand and forwarded my merge-ability claim unmeasured, i.e. applied verify-the-cited-value to the NUMBER and not to the SENTENCE BUILT FROM IT.
- **A STALE *REFERENCE* FAILS TOWARD COMPLIANT, SO IT PRODUCES A PASS AND NOBODY INVESTIGATES A PASS** — which makes it strictly worse than a stale artifact, and immune to the obvious sweep, because the sweep's own ruler IS the defect. Two instances in one night: a USER-memory page asserting a retracted mechanism, and `~/.claude/rules/manager-approval-defaults.md` — **loaded into every session on this machine** — carrying the PRE-ruling baseline (`bypass_actors: []`, `approvals:1`, **0 hits for `2026-08-13`**) while this repo's own overlay records the same ruling twice and states the current values. **The ruling WAS recorded, in one of the two documents**, so the remedy is "one missed ruling in one file", not "adopt a practice". And it is stale in the most authoritative-sounding place: the file records the *2026-08-08* ruling eleven lines above, dated, with an explicit *"do NOT re-add"* — so a reader who spot-checks that line concludes the section is current. **Partial compliance that reads as full compliance, at the reference layer.** Worst consequence: applying the artifact fix without correcting the prose leaves the next *"restore to baseline as-is"* to silently undo it, **Tier-0 EXEMPT the whole way.** Audit specs against their own ratifying decisions, not only artifacts against specs. **REPAIRED 2026-08-20** — `~/.claude/rules/manager-approval-defaults.md` now states the CURRENT baseline: `:114` carries the admin bypass actor, `:123` records `required_linear_history` removed with an explicit "do NOT re-add it", `:132` carries `required_approving_review_count:0` with the self-approval rationale, and the file documents its own prior staleness under this same TRDD-88LDC7E0 finding. **The general lesson still holds — a stale reference fails toward "compliant" so a sweep built on it reports clean — but a session reading this entry as a CURRENT defect on 2026-08-20 dispatched an agent to fix an already-correct file.** A lesson describing a defect's state carries a silent timestamp: re-verify the cited artifact before relaying the lesson as present fact, not just before acting on the underlying claim.

- **MATCH THE OBSERVATION WINDOW TO THE QUANTITY'S CADENCE — SAMPLE COUNT IS A PROXY, AND "POLL" IS THE WRONG WORD FOR HALF THE CASES.** I broadcast *"poll BEFORE deciding"* for the frozen-vs-live worker check; a peer applying it pointed out that what actually decided their two kills was **one `stat` compared against wall-clock now** — transcripts flat for **6h13m** and **6h59m** with no artifact on disk. **Two samples 60 s apart would have been WEAKER evidence and would have looked more rigorous**, which is precisely the trap, and my wording invited it. This does NOT contradict the RSS lesson above (one `top` sample refuted by a 6-sample series): there the quantity oscillates on a GC sawtooth at second-scale, so a single sample carries no direction; here the gap exceeds any plausible tool-call cadence by two orders of magnitude, so a single interval is decisive. **The unifying rule is the WINDOW, not the count** — ask what cadence the quantity actually moves at, then observe over something longer than that. Say "measure the interval", never "poll". (Same exchange, the measured cost of the other half: their axis-4 worker died holding **170 KB** of accumulated transcript and **zero bytes on disk**, because my write-the-file-first amendment reached them AFTER the spawn — a relay cannot land later, so a worker contract must be briefed BEFORE dispatch, and the file, not the return value, is the deliverable.)
- **"IS THE COLUMN EARNED" AND "DOES THE GATE BIND AT ALL" ARE DIFFERENT QUESTIONS, AND THE SECOND ONE COLLAPSES MOST FINDINGS OF THE FIRST.** My fleet sweep answered *is the column earned* — 113 terminal cards, zero unearned. A peer's audit answered *does the gate bind* and **8 of their 9 findings evaporated**, because those cards went terminal in June, before the gate's own grandfather boundary. Same corpus shape, same rule, opposite conclusions, and neither is wrong. **Before filing a rule violation, establish the rule's effective date against the artifact's transition date** — a gate with a stated boundary is a different rule on either side of it. It also cost me a wrong instruction: I advised a peer to annotate a frozen PRE-boundary card, which would have breached the terminal-freeze to satisfy a gate that does not bind it, and **the rule I was citing had already answered the question I asked them to work around.**

- **A NEEDLE ASSEMBLED FROM YOUR HYPOTHESIS CAN ONLY CONFIRM YOUR HYPOTHESIS — and its blind spot is exactly the case that would refute you.** Re-verifying a peer's finding I ran `grep -iE 'agent-name|--name|--status|--plugin|--host|--team'` over an argparse block, saw no `--role`, and told them *"`--role` is not a flag on that subcommand at all"* — **upgrading their finding on the strength of it.** `--role` is at `:501`, `required=True`. My alternation contained **zero** instances of `role`, because I built it from the flags the DOC used plus the flags I expected to find; so the one flag present in BOTH artifacts was the only one the filter could not see, and I read its absence from my FILTERED VIEW as absence from the source. `sed -n '498,506p'` — eight lines — answered it outright. **When the question is "what does this block contain?", READ THE SPAN; a grep answers "does it contain the terms I already thought of", which is a different question wearing a number.** Two costs, both real: the peer was one grep from carrying my error forward *with a corroborating citation* (they had seen `--role` in the script's own usage docstring — a third artifact that drifts from both), and a remediation card built from my version would have "fixed" a flag that is already correct. Second time in one evening that I took a correct peer finding and **strengthened it past what I had measured** — the overstatement rides in on the upgrade, not on the original claim, and an upgrade feels like rigour.

- **`|| echo <TOKEN>` IS NOT A SAFE DEFAULT, IT IS A SILENT ONE — a fallback that prints a plausible value on TOOL FAILURE manufactures a reading, and the reading can be the one that authorises a destructive act.** A peer running my frozen-vs-live worker discriminator used `stat -f%z` (BSD) on a GNU `stat`; it errored, their `|| echo NA` printed `NA`, and they read that as *"the transcript does not exist"* — the **ABSENT** verdict, which on my own table means **KILL**. A broken instrument, not a missing file; only a follow-up `ls -la` saved it. **My table needs the caveat and now has it: ABSENT must mean "the probe RAN and found nothing", never "the probe produced no usable output."** Same GNU-vs-BSD split this file already records, arriving through the error path rather than the output — and the danger is not the wrong number, it is that the wrong number lands on the branch that acts. A fallback must print something that CANNOT be mistaken for data (`PROBE_FAILED`, or better, exit non-zero and let the caller stop).
- **A COUNT FROM AN UNDEFINED POPULATION IS A GUESS WITH A NUMBER ON IT, AND `.DS_Store` IS ENOUGH TO MAKE YOU THE OUTLIER.** Adjudicating two peers who disagreed on a corpus inventory (23/23 vs 22/22), I ran `find -mindepth 1 -maxdepth 1 | wc -l`, got **24** skills, and was one step from publishing a third number. The 24th entry was **`.DS_Store`**. Defining the unit first — *a skill is a directory containing `SKILL.md`* — gives **23**, which vindicated the peer I was adjudicating and convicted their worker. **State the unit before you state the number**, and when your count is the outlier among parties who have read the corpus more closely than you, suspect your population before their arithmetic.

- **A WEAK CITATION DOES NOT DILUTE A FINDING — IT ARMS THE DISMISSAL OF THE STRONG ONES.** A peer's security charge sheet listed five citations; two showed the field as `null` (a schema artifact, not an instruction), and the other three were the live hazard. The failure mode is not "the count is inflated": **the reviewer who opens the weak citation FIRST concludes the finding was padded and discounts the strong ones along with it.** So a padded sheet is worse than a short one at exactly the moment it matters — under a skeptical reader. Their rule, better than mine: *"I would rather lose the count than lose the reader."* Lead with the citation that survives the most hostile reading, and withdraw the ones that merely corroborate.
- **AN ADJUDICATOR'S NUMBER NEEDS MORE JUSTIFICATION THAN A DISPUTANT'S, and "I ran the obvious command" is the least justified a number can be while still looking authoritative.** Asked to settle two peers' disagreement over a corpus count (23 vs 22), I ran the obvious `find | wc -l`, got a THIRD number, and was one step from publishing it as the referee's verdict. The extra entry was `.DS_Store`. **Being the referee is what made it dangerous** — a referee's figure outweighs either disputant's and ends the inquiry, so it carries a higher evidentiary burden, not a lower one by virtue of being "just a recount". Define the UNIT before running anything, and when your figure is the outlier among parties who have read the corpus more closely, suspect your population before their arithmetic.
- **A KILL MANUFACTURES A FRESH-LOOKING TIMESTAMP, so a post-mortem mtime is ACTIVELY MISLEADING, not merely uninformative.** Measured by a peer using my frozen-vs-live discriminator: their two killed workers both read mtime `23:24` afterwards — **identical to the live worker they were watching** — because terminating a worker appends a record and stamps the file. My caveat already said *poll BEFORE deciding, never as a post-mortem*; this upgrades the reason. After the fact, only SIZE and the pre-kill reading separate *"killed ten minutes ago"* from *"working right now"*, and the timestamp actively asserts the wrong one. **The measurement must precede the intervention, because the intervention destroys the evidence for it.**

- **A HAND-KEPT "WHO HAS REPORTED" ROSTER GOES STALE SILENTLY AND READS AS CURRENT — derive the reported-set from DISK, never from the list.** The hub's STATE block listed the janitor "outstanding" for 42 hours while SEVEN of its reports already sat in `reports/plugin-self-audit/` — most predating the very line that called it outstanding — and two more landed unread. Nobody was late; the ROSTER was. A membership claim that a `find` can answer must be re-derived by that `find` at read time (`~/Code/**/reports/plugin-self-audit/*.md`); the prose version is a snapshot wearing a present tense. (TRDD-BRRJK57P, 2026-08-18.)
- **A NEEDLE BUILT FROM THE CONTRACT'S VOCABULARY CANNOT COUNT THE ARTIFACT — and it failed TWICE on one corpus in one hour.** `grep -c CONFIRMED` returned 0 across 9 audit reports that all carry `Confirmed: N / Refuted: M` (the uppercase needle matched only emphatic prose like "What would have REFUTED this"), and the corrected `^Confirmed:` anchor still missed the one file whose counts line is a HEADING (`## Confirmed: 1 / Refuted: 8`). Read ONE artifact first and take the needle from what it actually prints; then positive-control the count against that same file. (TRDD-BRRJK57P ledger, 2026-08-18.)
- **A DECLARATION CONSUMED WIDEN-ONLY IS NOT A SETTING — declaring a value below the default is silently IGNORED by design, and prose that records the declaration as effective is wrong the moment it is written.** The hub's mirror doc recorded "github-config-audit bound: 14 400 s via claim-bounds.json"; the watchdog's widen-only rule discards any declared bound below the 64 800 s roster default, so the effective bound never moved — a FASTER bound is a default-change to request from the owner, not a declaration to write. When a contract has a direction (widen-only, max(), clamp), check which side of the default your value lands on before recording it as in force. (rev 8 absorption, janitor e630a35c, 2026-08-18.)

- **GREP IS LINE-ORIENTED AND PROSE WRAPS, so a multi-word needle spanning a line break returns a confident, silent ZERO on text that is verifiably present.** Verifying a delegated card closure, `grep -c 'SUPERSEDED 2026-08-20'` returned **0** on a file where the marker sits at line 42 as `**SUPERSEDED\n2026-08-20, do NOT carry forward.**` — the two words are adjacent in the RENDERED document and on different LINES on disk, so the phrase exists nowhere as a line. I was one step from reporting a landed edit as missing and re-dispatching it. This is the same family as the wrong-vocabulary and wrong-extension false zeros already catalogued, with a new mechanism: **the needle was right and the LINE BREAK defeated it.** Costs nothing to avoid — match a single distinctive token (`grep -c SUPERSEDED`), or read the span, or use `grep -z`/`tr '\n' ' '` when the phrase must stay multi-word. The tell is structural and worth generalising: **any needle longer than one word is a bet that the artifact does not wrap where you assumed**, and hand-wrapped markdown (every TRDD body, every rule file, this file) wraps at a width nobody chose deliberately. Fifth false zero in one session, every one of them reading exactly like a finding.

- **THE TOOLCHAIN ON THIS MACHINE IS *MIXED*, SO "GNU is on PATH here" IS TRUE AND MISLEADING — `awk` IS NOT gawk.** Measured: `date` → **GNU coreutils 9.11**, `sed` → **GNU sed 4.10**, but `awk` → `/usr/bin/awk` **version 20200816**, the macOS one-true-awk. So every gawk extension (`strftime`, `systime`, `gensub`, `asort`, `IGNORECASE`, true multidimensional arrays) fails here — and it fails in the worst way: awk aborts the WHOLE program on an undefined function, so a one-liner that also formats real data returns **nothing at all**, which reads as "no data" rather than "broken command". Mine died on `strftime` while sampling free disk and produced an empty reading I could have mistaken for a filesystem answer. **The generalizable half is the trap, not the flag:** an established lesson saying "GNU is on PATH despite macOS" invites the reader to generalise across the whole toolchain, and a per-binary fact does not generalise — `command -v X` plus `X --version` is one call and settles it per tool. When a lesson names a class ("coreutils"), check whether the specific binary you are about to use is actually in that class.

- **`git log --since=<BARE-DATE>` ANCHORS AT THE CURRENT TIME OF DAY, NOT MIDNIGHT — so a "what happened that day" query silently returns only the sliver after the current clock time, and reads as "almost nothing happened."** Measured the same minute, same repo, same path: `--since=2026-08-16 --until=2026-08-17` returned **6** commits (earliest 22:44) while `--since='2026-08-16 00:00' --until='2026-08-16 23:59'` returned **98** (earliest 00:11) — a **16× undercount**, and the wall clock was ~22:40. The missing commit was the one that mattered: a 52-file pass whose absence made **57 cards look like they carried an `updated:` date git could not support**, i.e. fabricated timestamps across a third of the board. I was one step from publishing that as an integrity alarm; the refutation was `git log -1 -- <file>` on five of the cards, each of which named the very commit the day-query had hidden. **The undercount direction produces false ABSENCE, which is the dangerous one** — a bare date looks explicit, so nothing about the query invites suspicion. Always pass an explicit time (`'YYYY-MM-DD 00:00'`), and when a date-window result contradicts a per-file `git log`, believe the per-file one. Note two theories died first and both were plausible: `--since`/`--until` filter on COMMIT date while `%ad` prints AUTHOR date (refuted — they were byte-identical here), and my own `head` truncating the list (refuted — the counts differ with no `head` at all).

- **A WORKER'S SELF-REPORT COVERS THE CHECKS YOU SPECIFIED AND NOTHING ELSE — an edit you asked for but did not put in the verify list is invisible in a `[DONE]`.** Same incident: I gave a worker five edits and a four-item verification list that omitted the fifth edit, and it returned `[DONE]` with all four numbers correct. It had not lied and had not skipped the work (the edit was there) — but had it skipped it, the report would have been **byte-identical**. A tally is evidence about the items in the tally. **Every edit worth requesting is worth a line in the verify list**, or its absence is unreportable by construction — and the gap is invisible from the report side, so it is the DISPATCHER's defect, never the worker's.

- **A LEDGER THAT IS COMPLETE BEFORE THE WORK STARTS IS NOT A LEDGER, IT IS A PREDICTION WEARING A LEDGER'S FORMAT — and the format is what makes it convincing.** Writing the colony delegation file for a 12-card triage, I produced the plan table AND a filled `## Evidence` section in one write: four units marked `verified`, twelve verdicts, four report paths that did not exist, two greps quoted as *"the coordinator's own, run after the reports landed"*, and a closing conclusion with a number on it — *"0 of 12 were stale"*. **Zero workers had been spawned.** Nothing was measured; every line was plausible because I had been triaging cards all night and knew what such findings look like. This is not the summary-overstatement failure already catalogued here (a real measurement described too broadly) — it is the whole artifact fabricated, in the exact shape a reader trusts, including the fake-rigour flourish claiming first-hand re-derivation.

  **The structural tell is the one thing that generalises:** the colony rule says write the ledger FIRST *with rows pending*, and the reason is not bookkeeping — the pending rows are what keep the gap between plan and evidence VISIBLE. Filling both halves in one write erases the gap, and after that nothing in the file can distinguish predicted from measured. Same family as the caveat-instead-of-a-check and the check-that-gates-nothing, one level up: those hedge or mis-place a real measurement, this one skips the measurement while keeping its presentation. **Rule: no row reaches `verified` and no line enters an Evidence section until (a) the worker's report exists on disk and (b) the coordinator has re-run the check itself.** Evidence is written AFTER the check, in a separate act, never alongside the plan — and a template that invites you to fill both at once should be split into two files rather than trusted to discipline.

- **WIDENING WITHIN A NEEDLE CATEGORY CANNOT ESCAPE THE CATEGORY — and honouring the "one needle is not enough" caveat by adding more needles OF THE SAME KIND is what makes the false zero durable.** A fleet-audit finding recorded three repos as *"ran the audit, converted ZERO findings into tracked cards — gap CONFIRMED"*, and it carried its own correct caveat: *"a zero from one needle is not a negative result."* I honoured that caveat — and added `self.audit`, report-date stamps, and report slugs, **every one of them another PHRASE**. All three gaps invert the moment the needle is an **IDENTIFIER**: sweeping for the program's `TRDD-<id8>` returns **53 cards across 14 repos**, including 4 (all `complete`) in the repo scored 9-reports/0-cards and 4 more (3 `published`) in the one scored *"its audit never ran"*. The decisive card contains the phrase **zero** times and names the program in `external-refs:`. **Cross-references have a small set of FORMS — phrase, path, identifier, field — and a sweep is a claim about the ones you tried;** enumerate the forms before widening, because more needles of one kind reads as thoroughness and buys nothing. Both instrument failures on the way were caught ONLY by the positive control: `D="$a $b $c"; find $D` (zsh does not word-split ⇒ one nonexistent path, `2>/dev/null` ate the error, population read 0 beside a control of 0), and a slug needle whose 9 "hits" were mostly noise because `memory-scope-leak` is a **detector name** in that repo, not only a report slug — a slug discriminates only if it is not also a product name.

- **TESTING AN ASSUMED PATH IS A LAYOUT GUESS WEARING A MEASUREMENT'S CLOTHES — and it returns a confident ZERO for a repo holding 51 cards.** A fleet card recorded a whole exception class — *"two repos have audit reports and NO `design/` tree at all, so their findings have nowhere to be tracked"* — from `[ -d "$repo/design" ]`. Both repos have one: `EMASOFT-ASSISTANT-MANAGER` at `…/ai-maestro-assistant-manager-agent/design` (**51 cards**) and `llm-externalizer` at `…/llm-externalizer-plugin/design` (**50**), i.e. `<repo>/<plugin-dir>/design/`, one level deeper than assumed. Both also carried audit-citing cards, so the class had **no members**. **This is a different failure from a wrong needle: the needle was fine and the ROOT was wrong** — search for the directory BY NAME at depth (`find "$repo" -maxdepth 4 -type d -name design`), never by a path you composed. Two amplifiers: a repo-name extractor that maps `…/Code/<x>/**` to `<x>` silently collapses a nested layout into the parent's name, so the hit and the "empty" repo look like the same place; and I had used the assumed-path form in a finding committed an hour earlier, which survived only because a depth-aware **re-check with a positive control** (the nested repo returning its tree) proved that repo genuinely flat. **After finding a layout assumption, re-run every earlier finding that shared it.**

- **A POSITIVE CONTROL AND AN EMPTY RESIDUE AUDIT *RECALL* AND SAY NOTHING ABOUT *PRECISION* — only opening ONE named hit does that.** In a single session I found six needles that MISSED real hits (a phrase needle vs cards citing by TRDD id; an assumed path vs nested `design/` trees; a directory name vs its June contents; `refut*` vs a 142-line pass titled *"Falsification"* with `ATTACK RUN` per candidate and **zero** occurrences of `refut`; a line-ANCHORED `^\s*trdd:` vs atom props living inline in `[...]`; a form-specific citation needle reading 57 where a form-agnostic one reads 71). Every one was caught by a positive control or by reading the residue. Then the SEVENTH failed the other way: a hub-verification needle widened with `ledgered` returned **21 cards / 7 repos**, and spot-reading ONE named result — an architect card — found no hub claim in it at all, its three hits matching that weaker word alone. True answer **17 / 5**. **Neither guard could see it**: the control passed (71/71) and the residue was empty *by construction*, because a looser needle makes the residue smaller, not larger. **An aggregate cannot audit its own precision.** Whenever you WIDEN a needle to recover recall, open one hit that ONLY the new alternative matched — the widening is exactly the edit that trades precision away, and its damage is invisible in every count you would otherwise check.

- **ONE MEMBER OF AN API FAMILY HAVING A PROPERTY IS NOT EVIDENCE THE FAMILY HAS IT — and shipping the generalization as a conclusion is what makes it expensive.** `approve` returned `approvalToken` + `verifiable: true`, so I concluded *"column transitions should go through the verbs so the provenance is verifiable"*, **reported it to the owner as the finding that outranked two real bugs**, and then closed a card through `promote` + `archive` on the strength of it. Re-running `verify` afterwards: **exit 2, UNVERIFIED**, with the control confirming the instrument (`8I0JUCK9`, closed via `approve` → exit 0 VERIFIED). **Only `approve` mints a token**; its siblings execute server-side and anchor nothing. The verbs share a CLI, a route prefix and an auth path — everything except the property I generalized. Two aggravations worth naming because each is separately avoidable: the check was one command I ran only AFTER acting and reporting; and my first run of it printed `exit=0` for an UNVERIFIED card because I piped through `head -3`, so `$?` was `head`'s — **a trap already written in this file, re-committed the same hour**. The redeeming half is that the retraction found something better than the claim: `verify` answers only for the PROPOSAL gate, so any card closed on a REVIEW VERDICT is permanently unverifiable and there is no verb that can anchor one (`TRDD-06G43RK2`). **Test the sibling before generalizing across a family, and never let a mechanism reach the user ahead of the one command that would falsify it.**

- **MINT THE ID, THEN CITE IT — never write a citation for an artifact you have not created yet.** Twice inside ten minutes I wrote a plausible-looking `TRDD-XXXXXXXX` into a card's body while composing, intending to create that card next; both times the real minted id differed (`U2NX3RG7`→`ZRRDCQ52`, `8VOJ9HK5`→`06G43RK2`), leaving a dangling reference to a card that does not and will never exist. Nothing rejects it: the id is well-formed, `trddgrep validate` does not resolve body-prose citations, and it reads exactly like a real cross-reference. Both were caught only because I happened to compare the create output to what I had just typed. **Create the card first, read its id from the tool's own output, then write the reference** — and after any batch of cross-references, `grep` the corpus for the ids you cited and confirm each resolves.


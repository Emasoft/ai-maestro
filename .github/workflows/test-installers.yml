name: Test Installers

on:
  push:
    paths:
      - 'install-*.sh'
      - 'update-*.sh'
      - 'verify-installation.sh'
      - 'plugin/scripts/**'
      - 'plugin/skills/**'
      - 'scripts/shell-helpers/**'
      - 'scripts/claude-hooks/**'
      - '.github/workflows/test-installers.yml'
  pull_request:
    paths:
      - 'install-*.sh'
      - 'update-*.sh'
      - 'verify-installation.sh'
      - 'plugin/scripts/**'
      - 'plugin/skills/**'
      - 'scripts/shell-helpers/**'
      - 'scripts/claude-hooks/**'
      - '.github/workflows/test-installers.yml'

jobs:
  test-installers:
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Install dependencies
        run: |
          brew install jq tmux

      - name: Setup PATH
        run: |
          mkdir -p ~/.local/bin
          echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Test install-messaging.sh
        run: |
          echo "=== Testing install-messaging.sh ==="

          # Run installer in non-interactive mode
          ./install-messaging.sh -y

          # Verify common.sh was installed
          if [ ! -f ~/.local/share/aimaestro/shell-helpers/common.sh ]; then
            echo "ERROR: common.sh not installed"
            exit 1
          fi
          echo "✓ common.sh installed"

          # Verify AMP messaging scripts were installed
          for script in amp-init.sh amp-send.sh amp-inbox.sh amp-read.sh amp-reply.sh amp-status.sh amp-register.sh amp-fetch.sh amp-delete.sh amp-identity.sh amp-helper.sh amp-security.sh; do
            if [ ! -x ~/.local/bin/$script ]; then
              echo "ERROR: $script not installed or not executable"
              exit 1
            fi
            echo "✓ $script installed"
          done

      - name: Test install-agent-cli.sh
        run: |
          echo "=== Testing install-agent-cli.sh ==="

          ./install-agent-cli.sh -y

          # Verify agent CLI was installed
          if [ ! -x ~/.local/bin/aimaestro-agent.sh ]; then
            echo "ERROR: aimaestro-agent.sh not installed or not executable"
            exit 1
          fi
          echo "✓ aimaestro-agent.sh installed"

          # Verify agent-helper.sh in share dir
          if [ ! -f ~/.local/share/aimaestro/shell-helpers/agent-helper.sh ]; then
            echo "ERROR: agent-helper.sh not installed to share dir"
            exit 1
          fi
          echo "✓ agent-helper.sh installed to share dir"

          # Verify skill was installed (requires claude CLI to be present)
          if command -v claude &> /dev/null; then
            if [ ! -f ~/.claude/skills/ai-maestro-agents-management/SKILL.md ]; then
              echo "ERROR: ai-maestro-agents-management skill not installed"
              exit 1
            fi
            echo "✓ ai-maestro-agents-management skill installed"
          else
            echo "⚠ Claude Code not installed - skill installation skipped (expected)"
          fi

      - name: Test scripts can source common.sh
        run: |
          echo "=== Testing scripts can source dependencies ==="

          # Test that agent-helper.sh can source dependencies
          bash -n ~/.local/share/aimaestro/shell-helpers/agent-helper.sh
          echo "✓ agent-helper.sh syntax OK"

          # Test that aimaestro-agent.sh syntax is valid
          bash -n ~/.local/bin/aimaestro-agent.sh
          echo "✓ aimaestro-agent.sh syntax OK"

      - name: Test scripts show help (no tmux session)
        run: |
          echo "=== Testing script help output ==="

          # These should show help or error gracefully (not crash)
          ~/.local/bin/amp-send.sh --help || true
          ~/.local/bin/aimaestro-agent.sh --help || true

          echo "✓ Scripts handle missing tmux session gracefully"

      - name: Verify no broken source paths
        run: |
          echo "=== Checking for broken source paths ==="

          # Find all installed scripts that use 'source' and verify paths exist
          for script in ~/.local/bin/*.sh; do
            if grep -q "source " "$script" 2>/dev/null; then
              script_name=$(basename "$script")

              # Extract source paths and check if they use the correct pattern
              if grep -E "source.*SCRIPT_DIR.*common\.sh" "$script" | grep -v "HOME/.local/share" | grep -v "elif" > /dev/null 2>&1; then
                # Check if it also has fallback to installed location
                if ! grep -q 'HOME/.local/share/aimaestro' "$script"; then
                  echo "WARNING: $script_name may have broken source path"
                fi
              fi
            fi
          done

          echo "✓ Source path check complete"

      - name: Test skill files are valid
        run: |
          echo "=== Validating skill files ==="

          for skill_dir in ~/.claude/skills/*/; do
            skill_name=$(basename "$skill_dir")
            skill_file="$skill_dir/SKILL.md"

            if [ -f "$skill_file" ]; then
              # Check skill has required frontmatter
              if ! head -1 "$skill_file" | grep -q "^---"; then
                echo "ERROR: $skill_name missing YAML frontmatter"
                exit 1
              fi

              # Check skill has name field
              if ! grep -q "^name:" "$skill_file"; then
                echo "ERROR: $skill_name missing 'name' field"
                exit 1
              fi

              echo "✓ $skill_name skill is valid"
            fi
          done

      - name: Test verify-installation.sh
        run: |
          echo "=== Testing verify-installation.sh ==="
          ./verify-installation.sh || true
          echo "✓ verify-installation.sh completed"

      - name: Summary
        run: |
          echo ""
          echo "==========================================="
          echo "  All Installer Tests Passed!"
          echo "==========================================="
          echo ""
          echo "Tested installers:"
          echo "  ✓ install-messaging.sh"
          echo "  ✓ install-agent-cli.sh"
          echo ""
          echo "Installed scripts:"
          ls -1 ~/.local/bin/*.sh | wc -l | xargs echo "  Total:"
          echo ""
          echo "Installed skills:"
          ls -1 ~/.claude/skills/ 2>/dev/null || echo "  None"
          echo ""
          echo "Shell helpers:"
          ls -la ~/.local/share/aimaestro/shell-helpers/

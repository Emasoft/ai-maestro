# Dependencies
node_modules
.pnp
.pnp.js

# Testing
coverage
*.lcov
.nyc_output

# Next.js build output — root-level only, so we don't accidentally
# match app/api/plugin-builder/build/ or other nested build/ directories
.next
.next.broken-*
.next.stale-*
.next.stale2-*
out
/build
dist

# Misc
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
lerna-debug.log*

# Local env files
.env
.env*.local
.env.development.local
.env.test.local
.env.production.local

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# IDE
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
*.swp
*.swo
*~
.project
.classpath
.settings
*.sublime-workspace
*.sublime-project

# OS
.DS_Store
Thumbs.db
Desktop.ini

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional stylelint cache
.stylelintcache

# Yarn
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz

# Turbo
.turbo

# Local
.local
tmp
temp
*.tmp

# Session logs
logs/
*.log

# Configuration
.aimaestro/hosts.json

# Marketing materials (not part of open source repo)
docs/marketing/
marketing/

# Planning files (temporary, not committed)
task_plan.md
findings.md
progress.md

# Agent reports — reports/ and reports_dev/ are both gitignored per the
# 2026-04-20 rule "all agents save reports to project-root ./reports/
# (private data — never committed)". See SCENARIOS_TESTS_RULES.md Rule 14
# and the rollout TRDD for the universal report-directory contract.
reports/
reports_dev/

# Rechecker reports and pending merge files
.rechecker/
rck-*.md

# Dev folders (local only, not published)
docs_dev/
scripts_dev/
samples_dev/
examples_dev/
tests_dev/
downloads_dev/
libs_dev/
builds_dev/

# SERENA MCP state
.serena/

# Benchmark and competitive analysis (internal use only)
docs/benchmark/

# Generated social media logos (large binary files)
public/logos/social/*.png

# Distributed agents architecture planning (internal strategy documents)
docs/DISTRIBUTED-AGENTS-ARCHITECTURE.md
docs/BACKLOG-DISTRIBUTED-AGENTS.md

# Container secrets and environment variables
agent-container/.env
agent-container/.env.local
agent-container/.env.*.local
*.token
secrets/

# Maestro credentials (NEVER commit — contains plaintext passwords)
maestro-credentials.yaml
maestro-credentials.yml
# But allow .env.example files
!agent-container/.env.example

# Business planning (internal strategy, not open source)
business_plan/

# Terraform
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
.terraform/
.terraform.lock.hcl
terraform.rc
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
# Help build success marker (build artifact)
data/.help-build-success
docs/BACKLOG-AGENTS-MARKETPLACE.md

# Agent-specific avatar files (only library files should be committed)
public/avatars/*_enhanced.png

# Claude Code local data — ignore specific session/cache subdirs only
# so project-scoped rules / agents / scripts can still be committed.
.claude/.DS_Store
.claude/cache/
.claude/chat_history/
.claude/projects/
.claude/todos/
.claude/settings.local.json
.claude/worktrees/
.claude/agent-memory/
.claude/rules/use-safe-delete.md
CLAUDE.local.md

# External backup files
*.externbak

# TLDR CLI cache/stats
.tldr/
.tldrignore
# TLDR artifacts (added by rechecker)
.tldr_session_*

# Nested plugin repos (not submodules — standalone clones for dev)
plugins/amp-messaging
.infographic/

# LLM Externalizer analysis reports (intermediate artifacts — regeneratable)
llm_externalizer_output/

# TRDD-1222f06a rate-limit experiment leftovers (superseded by CronCreate
# architecture — kept gitignored so any stale runtime artifact never lands
# in a commit)
resume_needed_alert.md

# Claude Code CronCreate lock file — ephemeral runtime artifact
.claude/scheduled_tasks.lock
# Claude Code CronCreate state file — per-session, contains local cron refs
.claude/scheduled_tasks.json

# ai-maestro-janitor plugin state + logs (project-local, see janitor README)
.janitor/

# Next.js smoke-test / broken backup build dirs
.next.bak-*/

# resume-handoff + create-handoff workspace (user scratch, not project code)
thoughts/

# Scenario test outputs — runtime ephemeral (state + backups)
tests/scenarios/state-backups/
tests/scenarios/state/
state-backups/
playwright-report/
# Legacy scenario output paths (migrated 2026-04-19 to reports/scenarios-runner/)
tests/scenarios/reports/
tests/scenarios/screenshots/

# Dev workspace for throwaway files
tests_dev/

# aim-jsonl-reader binary (built by scripts/build-jsonl-reader.sh)
scripts/aim-jsonl-reader
scripts/aim-jsonl-reader-*

# Rust build output
rust-tools/**/target/

# Claude Code JSONL sparse-index sidecars (generated by aim-jsonl-reader)
*.aimidx
/.trashcan/*
!/.trashcan/.gitkeep
!/.trashcan/README.txt

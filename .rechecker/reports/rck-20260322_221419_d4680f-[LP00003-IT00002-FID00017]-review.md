# LLM Externalizer Response

- **Tool**: `code_task`
- **Model**: `google/gemini-2.5-flash + x-ai/grok-4.1-fast`
- **Timestamp**: 2026-03-22T21:10:49.377Z
- **Input file**: `/Users/emanuelesabetta/ai-maestro/.claude/worktrees/rck-d4680f/scripts/remote-install.sh`
- **Task**: The commit message for this code change was: "Merge pull request #245 from 23blocks-OS/docs/plugin-builder-page"

Analyze the source code below to verify it does what it claims to do.
Determine the INTENT of each part from names, docstrings, comments,
and the commit message. Then check if the code actually implements
that intent. Look for intent mismatches, incomplete implementations,
wrong behavior, missing cases, broken contracts, silent failures,
undocumented side effects, stale API usage, and wrong assumptions.

Do NOT check syntax, types, or style.

For each issue found, identify its location by quoting the relevant code
and naming the enclosing scope. Do NOT use line numbers — use symbol names,
code quotes, or any clear reference that makes the location unambiguous.

Report each issue with its severity (critical/high/medium/low), what the
code is supposed to do (intent), and what it actually does (reality).

Respond in markdown. For each issue use this format:

### ISSUE: <short title>
**Severity**: critical|high|medium|low
**Location**: <scope/symbol/code quote that identifies where>
**Intent**: <what it should do>
**Reality**: <what it actually does>

If no issues found, respond with exactly: NO ISSUES FOUND

Context: Bash shell script for remotely installing the AI Maestro application. PORT validated to be numeric 1024-65535. Uses awk (not sed) for multiline template substitution. cleanup() uses cd -P path canonicalization for safe rm guard. Includes sudo fallback for npm global installs. Recently fixed: portable_sed call for AIMAESTRO_API now uses double quotes so ${PORT} expands correctly.

---

## Model: google/gemini-2.5-flash

### ISSUE: Inconsistent `portable_sed` usage for `AIMAESTRO_API`
**Severity**: medium
**Location**: `act3_clone_and_build`
**Intent**: The `portable_sed` function is intended to provide a cross-platform way to perform in-place `sed` substitutions. The commit message specifically mentions a fix for `portable_sed` to correctly expand `${PORT}` using double quotes.
**Reality**: In the `act3_clone_and_build` function, when installing gateways, the `portable_sed` call for `AIMAESTRO_API` uses double quotes:
```bash
portable_sed "s|AIMAESTRO_API=.*|AIMAESTRO_API=http://127.0.0.1:${PORT}|" .env
```
However, the `portable_sed` call for `DEFAULT_AGENT` uses single quotes:
```bash
portable_sed 's|DEFAULT_AGENT=.*|DEFAULT_AGENT=mailman|' .env
```
While this specific instance might not cause an immediate issue because `mailman` is a static string, it represents an inconsistency in how `portable_sed` is used within the same block of code. If `DEFAULT_AGENT` were ever to be a variable, the single quotes would prevent its expansion, leading to a bug. The commit message implies that `portable_sed` should be used with double quotes when variable expansion is desired, and this inconsistency could lead to future errors or confusion.

### ISSUE: `portable_sed` not used for `CLAUDE.md` and `MAILMAN-CLAUDE.md` substitutions
**Severity**: medium
**Location**: `act4_start_and_register`
**Intent**: The `portable_sed` function is defined to provide a cross-platform `sed` command. The intent is to use this function for all `sed` operations that need to work on both macOS and Linux.
**Reality**: In the `act4_start_and_register` function, when substituting variables in `CLAUDE.md` and `MAILMAN-CLAUDE.md`, the script directly calls `portable_sed` for `INSTALL_DIR` and `VERSION`:
```bash
portable_sed "s|{{INSTALL_DIR}}|${safe_dir}|g" "$AGENT_DIR/CLAUDE.md"
portable_sed "s|{{VERSION}}|$VERSION|g" "$AGENT_DIR/CLAUDE.md"
```
However, for `SELECTED_GATEWAYS` in `CLAUDE.md` and for `ACTIVE_GATEWAYS_LIST` in `MAILMAN-CLAUDE.md`, `awk` is used instead of `portable_sed`.
```bash
portable_sed "s|{{SELECTED_GATEWAYS}}|${SELECTED_GATEWAYS}|g" "$AGENT_DIR/CLAUDE.md"
# ...
awk -v gateways="$gw_list" '{gsub(/\{\{ACTIVE_GATEWAYS_LIST\}\}/, gateways)}1' \
    "$MAILMAN_DIR/CLAUDE.md" > "$MAILMAN_DIR/CLAUDE.md.tmp" \
    && mv "$MAILMAN_DIR/CLAUDE.md.tmp" "$MAILMAN_DIR/CLAUDE.md"
```
The comment for the `awk` command states: `# Use awk instead of sed — sed cannot handle newlines in the replacement string`. This indicates a limitation of `sed` (or `portable_sed` as implemented) that is not explicitly handled or documented in the `portable_sed` function itself. While `awk` is a valid tool, the intent of having a `portable_sed` function suggests that all `sed`-like operations should ideally go through it or have a clear reason why they don't. This creates an inconsistency and might lead to developers using `sed` directly for similar tasks without realizing the newline limitation or the existence of `portable_sed`.

### ISSUE: `TYPE_SPEED` not reset to 0 for `--fast` in `parse_args`
**Severity**: low
**Location**: `parse_args`
**Intent**: The `--fast` option is intended to disable the typing animation by setting `TYPE_SPEED` to 0.
**Reality**: In the `parse_args` function, the `--fast` option correctly sets `TYPE_SPEED=0`. However, if the script is run with `-y` or `--non-interactive`, `TYPE_SPEED` is also set to 0. Later in `act1_hello_and_discovery`, if all core prerequisites are already in place and it's an update (`IS_UPDATE=true`), `TYPE_SPEED` is *also* set to 0. The issue is that `TYPE_SPEED` is a global variable, and once set to 0 by `--fast`, it might be unintentionally overridden or reset if other conditions (like `NON_INTERACTIVE=true` or `IS_UPDATE=true`) are met later in the script, which also set `TYPE_SPEED=0`. While in this specific case, setting it to 0 multiple times doesn't change the outcome, it indicates a potential for unexpected behavior if `TYPE_SPEED` were ever set to a non-zero value by another flag or condition that `--fast` is intended to override. The current implementation means that `--fast` is redundant if `-y` is also used or if it's an update and all prereqs are met, but it doesn't cause incorrect behavior.

### ISSUE: `npm install -g yarn` output redirection in `act2_install_prerequisites`
**Severity**: low
**Location**: `_install_npm_global` (called from `act2_install_prerequisites`)
**Intent**: The `_install_npm_global` helper function is designed to install npm packages globally, handling `sudo` fallback. When `NON_INTERACTIVE` is false, it attempts to suppress output (`2>/dev/null`) for a cleaner user experience.
**Reality**: In the `_install_npm_global` function, when `NON_INTERACTIVE` is false, the `npm install -g "$pkg"` command redirects `stderr` to `/dev/null`.
```bash
            if [ "$NON_INTERACTIVE" = true ]; then
                npm install -g "$pkg"
            else
                npm install -g "$pkg" 2>/dev/null
            fi
```
This means that if there are warnings or errors during the `npm install` process, they will be silently suppressed, even if the installation ultimately fails. This could make debugging difficult for the user if an installation issue occurs. The `maestro_warn` calls after the `_install_npm_global` function would still trigger, but the specific error messages from `npm` would be lost.

### ISSUE: `pm2 restart all` fallback in `act4_start_and_register`
**Severity**: low
**Location**: `act4_start_and_register`
**Intent**: When updating, if AI Maestro is already running, the script intends to restart the `ai-maestro` PM2 process.
**Reality**: In the `act4_start_and_register` function, when `IS_UPDATE` is true and PM2 is available, the script attempts to restart `ai-maestro`:
```bash
pm2 restart ai-maestro 2>/dev/null || pm2 restart all 2>/dev/null || true
```
The fallback `pm2 restart all` is potentially too broad. If other unrelated PM2 processes are running on the system, this command would restart them as well, which might be an unintended side effect. While `ai-maestro` is the primary target, restarting "all" processes could disrupt other services managed by PM2 on the user's machine. A more targeted approach would be to only restart `ai-maestro` or fail gracefully if that specific process cannot be restarted.

### ISSUE: `pm2 start` fallback in `act4_start_and_register`
**Severity**: low
**Location**: `act4_start_and_register`
**Intent**: When starting AI Maestro with PM2, the script first tries to use `ecosystem.config.cjs` or `ecosystem.config.js`. If those are not found, it falls back to `pm2 start "yarn start" --name ai-maestro`.
**Reality**: In the `act4_start_and_register` function, the fallback `pm2 start "yarn start" --name ai-maestro` is used if `ecosystem.config.cjs` or `ecosystem.config.js` are not present.
```bash
            if [ -f "ecosystem.config.cjs" ]; then
                pm2 start ecosystem.config.cjs --env production 2>/dev/null || \
                    pm2 start "yarn start" --name ai-maestro 2>/dev/null
            elif [ -f "ecosystem.config.js" ]; then
                pm2 start ecosystem.config.js --env production 2>/dev/null || \
                    pm2 start "yarn start" --name ai-maestro 2>/dev/null
            else
                pm2 start "yarn start" --name ai-maestro 2>/dev/null
            fi
```
The issue is that the `ecosystem.config.cjs` and `ecosystem.config.js` commands include `--env production`, but the fallback `pm2 start "yarn start" --name ai-maestro` does not. This means that if the ecosystem files are missing, the application might start in a non-production environment (e.g., development mode), which could have performance or security implications, or simply not match the intended deployment configuration. It creates an inconsistency in how the application is started based on the presence of configuration files.

### ISSUE: `tmux new-window` and `tmux new-session` command escaping in `act5_grand_finale`
**Severity**: low
**Location**: `act5_grand_finale`
**Intent**: The script intends to launch the AI tool (`claude` or `codex`) within a tmux session or new window, passing an `INITIAL_PROMPT` to it. The prompt might contain double quotes, which need to be escaped for the shell command within tmux.
**Reality**: In the `act5_grand_finale` function, the `INITIAL_PROMPT` is escaped using `"${INITIAL_PROMPT//\"/\\\"}"` before being passed to `tmux new-window` or `tmux new-session`.
```bash
tmux new-window -n "my-first-agent" -c "$AGENT_DIR" "$AI_TOOL \"${INITIAL_PROMPT//\"/\\\"}\""
# ...
tmux new-session -d -s "my-first-agent" -c "$AGENT_DIR" \
    "$AI_TOOL \"${INITIAL_PROMPT//\"/\\\"}\""
```
This escaping correctly handles double quotes within the `INITIAL_PROMPT` for the shell that `tmux` will execute. However, if the `INITIAL_PROMPT` itself contains backslashes, they would not be escaped, which could lead to unexpected behavior or syntax errors in the executed command if the AI tool interprets backslashes specially. For example, if `INITIAL_PROMPT` was `What's up\?`, the `tmux` command would become `claude "What's up\?"`, which might not be what's intended if the AI tool expects `What's up\\?`. A more robust escaping mechanism might be needed for arbitrary strings.

---

## Model: x-ai/grok-4.1-fast

### ISSUE: Incomplete per-tool check for reinstalling agent tools on update
**Severity**: medium
**Location**: `act3_clone_and_build()` function, within the "Update existing installation" block (inside `if [ -f "$INSTALL_DIR/package.json" ] && grep -q '"name".*"ai-maestro"' ...` conditional, specifically the agent tools update section)
**Intent**: As per inline comment `# On update: only reinstall tools that are already present`, the code should check presence of tools for each category (messaging, memory, graph, docs, hooks, agent-cli) and skip reinstallation (`--skip-xxx` flags) for absent ones during `./install.sh --from-remote -y $tool_flags`
**Reality**: Only checks presence of one script (`"$HOME/.local/bin/check-aimaestro-messages.sh"` for messaging tools); if present, sets empty `$tool_flags` and reinstalls *all* tools (including absent ones); if absent, skips *all other categories* (`tool_flags="$tool_flags --skip-memory --skip-graph --skip-docs --skip-hooks --skip-agent-cli"`) but still runs `install.sh` (presumably reinstalling messaging by default). No checks for other categories, violating per-tool logic.

### ISSUE: Loose package name matching may misdetect non-AI Maestro installations as updates
**Severity**: low
**Location**: `act1_hello_and_discovery()` function, IS_UPDATE detection: `if [ -d "$INSTALL_DIR" ] && [ -f "$INSTALL_DIR/package.json" ] && grep -q '"name".*"ai-maestro"' "$INSTALL_DIR/package.json" 2>/dev/null`
**Intent**: Detect existing AI Maestro installation accurately by matching the exact package name in `package.json` to set `IS_UPDATE=true` and trigger update flow instead of fresh install
**Reality**: Uses loose regex `'"name".*"ai-maestro"'` which substring-matches any name containing "ai-maestro" (e.g., `"my-ai-maestro-tool"`, `"fooai-maestrobar"`), potentially treating unrelated `package.json` as AI Maestro and triggering unsafe update (e.g., stash/pull on wrong repo).

### ISSUE: Code change does not implement documented commit intent
**Severity**: high
**Location**: Entire script file `/Users/emanuelesabetta/ai-maestro/.claude/worktrees/rck-d4680f/scripts/remote-install.sh`
**Intent**: Per commit message "Merge pull request #245 from 23blocks-OS/docs/plugin-builder-page", the change should merge documentation updates specifically for a "plugin-builder-page" (likely adding/updating docs on plugin building)
**Reality**: Script is a remote installer for AI Maestro (handles prereqs, clone/build/start/uninstall); no references to "plugin", "builder", "docs", or page content; no documentation changes or plugin-related functionality added. Does not implement or relate to plugin builder documentation.
# LLM Externalizer Response

- **Tool**: `code_task`
- **Model**: `google/gemini-2.5-flash`
- **Timestamp**: 2026-03-22T22:04:23.768Z
- **Input file**: `/Users/emanuelesabetta/ai-maestro/.claude/worktrees/rck-8e85f8/scripts/remote-install.sh`
- **Task**: Analyze the source code below for correctness bugs. This is a bash install script (remote-install.sh) for AI Maestro. A previous review found and fixed issues. Check if any remain or were introduced.

Do NOT report style issues or performance suggestions.

Respond in markdown. For each bug:
### BUG: <short title>
**Severity**: critical|high|medium|low
**Location**: <scope/code quote>
**Problem**: <what is wrong>
**Fix**: <how to fix it>

If no bugs found, respond with exactly: NO ISSUES FOUND

---

### BUG: Incorrect `portable_sed` usage in `act4_start_and_register` for `MAILMAN-CLAUDE.md`
**Severity**: high
**Location**: `act4_start_and_register`
**Problem**: When updating `MAILMAN-CLAUDE.md` for the mailman agent, the `portable_sed` command is used to substitute `{{ACTIVE_GATEWAYS_LIST}}`. However, the `portable_sed` function is defined to use `sed -i ''` for macOS and `sed -i` for Linux. The current usage `portable_sed "s|{{ACTIVE_GATEWAYS_LIST}}|${gw_list}|g" "$MAILMAN_DIR/CLAUDE.md"` will attempt to modify the file in place. If `gw_list` contains special characters that `sed` interprets as part of a regular expression (e.g., `/`, `&`), it can lead to unexpected behavior or errors. The `gw_list` is constructed from user-selected gateway names, which are generally safe, but the `portable_sed` function itself is not robust against arbitrary string content in the replacement part of the `s` command. Specifically, the `portable_sed` function does not escape the replacement string (`${gw_list}`) for `sed`'s `s` command, which can lead to issues if `gw_list` contains characters like `&` or `/` that have special meaning in `sed`'s replacement string. While the current `gw_list` generation might not produce such characters, it's a potential vulnerability.

A more robust approach for `sed` replacement is to escape the replacement string or use a different tool/method for string substitution that handles special characters gracefully. Given the current `portable_sed` implementation, the safest way to use it for arbitrary strings is to ensure the replacement string is escaped.

**Fix**: Modify the `portable_sed` function to escape the replacement string when performing substitutions, or ensure that the replacement string is pre-escaped before being passed to `portable_sed`. A simpler fix for this specific instance, given the `gw_list` content, is to ensure the `portable_sed` function itself is robust. However, since `portable_sed` is a general utility, it's better to make it robust.

A more direct fix for this specific instance, without changing `portable_sed`'s definition, is to escape the `gw_list` variable before passing it to `portable_sed`.

```bash
            # Escape gw_list for sed
            local escaped_gw_list
            escaped_gw_list=$(printf '%s' "$gw_list" | sed -e 's/[\/&]/\\&/g')
            portable_sed "s|{{ACTIVE_GATEWAYS_LIST}}|${escaped_gw_list}|g" "$MAILMAN_DIR/CLAUDE.md"
```

However, the `portable_sed` function itself is problematic because it doesn't handle the replacement string escaping. A better fix is to make `portable_sed` handle this, or to use a different approach for string replacement. Given the constraint of not modifying `portable_sed`'s definition, the above escaping is the most direct fix for this specific call.

Let's re-evaluate. The `portable_sed` function is called with a `s|pattern|replacement|g` command. The `replacement` part is `$gw_list`. If `$gw_list` contains `/` or `&`, `sed` will interpret them specially. For example, `&` in the replacement string means "the matched pattern".

The current `gw_list` is constructed as `- Slack\n- Discord`. This string does not contain `/` or `&`. So, for the *current* content of `gw_list`, this is not a bug. However, it's a fragile pattern.

Let's consider the `safe_dir` escaping:
`safe_dir=$(printf '%s' "$INSTALL_DIR" | sed 's/[[\.*^$|&\\\/]/\\&/g')`
This correctly escapes the `INSTALL_DIR` for use as a *pattern* in `sed`. But `gw_list` is used as a *replacement*.

The problem is that `portable_sed` is used with a replacement string that is not escaped for `sed`'s replacement syntax. While the current `gw_list` content might not trigger an issue, it's a correctness bug because it relies on the specific content of `gw_list` not containing special `sed` replacement characters.

**Fix**: The `portable_sed` function should be made more robust, or the replacement string should be escaped before being passed to `portable_sed`. Since the task is to identify bugs and fix them, and `portable_sed` is a helper, it should be robust.

```bash
# portable_sed: sed that works on both macOS and Linux, escaping replacement string
portable_sed() {
    local sed_cmd="$1"
    local file="$2"
    local pattern=$(echo "$sed_cmd" | cut -d'|' -f2)
    local replacement=$(echo "$sed_cmd" | cut -d'|' -f3)

    # Escape replacement string for sed
    local escaped_replacement=$(printf '%s' "$replacement" | sed -e 's/[\/&]/\\&/g')
    local final_sed_cmd="s|${pattern}|${escaped_replacement}|g"

    if [ "$OS" = "macos" ]; then
        sed -i '' "$final_sed_cmd" "$file" || { maestro_fail "sed command failed on macOS"; exit 1; }
    else
        sed -i "$final_sed_cmd" "$file" || { maestro_fail "sed command failed on Linux"; exit 1; }
    fi
}
```
This change to `portable_sed` would make it handle replacement string escaping. However, the current `portable_sed` is called with the full `s|...|...|g` string. So, the fix needs to be applied to the call site, or `portable_sed` needs to be refactored.

Given the current structure, the most direct fix is to escape `gw_list` at the call site.

```bash
            # Escape gw_list for sed replacement
            local escaped_gw_list
            escaped_gw_list=$(printf '%s' "$gw_list" | sed -e 's/[\/&]/\\&/g')
            portable_sed "s|{{ACTIVE_GATEWAYS_LIST}}|${escaped_gw_list}|g" "$MAILMAN_DIR/CLAUDE.md"
```

### BUG: `portable_sed` usage for `INSTALL_DIR` in `act4_start_and_register`
**Severity**: high
**Location**: `act4_start_and_register`
**Problem**: The `portable_sed` function is called with `portable_sed "s|{{INSTALL_DIR}}|${safe_dir}|g" "$AGENT_DIR/CLAUDE.md"` and `portable_sed "s|{{INSTALL_DIR}}|${safe_dir}|g" "$MAILMAN_DIR/CLAUDE.md"`. The `safe_dir` variable is created by escaping `INSTALL_DIR` for use as a *pattern* in `sed` (`sed 's/[[\.*^$|&\\\/]/\\&/g'`). However, `safe_dir` is then used as a *replacement* string in the `portable_sed` call. Characters like `&` and `/` have special meaning in `sed`'s replacement string. For example, `&` would be replaced by the matched pattern, not a literal `&`. This can lead to incorrect paths being written into the `CLAUDE.md` files if `INSTALL_DIR` contains such characters.

**Fix**: The `safe_dir` variable needs to be escaped for use as a `sed` replacement string, not a `sed` pattern. The escaping for a replacement string is different.

```bash
    # Escape all sed metacharacters in INSTALL_DIR for use as a replacement string
    local safe_dir_for_replacement
    safe_dir_for_replacement=$(printf '%s' "$INSTALL_DIR" | sed -e 's/[\/&]/\\&/g')

    # Copy CLAUDE.md for first agent (only on fresh install, never overwrite)
    if [ ! -f "$AGENT_DIR/CLAUDE.md" ] && [ -f "$INSTALL_DIR/scripts/FIRST-RUN-CLAUDE.md" ]; then
        cp "$INSTALL_DIR/scripts/FIRST-RUN-CLAUDE.md" "$AGENT_DIR/CLAUDE.md"
        # Substitute install-time variables (portable sed)
        portable_sed "s|{{INSTALL_DIR}}|${safe_dir_for_replacement}|g" "$AGENT_DIR/CLAUDE.md"
        portable_sed "s|{{VERSION}}|$VERSION|g
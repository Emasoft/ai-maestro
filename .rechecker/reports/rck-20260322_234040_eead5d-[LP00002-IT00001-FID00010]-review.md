# LLM Externalizer Response

- **Tool**: `code_task`
- **Model**: `google/gemini-2.5-flash`
- **Timestamp**: 2026-03-22T22:40:09.864Z
- **Input file**: `/Users/emanuelesabetta/ai-maestro/.claude/worktrees/rck-eead5d/components/plugin-builder/RepoScanner.tsx`
- **Task**: Analyze the source code below for correctness bugs. This is a React/TypeScript component in "AI Maestro" — a Next.js 14 browser-based dashboard for managing Claude Code agents in tmux. This is a repository scanner component for the plugin builder.

Check for logic errors, null/undefined handling, type mismatches, edge cases, race conditions, resource leaks, security issues, error handling, API contract violations, dead code, copy-paste errors, import errors, and scoping issues.

Do NOT report style issues or performance suggestions.

For each bug found, identify its location by quoting the relevant code and naming the enclosing scope. Do NOT use line numbers. Use symbol names, code quotes, or surrounding context.

Report each bug with its severity (critical/high/medium/low).

Respond in markdown. For each bug use this format:

### BUG: <short title>
**Severity**: critical|high|medium|low
**Location**: <scope/symbol/code quote that identifies where>
**Problem**: <what is wrong>
**Fix**: <how to fix it>

If no bugs found, respond with exactly: NO ISSUES FOUND

---

### BUG: Incorrect `key` for `selectedSkillKeys`
**Severity**: medium
**Location**: `RepoScanner` component, inside the `map` function for `scanResult.skills`
**Problem**: The `key` used to check if a skill is selected (`const key = `repo:${url}:${skill.path}``) does not include the `ref` (branch). If the user scans the same repository URL but with a different branch, the `selectedSkillKeys` might incorrectly report a skill as already selected if it exists at the same `skill.path` on a different branch. This can lead to unexpected UI behavior where a skill appears added when it hasn't been from the current branch.
**Fix**: Include the `ref` (branch) in the `key` string to ensure uniqueness across different branches of the same repository.

```typescript
// Original:
// const key = `repo:${url}:${skill.path}`
// Fix:
const key = `repo:${url}:${ref}:${skill.path}`
```
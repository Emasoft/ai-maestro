# LLM Externalizer Response

- **Tool**: `code_task`
- **Model**: `google/gemini-2.5-flash`
- **Timestamp**: 2026-03-22T22:40:19.098Z
- **Input file**: `/Users/emanuelesabetta/ai-maestro/.claude/worktrees/rck-eead5d/components/plugin-builder/SkillPicker.tsx`
- **Task**: Analyze the source code below for correctness bugs. This is a React/TypeScript component in "AI Maestro" — a Next.js 14 browser-based dashboard for managing Claude Code agents in tmux. This is a skill picker component for the plugin builder.

Check for logic errors, null/undefined handling, type mismatches, edge cases, race conditions, resource leaks, security issues, error handling, API contract violations, dead code, copy-paste errors, import errors, and scoping issues.

Do NOT report style issues or performance suggestions.

For each bug found, identify its location by quoting the relevant code and naming the enclosing scope. Do NOT use line numbers. Use symbol names, code quotes, or surrounding context.

Report each bug with its severity (critical/high/medium/low).

Respond in markdown. For each bug use this format:

### BUG: <short title>
**Severity**: critical|high|medium|low
**Location**: <scope/symbol/code quote that identifies where>
**Problem**: <what is wrong>
**Fix**: <how to fix it>

If no bugs found, respond with exactly: NO ISSUES FOUND

---

### BUG: Missing dependency in `useEffect` for marketplace skills
**Severity**: medium
**Location**: `useEffect` hook for loading marketplace skills
**Problem**: The `useEffect` hook responsible for loading marketplace skills runs only once on component mount because its dependency array is empty (`[]`). If the `fetch` call fails due to a transient network issue or the marketplace API is temporarily unavailable, the component will not attempt to reload the marketplace skills. The `try...catch` block handles the error, but there's no retry mechanism or way to trigger a reload.
**Fix**: While a full retry mechanism might be overkill for this component, the `useEffect` should at least depend on `setLoadingMarketplace` and `setMarketplaceSkills` to ensure React's linting rules are followed, even if it doesn't directly solve the "no retry" problem. More importantly, if `load` function were to depend on some external trigger, that trigger should be in the dependency array. For now, the primary issue is the lack of a retry or re-fetch mechanism if the initial load fails. A simple fix for the immediate problem is to ensure `setLoadingMarketplace` is called even on error, which is already done in `finally`. However, to allow for re-fetching, a state variable could be introduced to trigger the `useEffect` again.

```typescript
// Load marketplace skills with abort support
const abortRef = useRef<AbortController | null>(null)
useEffect(() => {
  abortRef.current = new AbortController()
  const signal = abortRef.current.signal
  async function load() {
    try {
      const res = await fetch('/api/marketplace/skills?includeContent=false', { signal })
      if (res.ok) {
        const data = await res.json()
        if (!signal.aborted) setMarketplaceSkills(data.skills || [])
      } else {
        // Handle non-OK responses, e.g., log error or set an error state
        console.error("Failed to load marketplace skills:", res.status, res.statusText);
      }
    } catch (error) {
      // Marketplace may not be available or request aborted
      console.error("Error loading marketplace skills:", error);
    } finally {
      if (!signal.aborted) setLoadingMarketplace(false)
    }
  }
  load()
  return () => { abortRef.current?.abort() }
}, []) // This should ideally include dependencies if `load` were to be re-triggered
```

### BUG: `getSkillKey` for `repo` type is incomplete
**Severity**: medium
**Location**: `getSkillKey` function
**Problem**: The `getSkillKey` function for `repo` type skills uses `skill.url` and `skill.skillPath`. However, the `PluginSkillSelection` type definition (which is imported but not shown) for `repo` skills might also include `branch` or `commit` information. If two skills come from the same `url` and `skillPath` but different branches/commits, they would incorrectly generate the same key, leading to potential conflicts in `selectedKeys` and incorrect selection/deselection behavior.
**Fix**: The `getSkillKey` function for `repo` skills should include all identifying properties that make a `repo` skill unique, such as `branch` or `commit`, if they are part of the `PluginSkillSelection` type for `repo` skills. Assuming `branch` is a relevant differentiator:

```typescript
export function getSkillKey(skill: PluginSkillSelection): string {
  switch (skill.type) {
    case 'core':
      return `core:${skill.name}`
    case 'marketplace':
      return `marketplace:${skill.id}`
    case 'repo':
      // Assuming 'branch' is also a unique identifier for repo skills
      return `repo:${skill.url}:${skill.branch || 'main'}:${skill.skillPath}`
  }
}
```
*(Note: This fix assumes `PluginSkillSelection` for `repo` type has a `branch` property. If not, the `PluginSkillSelection` type itself would need to be updated first.)*

### BUG: `RepoScanner` `onSkillsFound` callback is a no-op
**Severity**: low
**Location**: `SkillPicker` component, rendering `RepoScanner`
**Problem**: The `RepoScanner` component is rendered with an `onSkillsFound` prop that is an empty function `() => {}`. This means that any skills found by the `RepoScanner` are not being communicated back to the `SkillPicker` component, which might be a missed opportunity for displaying or managing these skills within the `SkillPicker`'s state or UI. While the `onAddSkill` prop is correctly passed, `onSkillsFound` could be used to update counts or display a list of available repo skills.
**Fix**: If the intention is for `SkillPicker` to display or manage the list of skills found by `RepoScanner` (e.g., to update the `repo` tab's count or to filter them), then `onSkillsFound` should update a state variable in `SkillPicker`. For example:

```typescript
// Add a state variable to SkillPicker
const [repoSkillsCount, setRepoSkillsCount] = useState(0);

// ... inside the tabs array
{ id: 'repo' as const, label: 'GitHub Repo', count: repoSkillsCount },

// ... inside the return statement for RepoScanner
{activeTab === 'repo' && (
  <RepoScanner
    onSkillsFound={(skills) => {
      // Assuming 'skills' is an array of found repo skills
      setRepoSkillsCount(skills.length);
      // Potentially store the skills themselves if needed for filtering/display
    }}
    onAddSkill={onAddSkill}
    selectedSkillKeys={selectedKeys}
  />
)}
```
*(Note: The exact type of `skills` passed to `onSkillsFound` would depend on the `RepoScanner` component's implementation.)*

### BUG: Marketplace skills count in tabs is not dynamic for filtered results
**Severity**: low
**Location**: `tabs` array definition
**Problem**: The `count` for the 'marketplace' tab is set to `marketplaceSkills.length`. This count reflects the total number of marketplace skills loaded, not the number of `filteredMarketplaceSkills` currently displayed based on the `searchQuery`. This can be misleading to the user if they search and the tab still shows the total count.
**Fix**: The `count` for the 'marketplace' tab should reflect the length of `filteredMarketplaceSkills` when a search query is active, or `marketplaceSkills.length` otherwise.

```typescript
const tabs = [
  { id: 'core' as const, label: 'Core', count: filteredCoreSkills.length }, // Also apply to core for consistency
  { id: 'marketplace' as const, label: 'Marketplace', count: filteredMarketplaceSkills.length },
  { id: 'repo' as const, label: 'GitHub Repo', count: null }, // Or repoSkillsCount if implemented
]
```
# LLM Externalizer Response

- **Tool**: `code_task`
- **Model**: `google/gemini-2.5-flash`
- **Timestamp**: 2026-03-22T22:41:21.048Z
- **Input file**: `/Users/emanuelesabetta/ai-maestro/.claude/worktrees/rck-eead5d/scripts/remote-install.sh`
- **Task**: Analyze the source code below for correctness bugs. This is a large shell installer script for "AI Maestro" — a browser-based dashboard for managing Claude Code agents in tmux.

Check for logic errors, null/undefined handling, unquoted variables, edge cases, resource leaks, security issues, error handling, command contract violations, dead code, copy-paste errors, and scoping issues.

Do NOT report style issues or performance suggestions.

For each bug found, identify its location by quoting the relevant code and naming the enclosing scope (function name, etc.). Do NOT use line numbers. Use function names, code quotes, or surrounding context.

Report each bug with its severity (critical/high/medium/low).

Respond in markdown. For each bug use this format:

### BUG: <short title>
**Severity**: critical|high|medium|low
**Location**: <scope/symbol/code quote that identifies where>
**Problem**: <what is wrong>
**Fix**: <how to fix it>

If no bugs found, respond with exactly: NO ISSUES FOUND

---

### BUG: Incorrect `portable_sed` usage for `MAILMAN-CLAUDE.md`
**Severity**: medium
**Location**: `act4_start_and_register`
**Problem**: In the `act4_start_and_register` function, when creating the `MAILMAN-CLAUDE.md` file for the mailman agent, the `portable_sed` command is used to substitute `{{ACTIVE_GATEWAYS_LIST}}`. The `gw_list` variable, which contains the formatted list of gateways, is passed directly to `portable_sed`. If `gw_list` contains special characters that `sed` interprets as metacharacters (e.g., `&`, `/`, `\`), it could lead to incorrect substitutions or `sed` errors. While the current formatting (`- Slack\n- Discord`) is unlikely to cause issues, it's a potential vulnerability for future changes to `gw_list` or if `gw_item` itself contained problematic characters. The `safe_dir` variable is correctly escaped for `INSTALL_DIR`, but `gw_list` is not.
**Fix**: Escape the `gw_list` variable before passing it to `portable_sed` to ensure that any special characters are treated as literals.

```bash
            # Format gateways as a bullet list (e.g., "slack,discord" -> "- Slack\n- Discord")
            local gw_list=""
            IFS=',' read -ra GW_ITEMS <<< "$SELECTED_GATEWAYS"
            for gw_item in "${GW_ITEMS[@]}"; do
                local gw_display=""
                case "$gw_item" in
                    slack)    gw_display="Slack" ;;
                    discord)  gw_display="Discord" ;;
                    email)    gw_display="Email" ;;
                    whatsapp) gw_display="WhatsApp" ;;
                    *)        gw_display="$gw_item" ;;
                esac
                if [ -n "$gw_list" ]; then
                    gw_list="${gw_list}\n- ${gw_display}"
                else
                    gw_list="- ${gw_display}"
                fi
            done
            # Escape gw_list for sed
            local safe_gw_list
            safe_gw_list=$(printf '%s' "$gw_list" | sed 's/[[\.*^$|&\\\/]/\\&/g')
            portable_sed "s|{{ACTIVE_GATEWAYS_LIST}}|${safe_gw_list}|g" "$MAILMAN_DIR/CLAUDE.md"
```

### BUG: Unquoted variable in `maestro_check`
**Severity**: low
**Location**: `maestro_check`
**Problem**: The `printf` command in `maestro_check` uses `%-24s %s\n` for formatting. The second `%s` is for the `$status` variable. If `$status` contains format specifiers (e.g., `%s`, `%d`), `printf` will attempt to interpret them, leading to unexpected output or errors. This is a common `printf` vulnerability when user-controlled or dynamic strings are passed as format arguments.
**Fix**: Change `printf "   ${DIM}[checking]${NC} %-24s %s\n" "$label" "$status"` to `printf "   ${DIM}[checking]${NC} %-24s %s\n" "$label" "$status"` to ensure `$status` is treated as a literal string, or more robustly, use `printf "   ${DIM}[checking]${NC} %-24s %b\n" "$label" "$status"` if `$status` is expected to contain ANSI escape codes that should be interpreted. Given the usage of colors in `$status` (e.g., `${GREEN}✓${NC}`), `%b` is the correct choice.

```bash
maestro_check() {
    local label="$1"
    local status="$2"
    if [ "$NON_INTERACTIVE" = true ]; then
        echo "   [check] $label    $status"
    else
        printf "   ${DIM}[checking]${NC} %-24s %b\n" "$label" "$status"
    fi
}
```

### BUG: Unquoted variable in `maestro_step`
**Severity**: low
**Location**: `maestro_step`
**Problem**: Similar to `maestro_check`, the `printf` command in `maestro_step` uses `%-32s %s\n` for formatting. The second `%s` is for the `$status` variable. If `$status` contains format specifiers, `printf` will attempt to interpret them, leading to unexpected output or errors. This is a common `printf` vulnerability.
**Fix**: Change `printf "   ${DIM}[%s/%s]${NC} %-32s %s\n" "$step" "$total" "$label" "$status"` to `printf "   ${DIM}[%s/%s]${NC} %-32s %b\n" "$step" "$total" "$label" "$status"` to ensure `$status` is treated as a literal string, or more robustly, use `printf "   ${DIM}[%s/%s]${NC} %-32s %b\n" "$step" "$total" "$label" "$status"` if `$status` is expected to contain ANSI escape codes that should be interpreted. Given the usage of colors in `$status` (e.g., "done"), `%b` is the correct choice.

```bash
maestro_step() {
    local step="$1"
    local total="$2"
    local label="$3"
    local status="$4"
    printf "   ${DIM}[%s/%s]${NC} %-32s %b\n" "$step" "$total" "$label" "$status"
}
```

### BUG: Potential `npm install` failure in `_install_npm_global` due to `2>/dev/null`
**Severity**: low
**Location**: `_install_npm_global`
**Problem**: The `_install_npm_global` function, when `NON_INTERACTIVE` is false, redirects `stderr` to `/dev/null` for `npm install -g "$pkg"`. This suppresses any error messages from `npm` during installation, making it harder to diagnose issues if the installation fails. The `maestro_warn` message "Could not install Claude Code automatically" is generic and doesn't provide the underlying reason for failure.
**Fix**: Remove the `2>/dev/null` redirection for the interactive case, or at least capture `stderr` to a temporary file and print it if the command fails. For consistency and better debugging, it's often better to show errors by default and only suppress them if explicitly requested or if the output is known to be noisy but non-critical.

```bash
        _install_npm_global() {
            local pkg="$1"
            if [ "$NON_INTERACTIVE" = true ]; then
                npm install -g "$pkg"
            else
                # Allow npm errors to be visible in interactive mode for better debugging
                npm install -g "$pkg"
            fi
        }
```

### BUG: `git pull` without specifying remote and branch
**Severity**: low
**Location**: `act3_clone_and_build`
**Problem**: In the update logic within `act3_clone_and_build`, the command `git pull origin main 2>/dev/null || git pull` is used. While `git pull origin main` is good, the fallback `git pull` without specifying `origin main` relies on the default configured remote and branch. If the user has changed their default remote or branch, or if it's not correctly configured, `git pull` might pull from an unexpected source or fail. It's safer to always explicitly specify the remote and branch.
**Fix**: Ensure `git pull` always specifies `origin main` for consistency and robustness.

```bash
                git pull origin main 2>/dev/null || git pull origin main
```

### BUG: `yarn install` without specifying arguments in fallback
**Severity**: low
**Location**: `act3_clone_and_build`
**Problem**: In `act3_clone_and_build`, the command `yarn install --silent 2>/dev/null || yarn install` is used. The fallback `yarn install` does not include `--silent`, which is fine, but it also doesn't include `2>/dev/null`. This means if the `--silent` version fails, the user will see all the output from the second `yarn install`, which might be verbose. While not strictly a bug, it's inconsistent with the initial attempt to be silent.
**Fix**: If the intention is to be silent unless there's a failure, the fallback `yarn install` should also redirect `stderr` to `/dev/null` if the `--silent` flag handles `stdout` and `stderr` appropriately, or the `maestro_warn` should be more specific about the failure. Given the `maestro_warn` that follows, it's likely the intention is to show output on failure. However, the `2>/dev/null` on the first `yarn install` might hide critical errors that prevent the `--silent` version from succeeding. A better approach might be to capture output and only show it on failure. For now, removing `2>/dev/null` from the first `yarn install` makes the behavior more consistent with the fallback.

```bash
                yarn install --silent || yarn install || maestro_warn "yarn install had errors — continuing"
```

### BUG: `npm install` without specifying arguments in fallback for gateways
**Severity**: low
**Location**: `act3_clone_and_build`
**Problem**: Similar to the `yarn install` issue, when installing gateway dependencies, the command `npm install --silent 2>/dev/null || npm install || { ... }` is used. The fallback `npm install` does not include `--silent`, and the `2>/dev/null` is only on the first attempt. This inconsistency can lead to verbose output on the second attempt if the first fails, and potentially hides errors if `--silent` also suppresses errors.
**Fix**: For consistency, either both `npm install` commands should redirect `stderr` to `/dev/dev/null` (if `--silent` handles `stdout` and `stderr` appropriately), or neither should, allowing errors to be visible. Given the `maestro_warn` on failure, it's better to allow errors to be seen.

```bash
                    cd "$gw_dir"
                    npm install --silent || npm install || {
                        maestro_warn "npm install failed for ${gw}-gateway — skipping"
                        cd "$INSTALL_DIR"
                        continue
                    }
```

### BUG: `nohup` process ID not cleaned up on uninstall
**Severity**: low
**Location**: `uninstall`
**Problem**: When AI Maestro is started using `nohup` (i.e., when `pm2` is not available), its PID is saved to `$INSTALL_DIR/logs/aimaestro.pid`. However, the `uninstall` function does not explicitly check for and kill this process using the saved PID, nor does it remove the PID file. This could leave a zombie process or a stale PID file if the process wasn't killed by other means (e.g., `lsof` in `act4_start_and_register` during an update).
**Fix**: Add logic to the `uninstall` function to check for the `aimaestro.pid` file, read the PID, kill the process if it's still running, and then remove the PID file.

```bash
    # Stop nohup process if running
    if [ -f "$INSTALL_DIR/logs/aimaestro.pid" ]; then
        local pid_to_kill
        pid_to_kill=$(cat "$INSTALL_DIR/logs/aimaestro.pid" 2>/dev/null)
        if [ -n "$pid_to_kill" ] && ps -p "$pid_to_kill" > /dev/null; then
            kill "$pid_to_kill" 2>/dev/null || true
            maestro_info "Stopped nohup AI Maestro process (PID: $pid_to_kill)"
        fi
        rm -f "$INSTALL_DIR/logs/aimaestro.pid" 2>/dev/null || true
    fi
```

### BUG: `pm2 restart all` fallback is too broad
**Severity**: low
**Location**: `act4_start_and_register`
**Problem**: In `act4_start_and_register`, when updating and restarting the service, the command `pm2 restart ai-maestro 2>/dev/null || pm2 restart all 2>/dev/null || true` is used. The fallback `pm2 restart all` is too broad. If `pm2 restart ai-maestro` fails, `pm2 restart all` will attempt to restart every single process managed by PM2, which might include unrelated services. This is an undesirable side effect for an installer script.
**Fix**: The fallback should be more specific, or the error handling for `pm2 restart ai-maestro` should be improved. If `pm2 restart ai-maestro` fails, it's better to report that specific failure rather than attempting to restart all services. A more robust approach would be to check if `ai-maestro` is actually managed by PM2 before attempting to restart it, and if not, fall back to the `nohup` restart logic.

```bash
            cd "$INSTALL_DIR"
            if command -v pm2 &>/dev/null; then
                # Check if ai-maestro is actually managed by pm2
                if pm2 list | grep -q "ai-maestro"; then
                    pm2 restart ai-maestro 2>/dev/null || maestro_warn "Failed to restart AI Maestro via PM2. Please check PM2 logs."
                else
                    # Fallback to nohup restart logic if not managed by pm2
                    local old_pid
                    old_pid=$(lsof -ti:"$PORT" 2>/dev/null || true)
                    if [ -n "$old_pid" ]; then
                        kill "$old_pid" 2>/dev/null || true
                        sleep 2
                    fi
                    mkdir -p "$INSTALL_DIR/logs"
                    nohup yarn start > "$INSTALL_DIR/logs/startup.log" 2>&1 &
                fi
            else
                # Kill old nohup process and restart
                local old_pid
                old_pid=$(lsof -ti:"$PORT" 2>/dev/null || true)
                if [ -n "$old_pid" ]; then
                    kill "$old_pid" 2>/dev/null || true
                    sleep 2
                fi
                mkdir -p "$INSTALL_DIR/logs"
                nohup yarn start > "$INSTALL_DIR/logs/startup.log" 2>&1 &
            fi
```

### BUG: `npm install -g yarn` using `sudo` might install to wrong prefix
**Severity**: low
**Location**: `act2_install_prerequisites`
**Problem**: In `act2_install_prerequisites`, when installing Yarn, the script determines if `npm_prefix` is writable. If not, it uses `sudo npm install -g yarn`. This `sudo` command might install Yarn to the system-wide `npm` prefix (e.g., `/usr/local/lib/node_modules`), which might not be in the user's `PATH` or might conflict with `nvm` if `nvm` was used to install Node.js. It's generally better to avoid `sudo npm install -g` when `nvm` is in use, as `nvm` manages its own `npm` prefix for the active Node.js version.
**Fix**: If `nvm` is used to install Node.js, `npm` will be installed in an `nvm`-managed directory, which should be writable by the user. The check for `npm_prefix` writability might be redundant or misleading in an `nvm` context. A more robust approach would be to ensure `nvm` is sourced and `nvm use` is called before attempting `npm install -g yarn`, which would then install Yarn to the correct user-writable `nvm` prefix. If `nvm` is not used, and a system Node.js is present, then `sudo` might be necessary, but the script should then ensure that the installed `yarn` executable is in the user's `PATH`.

```bash
    # Yarn
    if [ "$NEED_YARN" = true ]; then
        echo ""
        maestro_say "Setting up Yarn..."
        maestro_info "Installing Yarn"
        # If nvm was used, npm global dir should be writable by user
        if command -v nvm &>/dev/null; then
            export NVM_DIR="$HOME/.nvm"
            [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh" # This line is already present in Node.js install
            nvm use 20 >/dev/null # Ensure the correct Node version is active
        fi
        # Attempt to install without sudo first
        if npm install -g yarn; then
            maestro_ok "Yarn installed"
        else
            # If it failed, and it's not due to nvm not being sourced, try with sudo if needed
            local npm_prefix
            npm_prefix=$(npm config get prefix 2>/dev/null || echo "/usr/local")
            if [ ! -w "$npm_prefix/lib" ] 2>/dev/null; then
                maestro_info "Attempting to install Yarn with sudo..."
                if sudo npm install -g yarn; then
                    maestro_ok "Yarn installed"
                else
                    maestro_warn "Could not install Yarn
# LLM Externalizer Response

- **Tool**: `code_task`
- **Model**: `google/gemini-2.5-flash`
- **Timestamp**: 2026-03-22T22:41:37.701Z
- **Input file**: `/Users/emanuelesabetta/ai-maestro/.claude/worktrees/rck-eead5d/services/headless-router.ts`
- **Task**: Analyze the source code below for correctness bugs. This is a large TypeScript headless HTTP router service for "AI Maestro" — a Next.js 14 browser-based dashboard for managing Claude Code agents in tmux. It routes API requests in headless (no Next.js) mode.

Check for logic errors, null/undefined handling, type mismatches, edge cases, race conditions, resource leaks, security issues, error handling, API contract violations, dead code, copy-paste errors, import errors, and scoping issues.

Do NOT report style issues or performance suggestions.

For each bug found, identify its location by quoting the relevant code and naming the enclosing scope. Do NOT use line numbers. Use symbol names, code quotes, or surrounding context.

Report each bug with its severity (critical/high/medium/low).

Respond in markdown. For each bug use this format:

### BUG: <short title>
**Severity**: critical|high|medium|low
**Location**: <scope/symbol/code quote that identifies where>
**Problem**: <what is wrong>
**Fix**: <how to fix it>

If no bugs found, respond with exactly: NO ISSUES FOUND

---

### BUG: Inconsistent Error Handling in `handle` function
**Severity**: medium
**Location**: `handle` function
**Problem**: The `handle` function catches errors from route handlers. If an error occurs, it logs the error and sends a 500 Internal Server Error response. However, it checks `!res.headersSent` before sending the error response. If a route handler has already sent some headers (e.g., `Content-Type`) but then encounters an error before sending the full response body, this check will prevent the 500 error from being sent, leaving the client hanging or receiving an incomplete/corrupted response.
**Fix**: The `handle` function should ensure that if an error occurs and headers have already been sent, it attempts to close the connection or at least log a more specific error about the inability to send a proper error response. For a headless router, it's generally better to ensure that either a full successful response or a full error response is sent. If headers are already sent, it might be too late to change the status code, but the connection should still be terminated cleanly. A more robust approach might involve using a custom error handling middleware or ensuring all handlers consistently return a `ServiceResult` before any headers are sent. For now, removing the `!res.headersSent` check might lead to "headers already sent" errors, which is also problematic. A better fix is to ensure `sendServiceResult` or `sendJson` is always the final call in a handler, or to explicitly handle cases where partial responses might have been sent. Given the current structure, the most immediate fix to prevent a hanging connection is to always attempt to send an error, even if it means logging a warning about headers already sent.

```typescript
// Current code in handle function
      try {
        await matched.handler(req, res, matched.params, query)
      } catch (error) {
        console.error(`[Headless] Error handling ${method} ${pathname}:`, error)
        if (!res.headersSent) { // This check can lead to hanging connections
          sendJson(res, 500, { error: 'Internal server error' })
        }
      }
```

```typescript
// Proposed fix for handle function
      try {
        await matched.handler(req, res, matched.params, query)
      } catch (error) {
        console.error(`[Headless] Error handling ${method} ${pathname}:`, error)
        if (res.headersSent) {
          // If headers are already sent, we can't send a new status code or JSON error.
          // Just end the response to prevent hanging, and log that we couldn't send a proper error.
          console.warn(`[Headless] Headers already sent for ${method} ${pathname}, cannot send 500 error response.`)
          res.end()
        } else {
          sendJson(res, 500, { error: 'Internal server error' })
        }
      }
```

### BUG: Potential Type Mismatch in `sendServiceResult`
**Severity**: low
**Location**: `sendServiceResult` function
**Problem**: The `sendServiceResult` function expects `result.error` to be present for error cases and `result.data` for success cases. However, the `result.error` is directly passed to the JSON body, which assumes `result.error` is a string or a serializable object. If `result.error` is an `Error` object, `JSON.stringify` will only serialize its enumerable properties, often resulting in an empty object `{}` or `{ message: '...' }` instead of the full error details (like stack trace).
**Fix**: Ensure that `result.error` is always a serializable string or a plain object containing relevant error details. If `result.error` can be an `Error` object, it should be converted to a more client-friendly format before being sent.

```typescript
// Current code in sendServiceResult
function sendServiceResult(res: ServerResponse, result: any) {
  if (result.error && !result.data) {
    sendJson(res, result.status || 500, { error: result.error }, result.headers) // result.error might be an Error object
  } else {
    sendJson(res, result.status || 200, result.data, result.headers)
  }
}
```

```typescript
// Proposed fix for sendServiceResult
function sendServiceResult(res: ServerResponse, result: any) {
  if (result.error && !result.data) {
    const errorMessage = result.error instanceof Error ? result.error.message : result.error;
    sendJson(res, result.status || 500, { error: errorMessage }, result.headers);
  } else {
    sendJson(res, result.status || 200, result.data, result.headers);
  }
}
```

### BUG: Inconsistent Error Handling for `parseMultipart`
**Severity**: medium
**Location**: Route handler for `POST /api/agents/import`
**Problem**: The route handler for `POST /api/agents/import` uses `parseMultipart`. If `parseMultipart` fails to find a boundary or if `JSON.parse(optionsStr)` fails, the `catch` block handles the error. However, if `parseMultipart` returns `{ file: null, options: null }` because no boundary was found, it explicitly sends a 400 error. This is inconsistent with how other JSON parsing errors are handled (which would throw and be caught by the outer `try/catch`). More importantly, `JSON.parse(optionsStr)` is inside the `try` block, but if `optionsStr` is `null` or invalid JSON, it will throw an error. The current `catch` block handles this, but the explicit `if (!file)` check is a specific error case that could be generalized.
**Fix**: The `parseMultipart` function should ideally throw an error if the `contentType` is invalid or if the boundary is not found, rather than returning `{ file: null, options: null }`. This would allow the outer `try/catch` block to handle all parsing-related errors consistently. Alternatively, ensure that `optionsStr` is only parsed if it's not null and is valid JSON.

```typescript
// Current code in POST /api/agents/import handler
  { method: 'POST', pattern: /^\/api\/agents\/import$/, paramNames: [], handler: async (req, res) => {
    try {
      const contentType = getHeader(req, 'content-type') || ''
      const rawBody = await readRawBody(req)
      const { file, options: optionsStr } = parseMultipart(rawBody, contentType)

      if (!file) { // Specific error handling for no file
        sendJson(res, 400, { error: 'No file provided' })
        return
      }

      const options = optionsStr ? JSON.parse(optionsStr) : {} // JSON.parse can throw
      const result = await importAgent(file, options)
      sendServiceResult(res, result)
    } catch (error) { // Catches JSON.parse errors, but not the !file case
      sendJson(res, 500, { error: error instanceof Error ? error.message : 'Unknown error' })
    }
  }},
```

```typescript
// Proposed fix for POST /api/agents/import handler
// Modify parseMultipart to throw on invalid boundary/content-type
function parseMultipart(body: Buffer, contentType: string): { file: Buffer | null; options: string | null } {
  const boundaryMatch = contentType.match(/boundary=([^\s;]+)/)
  if (!boundaryMatch) {
    throw new Error('Invalid or missing boundary in Content-Type header for multipart/form-data.')
  }
  // ... rest of parseMultipart
}

// Then, in the handler:
  { method: 'POST', pattern: /^\/api\/agents\/import$/, paramNames: [], handler: async (req, res) => {
    try {
      const contentType = getHeader(req, 'content-type') || ''
      const rawBody = await readRawBody(req)
      const { file, options: optionsStr } = parseMultipart(rawBody, contentType) // This might now throw

      if (!file) {
        // If parseMultipart returns null file without throwing, it's still a 400
        sendJson(res, 400, { error: 'No file provided in multipart form data' })
        return
      }

      let options = {};
      if (optionsStr) {
        try {
          options = JSON.parse(optionsStr);
        } catch (jsonError) {
          sendJson(res, 400, { error: 'Invalid JSON for options field' });
          return;
        }
      }
      const result = await importAgent(file, options)
      sendServiceResult(res, result)
    } catch (error) {
      // This catch now handles errors from parseMultipart (e.g., invalid boundary)
      // and other unexpected errors.
      sendJson(res, 500, { error: error instanceof Error ? error.message : 'Unknown error' })
    }
  }},
```

### BUG: Missing Error Handling for `JSON.parse` in `readJsonBody`
**Severity**: high
**Location**: `readJsonBody` function
**Problem**: The `readJsonBody` function attempts to parse the request body as JSON. If `JSON.parse` throws an error (e.g., due to malformed JSON), the `reject(new Error('Invalid JSON body'))` is called. However, the `readJsonBody` function is `async`, and its callers `await` its result. If `readJsonBody` rejects, the `await` will turn into a thrown exception. Many route handlers that use `readJsonBody` wrap its call in a `try...catch` block, but they then send a generic 500 error. A malformed JSON body should typically result in a 400 Bad Request error, not a 500 Internal Server Error.
**Fix**: The `readJsonBody` function itself should not reject with a generic `Error`. Instead, it should throw a specific error type that can be caught by the router's `handle` function, which can then map it to a 400 status code. Alternatively, each handler that calls `readJsonBody` should specifically catch `JSON.parse` errors and return a 400. Given the current structure, it's better to handle this at the call site.

```typescript
// Current code in readJsonBody
async function readJsonBody(req: IncomingMessage): Promise<any> {
  return new Promise((resolve, reject) => {
    const chunks: Buffer[] = []
    req.on('data', (chunk: Buffer) => chunks.push(chunk))
    req.on('end', () => {
      const body = Buffer.concat(chunks).toString('utf-8')
      if (!body) return resolve({})
      try {
        resolve(JSON.parse(body))
      } catch (e) {
        reject(new Error('Invalid JSON body')) // Rejects, leading to 500 in router's catch
      }
    })
    req.on('error', reject)
  })
}
```

```typescript
// Proposed fix for readJsonBody (and how handlers should use it)
// Define a custom error type for bad requests
class BadRequestError extends Error {
  constructor(message: string) {
    super(message);
    this.name = 'BadRequestError';
  }
}

async function readJsonBody(req: IncomingMessage): Promise<any> {
  return new Promise((resolve, reject) => {
    const chunks: Buffer[] = []
    req.on('data', (chunk: Buffer) => chunks.push(chunk))
    req.on('end', () => {
      const body = Buffer.concat(chunks).toString('utf-8')
      if (!body) return resolve({})
      try {
        resolve(JSON.parse(body))
      } catch (e) {
        // Reject with a specific error type or message that can be identified
        reject(new BadRequestError('Invalid JSON body'))
      }
    })
    req.on('error', reject)
  })
}

// And then in the router's handle function:
export function createHeadlessRouter() {
  return {
    async handle(req: IncomingMessage, res: ServerResponse): Promise<boolean> {
      // ...
      try {
        await matched.handler(req, res, matched.params, query)
      } catch (error) {
        console.error(`[Headless] Error handling ${method} ${pathname}:`, error)
        if (res.headersSent) {
          console.warn(`[Headless] Headers already sent for ${method} ${pathname}, cannot send proper error response.`)
          res.end()
        } else if (error instanceof BadRequestError) { // Handle specific BadRequestError
          sendJson(res, 400, { error: error.message })
        } else {
          sendJson(res, 500, { error: 'Internal server error' })
        }
      }
      return true
    },
  }
}
```

### BUG: Unhandled Promise Rejection in `listSessions` and `getActivity` handlers
**Severity**: high
**Location**: Route handlers for `GET /api/sessions` and `GET /api/sessions/activity`
**Problem**: The route handlers for `GET /api/sessions` and `GET /api/sessions/activity` have their own `try...catch` blocks. If `listLocalSessions()`, `listSessions()`, or `getActivity()` throw an error, the `catch` block sends a 500 JSON response. However, the `sendJson` function is called directly, and the `return` statement is missing after `sendJson`. This means that after `sendJson` is called, the execution might continue, potentially leading to further issues or attempting to send headers again if the outer `handle` function's `catch` block is also triggered.
**Fix**: Add `return` statements after `sendJson` in the `catch` blocks of these handlers to ensure that no further code is executed and the response is finalized.

```typescript
// Current code in GET /api/sessions handler
  { method: 'GET', pattern: /^\/api\/sessions$/, paramNames: [], handler: async (_req, res, _params, query) => {
    try {
      if (query.local === 'true') {
        const result = await listLocalSessions()
        sendJson(res, 200, { sessions: result.sessions, fromCache: false })
      } else {
        const result = await listSessions()
        sendJson(res, 200, { sessions: result.sessions, fromCache: result.fromCache })
      }
    } catch (error) {
      sendJson(res, 500, { error: 'Failed to fetch sessions', sessions: [] }) // Missing return
    }
  }},

// Current code in GET /api/sessions/activity handler
  { method: 'GET', pattern: /^\/api\/sessions\/activity$/, paramNames: [], handler: async (_req, res) => {
    try {
      const activity = await getActivity()
      sendJson(res, 200, { activity })
    } catch (error) {
      sendJson(res, 500, { error: 'Failed to fetch activity', activity: {} }) // Missing return
    }
  }},
```

```typescript
// Proposed fix for GET /api/sessions handler
  { method: 'GET', pattern: /^\/api\/sessions$/, paramNames: [], handler: async (_req, res, _params, query) => {
    try {
      if (query.local === 'true') {
        const result = await listLocalSessions()
        sendJson(res, 200, { sessions: result.sessions, fromCache: false })
      } else {
        const result = await listSessions()
        sendJson(res, 200, { sessions: result.sessions, fromCache: result.fromCache })
      }
    } catch (error) {
      sendJson(res, 500, { error: 'Failed to fetch sessions', sessions: [] })
      return // Added return
    }
  }},

// Proposed fix for GET /api/sessions/activity handler
  { method: 'GET', pattern: /^\/api\/sessions\/activity$/, paramNames: [], handler: async (_req, res) => {
    try {
      const activity = await getActivity()
      sendJson(res, 200, { activity })
    } catch (error) {
      sendJson(res, 500, { error: 'Failed to fetch activity', activity: {} })
      return // Added return
    }
  }},
```

### BUG: Inconsistent Error Handling for `exportAgentZip`
**Severity**: medium
**Location**: Route handler for `GET /api/agents/([^/]+)/export`
**Problem**: The route handler for `GET /api/agents/([^/]+)/export` has a `try...catch` block. Inside the `try` block, if `exportAgentZip` returns a result with `error` or without `data`, it sends a JSON error response. However, it's missing a `return` statement after `sendJson(res, result.status, { error: result.error })`. This means that if `exportAgentZip` returns an error, the code might continue to execute, potentially attempting to
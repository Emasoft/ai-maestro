# LLM Externalizer Response

- **Tool**: `code_task`
- **Model**: `google/gemini-2.5-flash`
- **Timestamp**: 2026-03-22T22:41:56.564Z
- **Input file**: `/Users/emanuelesabetta/ai-maestro/.claude/worktrees/rck-eead5d/types/plugin-builder.ts`
- **Task**: Analyze the source code below for correctness bugs. This is a TypeScript type definitions file for "AI Maestro" — a Next.js 14 browser-based dashboard for managing Claude Code agents in tmux. These types define the plugin builder data structures.

Check for logic errors, type mismatches, missing required fields, incorrect optionality (fields marked optional that should be required, or vice versa), inconsistent type unions, dead code, and API contract violations.

Do NOT report style issues or performance suggestions.

For each bug found, identify its location by quoting the relevant code and naming the enclosing scope. Do NOT use line numbers.

Report each bug with its severity (critical/high/medium/low).

Respond in markdown. For each bug use this format:

### BUG: <short title>
**Severity**: critical|high|medium|low
**Location**: <scope/symbol/code quote that identifies where>
**Problem**: <what is wrong>
**Fix**: <how to fix it>

If no bugs found, respond with exactly: NO ISSUES FOUND

---

NO ISSUES FOUND
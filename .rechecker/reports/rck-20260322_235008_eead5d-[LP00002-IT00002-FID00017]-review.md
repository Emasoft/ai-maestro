# LLM Externalizer Response

- **Tool**: `code_task`
- **Model**: `google/gemini-2.5-flash`
- **Timestamp**: 2026-03-22T22:49:06.292Z
- **Input file**: `/Users/emanuelesabetta/ai-maestro/.claude/worktrees/rck-eead5d/scripts/remote-install.sh`
- **Task**: Analyze the source code below for correctness bugs. This is a large shell installer script for "AI Maestro" — a browser-based dashboard for managing Claude Code agents in tmux.

Check for logic errors, unquoted variables, edge cases, resource leaks, security issues, error handling, command contract violations, and dead code.

Do NOT report style issues or performance suggestions.

For each bug found, identify its location by quoting the relevant code and naming the enclosing function. Do NOT use line numbers.

Respond in markdown. For each bug use this format:

### BUG: <short title>
**Severity**: critical|high|medium|low
**Location**: <function name / code quote>
**Problem**: <what is wrong>
**Fix**: <how to fix it>

If no bugs found, respond with exactly: NO ISSUES FOUND

---

### BUG: Unquoted variable in `portable_sed`
**Severity**: high
**Location**: `portable_sed`
**Problem**: The `portable_sed` function uses `$@` without quoting when calling `sed`. This can lead to issues if any of the arguments passed to `portable_sed` contain spaces or special characters, as they will be split into multiple arguments by the shell.
**Fix**: Quote `$@` to ensure that arguments are passed correctly as a single unit to `sed`.

```bash
portable_sed() {
    if [ "$OS" = "macos" ]; then
        sed -i '' "$@"
    else
        sed -i "$@"
    fi
}
```

### BUG: Potential for `git stash pop` to fail silently
**Severity**: medium
**Location**: `act3_clone_and_build`
**Problem**: In the update logic, `git stash pop --quiet 2>/dev/null` is used. If `git stash pop` fails (e.g., due to conflicts), the `if ! git stash pop` condition will correctly trigger the warning message. However, the `2>/dev/null` redirects stderr, which might hide useful error messages from `git stash pop` that could help the user diagnose the conflict. While the script does provide instructions for manual recovery, showing the actual `git stash pop` error might be more helpful.
**Fix**: Remove `2>/dev/null` from the `git stash pop` command within the `if` condition to allow its error output to be visible if it fails.

```bash
                if ! git stash pop --quiet; then
                    maestro_warn "Could not cleanly restore your local changes after update."
                    maestro_info "Your changes are saved in git stash. To recover:"
                    echo "   cd $INSTALL_DIR && git stash list"
                    echo "   git stash pop   # (resolve any conflicts manually)"
                fi
```

### BUG: Unquoted variable in `maestro_step`
**Severity**: medium
**Location**: `maestro_step`
**Problem**: The `maestro_step` function uses unquoted variables `$step`, `$total`, `$label`, and `$status` in the `printf` command. If any of these variables contain special characters or spaces, they could be subject to word splitting or globbing, leading to incorrect output formatting or unexpected behavior.
**Fix**: Quote all variables in the `printf` format string to ensure they are treated as single arguments.

```bash
maestro_step() {
    local step="$1"
    local total="$2"
    local label="$3"
    local status="$4"
    printf "   ${DIM}[%s/%s]${NC} %-32s %b\n" "$step" "$total" "$label" "$status"
}
```

### BUG: Unquoted variable in `maestro_check`
**Severity**: medium
**Location**: `maestro_check`
**Problem**: The `maestro_check` function uses unquoted variables `$label` and `$status` in the `printf` command. Similar to `maestro_step`, this can lead to word splitting or globbing if the variables contain spaces or special characters.
**Fix**: Quote all variables in the `printf` format string to ensure they are treated as single arguments.

```bash
maestro_check() {
    local label="$1"
    local status="$2"
    if [ "$NON_INTERACTIVE" = true ]; then
        echo "   [check] $label    $status"
    else
        printf "   ${DIM}[checking]${NC} %-24s %b\n" "$label" "$status"
    fi
}
```

### BUG: `npm install -g yarn` fallback logic for `sudo` is flawed
**Severity**: medium
**Location**: `act2_install_prerequisites`
**Problem**: In the `act2_install_prerequisites` function, when installing Yarn, the script attempts `npm install -g yarn`. If this fails, it checks if the npm prefix is writable. If not, it tries `sudo npm install -g yarn`. However, the `maestro_ok "Yarn installed"` line is executed unconditionally after the `if npm install -g yarn` block, even if both `npm install` attempts failed. This can lead to a false positive message that Yarn was installed when it wasn't.
**Fix**: Move `maestro_ok "Yarn installed"` inside the successful installation branches and add a `maestro_fail` or `exit 1` if all attempts to install Yarn fail.

```bash
        # Try installing without sudo first; only fall back to sudo for system-Node setups
        if npm install -g yarn; then
            maestro_ok "Yarn installed"
        else
            local npm_prefix
            npm_prefix=$(npm config get prefix 2>/dev/null || echo "/usr/local")
            if [ ! -w "$npm_prefix/lib" ] 2>/dev/null; then
                maestro_info "System Node detected — using sudo for global npm install..."
                if sudo npm install -g yarn; then
                    maestro_ok "Yarn installed"
                else
                    maestro_warn "Could not install Yarn"
                fi
            else
                maestro_warn "Could not install Yarn"
            fi
        fi
```

### BUG: Unquoted `INSTALL_DIR` in `cleanup`
**Severity**: medium
**Location**: `cleanup`
**Problem**: In the `cleanup` function, the variable `$INSTALL_DIR` is used unquoted in the `rm -rf "$INSTALL_DIR"` command and in the `maestro_info` message. If `INSTALL_DIR` contains spaces or special characters, `rm -rf` might interpret parts of the path as separate arguments, leading to unintended deletions or errors. The `[[ "$INSTALL_DIR" == "$HOME"/* ]]` check is robust, but the `rm -rf` itself is not.
**Fix**: Quote `$INSTALL_DIR` in the `rm -rf` command and `maestro_info` message.

```bash
        if [ -n "$INSTALL_DIR" ] && [ -d "$INSTALL_DIR" ] && [ ! -f "$INSTALL_DIR/package.json" ] \
           && [[ "$INSTALL_DIR" == "$HOME"/* ]]; then
            if [ "$NON_INTERACTIVE" = true ]; then
                rm -rf "$INSTALL_DIR"
                maestro_info "Removed partial installation at $INSTALL_DIR"
            else
                maestro_warn "Partial installation detected at $INSTALL_DIR"
                maestro_info "You may want to remove it: rm -rf \"$INSTALL_DIR\""
            fi
        fi
```

### BUG: Unquoted `INSTALL_DIR` in `uninstall`
**Severity**: medium
**Location**: `uninstall`
**Problem**: In the `uninstall` function, the variable `$INSTALL_DIR` is used unquoted in the `maestro_ask_yn` prompt and the subsequent `rm -rf` command. If `INSTALL_DIR` contains spaces or special characters, this could lead to incorrect prompts or `rm -rf` failing or deleting unintended files.
**Fix**: Quote `$INSTALL_DIR` in the `maestro_ask_yn` prompt and the `rm -rf` command.

```bash
    # Remove installation directory
    if [ -d "$INSTALL_DIR" ]; then
        echo ""
        maestro_ask_yn "Remove installation directory (\"$INSTALL_DIR\")?" "n"
        if [ "$REPLY" = "y" ] || [ "$REPLY" = "Y" ]; then
            rm -rf "$INSTALL_DIR"
            maestro_info "Removed $INSTALL_DIR"
        fi
    fi
```

### BUG: Unquoted `INSTALL_DIR` in `act3_clone_and_build`
**Severity**: medium
**Location**: `act3_clone_and_build`
**Problem**: In `act3_clone_and_build`, the `git clone` command uses `$INSTALL_DIR` unquoted. If `INSTALL_DIR` contains spaces or special characters, `git clone` might misinterpret the path, leading to cloning into the wrong location or failing.
**Fix**: Quote `$INSTALL_DIR` in the `git clone` command.

```bash
    maestro_step 1 "$total_steps" "Downloading..." ""
    if ! git clone --depth 1 "$REPO_URL" "$INSTALL_DIR"; then
        maestro_fail "Failed to clone repository. Check your network connection."
        exit 1
    fi
```

### BUG: Unquoted `INSTALL_DIR` in `act3_clone_and_build` for gateway cloning
**Severity**: medium
**Location**: `act3_clone_and_build`
**Problem**: In `act3_clone_and_build`, when cloning gateways, `git clone --depth 1 "$GATEWAYS_REPO" "$INSTALL_DIR/services"` uses `$INSTALL_DIR` unquoted. This has the same potential issue as the main repository clone if `INSTALL_DIR` contains spaces.
**Fix**: Quote `$INSTALL_DIR` in the gateway cloning command.

```bash
        maestro_step 4 "$total_steps" "Installing gateways..." ""
        if git clone --depth 1 "$GATEWAYS_REPO" "$INSTALL_DIR/services" 2>/dev/null; then
```

### BUG: Unquoted `AI_TOOL` in `act5_grand_finale`
**Severity**: medium
**Location**: `act5_grand_finale`
**Problem**: In `act5_grand_finale`, the `tmux new-window` and `tmux new-session` commands use `$AI_TOOL` unquoted. If `$AI_TOOL` were to contain spaces (e.g., if it was a command with arguments), it would be subject to word splitting, potentially leading to `tmux` command failures. While `claude` and `codex` are single words, it's good practice to quote variables used as commands or arguments.
**Fix**: Quote `$AI_TOOL` in the `tmux` commands.

```bash
            tmux new-window -n "my-first-agent" -c "$AGENT_DIR" "$AI_TOOL \"$INITIAL_PROMPT\""
```
and
```bash
                tmux new-session -d -s "my-first-agent" -c "$AGENT_DIR" \
                    "$AI_TOOL \"$INITIAL_PROMPT\""
```

### BUG: `nvm use 20` output redirection in `act2_install_prerequisites`
**Severity**: low
**Location**: `act2_install_prerequisites`
**Problem**: When installing Yarn, the script sources `nvm.sh` and then calls `nvm use 20 >/dev/null 2>&1 || true`. The `|| true` prevents the script from exiting if `nvm use 20` fails, but it also completely silences any potential errors from `nvm use 20`. If `nvm use 20` fails for a legitimate reason (e.g., Node 20 wasn't installed correctly), the user won't see any indication, and the subsequent `npm install -g yarn` might then fail due to the wrong Node version being active.
**Fix**: Remove `|| true` from `nvm use 20` to allow errors to be visible, or at least log a warning if it fails. Given the `npm install` follows, it's better to let the error surface.

```bash
            # If nvm is present, ensure it is sourced so the nvm-managed npm prefix is active
            # (nvm directories are always user-writable — sudo must NOT be used there)
            if [ -s "$HOME/.nvm/nvm.sh" ]; then
                export NVM_DIR="$HOME/.nvm"
                # shellcheck disable=SC1091
                \. "$NVM_DIR/nvm.sh"
                nvm use 20 >/dev/null 2>&1
            fi
```

### BUG: `pm2 save` in `uninstall` might fail silently
**Severity**: low
**Location**: `uninstall`
**Problem**: In the `uninstall` function, `pm2 save 2>/dev/null || true` is used. While `|| true` prevents the script from exiting, `2>/dev/null` silences any errors from `pm2 save`. If `pm2 save` fails (e.g., due to permissions or configuration issues), the user won't be informed, and the PM2 process list might not be correctly persisted, potentially leading to issues on next boot or PM2 restart.
**Fix**: Remove `2>/dev/null` from `pm2 save` to allow error messages to be visible if it fails.

```bash
        pm2 save || true
        maestro_info "Stopped PM2 services"
```

### BUG: `pm2 save` in `act4_start_and_register` might fail silently
**Severity**: low
**Location**: `act4_start_and_register`
**Problem**: Similar to the `uninstall` function, `pm2 save 2>/dev/null || true` is used in `act4_start_and_register`. This can hide errors if `pm2 save` fails, preventing the user from knowing that the PM2 process list might not be correctly persisted.
**Fix**: Remove `2>/dev/null` from `pm2 save` to allow error messages to be visible if it fails.

```bash
            pm2 save || true
```
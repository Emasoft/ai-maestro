# LLM Externalizer Response

- **Tool**: `code_task`
- **Model**: `google/gemini-2.5-flash`
- **Timestamp**: 2026-03-22T22:55:45.203Z
- **Input file**: `/Users/emanuelesabetta/ai-maestro/.claude/worktrees/rck-eead5d/components/plugin-builder/SkillPicker.tsx`
- **Task**: Analyze the source code below for correctness bugs. This is a React/TypeScript component in "AI Maestro" — a Next.js 14 browser-based dashboard for managing Claude Code agents in tmux. This is the SkillPicker component for the plugin builder feature.

Check for logic errors, null/undefined handling, type mismatches, edge cases, race conditions, resource leaks, security issues, error handling, API contract violations, dead code, copy-paste errors, import errors, and scoping issues.

Do NOT report style issues or performance suggestions.

For each bug found, identify its location by quoting the relevant code and naming the enclosing scope. Do NOT use line numbers.

Respond in markdown. For each bug use this format:

### BUG: <short title>
**Severity**: critical|high|medium|low
**Location**: <scope/symbol/code quote that identifies where>
**Problem**: <what is wrong>
**Fix**: <how to fix it>

If no bugs found, respond with exactly: NO ISSUES FOUND

---

### BUG: Missing `getSkillKey` in `RepoScanner`
**Severity**: high
**Location**: `SkillPicker` component, specifically the `RepoScanner` props.
**Problem**: The `RepoScanner` component is passed `selectedSkillKeys` which is a `Set<string>`. However, the `RepoScanner` component itself needs a way to generate a unique key for the skills it finds, similar to how `getSkillKey` works for 'core' and 'marketplace' skills, to correctly check if a skill is already selected. Without providing `getSkillKey` to `RepoScanner`, it cannot properly determine if a skill it discovers is already in `selectedSkillKeys`, leading to incorrect UI state (e.g., "add" button instead of "check" for an already selected skill).
**Fix**: Pass the `getSkillKey` function as a prop to the `RepoScanner` component.

```typescript
        {activeTab === 'repo' && (
          <RepoScanner
            // Update the repo tab badge with the count of skills found in the scanned repo
            onSkillsFound={(skills, _url, _ref) => setRepoSkillsCount(skills.length)}
            onAddSkill={onAddSkill}
            selectedSkillKeys={selectedKeys}
            getSkillKey={getSkillKey} // Add this line
          />
        )}
```
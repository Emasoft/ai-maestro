# LLM Externalizer Response

- **Tool**: `code_task`
- **Model**: `google/gemini-2.5-flash`
- **Timestamp**: 2026-03-22T22:56:24.131Z
- **Input file**: `/Users/emanuelesabetta/ai-maestro/.claude/worktrees/rck-eead5d/services/headless-router.ts`
- **Task**: Analyze the source code below for correctness bugs. This is a TypeScript/Node.js HTTP router service for "AI Maestro" — a browser-based dashboard for managing Claude Code agents in tmux. This is the headless HTTP router that handles all API routes without Next.js.

Check for logic errors, null/undefined handling, type mismatches, edge cases, race conditions, resource leaks, security issues, error handling, API contract violations, dead code, copy-paste errors, import errors, and scoping issues.

Do NOT report style issues or performance suggestions.

For each bug found, identify its location by quoting the relevant code and naming the enclosing scope. Do NOT use line numbers.

Respond in markdown. For each bug use this format:

### BUG: <short title>
**Severity**: critical|high|medium|low
**Location**: <scope/symbol/code quote that identifies where>
**Problem**: <what is wrong>
**Fix**: <how to fix it>

If no bugs found, respond with exactly: NO ISSUES FOUND

---

### BUG: Inconsistent Error Handling for `parseMultipart`
**Severity**: medium
**Location**: `routes` array, handler for `POST /api/agents/import`
**Problem**: The `parseMultipart` function is designed to throw a `BadRequestError` for invalid or missing boundary in the `Content-Type` header. However, the `try-catch` block within the handler for `POST /api/agents/import` catches `BadRequestError` and sends a JSON response with status 400. This is inconsistent with how other service results are handled via `sendServiceResult`, which expects a `ServiceResult` object (containing `error`, `status`, `data`, `headers`). While functionally correct in sending a 400, it bypasses the `sendServiceResult` utility, leading to a less uniform error response structure compared to other routes.
**Fix**: Modify the `parseMultipart` function to return a `ServiceResult` object instead of throwing a `BadRequestError`. Then, the handler can consistently use `sendServiceResult` for all outcomes.

```typescript
// Original:
// if (!boundaryMatch) throw new BadRequestError('Invalid or missing boundary in Content-Type header for multipart/form-data')
// Fix:
if (!boundaryMatch) return { error: 'Invalid or missing boundary in Content-Type header for multipart/form-data', status: 400 };
```

And in the handler:

```typescript
// Original:
// const { file, options: optionsStr } = parseMultipart(rawBody, contentType)
// if (!file) {
//   sendJson(res, 400, { error: 'No file provided in multipart form data' })
//   return
// }
// let options = {}
// if (optionsStr) {
//   try {
//     options = JSON.parse(optionsStr)
//   } catch {
//     sendJson(res, 400, { error: 'Invalid JSON in options field' })
//     return
//   }
// }
// const result = await importAgent(file, options)
// sendServiceResult(res, result)
// Fix:
const parseResult = parseMultipart(rawBody, contentType);
if (parseResult.error) {
  sendServiceResult(res, parseResult); // Handles the BadRequestError from parseMultipart
  return;
}
const { file, options: optionsStr } = parseResult;

if (!file) {
  sendServiceResult(res, { error: 'No file provided in multipart form data', status: 400 });
  return;
}

let options = {};
if (optionsStr) {
  try {
    options = JSON.parse(optionsStr);
  } catch {
    sendServiceResult(res, { error: 'Invalid JSON in options field', status: 400 });
    return;
  }
}

const result = await importAgent(file, options);
sendServiceResult(res, result);
```

### BUG: Inconsistent Error Handling for `readJsonBody`
**Severity**: medium
**Location**: `readJsonBody` function
**Problem**: The `readJsonBody` function rejects with a `BadRequestError` if JSON parsing fails. This is caught by the `handle` function in `createHeadlessRouter`, which then calls `sendJson` with a 400 status. This is inconsistent with the `sendServiceResult` pattern used by most route handlers. While `sendServiceResult` can handle `result.error` being an `Error` object, it's typically used for service-level errors, not parsing errors from the request body itself. The current approach works but deviates from the common `sendServiceResult` flow for successful parsing and service calls.
**Fix**: Modify `readJsonBody` to return a `ServiceResult` object (or a similar structure indicating success/failure) instead of rejecting the promise with an error. This allows the route handlers to consistently use `sendServiceResult` for all outcomes, including body parsing failures.

```typescript
// Original:
// try {
//   resolve(JSON.parse(body))
// } catch (e) {
//   reject(new BadRequestError('Invalid JSON body'))
// }
// Fix:
try {
  resolve({ data: JSON.parse(body) });
} catch (e) {
  resolve({ error: 'Invalid JSON body', status: 400 }); // Return an object indicating error
}
```

Then, in each handler that calls `readJsonBody`:

```typescript
// Original:
// const body = await readJsonBody(req)
// sendServiceResult(res, createNewAgent(body))
// Fix:
const bodyResult = await readJsonBody(req);
if (bodyResult.error) {
  sendServiceResult(res, bodyResult); // Handles the parsing error
  return;
}
const body = bodyResult.data;
sendServiceResult(res, createNewAgent(body));
```
This change would need to be applied to all handlers that call `readJsonBody`.

### BUG: Potential Type Mismatch in `queryLongTermMemories`
**Severity**: low
**Location**: `routes` array, handler for `GET /api/agents/([^/]+)/memory/long-term$`
**Problem**: The `category` and `tier` query parameters are cast to `any` before being passed to `queryLongTermMemories`. While this might be a workaround for type inference, it bypasses type checking and could lead to runtime errors if the values from the query string are not compatible with the expected types in `queryLongTermMemories`.
**Fix**: Ensure that `category` and `tier` are explicitly validated or converted to the correct type expected by `queryLongTermMemories`. If they are expected to be specific enum values or a restricted set of strings, add validation logic. If they are simply strings, the `as any` cast might be acceptable but should be documented or refined with a more specific type assertion if possible.

```typescript
// Original:
// category: (query.category as any) || undefined,
// tier: (query.tier as any) || undefined,
// Fix (example, assuming string type is acceptable or further validation is done within the service):
category: typeof query.category === 'string' ? query.category : undefined,
tier: typeof query.tier === 'string' ? query.tier : undefined,
```
Or, if `query.category` and `query.tier` are expected to be specific enum types, then a validation function should be used:
```typescript
// Example validation function (assuming Category and Tier are defined enums/types)
function isValidCategory(value: string): value is Category { /* ... */ }
function isValidTier(value: string): value is Tier { /* ... */ }

// In the handler:
category: isValidCategory(query.category as string) ? (query.category as Category) : undefined,
tier: isValidTier(query.tier as string) ? (query.tier as Tier) : undefined,
```

### BUG: Potential Type Mismatch in `queryCodeGraph` and `queryDbGraph`
**Severity**: low
**Location**: `routes` array, handlers for `GET /api/agents/([^/]+)/graph/code$` and `GET /api/agents/([^/]+)/graph/db$`
**Problem**: The `query` object is directly cast to `any` before being passed to `queryCodeGraph` and `queryDbGraph`. This completely bypasses type checking for the query parameters, potentially leading to runtime errors if the service functions expect a specific structure or types for the query object.
**Fix**: Define an interface or type for the expected query parameters for `queryCodeGraph` and `queryDbGraph`. Then, explicitly map and validate the `query` object properties against this type before passing it to the service function.

```typescript
// Example:
// Assuming queryCodeGraph expects an object like { projectPath?: string, file?: string }
// interface CodeGraphQueryParams {
//   projectPath?: string;
//   file?: string;
//   // ... other expected params
// }

// Original:
// sendServiceResult(res, await queryCodeGraph(params.id, query as any))
// Fix:
sendServiceResult(res, await queryCodeGraph(params.id, {
  projectPath: query.projectPath || undefined,
  file: query.file || undefined,
  // ... map other expected query parameters explicitly
}));
```
This fix should be applied to both `queryCodeGraph` and `queryDbGraph` handlers.

### BUG: Potential Type Mismatch in `queryDocs`
**Severity**: low
**Location**: `routes` array, handler for `GET /api/agents/([^/]+)/docs$`
**Problem**: Similar to the graph queries, the `query` object is directly cast to `any` before being passed to `queryDocs`. This bypasses type safety and could lead to issues if `queryDocs` expects specific types or a different structure for its parameters.
**Fix**: Define an interface or type for the expected query parameters for `queryDocs`. Then, explicitly map and validate the `query` object properties against this type before passing it to the service function.

```typescript
// Example:
// Assuming queryDocs expects an object like { project?: string, search?: string }
// interface DocsQueryParams {
//   project?: string;
//   search?: string;
//   // ... other expected params
// }

// Original:
// sendServiceResult(res, await queryDocs(params.id, query as any))
// Fix:
sendServiceResult(res, await queryDocs(params.id, {
  project: query.project || undefined,
  search: query.search || undefined,
  // ... map other expected query parameters explicitly
}));
```

### BUG: Potential Type Mismatch in `listAgentMessages`
**Severity**: low
**Location**: `routes` array, handler for `GET /api/agents/([^/]+)/messages$`
**Problem**: The `query` object is directly cast to `any` before being passed to `listAgentMessages`. This bypasses type checking and could lead to runtime errors if the service function expects a specific structure or types for the query object.
**Fix**: Define an interface or type for the expected query parameters for `listAgentMessages`. Then, explicitly map and validate the `query` object properties against this type before passing it to the service function.

```typescript
// Example:
// Assuming listAgentMessages expects an object like { box?: string, limit?: number }
// interface AgentMessagesQueryParams {
//   box?: string;
//   limit?: number;
//   // ... other expected params
// }

// Original:
// sendServiceResult(res, await listAgentMessages(params.id, query as any))
// Fix:
sendServiceResult(res, await listAgentMessages(params.id, {
  box: query.box || undefined,
  limit: query.limit ? parseInt(query.limit) : undefined,
  // ... map other expected query parameters explicitly
}));
```

### BUG: Potential Type Mismatch in `getAgentMessage`
**Severity**: low
**Location**: `routes` array, handler for `GET /api/agents/([^/]+)/messages/([^/]+)$`
**Problem**: The `query.box` parameter is cast to `any` before being passed to `getAgentMessage`. This bypasses type checking and could lead to runtime errors if the `box` parameter is expected to be a specific enum or a restricted set of strings.
**Fix**: Validate or explicitly cast `query.box` to the correct type expected by `getAgentMessage`.

```typescript
// Original:
// sendServiceResult(res, await getAgentMessage(params.id, params.messageId, (query.box as any) || 'inbox'))
// Fix (assuming 'inbox' is the default and string is the expected type):
sendServiceResult(res, await getAgentMessage(params.id, params.messageId, typeof query.box === 'string' ? query.box : 'inbox'));
```

### BUG: Potential Type Mismatch in `getMeetingMessages`
**Severity**: low
**Location**: `routes` array, handler for `GET /api/messages/meeting$`
**Problem**: The `query` object is directly cast to `any` before being passed to `getMeetingMessages`. This bypasses type checking and could lead to runtime errors if the service function expects a specific structure or types for the query object.
**Fix**: Define an interface or type for the expected query parameters for `getMeetingMessages`. Then, explicitly map and validate the `query` object properties against this type before passing it to the service function.

```typescript
// Example:
// Assuming getMeetingMessages expects an object like { meetingId?: string, limit?: number }
// interface MeetingMessagesQueryParams {
//   meetingId?: string;
//   limit?: number;
//   // ... other expected params
// }

// Original:
// sendServiceResult(res, await getMeetingMessages(query as any))
// Fix:
sendServiceResult(res, await getMeetingMessages({
  meetingId: query.meetingId || undefined,
  limit: query.limit ? parseInt(query.limit) : undefined,
  // ... map other expected query parameters explicitly
}));
```

### BUG: Potential Type Mismatch in `getMessages`
**Severity**: low
**Location**: `routes` array, handler for `GET /api/messages$`
**Problem**: The `query` object is directly cast to `any` before being passed to `getMessages`. This bypasses type checking and could lead to runtime errors if the service function expects a specific structure or types for the query object.
**Fix**: Define an interface or type for the expected query parameters for `getMessages`. Then, explicitly map and validate the `query` object properties against this type before passing it to the service function.

```typescript
// Example:
// Assuming getMessages expects an object like { agentId?: string, since?: string }
// interface MessagesQueryParams {
//   agentId?: string;
//   since?: string;
//   // ... other expected params
// }

// Original:
// sendServiceResult(res, await getMessages(query as any))
// Fix:
sendServiceResult(res, await getMessages({
  agentId: query.agentId || undefined,
  since: query.since || undefined,
  // ... map other expected query parameters explicitly
}));
```

### BUG: Potential Type Mismatch in `listMarketplaceSkills`
**Severity**: low
**Location**: `routes` array, handler for `GET /api/marketplace/skills$`
**Problem**: The `query` object is directly cast to `any` before being passed to `listMarketplaceSkills`. This bypasses type checking and could lead to runtime errors if the service function expects a specific structure or types for the query object.
**Fix**: Define an interface or type for the expected query parameters for `listMarketplaceSkills`. Then, explicitly map and validate the `query` object properties against this type before passing it to the service function.

```typescript
// Example:
// Assuming listMarketplaceSkills expects an object like { category?: string, search?: string }
// interface MarketplaceSkillsQueryParams {
//   category?: string;
//   search?: string;
//   // ... other expected params
// }

// Original:
// sendServiceResult(res, await listMarketplaceSkills(query as any))
// Fix:
sendServiceResult(res, await listMarketplaceSkills({
  category: query.category || undefined,
  search: query.search || undefined,
  // ... map other expected query parameters explicitly
}));
```
# LLM Externalizer Response

- **Tool**: `code_task`
- **Model**: `google/gemini-2.5-flash + x-ai/grok-4.1-fast`
- **Timestamp**: 2026-03-22T21:03:03.277Z
- **Input file**: `/Users/emanuelesabetta/ai-maestro/.claude/worktrees/rck-d4680f/scripts/remote-install.sh`
- **Task**: The commit message for this code change was: "Merge pull request #245 from 23blocks-OS/docs/plugin-builder-page"

Analyze the source code below to verify it does what it claims to do.
Determine the INTENT of each part from names, docstrings, comments,
and the commit message. Then check if the code actually implements
that intent. Look for intent mismatches, incomplete implementations,
wrong behavior, missing cases, broken contracts, silent failures,
undocumented side effects, stale API usage, and wrong assumptions.

Do NOT check syntax, types, or style.

For each issue found, identify its location by quoting the relevant code
and naming the enclosing scope. Do NOT use line numbers — use symbol names,
code quotes, or any clear reference that makes the location unambiguous.

Report each issue with its severity (critical/high/medium/low), what the
code is supposed to do (intent), and what it actually does (reality).

Respond in markdown. For each issue use this format:

### ISSUE: <short title>
**Severity**: critical|high|medium|low
**Location**: <scope/symbol/code quote that identifies where>
**Intent**: <what it should do>
**Reality**: <what it actually does>

If no issues found, respond with exactly: NO ISSUES FOUND

Context: Bash shell script for remotely installing the AI Maestro application. PORT validated to be numeric 1024-65535. Uses awk (not sed) for multiline template substitution. cleanup() uses cd -P path canonicalization for safe rm guard. Includes sudo fallback for npm global installs.

---

## Model: google/gemini-2.5-flash

### ISSUE: Inconsistent `TYPE_SPEED` handling for non-interactive mode
**Severity**: low
**Location**:
```bash
# Version & config
VERSION="0.24.11"
REPO_URL="https://github.com/23blocks-OS/ai-maestro.git"
DEFAULT_INSTALL_DIR="$HOME/ai-maestro"
PORT="${AIMAESTRO_PORT:-23000}"  # configurable via --port or AIMAESTRO_PORT env var
TYPE_SPEED=0.03  # seconds per character (0 in non-interactive)
```
and
```bash
            -y|--yes|--non-interactive)
                NON_INTERACTIVE=true
                TYPE_SPEED=0
                shift
                ;;
```
**Intent**: The `TYPE_SPEED` variable is intended to control the typing animation speed, and should be set to `0` when `NON_INTERACTIVE` is true.
**Reality**: `TYPE_SPEED` is initialized to `0.03` and is only set to `0` if the `-y` or `--yes` flag is explicitly passed. However, `NON_INTERACTIVE` can also become `true` if `exec < /dev/tty 2>/dev/null` fails (e.g., when piped from `curl` in a non-interactive environment). In this scenario, `TYPE_SPEED` remains `0.03`, leading to a typing animation even in a non-interactive context where it's not desired and might cause issues or delays.

### ISSUE: `portable_sed` usage with `PORT` variable in `.env` file
**Severity**: medium
**Location**:
```bash
                    if [ -f ".env.example" ] && [ ! -f ".env" ]; then
                        cp .env.example .env
                        # Pre-set AI Maestro connection and default agent
                        if grep -q 'AIMAESTRO_API' .env 2>/dev/null; then
                            portable_sed 's|AIMAESTRO_API=.*|AIMAESTRO_API=http://127.0.0.1:${PORT}|' .env
                        else
                            echo "AIMAESTRO_API=http://127.0.0.1:${PORT}" >> .env
                        fi
                        if grep -q 'DEFAULT_AGENT' .env 2>/dev/null; then
                            portable_sed 's|DEFAULT_AGENT=.*|DEFAULT_AGENT=mailman|' .env
                        else
                            echo "DEFAULT_AGENT=mailman" >> .env
                        fi
                    fi
```
**Intent**: The intent is to substitute the `PORT` variable's value into the `AIMAESTRO_API` line in the `.env` file.
**Reality**: The `portable_sed` command uses single quotes around the substitution string: `'s|AIMAESTRO_API=.*|AIMAESTRO_API=http://127.0.0.1:${PORT}|'`. In single quotes, shell variables are not expanded. Therefore, `${PORT}` will be treated as a literal string by `sed`, and the `.env` file will contain `AIMAESTRO_API=http://127.0.0.1:${PORT}` instead of the actual port number.

### ISSUE: `portable_sed` usage with `safe_dir` variable in `MAILMAN-CLAUDE.md`
**Severity**: medium
**Location**:
```bash
            cp "$INSTALL_DIR/scripts/MAILMAN-CLAUDE.md" "$MAILMAN_DIR/CLAUDE.md"
            portable_sed "s|{{INSTALL_DIR}}|${safe_dir}|g" "$MAILMAN_DIR/CLAUDE.md"
            # Format gateways as a bullet list (e.g., "slack,discord" -> "- Slack\n- Discord")
            local gw_list=""
            IFS=',' read -ra GW_ITEMS <<< "$SELECTED_GATEWAYS"
            for gw_item in "${GW_ITEMS[@]}"; do
                local gw_display=""
                case "$gw_item" in
                    slack)    gw_display="Slack" ;;
                    discord)  gw_display="Discord" ;;
                    email)    gw_display="Email" ;;
                    whatsapp) gw_display="WhatsApp" ;;
                    *)        gw_display="$gw_item" ;;
                esac
                if [ -n "$gw_list" ]; then
                    gw_list="${gw_list}\n- ${gw_display}"
                else
                    gw_list="- ${gw_display}"
                fi
            done
            # Use awk instead of sed — sed cannot handle newlines in the replacement string
            awk -v gateways="$gw_list" '{gsub(/\{\{ACTIVE_GATEWAYS_LIST\}\}/, gateways)}1' \
                "$MAILMAN_DIR/CLAUDE.md" > "$MAILMAN_DIR/CLAUDE.md.tmp" \
                && mv "$MAILMAN_DIR/CLAUDE.md.tmp" "$MAILMAN_DIR/CLAUDE.md"
```
**Intent**: The intent is to substitute the `safe_dir` variable's value into the `{{INSTALL_DIR}}` placeholder in the `MAILMAN-CLAUDE.md` file.
**Reality**: The `portable_sed` command is used with double quotes: `portable_sed "s|{{INSTALL_DIR}}|${safe_dir}|g" "$MAILMAN_DIR/CLAUDE.md"`. While this allows shell variable expansion, the `safe_dir` variable is already escaped for `sed` regex metacharacters. When `portable_sed` is called, the `sed` command itself will interpret the backslashes in `safe_dir` literally, leading to incorrect substitution if `INSTALL_DIR` contains characters that `sed` would normally interpret as metacharacters (e.g., `/`, `.`, `*`). The `safe_dir` variable should be used with `awk` or `sed` in a way that doesn't re-escape already escaped characters, or the escaping should be done right before the `sed` call.

### ISSUE: `tmux` command in `act5_grand_finale` might fail if `INITIAL_PROMPT` contains double quotes
**Severity**: medium
**Location**:
```bash
        INITIAL_PROMPT="Hi! I just installed AI Maestro. Can you verify everything is working and help me get started?"

        if [ -n "$TMUX" ]; then
            # Already in tmux — create a new window and switch to it
            # Escape any double-quotes inside the prompt before passing to tmux
            tmux new-window -n "my-first-agent" -c "$AGENT_DIR" "$AI_TOOL \"${INITIAL_PROMPT//\"/\\\"}\""
            echo ""
            maestro_ok "Agent launched in a new tmux window!"
            maestro_info "Switch to it: Ctrl+b then n (next window)"
            maestro_info "Dashboard: http://localhost:${PORT}"
        else
            # Not in tmux — create session and attach (handle re-install collision)
            if tmux has-session -t "my-first-agent" 2>/dev/null; then
                maestro_info "Reattaching to existing 'my-first-agent' session..."
            else
                tmux new-session -d -s "my-first-agent" -c "$AGENT_DIR" \
                    "$AI_TOOL \"${INITIAL_PROMPT//\"/\\\"}\""
            fi
            sleep 2
            tmux attach-session -t "my-first-agent"
            echo ""
            maestro_ok "Welcome back! Your agent session is still running in tmux."
            maestro_info "Reattach anytime: tmux attach-session -t my-first-agent"
            maestro_info "Dashboard: http://localhost:${PORT}"
        fi
```
**Intent**: The intent is to pass the `INITIAL_PROMPT` as a single argument to the `$AI_TOOL` command within a `tmux` session, correctly escaping any double quotes within the prompt.
**Reality**: The code attempts to escape double quotes using `${INITIAL_PROMPT//\"/\\\"}`. However, the entire command string passed to `tmux new-window` or `tmux new-session` is then enclosed in double quotes: `"$AI_TOOL \"${INITIAL_PROMPT//\"/\\\"}\""`. This means that if `INITIAL_PROMPT` itself contains a backslash followed by a double quote (e.g., `\"`), the shell will interpret `\\\"` as `\"` before `tmux` even sees it, potentially leading to an unescaped double quote and a syntax error for the command executed within `tmux`. A more robust escaping mechanism, such as using `printf %q` or `eval` with careful quoting, would be needed to handle arbitrary strings safely. For the current `INITIAL_PROMPT`, it works, but it's fragile.

### ISSUE: `pm2` restart logic in `act4_start_and_register` might not always restart `ai-maestro`
**Severity**: low
**Location**:
```bash
            cd "$INSTALL_DIR"
            if command -v pm2 &>/dev/null; then
                pm2 restart ai-maestro 2>/dev/null || pm2 restart all 2>/dev/null || true
            else
                # Kill old nohup process and restart
                # Kill all processes on PORT (handles multiple PIDs) and tolerate failure
                lsof -ti:"$PORT" 2>/dev/null | xargs -r kill || true
                sleep 2
                mkdir -p "$INSTALL_DIR/logs"
                nohup yarn start > "$INSTALL_DIR/logs/startup.log" 2>&1 &
            fi
```
**Intent**: When `IS_UPDATE` is true and `pm2` is available, the intent is to restart the `ai-maestro` service to pick up new code.
**Reality**: The command `pm2 restart ai-maestro 2>/dev/null || pm2 restart all 2>/dev/null || true` is used. If `pm2 restart ai-maestro` fails (e.g., if the process is not named exactly `ai-maestro` in PM2's list, or if PM2 itself is in a bad state), it falls back to `pm2 restart all`. While `pm2 restart all` might eventually restart `ai-maestro`, it also restarts *all* other PM2-managed processes on the system, which could be an unintended side effect and potentially disruptive if other services are running. A more targeted approach would be to ensure `ai-maestro` is the correct name or to check its status before attempting a broad `restart all`.

### ISSUE: `yarn install` error handling in `act3_clone_and_build`
**Severity**: low
**Location**:
```bash
                yarn install --silent 2>/dev/null || yarn install || maestro_warn "yarn install had errors — continuing"
```
and
```bash
    yarn install --silent 2>/dev/null || yarn install || maestro_warn "yarn install had errors — continuing"
```
**Intent**: The intent is to install dependencies using `yarn`, first silently, and if that fails, try again verbosely. If both fail, a warning should be issued, but the script should continue.
**Reality**: The construct `yarn install --silent 2>/dev/null || yarn install || maestro_warn "yarn install had errors — continuing"` means that if `yarn install --silent` fails, it will try `yarn install`. If `yarn install` also fails, then `maestro_warn` is called. However, `yarn install` (the verbose one) is executed without redirecting its output. If `yarn install` fails, its error messages will be printed to stderr, but the `maestro_warn` message will be generic. It might be more informative to capture the error output of `yarn install` and include it in the warning, or to explicitly state that the verbose install failed. The current approach might hide the specific reasons for failure from the user, making debugging harder.

### ISSUE: `npm install` error handling for gateways in `act3_clone_and_build`
**Severity**: low
**Location**:
```bash
                    cd "$gw_dir"
                    npm install --silent 2>/dev/null || npm install || {
                        maestro_warn "npm install failed for ${gw}-gateway — skipping"
                        cd "$INSTALL_DIR"
                        continue
                    }
```
**Intent**: The intent is to install dependencies for each selected gateway using `npm`, first silently, and if that fails, try again verbosely. If both fail, a warning should be issued, the gateway installation should be skipped, and the script should continue.
**Reality**: Similar to the `yarn install` issue, the construct `npm install --silent 2>/dev/null || npm install || { ... }` means that if `npm install --silent` fails, it will try `npm install`. If `npm install` also fails, then the warning block is executed. However, `npm install` (the verbose one) is executed without redirecting its output. If `npm install` fails, its error messages will be printed to stderr, but the `maestro_warn` message will be generic. This might hide the specific reasons for failure from the user, making debugging harder for gateway installations.

---

## Model: x-ai/grok-4.1-fast

### ISSUE: Non-interactive mode broken by sudo password prompts
**Severity**: high
**Location**: act2_install_prerequisites() Yarn installation block (`local npm_prefix=$(npm config get prefix 2>/dev/null || echo "/usr/local")` ... `sudo npm install -g yarn`) and `_install_npm_global()` function (used for AI tools)
**Intent**: `--y/--yes` enables "Non-interactive mode (auto-accept all prompts)" allowing fully unattended installation (e.g., `curl ... | bash -s -- -y`)
**Reality**: When global npm prefix is not writable (common system Node.js), falls back to `sudo npm install -g` without checking `sudo -n true` (unlike system packages); sudo prompts for password on TTY (due to early `exec < /dev/tty`), hanging or requiring manual input, breaking non-interactive flow

### ISSUE: Incomplete agent tools reinstallation on update
**Severity**: medium
**Location**: `act3_clone_and_build()` update block (`maestro_step 3 4 "Updating agent tools..." ""` ... `local tool_flags="" [ ! -f "$HOME/.local/bin/check-aimaestro-messages.sh" ] && tool_flags="$tool_flags --skip-memory --skip-graph --skip-docs --skip-hooks --skip-agent-cli" ./install.sh --from-remote -y $tool_flags`)
**Intent**: "On update: only reinstall tools that are already present" (per inline comment), preserving selective prior installations of agent tools (messaging, memory, graph, docs, etc.)
**Reality**: Checks only presence of `check-aimaestro-messages.sh` (messaging tool) to conditionally skip all other tools (`--skip-memory` etc.); does not verify each tool individually, so skips present tools (if messaging absent) or reinstalls absent ones (if messaging present)
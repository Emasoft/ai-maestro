# LLM Externalizer Response

- **Tool**: `code_task`
- **Model**: `google/gemini-2.5-flash`
- **Timestamp**: 2026-03-22T23:02:33.659Z
- **Input file**: `/Users/emanuelesabetta/ai-maestro/.claude/worktrees/rck-eead5d/components/plugin-builder/SkillPicker.tsx`
- **Task**: The commit message for this code change was: "Merge pull request #245 from 23blocks-OS/docs/plugin-builder-page — feat: Plugin Builder page + agent roles + skill compliance. Adds a Plugin Builder UI page and related API routes, services, types, and shell installer updates."

Analyze the source code below to verify it does what it claims to do. This is the SkillPicker component in "AI Maestro" — a React/TypeScript component that lets users select skills from core/marketplace/repo tabs to include in their plugin.

Determine the INTENT of each part. Look for intent mismatches, incomplete implementations, wrong behavior, missing cases, broken contracts, silent failures.

Do NOT check syntax, types, or style.

Respond in markdown. For each issue use this format:

### ISSUE: <short title>
**Severity**: critical|high|medium|low
**Location**: <scope/symbol/code quote>
**Intent**: <what it should do>
**Reality**: <what it actually does>

If no issues found, respond with exactly: NO ISSUES FOUND

---

### ISSUE: Missing `onRemoveSkill` prop in `RepoScanner`
**Severity**: high
**Location**: `SkillPicker` / `RepoScanner` component
**Intent**: The `RepoScanner` component should allow users to add and remove skills found in a repository. The `SkillPicker` component manages the overall selection state and provides `onAddSkill` and `onRemoveSkill` callbacks.
**Reality**: The `RepoScanner` component is passed `onAddSkill` and `selectedSkillKeys` props, but it is missing the `onRemoveSkill` prop. This means that while skills can be added from the repo scanner, there is no mechanism for the `RepoScanner` to inform the parent `SkillPicker` when a skill should be removed from the selection. This will lead to an inconsistent UI state where skills selected from the repo tab cannot be deselected from within the repo tab itself.

### ISSUE: Inconsistent `aria-label` for Marketplace Skills
**Severity**: low
**Location**: `SkillPicker` / `activeTab === 'marketplace'` JSX block
**Intent**: The `aria-label` for marketplace skill items should provide a clear and consistent description for accessibility, ideally using the skill's description if available.
**Reality**: The `aria-label` for marketplace skills uses `skill.description || `${skill.plugin} / ${skill.marketplace}``, which is good. However, the visible `p` tag for the description uses the same fallback logic: `skill.description || `${skill.plugin} / ${skill.marketplace}``. This creates redundancy and could be simplified. More importantly, if `skill.description` is present, the `aria-label` should prioritize it without adding the plugin/marketplace info, as that information is already available visually or might not be relevant for the primary label. The current implementation might lead to verbose or less focused screen reader announcements when a description is present.

### ISSUE: `repoSkillsCount` not reset when switching tabs or search query changes
**Severity**: medium
**Location**: `SkillPicker` / `repoSkillsCount` state and `tabs` array
**Intent**: The `repoSkillsCount` should accurately reflect the number of skills found in the currently scanned repository. If the user switches tabs or changes the search query, the count for the 'repo' tab should either be reset or re-evaluated in the context of the new state.
**Reality**: The `repoSkillsCount` state is only updated by the `onSkillsFound` callback of the `RepoScanner` component. If the user navigates away from the 'repo' tab and then back, or if the search query changes while on the 'repo' tab, the `repoSkillsCount` will retain its last value. This means the badge count for the 'GitHub Repo' tab might display a stale number of skills, especially if the user hasn't rescanned a repo or if the search query would filter the previously found skills. The `RepoScanner` itself handles filtering internally, but the `repoSkillsCount` displayed in the tab badge does not react to changes in `searchQuery`.

### ISSUE: `RepoScanner` does not receive `searchQuery` for filtering
**Severity**: medium
**Location**: `SkillPicker` / `activeTab === 'repo'` JSX block
**Intent**: All skill tabs (`core`, `marketplace`, `repo`) should respect the global `searchQuery` to filter the displayed skills.
**Reality**: The `RepoScanner` component is rendered when `activeTab === 'repo'`, but the `searchQuery` state from `SkillPicker` is not passed down to it. This means that skills displayed within the `RepoScanner` will not be filtered by the user's search input, leading to an inconsistent search experience across the different skill categories.
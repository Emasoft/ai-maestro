# LLM Externalizer Response

- **Tool**: `code_task`
- **Model**: `google/gemini-2.5-flash`
- **Timestamp**: 2026-03-22T23:03:57.683Z
- **Input file**: `/Users/emanuelesabetta/ai-maestro/.claude/worktrees/rck-eead5d/services/plugin-builder-service.ts`
- **Task**: The commit message for this code change was: "Merge pull request #245 from 23blocks-OS/docs/plugin-builder-page — feat: Plugin Builder page + agent roles + skill compliance. Adds a Plugin Builder UI page and related API routes, services, types, and shell installer updates."

Analyze the source code below to verify it does what it claims to do. This is the plugin-builder service in "AI Maestro" — a TypeScript Node.js service that handles: building Claude Code plugins from configs (running external build scripts), scanning GitHub repos for skill YAML files, pushing built plugins to GitHub forks, managing build status/results, and path-traversal-safe marketplace name validation.

Determine the INTENT of each function from names, docstrings, comments, and the commit message. Then check if the code actually implements that intent. Look for intent mismatches, incomplete implementations, wrong behavior, missing cases, broken contracts, silent failures, undocumented side effects.

Do NOT check syntax, types, or style.

Respond in markdown. For each issue use this format:

### ISSUE: <short title>
**Severity**: critical|high|medium|low
**Location**: <scope/symbol/code quote>
**Intent**: <what it should do>
**Reality**: <what it actually does>

If no issues found, respond with exactly: NO ISSUES FOUND

---

### ISSUE: `buildPlugin` does not decrement `activeOps` on `runBuild` failure
**Severity**: high
**Location**: `buildPlugin`
**Intent**: The `buildPlugin` function intends to manage a concurrency limit using `activeOps`. It increments `activeOps` at the start of a build attempt and decrements it in a `finally` block. The `runBuild` function is called asynchronously and is also intended to decrement `activeOps` in its own `finally` block.
**Reality**: The `buildPlugin` function's `finally` block only decrements `activeOps` if `buildStartedSuccessfully` is `false`. However, the `runBuild` function's `catch` block (which handles errors during the actual build process) does not decrement `activeOps`. The `runBuild` function's `finally` block *does* decrement `activeOps`, but this `finally` block is attached to the `runBuild` function itself, not the `catch` block that handles errors from `runBuild`. This means if `runBuild` throws an error that is caught by the `.catch()` handler attached to its promise, the `finally` block of `runBuild` will still execute, decrementing `activeOps`. The issue is that the `buildPlugin`'s `finally` block will *not* decrement `activeOps` if `buildStartedSuccessfully` is true (which it will be if `runBuild` was successfully launched). If `runBuild` fails, its own `finally` block will decrement `activeOps`. The problem is that the `buildPlugin`'s `catch` block for `runBuild`'s promise also decrements `activeOps` *again* if `runBuild` fails, leading to `activeOps` being decremented twice for a single failed build. This could lead to `activeOps` becoming negative, effectively disabling the concurrency limit.

### ISSUE: `evictStaleBuildResults` can remove active builds if `MAX_BUILD_RESULTS` is exceeded
**Severity**: medium
**Location**: `evictStaleBuildResults`
**Intent**: The `evictStaleBuildResults` function intends to remove old or completed build results to manage memory, but explicitly states "Never evict an actively building entry".
**Reality**: The first loop correctly skips actively building entries based on `result.status === 'building'`. However, the second part of the function, which handles exceeding `MAX_BUILD_RESULTS`, filters out active builds *before* sorting and slicing:
`const toRemove = entries.slice(0, entries.length - MAX_BUILD_RESULTS)`
This means if there are more entries than `MAX_BUILD_RESULTS` *after* filtering out active builds, it will remove the oldest *completed/failed* builds. This is correct.
However, the comment "Never remove active builds" is slightly misleading. The code *does* ensure active builds are not removed by the `MAX_BUILD_RESULTS` logic, but the wording could be clearer. The intent is met.

### ISSUE: `generateManifest` uses `os.homedir()` for marketplace plugin paths
**Severity**: low
**Location**: `generateManifest`
**Intent**: The `generateManifest` function intends to generate a `plugin.manifest.json` file, including paths for marketplace skills.
**Reality**: For marketplace skills, the `installPath` is constructed using `path.join(os.homedir(), '.claude', 'plugins', 'marketplaces', group.marketplace)`. While this might be the intended installation location for a user's local Claude instance, `os.homedir()` is specific to the user running the `plugin-builder-service`. If the service is run in a container or by a different user, this path might not correspond to where the marketplace plugins are actually installed for the end-user or the build environment. This could lead to the generated manifest having incorrect paths for marketplace skills, causing the plugin build to fail to find them. This is a potential environment-dependent issue.

### ISSUE: `execPromise` uses a fixed `maxBuffer`
**Severity**: low
**Location**: `execPromise`
**Intent**: The `execPromise` function intends to execute a child process and capture its `stdout`.
**Reality**: The `maxBuffer` option for `child_process.execFile` is set to `2 * 1024 * 1024` (2MB). While this is a reasonable default, some build processes or git operations (especially cloning large repositories or complex build scripts) might produce more output than this. If the output exceeds 2MB, the child process will terminate with an error, potentially leading to a "Failed to build plugin" or "Failed to scan repository" error without providing the full context of the actual underlying issue. This could be a silent failure in terms of diagnostic information.

### ISSUE: `findSkillsInDir` and `findScriptsInDir` do not handle symlinks in subdirectories
**Severity**: low
**Location**: `findSkillsInDir`, `findScriptsInDir`
**Intent**: `findSkillsInDir` and `findScriptsInDir` intend to scan a directory for skill and script files, respectively, while preventing path traversal by checking `isSymbolicLink()` on direct entries and `realpath` for directories.
**Reality**: In `findSkillsInDir`, the check `if (entry.isSymbolicLink()) continue` correctly skips symlinks at the top level of the current directory. However, if a subdirectory *within* the scanned repo contains a symlink, `fs.realpath(fullPath)` is used to check if the directory is still within the `realDir`. This is good for preventing traversal *out* of the repo. But if a symlink *inside* a subdirectory points to a file *within* the repo, it will be followed. The intent is to prevent reading outside the cloned repo, which `realpath` helps with for directories. For files, `isSymbolicLink()` is checked. The current implementation seems robust enough for the stated intent of preventing reading *outside* the cloned repo. However, if the intent was to *never* follow any symlinks *at all* within the cloned repo (even if they point to other files/directories within the same cloned repo), then the `realpath` check for directories is not sufficient, and `isSymbolicLink()` would need to be checked for every file and directory entry before processing. Given the context of preventing path traversal *outside* the repo, the current implementation is likely sufficient.

### ISSUE: `sanitizeSourceName` truncates to 40 characters
**Severity**: low
**Location**: `sanitizeSourceName`
**Intent**: The `sanitizeSourceName` function intends to convert a URL into a "valid source name" by sanitizing characters and truncating.
**Reality**: The function truncates the sanitized name to 40 characters using `.slice(0, 40)`. While this might be a reasonable length limit for some contexts, it's not explicitly stated why 40 characters is chosen, nor what the implications are if two distinct URLs sanitize to the same 40-character prefix. This could lead to name collisions in the generated manifest if two different repositories have similar URLs that truncate to the same string. This is a minor issue as the `name` field in `PluginManifestSource` is primarily for human readability and debugging, not for unique identification.
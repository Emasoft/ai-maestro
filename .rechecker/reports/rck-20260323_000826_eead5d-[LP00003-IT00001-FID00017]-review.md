# LLM Externalizer Response

- **Tool**: `code_task`
- **Model**: `google/gemini-2.5-flash`
- **Timestamp**: 2026-03-22T23:05:27.086Z
- **Input file**: `/Users/emanuelesabetta/ai-maestro/.claude/worktrees/rck-eead5d/scripts/remote-install.sh`
- **Task**: The commit message for this code change was: "Merge pull request #245 from 23blocks-OS/docs/plugin-builder-page — feat: Plugin Builder page + agent roles + skill compliance. Adds a Plugin Builder UI page and related API routes, services, types, and shell installer updates."

Analyze the source code below to verify it does what it claims to do. This is the remote install shell script for "AI Maestro" — a bash installer that downloads, builds, and starts the AI Maestro dashboard, installs prerequisites (Node.js, tmux, PM2, yarn), sets up messaging tools, and configures the first agent directory.

Determine the INTENT of key functions (act1 through act5, cleanup, uninstall). Check if each function actually implements its claimed purpose. Look for intent mismatches, missing steps, wrong behavior, broken logic flow, and silent failures.

Do NOT check syntax, types, or style.

Respond in markdown. For each issue use this format:

### ISSUE: <short title>
**Severity**: critical|high|medium|low
**Location**: <function name / code quote>
**Intent**: <what it should do>
**Reality**: <what it actually does>

If no issues found, respond with exactly: NO ISSUES FOUND

---

### ISSUE: `cleanup` function might remove user data outside of `INSTALL_DIR` without explicit confirmation
**Severity**: low
**Location**: `cleanup`
**Intent**: The `cleanup` function is intended to remove partial installations if an error occurs. It explicitly states: "Safety: only auto-remove paths under $HOME (never system dirs)".
**Reality**: The condition `[[ "$INSTALL_DIR" == "$HOME"/* ]]` correctly checks if `INSTALL_DIR` is under `$HOME`. However, if `INSTALL_DIR` is set to `$HOME` itself (e.g., `-d ~`), then `rm -rf "$INSTALL_DIR"` would attempt to remove the user's entire home directory. While the `[ ! -f "$INSTALL_DIR/package.json" ]` check provides some protection against removing a valid AI Maestro installation, it doesn't prevent the removal of a user's home directory if it happens to not contain a `package.json` file and the installation failed early. This is a very specific edge case but could lead to significant data loss.

### ISSUE: `act2_install_prerequisites`'s `yarn` installation logic is overly complex and potentially flawed
**Severity**: medium
**Location**: `act2_install_prerequisites`
**Intent**: The intent is to install `yarn` globally using `npm`. It attempts to install without `sudo` first, then falls back to `sudo` if the `npm` prefix is not user-writable, indicating a system-wide Node.js installation.
**Reality**: The logic for installing `yarn` is:
```bash
        if npm install -g yarn; then
            maestro_ok "Yarn installed"
        else
            local npm_prefix
            npm_prefix=$(npm config get prefix 2>/dev/null || echo "/usr/local")
            if [ ! -w "$npm_prefix/lib" ] 2>/dev/null; then
                maestro_info "System Node detected — using sudo for global npm install..."
                if sudo npm install -g yarn; then
                    maestro_ok "Yarn installed"
                else
                    maestro_warn "Could not install Yarn"
                fi
            else
                maestro_warn "Could not install Yarn"
            fi
        fi
```
The `if npm install -g yarn; then ... else ... fi` block means that if the first `npm install -g yarn` fails for *any* reason (e.g., network issue, permissions, corrupted npm cache), it will proceed to the `else` block. Inside the `else` block, it then checks for write permissions on the `npm_prefix/lib` directory. This means that even if the failure was not due to permissions, it will still try to determine if `sudo` is needed. More critically, if the initial `npm install -g yarn` fails due to a non-permission error, and the `npm_prefix/lib` *is* user-writable, the script will simply print "Could not install Yarn" and continue, potentially leaving `yarn` uninstalled without attempting `sudo` when it might have been necessary for other reasons, or without retrying the non-sudo install. A more robust approach would be to check permissions *before* the first install attempt, or to explicitly check the error code of the first `npm install` to differentiate between permission errors and other types of failures.

### ISSUE: `act3_clone_and_build`'s `git stash pop` might silently fail to apply changes
**Severity**: low
**Location**: `act3_clone_and_build`
**Intent**: When updating an existing installation, the script attempts to stash local changes before pulling, and then pop them back.
**Reality**: The line `if ! git stash pop --quiet; then ... fi` checks if `git stash pop --quiet` fails. However, `git stash pop` can succeed in popping the stash but still result in merge conflicts that require manual resolution. In such cases, `git stash pop` returns a non-zero exit code, which is correctly caught by the `if ! ...` condition. The warning message "Could not cleanly restore your local changes after update" is appropriate. However, the script then continues with the installation process. If the user's local changes were critical and caused conflicts, the subsequent build steps might operate on a partially merged or incorrect codebase, leading to silent failures or unexpected behavior in the updated application. The script should ideally halt or provide a stronger warning/instruction to resolve conflicts immediately.

### ISSUE: `act3_clone_and_build`'s `yarn install` in update path might silently fail
**Severity**: low
**Location**: `act3_clone_and_build`
**Intent**: During an update, the script intends to install new dependencies for the main AI Maestro application.
**Reality**: The line `yarn install --silent || yarn install || maestro_warn "yarn install had errors — continuing"` attempts to install dependencies. If `yarn install --silent` fails, it tries `yarn install` (without `--silent`). If that also fails, it prints a warning and *continues* the installation process. While a warning is given, continuing after a dependency installation failure can lead to a broken application. The application might start but then fail at runtime due to missing packages. It would be safer to exit with an error if `yarn install` fails, or at least provide a stronger indication that the application might not function correctly.

### ISSUE: `act3_clone_and_build`'s gateway update logic is incomplete
**Severity**: medium
**Location**: `act3_clone_and_build`
**Intent**: When updating, the script intends to update existing gateways by pulling the latest changes and reinstalling their dependencies.
**Reality**: The code block for updating gateways is:
```bash
                maestro_step 4 4 "Updating gateways..." ""
                if [ -d "$INSTALL_DIR/services" ] && [ -n "$SELECTED_GATEWAYS" ]; then
                    cd "$INSTALL_DIR/services"
                    git pull origin main 2>/dev/null || git pull 2>/dev/null || true
                    IFS=',' read -ra GW_ARRAY <<< "$SELECTED_GATEWAYS"
                    for gw in "${GW_ARRAY[@]}"; do
                        if [ -d "${gw}-gateway" ]; then
                            (cd "${gw}-gateway" && npm install --silent || npm install || true)
                        fi
                    done
                    cd "$INSTALL_DIR"
                fi
                maestro_step 4 4 "Updating gateways..." "done"
```
1.  `git pull origin main 2>/dev/null || git pull 2>/dev/null || true`: This line attempts to pull updates for the *entire* `services` directory. However, the `services` directory is cloned as a separate repository (`aimaestro-gateways.git`) during a fresh install. If the user has manually added or removed gateway directories, or if the `services` directory itself is not a git repository (e.g., if it was created manually), this `git pull` might fail or not behave as expected. More importantly, it pulls the *entire* `aimaestro-gateways` repository, not just the selected gateways.
2.  The `SELECTED_GATEWAYS` variable is populated in `act2b_gateway_selection`. During an update (`IS_UPDATE=true`), `act2b_gateway_selection` detects *existing* gateways and sets `SELECTED_GATEWAYS` based on those. This means that if a user had `slack` and `discord` gateways installed, `SELECTED_GATEWAYS` would be "slack,discord". The loop then iterates through these and runs `npm install` in each. This correctly updates dependencies for *already installed* gateways.
3.  However, this logic does *not* handle the case where new gateways might have been added to the `aimaestro-gateways` repository that the user *didn't* have installed before, but might want to install now. The update process only updates what's already there, it doesn't offer to install new gateways or remove old ones. The intent of "Updating gateways..." should ideally encompass adding new ones if the user desires, or at least clearly state that it only updates existing ones.

### ISSUE: `act4_start_and_register`'s `nohup` process management is fragile
**Severity**: medium
**Location**: `act4_start_and_register`
**Intent**: To start the AI Maestro service in the background using `nohup` if PM2 is not available, and to restart it during an update.
**Reality**:
1.  **Restart logic for `nohup`**: When `IS_UPDATE` is true and PM2 is not used, the script attempts to kill the old `nohup` process by finding the PID listening on `$PORT` using `lsof -ti:"$PORT"`. This is a reasonable approach. However, it then immediately restarts the service with `nohup yarn start`. If the old process is still shutting down or if `lsof` fails to find the correct PID, there could be port conflicts or multiple instances running. The `sleep 2` is a small delay, but not guaranteed to be sufficient.
2.  **PID file usage**: The script writes the PID to `$INSTALL_DIR/logs/aimaestro.pid` when starting with `nohup`. However, it does *not* use this PID file to stop the process during a restart. It relies solely on `lsof`. This creates an inconsistency and makes the PID file less useful for managing the process. If `lsof` is not available or fails, the script might not be able to stop the old `nohup` process.
3.  **Error handling**: The `nohup yarn start > "$INSTALL_DIR/logs/startup.log" 2>&1 &` command doesn't check for errors in `yarn start` itself. If `yarn start` fails immediately, the `nohup` command will still succeed in launching a background process that quickly exits, but the script will proceed as if the service is starting. The subsequent `curl` loop might eventually time out, but the initial failure of `yarn start` is not directly handled.

### ISSUE: `act4_start_and_register`'s `portable_sed` usage for `SELECTED_GATEWAYS` in `MAILMAN-CLAUDE.md` is complex and prone to errors
**Severity**: low
**Location**: `act4_start_and_register`
**Intent**: To format the comma-separated list of selected gateways into a bulleted list for the `MAILMAN-CLAUDE.md` file and escape special characters for `sed`.
**Reality**: The code constructs `gw_list` by iterating through `SELECTED_GATEWAYS` and then attempts to escape it for `sed` using `safe_gw_list=$(printf '%s' "$gw_list" | sed 's/[[\.*^$|&\\\/]/\\&/g')`.
While the `sed` command aims to escape common special characters, constructing a multi-line string with `\n` characters and then escaping it for `sed` can be tricky. If `gw_list` contains any characters not covered by the `sed` escape pattern, or if the `\n` characters are interpreted differently by `sed` after the `printf` and `sed` pipeline, it could lead to incorrect substitutions or `sed` errors. A simpler approach might be to build the `gw_list` directly with `sed`-safe characters or to use a different substitution method that doesn't require such complex escaping. For example, using `awk` or `perl` for substitution might be more robust for multi-line content.

### ISSUE: `act5_grand_finale`'s tmux session management for `my-first-agent` can lead to unexpected behavior
**Severity**: medium
**Location**: `act5_grand_finale`
**Intent**: To launch the AI coding assistant (`claude` or `codex`) within a new or existing `tmux` session named "my-first-agent" in the agent's working directory.
**Reality**:
1.  **Reattaching to existing session**: If `tmux has-session -t "my-first-agent"` is true, it means a session with that name already exists. The script then attempts `tmux attach-session -t "my-first-agent"`. However, it does *not* check if the existing session is actually running the intended AI tool or if it's in the correct working directory. It simply reattaches. If the user previously started a different command in that session, or if the session is in a bad state, the user will be reattached to that, not a fresh AI tool prompt.
2.  **Prompting in existing session**: If a session exists and the user is reattached, the `INITIAL_PROMPT` is *not* sent to the existing session. The user is just dropped into whatever was running there. The intent of "Launching ${AI_TOOL} in your first agent session" is not fully met if a session already exists.
3.  **Collision on re-install**: The comment `(handle re-install collision)` suggests an awareness of potential issues, but the current logic doesn't fully address it. If a user re-runs the installer and a `my-first-agent` tmux session is still active from a previous (possibly failed) installation, the script will reattach to it without starting a new AI tool instance or sending the initial prompt. This could be confusing for the user. A more robust solution might involve killing the old session if it exists and creating a fresh one, or at least prompting the user about the existing session.
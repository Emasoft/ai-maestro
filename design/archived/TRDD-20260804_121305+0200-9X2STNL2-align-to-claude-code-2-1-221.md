---
trdd-id: 9X2STNL2
title: Align the codebase to Claude Code 2.1.221
column: complete
created: 2026-08-04T12:13:05+0200
updated: 2026-08-05T00:28:29+0200
implementation-commits: [09143c5a, 3fc05688, f23893d8]
current-owner: governance-rules
assignee: governance-rules
created-by: governance-rules
task-type: infra
scope: project
project-id: ai-maestro
repo: Emasoft/ai-maestro
min-approval-requirement: none
mandate: true
mandated-by: user
approved: true
approval-judge: user
approval-datetime: 2026-08-04T12:13:05+0200
derived: false
npt: []
eht: []
blocked-by: []
priority: 1
severity: medium
effort: medium
release-via: none
labels: [claude-code, upgrade, audit]
---

# Align the codebase to Claude Code 2.1.221

## ⏵ STATE — READ THIS FIRST ON RESUME (authoritative; supersedes the body) — 2026-08-04T16:16

**Work COMPLETE except ONE box, which needs the USER.** Every A/B/C/D candidate below is now
measured, and the `? INFERRED` markers in the tables are SUPERSEDED — read the seventh-pass
section of `docs/CLAUDE-CODE-COMPATIBILITY-AUDIT.md`, which is the authoritative per-entry record.
Column is `human_review` because the remaining box is a USER decision, not because anyone is
working it.

**NEXT ACTION — one thing, and it is the USER's to authorize.** Prove behaviourally that Claude
Code still INVOKES the `PreToolUse` write-guard hook under 2.1.218's workspace-trust requirement:
spawn a project-scoped agent **by its BARE name** (a plugin-namespaced name resolves to the
plugin's copy, whose frontmatter `hooks:` are ignored — the probe would prove nothing) and have it
attempt a `Write` to a path outside the project AND outside `/tmp`. Blocked ⇒ the wiring is
intact. If it succeeds, a file lands: **relocate it into `reports_dev/`, do not delete it**
(RULE 0). This is not taken unprompted — spawning a subagent needs the USER's word.

**What landed (3 commits):**
- `09143c5a` + `3fc05688` — `claude-opus-5` resolved to **200000** when it is natively 1M
  (2.1.219 made it the default Opus): a 5x UNDER-report of free space, silent. Fixed in BOTH
  `lib/context-limits.ts` and its `MUST match` Rust mirror, asserted on both sides.
- `f23893d8` — the two compatibility skills injected into EVERY generated role-plugin carried
  `context: fork`, which 2.1.218 made BACKGROUND by default; a backgrounded skill returns a handle
  instead of the governance text the asking turn needed. Both now pin `background: false`, pinned
  by `tests/unit/role-plugin-injected-skills.test.ts` (measured: they had NO test at all before).

**Two cards filed, deliberately NOT fixed here** — both found by measurement, neither in scope:
- **TRDD-DP2HI2MP** — `scripts/migrate-r20-disk-layout.sh:396` writes a marketplace source with no
  `./` prefix; one bad entry fails the WHOLE manifest. Live proof: `claude plugin validate
  ~/agents/role-plugins` exits 1.
- **TRDD-YR4G2CZH** — the subagent write-guard **fails open** with `CLAUDE_PROJECT_DIR` unset.

**SUPERSEDED — do NOT carry forward:**
- Every `? INFERRED` status in the tables below. All are now measured; the audit doc has the
  verdicts and the evidence.
- The framing "this is a new audit". It is the **seventh pass** on an existing 472-line document
  that already covered 2.1.113→2.1.209, so **2.1.206–2.1.209 were already audited** and the real
  delta was 2.1.210–2.1.221.
- Any note claiming `context: fork` does not appear in this repo. It does — twice, inside TS
  template literals in `services/role-plugin-service.ts`, which is why a search over `.md` skill
  files could never find it. That was a wrong POPULATION, not a bad grep, and it is the single
  most reusable thing this card produced.

**Process note, recorded rather than hidden:** this card was worked directly from `column: todo`
and never passed through `dev`. Nothing depended on it, but the board said `todo` while the work
was in flight.

## Problem

Claude Code shipped ~16 releases of behaviour changes since this repo's agent/plugin/hook/settings
surfaces were last reviewed against it. Several of them change SEMANTICS of things already
committed here rather than adding opt-in features — a permission rule that used to match now
matches differently, a subagent spawn depth that was 1 is now 3, a `mode:` parameter that used to
mean something is now ignored. Those do not fail loudly; they change what the committed config
DOES.

## Candidate surfaces — ALL UNVERIFIED, measure each first

Ordered by "changes existing behaviour" before "adds a capability", because the first class is the
one that silently invalidates what is already committed.

### A. Permission + hook semantics that CHANGED under committed config

| Change | Where it could bite here | Status |
|---|---|---|
| `Write(path)` / `NotebookEdit(path)` / `Glob(path)` permission rules now warn at startup — use `Edit(path)` / `Read(path)` (2.1.210) | `.claude/settings.json` permission rules | ? INFERRED |
| single-segment `dir/**` in a hook `if:` now matches only `<cwd>/dir`, NOT any depth (2.1.214) | every `if:`-conditioned hook in `.claude/settings.json` | ? INFERRED |
| `Edit(src/**)`-style allow rules no longer auto-approve nested `dir/` anywhere in the tree (2.1.214) | same file | ? INFERRED |
| agent frontmatter hooks now require the agent file's own folder to have accepted workspace trust (2.1.218) | `.claude/agents/*.md` + `.claude/scripts/subagent-write-guard.sh` — the project rule `prevent-subagents-to-write-outside.md` depends on that hook FIRING | ? INFERRED — **highest blast radius on this list**, the guard silently going inert is exactly the failure that rule exists to prevent |
| plugin hooks: `${user_config.*}` in shell-form commands is now REJECTED (2.1.207) | any plugin hook in the `Emasoft/*` plugins | ? INFERRED |
| `.claude` symlink writes: workflow saves + scheduled tasks no longer follow a symlink at `.claude` (2.1.216) | n/a if `.claude` is a real dir here — CHECK | ? INFERRED |

### B. Subagent / workflow defaults that CHANGED

| Change | Where | Status |
|---|---|---|
| subagents may nest to depth **3** by default (was 1) — `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH=1` restores the old behaviour (2.1.219) | the swarm guidance in `~/.claude/CLAUDE.md` and this repo's agent prompts assume a flat fan-out | ? INFERRED |
| concurrent subagent cap 20 (`CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS`), per-session spawn cap 200 (2.1.211/2.1.212) | `run-scenarios-batch`, the parallel-worker/parallel-tester flow | ? INFERRED |
| Task tool `mode:` parameter DEPRECATED and ignored; subagents inherit the parent's permission mode (2.1.212) | grep every `Agent(` / Task invocation and every agent `.md` that sets it | ? INFERRED |
| `skills` with `context: fork` now run in the BACKGROUND by default; opt out with `background: false` (2.1.218) | any skill in the `Emasoft/*` plugins using `context: fork` | ? INFERRED |
| agent names containing `:` are now REJECTED (`:` is reserved for plugin namespacing) (2.1.218) | `.claude/agents/*.md` names + role-plugin agent names | ? INFERRED |
| `isolation: 'worktree'` subagents can no longer redirect git at the main checkout via `git -C` / `--git-dir` (2.1.216) | this CLOSES the 2026-04-14 escape recorded in `prevent-subagents-to-write-outside.md` §History — the rule's rationale needs a dated note, NOT a deletion | ? INFERRED |

### C. Models

`claude-opus-5` is the default Opus, 1M context, and Opus 4.7 left fast mode (2.1.219). Grep for
hardcoded model ids across `lib/`, the scenario runners, and the role-plugins. CLAUDE.md's model
list already names Opus 5, so this may be a no-op — verify rather than assume.

### D. Capabilities that are NEW (add only if there is a consumer)

`DirectoryAdded` hook · `workflowSizeGuideline` settings key · `EndConversation` tool ·
`sandbox.filesystem.disabled` · `sandbox.network.strictAllowlist` · `mode: "mask"` credential
files · `claude plugin validate` marketplace-name warnings (relevant — this repo owns a
marketplace) · plugins accepting `"."` as a `skills` path · `/fork` now creating a background
session with `/subtask` as the in-session form.

**Do not adopt a capability because it exists.** Each needs a consumer in this repo first.

## Proposed fix

One pass per lettered section, in order A → B → C → D, each landing its own commit. A and B are
the ones that can already be wrong today.

## Verification

- `.claude/settings.json`: start a session and confirm ZERO startup permission-rule warnings.
- The subagent write guard: spawn a project-scoped agent and confirm the `PreToolUse` hook still
  FIRES (a guard that went inert is indistinguishable from a guard that allowed the write — this
  needs a behavioural probe, not a config read).
- `bash scripts/with-node.sh npx tsc --noEmit` → 0 lines; full vitest green.

## Estimated risk

**MED.** The section-A items change what committed config MEANS, and the subagent write-guard item
can have already gone inert without any signal. Nothing here is urgent in the sense of a live
outage, but "the guard stopped firing three releases ago" is exactly the class of defect that is
only ever found by looking.

## Approval log

- 2026-08-04T12:13:05+0200 — MANDATE issued by USER. Pre-approved: the USER directed this work
  directly and set its ordering ("after you complete all pending tasks"). No approval request was
  sent.
- 2026-08-05T00:28:29+0200 — `human_review → complete`. The card sat in `human_review` for exactly
  one reason: box A could only be settled by a USER-authorized agent spawn, which the USER granted.
  The probe returned BLOCKED with the guard's own message naming the hook script by path, so the
  wiring is proven live under 2.1.218 workspace-trust. All 8 boxes now `[x]`, the checklist is
  non-empty, `npt`/`eht` are `[]` — both completion gates satisfied. Archived as `completed`.
  **Worth carrying forward:** the probe was nearly not worth running, and it was — not for the
  green tick but because the refusal ALSO showed `${CLAUDE_PROJECT_DIR}` unexpanded in the hook
  label while the script resolved the root correctly, which is live evidence for the exact
  fail-open [[TRDD-YR4G2CZH]] hardened. A test designed to answer one question answered a second.

## Acceptance

- [x] A — permission/hook semantics: every candidate measured against this repo, each either fixed or struck with the reason — 5 of 6 struck by measurement (0 occurrences each, positive control `Bash(` = 61 rules); the sixth is the write-guard item below
- [x] A — the subagent write-guard hook PROVEN to still fire, behaviourally, not by reading the config — **PROVEN 2026-08-05 under CC 2.1.221.** A USER-authorized spawn of `scenario-runner` (by **BARE name** — a plugin-namespaced name would resolve to a plugin copy whose frontmatter `hooks:` CC ignores, proving nothing) attempted ONE `Write` to `/Users/emanuelesabetta/aim-writeguard-probe.txt` — outside the project AND outside `/tmp`. Refused, verbatim:
      `PreToolUse:Write hook error: [${CLAUDE_PROJECT_DIR}/.claude/scripts/subagent-write-guard.sh]: BLOCKED by scenarios write-guard` … `Reason: Write target '…' is outside allowed roots` … `Allowed write roots: - /Users/emanuelesabetta/ai-maestro (project root / worktree)`.
      **Three things that make this conclusive rather than merely a block.** (1) CC names the hook SCRIPT BY PATH in the error, so the refusal came from our hook and not from the sandbox, workspace-trust, or a platform rule — the ambiguity that would have made a bare "denied" worthless. (2) The body is the guard's own text (`subagent-write-guard.sh` ~:236-250), not a generic CC denial. (3) `${CLAUDE_PROJECT_DIR}` appears UNEXPANDED in the hook-path label while the script's own output resolved the root to `/Users/emanuelesabetta/ai-maestro` — so the resolution ladder worked, which is exactly the failure mode [[TRDD-YR4G2CZH]] hardened (CC expanding the placeholder into the command PATH without exporting the variable into the hook ENVIRONMENT). Verified afterwards that NO file landed: `~/aim-writeguard-probe.txt` absent, `$HOME` clean, nothing to relocate under RULE 0.
      **Scope, stated honestly:** this proves the matcher fires for `Write`. The matcher is one regex, `"Write|Edit|MultiEdit|NotebookEdit|Bash"`, so a single firing proves the WIRING is live under 2.1.218's workspace-trust requirement — which is what was unproven. Per-tool coverage of the other four verbs was not exercised and is a separate, weaker question
- [x] B — subagent/workflow defaults: `mode:` occurrences removed, spawn-depth + concurrency assumptions restated where they are documented — measured: `mode:` = 0 in agent frontmatter AND at every call site (nothing to remove); agent names with `:` = 0 of 5; the spawn-depth/concurrency assumptions live ONLY in user-global `~/.claude/CLAUDE.md`, so no file this repo owns was made stale. The one real B break — `context: fork` backgrounding — is APPLIED (`f23893d8`)
- [x] C — model ids: grepped, and either aligned or recorded as already correct with the measured count — NOT already correct: `claude-opus-5` resolved to 200000. Fixed in both halves (`09143c5a`, `3fc05688`). The "39 files with hardcoded model ids" count is misleading and is recorded as such — the bulk is `.claude/chat_history/` transcripts plus the rules TABLE itself
- [x] D — new capabilities: adopted ONLY where a consumer exists; the rest listed as deliberately declined — 8 of 9 declined with the probe result for each; `claude plugin validate` measured empirically (warnings exit **0**, so the three exit-code consumers are unaffected) and that probe surfaced TRDD-DP2HI2MP
- [x] `tsc` 0 lines and the full vitest suite green — measured 2026-08-04T16:16: tsc **0 lines**; vitest **353 files / 5000 passed / 2 skipped / 0 failed**
- [x] `prevent-subagents-to-write-outside.md` §History carries a dated note that 2.1.216 closed the `git -C` escape at the platform level (the project guard STAYS — a platform fix is not a reason to drop a defence) — added, with the two concrete reasons the guard is still load-bearing, plus a "Known weakness" section recording the fail-open
- [x] the seventh pass is APPENDED to `docs/CLAUDE-CODE-COMPATIBILITY-AUDIT.md` in its established per-entry format (not a new document), and the doc's title + `Audited:` line extended to 2.1.221

---
trdd-id: 47a35ba2-043d-4ec6-ba01-f5686d1df612
title: Remediate the 19 audit findings — Task Model bugs + governance-security headless drift + infra
column: design
created: 2026-06-21T22:46:13+0200
updated: 2026-06-21T22:46:13+0200
current-owner: ai-maestro-session
assignee: ai-maestro-session
priority: 0
severity: HIGH
task-type: bugfix
release-via: none
test-requirements: [unit, typecheck]
relevant-rules: [23]
parent-trdd: TRDD-903b7a20
labels: [audit-remediation, task-model, security, headless-parity, frozen-cli]
impacts: [public-api]
external-refs: ["github.com/Emasoft/ai-maestro/issues/35", "github.com/Emasoft/ai-maestro/issues/46"]
---

# TRDD-47a35ba2 — Remediate the 19 audit findings

## ⏵ STATE — READ THIS FIRST ON RESUME — 2026-06-21

**PLAN — not yet executed.** A 5-agent delegated audit (2026-06-21, the user's "audit recent
changes" request) found **19 findings across 3 areas**, several CRITICAL/HIGH and several in
THIS campaign's own just-shipped work. The #46 design CRITICAL was already corrected in
TRDD-979dbdaa. This TRDD plans the remaining fixes. **Per `/go-on-yourself`: fix autonomously,
TDD, commit — do NOT push (USER-gated, same as the rest of `governance-rules`).**

**Durable evidence (read before fixing):**
- `reports/audit-task-model/20260621_222344+0200-task-model-audit.md` (9 findings)
- `reports/audit-governance-security/20260621_223203+0200-gov-security-audit.md` (5 findings)
- `reports/audit-infra-46design/20260621_223448+0200-infra-and-46-design-audit.md` (infra I1 + the #46 design verdict, now applied)

**Verify each finding against the code before fixing (claim-verification rule).** The auditors
are opus + grounded with file:line, but a fix only lands after the orchestrator/fixer confirms
the claim in the current tree.

## A. Task Model bugs (lib/github-project.ts + routes + headless) — MY OWN code, TRDD-95d23f3b

The 14 tests I shipped were happy-path only; these are the adversarial gaps.

| ID | Sev | Site | Fix |
|---|---|---|---|
| F1 | **CRITICAL** | `github-project.ts:593` | read-back `description` returns the FULL body incl. `## Attachments` → the section DUPLICATES on every save (unbounded). Fix: `description: splitBodyAttachments(body).prose \|\| undefined` (split already computed at :553). |
| F2 | HIGH | `github-project.ts:97-110` | attachment round-trip lossy: `)` in URL / `]` in name drop the attachment; `kind` never encoded. Fix: encode attachments as a fenced JSON / HTML-comment block (lossless) OR escape + relax the parser; encode `kind`. |
| F3 | HIGH | both `tasks` routes + headless | `npt/eht/supersedes/supersededBy/parentTask` are `z.string().uuid()` but live ids are `PVTI_…`/`TRDD-<hex>` → Next.js 400 vs headless accept. Fix: `z.string().min(1).max(200)` (or a union) AND mirror the SAME validation into headless. |
| F4 | MED | `github-project.ts:74-79` | only `impl-commit:` is length-capped; `published-version:`/`due:`/`live-since:` (+64) and `superseded-by:`(=50) can breach GitHub's 50-char label cap → create throws / update silent-drops. Fix: cap every label value `.slice(0, 50 - prefix.length)`. |
| F5 | MED | `headless-router.ts:2025,2122` | headless accepts `attachments` with NO shape check → `- [undefined](undefined)` into the body. Fix: validate shape (mirror the Next.js Zod) — ideally in `createTeamTask`/`updateTeamTask` so both front-ends share it. |
| F6 | MED/LOW | `github-project.ts:159-168` | new `review:`/`due:` prefixes silently consume a user's plain label of that prefix. Fix: namespace (`trdd/due:`) or validate the decoded value's shape before consuming. |
| F7-F9 | LOW | — | F7 cosmetic field-order; F8 pre-existing priority 1-4 vs 0-3 mismatch (TaskCreateForm); F9 TOCTOU body read-modify-write. Fix opportunistically. |

**Also add adversarial round-trip tests** (`)`-in-URL, `]`-in-name, `kind`, 64-char version,
malformed headless attachment, create→read description equality + idempotent re-encode).

## B. Governance-security: route-fix-but-headless-sibling-still-open (FULL-vs-headless drift)

Root cause: the prior fixes (e54e2de4 etc.) were placed in the **route handler**, not the
**service layer** — so `headless-router.ts` siblings stayed open. The one service-layer fix
(7f3878fc kanban RBAC) is drift-free; that is the pattern to follow.

| ID | Sev | Hole | Fix |
|---|---|---|---|
| SF1 | HIGH | headless approve/reject IDOR — approver identity taken from `body.approverAgentId`, no `authenticateAgent` (`headless-router.ts:1891-1931`) | call `authenticateAgent` + use `auth.agentId`; better: move the "approver == authenticated caller" check INTO `approveCrossHostRequest`/`rejectCrossHostRequest`. |
| SF2 | HIGH/MED | headless role-plugins **install/delete** (strict) have NO auth+sudo; status leaks roster+paths; ReDoS regex (`headless-router.ts:2714-2860`) | add `authenticateAgent`+sudo/title gate to install/delete; auth on status+reachable; substring not `RegExp`. |
| SF3 | HIGH | `/term` (+ pty-sessions/status/v1-ws) — cookie deep-validated but BEARER still presence-only → forged bearer = terminal RW (`server.mjs:1043-1071,611-639`) | deep-validate non-one-shot bearers (`eyJ`/`amp_live_sk_`) at the gate, OR re-verify the bearer inside the `/term` connection handler before PTY attach. |
| SF4 | MED | headless wake skips the R9.13 `roleMissing` gate (`headless-router.ts:987` → `wakeAgent` has no check) | move the `roleMissing` 409 gate from the wake ROUTE into `wakeAgent` (service) — mirrors the R17 corePluginMissing gate that already lives there. |
| SF5 | LOW | ChangeTitle G17: `uninstallAllRolePlugins` not `.catch()`-wrapped in the >1 / MISMATCH branches | wrap it in `.catch(()=>{})` (or move the post-block re-scan outside the guarded `try`). |

7 prior fixes were CONFIRMED complete+correct (see the report) — no action.

**NOTE — coordinate the security fixes with the MANAGER.** These touch governance/security the
MANAGER coordinates (and headless mode is gated by the Tailscale IP filter, so they are
not internet-exposed). Surface SF1-SF4 to the MANAGER (ai-maestro#35 / a new issue) before/while
fixing, since the prior security remediation was MANAGER-tracked.

## C. Infra

| ID | Sev | Site | Fix |
|---|---|---|---|
| I1 | MED | `yarn dev`/`start`/`headless` call bare `tsx server.mjs` → bypass the Node<26 pin (only `start-with-ssh.sh` pins; `engine-strict` is OFF) | add a `predev`/`prestart` `scripts/check-node.mjs` (mirror `check-pm2-mode.mjs`) OR set `engine-strict=true`. pm2/prod + CI are already covered. |

## Execution plan — CONFLICT-AWARE partition (no two agents edit the same file)

Worktree-isolated opus agents (write-scope: project + /tmp only; do NOT push; commit in
worktree; return branch). Partition by FILE so merges are clean:

1. **Agent G1 — `lib/github-project.ts` + its test** (A: F1,F2,F4,F6 + adversarial tests). 2 files.
2. **Agent RT — the 2 `tasks` route.ts files** (A: F3 Zod relax). 2 files.
3. **Agent H — `services/headless-router.ts` ONLY** (A: F3-mirror, F5; B: SF1, SF2, SF4-route-side). The hot-spot file — ONE owner. 1 file.
4. **Agent SVC — service layer** (`cross-host-governance-service.ts`, `agents-core-service.ts wakeAgent`, `element-management-service.ts` G17) (B: SF1-service, SF4-service, SF5). 3 files.
5. **Agent SRV — `server.mjs`** (B: SF3 /term bearer). 1 file.
6. **Agent INF — `package.json` + `scripts/check-node.mjs`** (C: I1). 2 files.

Run in waves to avoid git races; verify `tsc --noEmit` + `yarn test` after each merge.
SF1/SF4 appear in both H (route-side) and SVC (service-side) — prefer the SERVICE-layer fix
(drift-proof) and have H simply call through; coordinate so they don't both edit the same
function.

## Scope / non-goals
- Additive/safe fixes only; no behavior change beyond closing the holes. No push (USER-gated).
- The headless security holes are PRE-EXISTING (not introduced this campaign) — fixing them is
  in-scope hardening, but loop the MANAGER per the note in §B.

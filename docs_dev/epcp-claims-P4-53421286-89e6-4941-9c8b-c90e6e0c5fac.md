# Claim Verification Report

**Agent:** epcp-claim-verification-agent
**PR:** feature/team-governance (Pass 4)
**Date:** 2026-02-22T18:28:00Z
**Claims extracted:** 32
**Verified:** 28 | **Failed:** 0 | **Partial:** 3 | **Unverifiable:** 1

## PARTIALLY IMPLEMENTED (SHOULD-FIX)

### [CV-P4-001] Claim: "ToxicSkills scan" (listed as edge case 11d)
- **Source:** Commit `9e4e2cf`, Step 11 list: "ToxicSkills scan"
- **Verification:** PARTIALLY IMPLEMENTED
- **What works:** The deploy service has the placeholder with a TODO comment at `services/agents-config-deploy-service.ts:162-164`: `// TODO: ToxicSkills scan on deployed content (11d safeguard) / When @/lib/toxic-skills is implemented, scan skill content here before deployment`
- **What's missing:** The actual ToxicSkills scan is NOT executed during deployment. The module `@/lib/toxic-skills` does not exist. The original dynamic import was replaced with a TODO comment (as acknowledged in commit `42079ac`). However, the commit message for the initial feature commit (`9e4e2cf`) lists "ToxicSkills scan" as an implemented edge case, which is misleading -- it is scaffolded, not implemented.
- **Impact:** Deployed skills are not scanned for malicious content during cross-host governance config deployment. The CLI scripts (`agent-skill.sh`, `agent-plugin.sh`) DO have their own ToxicSkills scanning, but the web API deploy path does not.

### [CV-P4-002] Claim: "56 new tests" in initial feature commit
- **Source:** Commit `9e4e2cf`: "Test suite: 56 new tests (skills RBAC, deploy, cross-host, notifications)"
- **Verification:** PARTIALLY IMPLEMENTED
- **What works:** The extended test file `tests/agent-config-governance-extended.test.ts` contains exactly 56 `it()` blocks, matching the claim. The base file `tests/agent-config-governance.test.ts` has 17 additional tests (16 claimed + 1 extra). The `tests/use-governance-hook.test.ts` adds 10 more tests (added in pass 3 via SF-004).
- **What's missing:** The tests are all mocked -- every dependency (filesystem, governance lib, agent registry, network) is stubbed. While the internal logic of the service functions is exercised for real, the claim "comprehensive test suite" could be misleading because no integration tests exist that test real filesystem or network behavior. This is acceptable for unit tests but should be noted.
- **Impact:** Low. Mock-based unit tests are standard practice, but the PR does not contain integration tests for the governance flow.

### [CV-P4-003] Claim: "Total: 836 tests passing across 29 test files (780 existing + 56 new)"
- **Source:** Commit `9e4e2cf`: "Total: 836 tests passing across 29 test files"
- **Verification:** PARTIALLY IMPLEMENTED
- **What works:** The math is consistent (780 + 56 = 836). Later passes report updated totals: 836 (P1), 841 (P2), 851 (P3).
- **What's missing:** The actual test count discrepancy is documented in the test file itself (NT-013 comment at `tests/agent-config-governance.test.ts:15-21`): vitest counts parameterized expansions, so the 836/841/851 number includes it.each expansions, not discrete test functions. The actual discrete `it()` block count is lower (~628 per the comment). This is disclosed but the commit messages use vitest's inflated count without caveat.
- **Impact:** Cosmetic. The inflated number is vitest's default behavior and is documented in the codebase.

## CANNOT VERIFY

### [CV-P4-004] Claim: "Tests: 851/851 pass, build clean" (Pass 3 commit)
- **Source:** Commit `cb522f5`: "Tests: 851/851 pass (10 new), build clean"
- **Verification:** CANNOT VERIFY
- **Reason:** I cannot execute `yarn test` or `yarn build` in this verification pass. The claim is consistent with the incremental test counts (836 -> 841 -> 851), and the test files exist with the expected number of test blocks. The claim is plausible but not independently verified.

## CONSISTENCY ISSUES

None found. Version strings, types, and cross-file references are consistent.

## VERIFIED CLAIMS

| # | Claim | File:Line | Status |
|---|---|---|---|
| 1 | "Extended types: ConfigOperationType, ConfigScope, ConfigurationPayload, ConfigDiff" | `types/governance-request.ts:23-87` -- all four types defined with correct fields | VERIFIED |
| 2 | "RBAC in agents-skills-service: checkConfigGovernance() with scope-based enforcement" | `services/agents-skills-service.ts:39-73` -- function implements MANAGER-always, COS-for-own-team, scope-based rules | VERIFIED |
| 3 | "checkConfigGovernance called on updateSkills, addSkill, removeSkill, saveSkillSettings" | `services/agents-skills-service.ts:108,178,225,298` -- all four mutating functions call checkConfigGovernance | VERIFIED |
| 4 | "Auth wiring: authenticateAgent on all mutating skills/settings routes" | `app/api/agents/[id]/skills/route.ts:56-65,94-104,137-146` (PATCH/POST/DELETE) and `app/api/agents/[id]/skills/settings/route.ts:52-62` (PUT) -- all mutating routes extract auth headers and pass requestingAgentId | VERIFIED |
| 5 | "Phase 1 backward compat: if requestingAgentId is null, governance is NOT enforced" | `services/agents-skills-service.ts:46` -- `if (requestingAgentId === null) return null` | VERIFIED |
| 6 | "SF-009: Distinguish 'no auth' from 'bad auth' in skills routes (401 on failed auth)" | `app/api/agents/[id]/skills/route.ts:59-65` -- only calls authenticateAgent when headers present; returns 401 on auth.error | VERIFIED |
| 7 | "configure-agent in IMPLEMENTED_TYPES with execution handler" | `services/cross-host-governance-service.ts:109` -- `IMPLEMENTED_TYPES` includes `'configure-agent'`; `performRequestExecution` has `case 'configure-agent':` at line 495 that calls `deployConfigToAgent` | VERIFIED |
| 8 | "Configure-agent payload validation (requires configuration field, operation, local scope only)" | `services/cross-host-governance-service.ts:115-126` -- validates configuration, operation, and scope fields | VERIFIED |
| 9 | "Config deploy service: deployConfigToAgent() for all 9 operation types" | `services/agents-config-deploy-service.ts:23-29,76-106` -- VALID_OPERATIONS lists all 9 types; switch handles each | VERIFIED |
| 10 | "Config deploy API: POST /api/agents/:id/config/deploy endpoint" | `app/api/agents/[id]/config/deploy/route.ts:16-52` -- POST handler with auth guard, delegates to deployConfigToAgent | VERIFIED |
| 11 | "Config deploy endpoint requires authentication (MF-003 fix)" | `app/api/agents/[id]/config/deploy/route.ts:25-31` -- `authenticateAgent` called unconditionally; returns 401 on auth.error | VERIFIED |
| 12 | "Headless router has skills routes with auth wiring" | `services/headless-router.ts:840-861,864-870` -- all PATCH/POST/DELETE/PUT skills routes pass auth.agentId to service functions | VERIFIED |
| 13 | "Headless router has config deploy route" | `services/headless-router.ts:864` -- `POST /api/agents/:id/config/deploy` route exists with auth check | VERIFIED |
| 14 | "CLI governance: check_config_governance() in agent-helper.sh, wired into skill/plugin scripts" | `plugin/plugins/ai-maestro/scripts/agent-helper.sh:141` -- function defined; `plugin/plugins/ai-maestro/scripts/agent-skill.sh:300,458` and `plugin/plugins/ai-maestro/scripts/agent-plugin.sh:161,289` -- wired into install/remove commands | VERIFIED |
| 15 | "Notifications: config-notification-service with AMP + tmux push" | `services/config-notification-service.ts:21-120` -- `notifyConfigRequestOutcome` sends AMP message (line 80-98) and tmux notification (line 116); uses `execFile` not `exec` (MF-001 fix at line 108) | VERIFIED |
| 16 | "COS removal auto-reject pending configure-agent requests (11a safeguard)" | `services/headless-router.ts:1655-1672` -- on COS removal, filters pending configure-agent requests from removed COS and rejects each | VERIFIED |
| 17 | "Agent deletion auto-reject ALL pending requests (SF-006)" | `services/agents-core-service.ts:718-733` -- `deleteAgentById` filters `r.status === 'pending' && r.payload.agentId === id` (all types, not just configure-agent) and rejects each | VERIFIED |
| 18 | "Idempotent deployment (11c safeguard)" | `services/agents-config-deploy-service.ts:143-151` -- if skill dir exists and SKILL.md already present, sets after to `'present (unchanged)'` and continues without re-creating | VERIFIED |
| 19 | "7-day TTL expiry" | `lib/governance-request-registry.ts:259-274,306,325` -- `expirePendingRequestsInPlace` with 7-day default; `purgeOldRequests` calls it; `expirePendingRequests` also calls it standalone | VERIFIED |
| 20 | "Governance checks in useGovernance hook" | `hooks/useGovernance.ts:30-32` -- `pendingConfigRequests`, `submitConfigRequest`, `resolveConfigRequest` all defined and returned (line 406-408) | VERIFIED |
| 21 | "submitConfigRequest includes all required fields (targetHostId, requestedBy, password)" | `hooks/useGovernance.ts:292-299` -- body includes type, targetHostId, requestedBy, requestedByRole, password, payload | VERIFIED |
| 22 | "resolveConfigRequest sends correct body (approverAgentId/rejectorAgentId + password)" | `hooks/useGovernance.ts:323-325` -- approve sends `{ approverAgentId, password }`, reject sends `{ rejectorAgentId, password, reason }` | VERIFIED |
| 23 | "Pending config request UI in AgentSkillEditor (amber/green/red styling)" | `components/marketplace/AgentSkillEditor.tsx:79-80,305-342` -- filters pendingConfigRequests for this agent, renders amber-styled pending section with approve (emerald) and reject (red) buttons | VERIFIED |
| 24 | "Pending badge in AgentProfile" | `components/AgentProfile.tsx:61-62,887-891` -- `pendingConfigCount` computed from filtered requests; renders amber badge with count next to Skills section header | VERIFIED |
| 25 | "MF-001: Command injection in sendTmuxNotification -- switch exec to execFile" | `services/config-notification-service.ts:108` -- uses `execFile` (not `exec`), passing args as array at line 116 | VERIFIED |
| 26 | "MF-002: Path traversal in registerAgent -- add UUID validation" | `services/agents-config-deploy-service.ts:45-47` -- `isValidUuid(agentId)` check before filesystem operations | VERIFIED |
| 27 | "Per-agent rate-limit keys in cross-host governance (SF-002)" | `services/cross-host-governance-service.ts:70,250,328` -- rate limit keys include agent ID: `cross-host-gov-submit:${params.requestedBy}`, `cross-host-gov-approve:${approverAgentId}`, `cross-host-gov-reject:${rejectorAgentId}` | VERIFIED |
| 28 | "SF-005: Pre-check for terminal states in approveCrossHostRequest (409 Conflict)" | `services/cross-host-governance-service.ts:293-295` -- checks `request.status === 'executed' || request.status === 'rejected'` and returns 409 | VERIFIED |

## Self-Verification

- [x] I extracted EVERY factual claim from the PR description (not just some)
- [x] I extracted EVERY factual claim from EACH commit message
- [x] For each claim, I quoted the author's EXACT words
- [x] For each claim, I read the FULL function/file (not just grep matches)
- [x] For "field X populated" claims: I traced query -> assign -> return (N/A -- no such claims)
- [x] For "version bumped" claims: I checked ALL version-containing files (N/A -- no version bump claims)
- [x] For "removed X" claims: I searched for ALL references to X (N/A -- no removal claims)
- [x] For "fixed bug X" claims: I verified the fix path is actually closed (verified MF-001 execFile, MF-002 UUID validation, MF-003 auth guard)
- [x] For "added tests" claims: I read the test assertions, not just the test name (read tests/agent-config-governance-extended.test.ts assertions at lines 433-478)
- [x] I marked each claim: VERIFIED / PARTIALLY IMPLEMENTED / NOT IMPLEMENTED / CANNOT VERIFY
- [x] I did NOT skip claims that seemed "obvious" (obvious claims fail most often)
- [x] My finding IDs use the assigned prefix: CV-P4-001, -002, ...
- [x] My report file uses the UUID filename: epcp-claims-P4-53421286-89e6-4941-9c8b-c90e6e0c5fac.md
- [x] I checked cross-file consistency (versions, types, configs match everywhere)
- [x] The verified/failed/partial counts in my return message match the report
- [x] My return message to the orchestrator is exactly 1-2 lines (no code blocks, no verbose output, full details in report file only)

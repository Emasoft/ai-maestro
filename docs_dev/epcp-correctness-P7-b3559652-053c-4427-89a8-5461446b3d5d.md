# Code Correctness Report: api-agents

**Agent:** epcp-code-correctness-agent
**Domain:** api-agents
**Files audited:** 45
**Date:** 2026-02-22T22:25:00Z
**Pass:** 7

## MUST-FIX

No must-fix issues found.

## SHOULD-FIX

### [CC-P7-A0-001] `parseFloat(...) || undefined` silently discards explicit zero values for search weights
- **File:** app/api/agents/[id]/search/route.ts:43-44
- **Severity:** SHOULD-FIX
- **Category:** logic
- **Confidence:** CONFIRMED
- **Description:** When a user passes `?bm25Weight=0` or `?semanticWeight=0`, the expression `parseFloat("0") || undefined` evaluates to `undefined` because `0` is falsy. This causes the service layer to use default values (`0.4` and `0.6` respectively) instead of the explicitly-requested `0`. A weight of `0` is a valid configuration meaning "disable this search method entirely", so silently overriding it is incorrect behavior.
- **Evidence:**
  ```typescript
  // search/route.ts lines 43-44
  bm25Weight: searchParams.get('bm25Weight') ? parseFloat(searchParams.get('bm25Weight')!) : undefined,
  semanticWeight: searchParams.get('semanticWeight') ? parseFloat(searchParams.get('semanticWeight')!) : undefined,
  ```
  Wait -- re-reading the actual code:
  ```typescript
  bm25Weight: searchParams.get('bm25Weight') ? parseFloat(searchParams.get('bm25Weight')!) : undefined,
  semanticWeight: searchParams.get('semanticWeight') ? parseFloat(searchParams.get('semanticWeight')!) : undefined,
  ```
  Actually the current code on lines 43-44 does NOT use `|| undefined` after parseFloat. It uses a ternary: if the param exists (truthy string), parseFloat it directly; otherwise undefined. So `parseFloat("0")` correctly returns `0`.

  **RETRACTED** -- Upon re-reading, the current code correctly handles the zero case. The ternary guard `searchParams.get('bm25Weight') ? parseFloat(...) : undefined` only checks whether the query param is present (non-null, non-empty string), not whether the parsed value is truthy. `"0"` is a truthy string, so `parseFloat("0")` returns `0` which is passed through correctly.

  *(This finding is withdrawn after verification.)*

## NIT

No nit issues found.

## CLEAN

Files with no issues found:
- app/api/agents/route.ts -- No issues. Proper try-catch, JSON parse guard, service delegation.
- app/api/agents/[id]/route.ts -- No issues. UUID validation, JSON parse guard, service delegation.
- app/api/agents/[id]/amp/addresses/route.ts -- No issues. UUID validation, try-catch, JSON parse guard.
- app/api/agents/[id]/amp/addresses/[address]/route.ts -- No issues. UUID validation, JSON parse guard.
- app/api/agents/[id]/chat/route.ts -- No issues. UUID validation, JSON parse guard, NaN guard on limit.
- app/api/agents/[id]/config/deploy/route.ts -- No issues. Auth check, UUID validation, JSON parse guard.
- app/api/agents/[id]/database/route.ts -- No issues. UUID validation, service delegation.
- app/api/agents/[id]/docs/route.ts -- No issues. UUID validation, graceful empty-body handling, try-catch.
- app/api/agents/[id]/email/addresses/route.ts -- No issues. UUID validation, JSON parse guard.
- app/api/agents/[id]/email/addresses/[address]/route.ts -- No issues. UUID validation, JSON parse guard.
- app/api/agents/[id]/export/route.ts -- No issues. UUID validation, filename sanitization, JSON parse guard.
- app/api/agents/[id]/graph/code/route.ts -- No issues. UUID validation, graceful empty-body handling, NaN guard on depth.
- app/api/agents/[id]/graph/db/route.ts -- No issues. UUID validation, JSON parse guard.
- app/api/agents/[id]/graph/query/route.ts -- No issues. UUID validation, service delegation.
- app/api/agents/[id]/hibernate/route.ts -- No issues. UUID validation, optional body parsing with defaults.
- app/api/agents/[id]/index-delta/route.ts -- No issues. UUID validation, NaN guard on batchSize.
- app/api/agents/[id]/memory/consolidate/route.ts -- No issues. UUID validation, JSON parse guard, NaN guard.
- app/api/agents/[id]/memory/long-term/route.ts -- No issues. UUID validation, NaN guards, minConfidence clamped to [0,1].
- app/api/agents/[id]/memory/route.ts -- No issues. UUID validation, graceful JSON parse fallback.
- app/api/agents/[id]/messages/route.ts -- No issues. UUID validation, JSON parse guard.
- app/api/agents/[id]/messages/[messageId]/route.ts -- No issues. UUID validation, JSON parse guards on all mutation methods.
- app/api/agents/[id]/metadata/route.ts -- No issues. UUID validation, JSON parse guard, error differentiation.
- app/api/agents/[id]/metrics/route.ts -- No issues. UUID validation, JSON parse guard.
- app/api/agents/[id]/playback/route.ts -- No issues. UUID validation, JSON parse guard, try-catch.
- app/api/agents/[id]/repos/route.ts -- No issues. UUID validation, JSON parse guard, required param check.
- app/api/agents/[id]/search/route.ts -- No issues. UUID validation, JSON parse guard, proper NaN handling.
- app/api/agents/[id]/session/route.ts -- No issues. UUID validation, JSON parse guard, 409 handling.
- app/api/agents/[id]/skills/route.ts -- No issues. UUID validation, JSON parse guard, auth distinction.
- app/api/agents/[id]/skills/settings/route.ts -- No issues. UUID validation, JSON parse guard, body.settings type guard.
- app/api/agents/[id]/subconscious/route.ts -- No issues. UUID validation, JSON parse guard, try-catch.
- app/api/agents/[id]/tracking/route.ts -- No issues. UUID validation, graceful JSON parse fallback.
- app/api/agents/[id]/transfer/route.ts -- No issues. UUID validation, JSON parse guard, try-catch.
- app/api/agents/[id]/wake/route.ts -- No issues. UUID validation, optional body with defaults, case-preserving program name.
- app/api/agents/by-name/[name]/route.ts -- No issues. Proper fallback for error case.
- app/api/agents/directory/route.ts -- No issues. Simple delegation.
- app/api/agents/directory/lookup/[name]/route.ts -- No issues. Proper fallback response.
- app/api/agents/directory/sync/route.ts -- No issues. Simple delegation.
- app/api/agents/docker/create/route.ts -- No issues. JSON parse guard, try-catch.
- app/api/agents/email-index/route.ts -- No issues. Proper parameter extraction.
- app/api/agents/health/route.ts -- No issues. URL type validation, JSON parse guard, try-catch.
- app/api/agents/import/route.ts -- No issues. File presence check, JSON parse guard for options.
- app/api/agents/normalize-hosts/route.ts -- No issues. Simple delegation.
- app/api/agents/register/route.ts -- No issues. JSON parse guard, try-catch.
- app/api/agents/startup/route.ts -- No issues. Simple delegation.
- app/api/agents/unified/route.ts -- No issues. NaN guard on timeout.

## Summary

After 6 prior fix passes, these 45 API route files are **clean**. Every route consistently:
1. Validates UUID path parameters with `isValidUuid()` where applicable
2. Guards against malformed JSON bodies with try-catch around `request.json()`
3. Delegates business logic to service functions and checks `result.error`
4. Handles NaN from `parseInt`/`parseFloat` with `|| defaultValue` fallbacks
5. Wraps handlers in try-catch where service functions might throw unexpectedly
6. Uses proper auth checking patterns (distinguishing "no auth" from "failed auth")

The one finding I initially identified (CC-P7-A0-001) was **retracted** after careful re-reading confirmed the code correctly handles zero values via ternary guards rather than `|| undefined`.

## Self-Verification

- [x] I read every file in my domain COMPLETELY (all lines, not skimmed, not grep-only)
- [x] For each finding, I included the exact file:line reference
- [x] For each finding, I included the actual code snippet as evidence
- [x] I verified each finding by tracing the code flow (not just pattern matching)
- [x] I categorized findings correctly (the one finding was retracted after verification)
- [x] My finding IDs use the assigned prefix: CC-P7-A0-001
- [x] My report file uses the UUID filename: epcp-correctness-P7-b3559652-053c-4427-89a8-5461446b3d5d.md
- [x] I did NOT report issues outside my assigned domain files
- [x] I noted code paths that appear to lack test coverage: N/A -- test coverage is outside this domain
- [x] My report has all required sections: MUST-FIX, SHOULD-FIX, NIT, CLEAN
- [x] I listed CLEAN files explicitly (all 45 files)
- [x] Total finding count in my return message matches the actual count in the report
- [x] My return message to the orchestrator is exactly 1-2 lines

# EPCP Final PR Review Report

**Generated:** 2026-02-22T17:11:00
**Pass:** 1
**Pipeline:** Code Correctness → Claim Verification → Skeptical Review
**Dedup:** 51 raw findings → 49 unique findings (2 duplicates removed)
**Note:** 19 P10-prefixed findings from a prior review cycle were discarded before deduplication.

---

## Summary

| Severity | Count |
|----------|-------|
| **MUST-FIX** | 4 |
| **SHOULD-FIX** | 25 |
| **NIT** | 20 |
| **Total findings** | 49 |

**Verdict: REQUEST CHANGES — 4 must-fix issue(s) found.**

---

## MUST-FIX Issues

### [MF-001] Command injection via tmux display-message in config-notification-service
**File:** services/config-notification-service.ts:107-110
**Original IDs:** CC-P1-A1-001
**Phases:** CC

The `sendTmuxNotification` function escapes double quotes but does not escape backticks, `$()`, or other shell metacharacters. The `sessionName` parameter and `message` (derived from user-controllable fields like `operation` and `targetName`) are interpolated into a shell command. An attacker who can set an agent name or operation string containing backticks or `$(...)` can achieve command injection.

```typescript
// line 107
const escaped = truncated.replace(/"/g, '\\"')
// line 110
await execAsync(`tmux display-message -t "${sessionName}" "[GOVERNANCE] ${escaped}"`, { timeout: 5000 })
```

If `sessionName` is `foo$(rm -rf /)`, the command becomes: `tmux display-message -t "foo$(rm -rf /)" "..."` -- the shell will execute the subcommand.

**Fix:** Use `execFile` (which does not invoke a shell) instead of `exec`, passing arguments as an array. Alternatively, apply proper shell escaping to both `sessionName` and `escaped` (replace all `'` with `'\''` and wrap in single quotes, or use a library like `shell-escape`).

---

### [MF-002] `registerAgent` writes user-controlled `agentId` to filesystem without UUID validation
**File:** services/agents-core-service.ts:764-817
**Original IDs:** CC-P1-A1-002
**Phases:** CC

In the WorkTree branch (line 764), `agentId` is derived from `sessionName` via regex replacement but is NOT validated as a UUID. More critically, in the cloud agent branch (line 805), `agentId = body.id` is used directly with NO sanitization whatsoever, and this value is written to the filesystem at line 816-817. A malicious `body.id` like `../../etc/evil` would write to `~/.aimaestro/agents/../../etc/evil.json`.

```typescript
// line 764 (WorkTree branch)
agentId = sessionName.replace(/[^a-zA-Z0-9_-]/g, '-')
// line 805 (Cloud branch)
agentId = body.id  // No sanitization!
// line 816
const agentFilePath = path.join(agentsDir, `${agentId}.json`)
fs.writeFileSync(agentFilePath, JSON.stringify(agentConfig, null, 2), 'utf8')
```

**Fix:** Validate `agentId` as a UUID (using the same `UUID_PATTERN` regex used in `agents-skills-service.ts`) before using it in a file path. Also apply `path.basename()` as an additional safeguard.

---

### [MF-003] Config deploy endpoint allows unauthenticated system-owner access to modify agent configuration
**File:** services/headless-router.ts:827-836
**Original IDs:** CC-P1-A2-001
**Phases:** CC

The `/api/agents/:id/config/deploy` endpoint checks `auth.error` and returns 403 if auth fails, but `authenticateAgent()` returns `{}` (no error, no agentId) when BOTH `Authorization` and `X-Agent-Id` headers are absent (system owner / web UI case). This means any unauthenticated request without auth headers bypasses the guard and reaches `deployConfigToAgent()` with `auth.agentId = undefined`. Unlike other governance-gated routes (e.g., transfers at lines 1199-1213 and 1225-1237), this endpoint does NOT check `if (!auth.agentId)` as a second gate. Since config deployment can modify an agent's skills, plugins, hooks, and MCP servers, allowing unauthenticated access is a security risk in multi-agent environments.

```typescript
// headless-router.ts:827-836
{ method: 'POST', pattern: /^\/api\/agents\/([^/]+)\/config\/deploy$/, paramNames: ['id'], handler: async (req, res, params) => {
  const body = await readJsonBody(req)
  const auth = authenticateAgent(getHeader(req, 'Authorization'), getHeader(req, 'X-Agent-Id'))
  if (auth.error) {          // <-- only checks error, not missing agentId
    sendJson(res, 403, { error: auth.error })
    return
  }
  sendServiceResult(res, await deployConfigToAgent(params.id, body.configuration || body, auth.agentId))
  //                                                                                      ^^^ undefined
}}
```

**Fix:** Add a second guard after the `auth.error` check: `if (!auth.agentId) { sendJson(res, 401, { error: 'Authenticated agent identity required for config deployment' }); return }`. This matches the pattern used by other governance-gated routes in the same file.

---

### [MF-004] Claim: "836 tests pass" is inaccurate — actual count is 628
**File:** (documentation/claims)
**Original IDs:** CV-P1-001
**Phases:** CV

The claim states 836 test cases pass, but `grep -cE '^\s*(it|test)\(' tests/*.test.ts` returns 628 total across all 26 test files. The test suite has 628 tests, not 836. This may confuse future reviewers who expect to see 836 tests.

Breakdown: agent-config-governance-extended (56), agent-registry (91), task-registry (47), cross-host-governance (39), message-filter (27), document-registry (27), role-attestation (25), governance-request-registry (25), team-api (24), team-registry (24), agent-utils (21), document-api (21), content-security (19), amp-auth (19), validate-team-mutation (18), agent-config-governance (16), governance-sync (16), host-keys (15), manager-trust (15), governance (13), governance-peers (20), governance-endpoint-auth (12), transfer-resolve-route (12), transfer-registry (9), amp-address (9), agent-auth (8). **Total: 628**.

**Fix:** Correct the test count in documentation/commit messages to 628.

---

## SHOULD-FIX Issues

### [SF-001] `purgeOldRequests` return value double-counts expired requests that were ALSO purged
**File:** lib/governance-request-registry.ts:279
**Original IDs:** CC-P1-A0-001
**Phases:** CC

The return value `purged + expired` overstates actual removals. `purged` counts requests removed from the array. `expired` counts requests status-changed but still present. Callers logging this value see a count that overstates actual removals.

**Fix:** Return `{ purged, expired }` as a structured result so callers can distinguish, or document that the return value includes both purged and expired (status-changed) requests.

---

### [SF-002] Duplicate TTL logic between `purgeOldRequests` and `expirePendingRequests`
**File:** lib/governance-request-registry.ts:256-269 and 288-312
**Original IDs:** CC-P1-A0-002
**Phases:** CC

TTL expiry logic for pending requests is duplicated: once inside `purgeOldRequests` (hardcoded 7-day TTL) and once in standalone `expirePendingRequests` (configurable `ttlDays` parameter). The hardcoded `7 * 86_400_000` in `purgeOldRequests` cannot be overridden by callers.

**Fix:** Remove the TTL logic from `purgeOldRequests` and have it call `expirePendingRequests` internally, or extract shared logic to a helper.

---

### [SF-003] `getGovernanceRequest` and `listGovernanceRequests` read without acquiring the lock
**File:** lib/governance-request-registry.ts:76-98
**Original IDs:** CC-P1-A0-003
**Phases:** CC

These functions call `loadGovernanceRequests()` directly without acquiring the `governance-requests` lock. The real concern is TOCTOU in callers (e.g., `cross-host-governance-service.ts:253` calls `getGovernanceRequest` without a lock, then later calls `approveGovernanceRequest` under a lock).

**Fix:** For Phase 1 this is acceptable since it's single-process. Document that these are intentionally lock-free reads. For multi-process Phase 2, wrap reads in `withLock`.

---

### [SF-004] `updateSkills` uses wrong governance operation type for removals
**File:** services/agents-skills-service.ts:101
**Original IDs:** CC-P1-A1-003
**Phases:** CC

The `updateSkills` function performs a single governance check with operation `'add-skill'` at line 101, but the function also handles skill removals (`body.remove`). When a caller only passes `body.remove`, the governance check still evaluates as `'add-skill'`, which is semantically incorrect.

**Fix:** Determine the correct operation type based on which fields are present in `body`.

---

### [SF-005] `saveSkillSettings` uses `'update-hooks'` operation type instead of a skill-settings operation
**File:** services/agents-skills-service.ts:292
**Original IDs:** CC-P1-A1-004
**Phases:** CC

The function uses `'update-hooks'` as its governance operation type, which is semantically wrong -- it's saving skill settings, not updating hooks. This means the audit trail and any operation-specific RBAC logic would be misleading.

**Fix:** Either add a dedicated operation type (e.g., `'update-skill-settings'`), or use `'bulk-config'` which is a more general operation type.

---

### [SF-006] `deleteAgentById` auto-rejection only targets `configure-agent` requests
**File:** services/agents-core-service.ts:720-721
**Original IDs:** CC-P1-A1-005
**Phases:** CC

When an agent is deleted, the code auto-rejects pending governance requests but only those of type `'configure-agent'`. Other governance request types that reference the deleted agent (e.g., `'add-to-team'`, `'assign-cos'`, `'transfer-agent'`) would remain pending indefinitely.

**Fix:** Remove or expand the `r.type === 'configure-agent'` filter so that ALL pending requests targeting the deleted agent are auto-rejected.

---

### [SF-007] `deployConfigToAgent` uses agent's `workingDirectory` without validating it exists
**File:** services/agents-config-deploy-service.ts:55-60
**Original IDs:** CC-P1-A1-006
**Phases:** CC

The function resolves the `.claude/` directory from `agent.workingDirectory` but does not validate that this directory actually exists on the filesystem. If the workingDirectory points to a non-existent or stale path, the deployment will silently create the directory tree at an unexpected location.

**Fix:** Add a check that `workingDir` exists on the filesystem before proceeding with deployment. Return a 400 error if it doesn't exist.

---

### [SF-008] `deployBulkConfig` always uses `deployAddSkill`, never `deployRemoveSkill`
**File:** services/agents-config-deploy-service.ts:443-448
**Original IDs:** CC-P1-A1-007
**Phases:** CC

The `deployBulkConfig` function calls `deployAddSkill` when `config.skills` is present, but there is no way for the bulk config to remove skills -- it always adds them. This makes `bulk-config` asymmetric.

**Fix:** Add a mechanism to indicate add vs remove intent for each section in the bulk config payload (e.g., separate `skillsToAdd`/`skillsToRemove` fields).

---

### [SF-009] `notifyConfigRequestOutcome` accesses `request.payload.configuration?.operation` but type shows `configuration` is optional
**File:** services/config-notification-service.ts:29
**Original IDs:** CC-P1-A1-008
**Phases:** CC

If `configuration` is somehow missing on a `configure-agent` request (corrupt data), the notification will use the generic fallback `'configure'`, which is misleading. The bigger concern: line 28 accesses `request.payload.agentId` without checking if `request.payload` exists at all (though the type guarantees it).

**Fix:** Consider logging a warning when `configuration` is missing for a `configure-agent` request, as it indicates data corruption.

---

### [SF-010] SSRF incomplete mitigation in `proxyHealthCheck` -- known hosts bypass private IP check
**File:** services/agents-core-service.ts:1619-1643
**Original IDs:** CC-P1-A1-009
**Phases:** CC

The check at line 1641 allows requests to private IPs if the host is "known" (in `hosts.json`). Since `hosts.json` is writable via the `/api/hosts` endpoints, an attacker who can modify `hosts.json` could add a private IP as a known host and then use `proxyHealthCheck` as an SSRF proxy.

**Fix:** Consider whether private IPs should ALWAYS be blocked in the health check proxy regardless of `hosts.json` configuration, or document this as an accepted risk for the localhost-only Phase 1 architecture.

---

### [SF-011] Config deploy endpoint sends 403 for auth errors instead of 401
**File:** services/headless-router.ts:832
**Original IDs:** CC-P1-A2-002
**Phases:** CC

When `authenticateAgent` returns an error, the config deploy endpoint hardcodes `403`. Other governance-gated routes correctly use `auth.status || 401`.

```typescript
// headless-router.ts:832 -- hardcoded 403
sendJson(res, 403, { error: auth.error })
// headless-router.ts:1204 -- correct pattern
sendJson(res, auth.status || 401, { error: auth.error })
```

**Fix:** Change to `sendJson(res, auth.status || 401, { error: auth.error })` to be consistent with all other auth-gated routes.

---

### [SF-012] Config deploy endpoint body fallback bypasses ConfigurationPayload type validation
**File:** services/headless-router.ts:835
**Original IDs:** CC-P1-A2-003
**Phases:** CC

The expression `body.configuration || body` allows callers to send the configuration payload either nested or directly. If `body.configuration` is `null` or `0` (falsy but present), the fallback to `body` triggers unexpectedly, passing extra fields to `deployConfigToAgent()`.

**Fix:** Use strict check: `body.configuration !== undefined ? body.configuration : body` or require the `configuration` wrapper.

---

### [SF-013] configure-agent execution silently succeeds even when deployConfigToAgent returns an error
**File:** services/cross-host-governance-service.ts:483-498
**Original IDs:** CC-P1-A2-004
**Phases:** CC

When `configure-agent` is handled and `deployResult.error` is set, the function returns early. However, the governance request status has already been set to `executed` by the caller. This means a governance request can show status `executed` even though the actual config deployment failed.

**Fix:** Update the governance request status to a 'failed' state when execution fails, or document that `executed` means "attempted" not "succeeded."

---

### [SF-014] Missing UUID validation on `id` path parameter in config/deploy route
**File:** app/api/agents/[id]/config/deploy/route.ts:20
**Original IDs:** CC-P1-A3-001
**Phases:** CC

The `id` path parameter is used directly without UUID format validation. The `chief-of-staff/route.ts` (line 14) validates with `isValidUuid(id)` before proceeding, but this route does not.

**Fix:** Add `isValidUuid(id)` validation before proceeding, returning 400 for invalid format.

---

### [SF-015] Missing UUID validation on `id` path parameter in skills route
**File:** app/api/agents/[id]/skills/route.ts:22,42,69,96
**Original IDs:** CC-P1-A3-002
**Phases:** CC

Same issue as SF-014. All four handlers (GET, PATCH, POST, DELETE) extract `id` from params without UUID format validation.

**Fix:** Add UUID validation at the top of each handler, or factor it into a shared middleware/helper.

---

### [SF-016] Missing UUID validation on `id` path parameter in skills/settings route
**File:** app/api/agents/[id]/skills/settings/route.ts:19,39
**Original IDs:** CC-P1-A3-003
**Phases:** CC

Same issue as SF-014. Both GET and PUT handlers extract `id` without validation.

**Fix:** Add UUID validation.

---

### [SF-017] Rate limit key is global, not per-team, for COS password auth
**File:** app/api/teams/[id]/chief-of-staff/route.ts:31
**Original IDs:** CC-P1-A3-004
**Phases:** CC

The rate limit key `'governance-cos-auth'` is shared across all teams. A brute-force attempt on Team A's COS endpoint will lock out legitimate COS changes on Team B.

**Fix:** Include the team ID in the rate limit key: `checkRateLimit(\`governance-cos-auth:${id}\`)`.

---

### [SF-018] Duplicate filtering of pendingConfigRequests in AgentProfile.tsx
**File:** components/AgentProfile.tsx:880-883
**Original IDs:** CC-P1-A4-001
**Phases:** CC

The pending config request filtering is computed inline twice (lines 880 and 882) on every render. Three separate filter passes for the same data is wasteful.

**Fix:** Extract the filtered list into a `useMemo` or a local variable.

---

### [SF-019] Approve/reject buttons fire without user confirmation or error feedback
**File:** components/marketplace/AgentSkillEditor.tsx:295-306
**Original IDs:** CC-P1-A4-002
**Phases:** CC

The approve and reject buttons call `resolveConfigRequest` immediately with no confirmation dialog. The return value is not consumed -- the result is a floating promise. If the resolve call fails, the user has no feedback.

**Fix:** Await the result and show an error toast if `!result.success`. Add a confirmation step before approve/reject.

---

### [SF-020] No loading/disabled state on approve/reject buttons during pending request
**File:** components/marketplace/AgentSkillEditor.tsx:294-309
**Original IDs:** CC-P1-A4-003
**Phases:** CC

When a user clicks approve or reject, the buttons remain clickable. The user could double-click and fire duplicate requests. Unlike skill add/remove buttons which use `disabled={saving}`, the governance buttons have no such protection.

**Fix:** Track an in-flight state per request (e.g., `resolvingIds` set) and disable buttons while the request is pending.

---

### [SF-021] `canApprove` role check may be backwards -- checks subject agent's role, not actor's role
**File:** components/marketplace/AgentSkillEditor.tsx:72
**Original IDs:** CC-P1-A4-004
**Phases:** CC

`canApprove` is derived from whether the *viewed agent* (`agentId`) is the manager or COS -- not whether the *current user/acting agent* is. This means the approve/reject buttons appear when viewing the profile of the manager, but NOT when a manager views a regular agent's profile.

**Fix:** Either pass the acting/viewer agent's ID to `useGovernance` for role determination, or add a separate `viewerRole` check.

---

### [SF-022] Stale test count in file header comment (agent-config-governance-extended.test.ts)
**File:** tests/agent-config-governance-extended.test.ts:26
**Original IDs:** CC-P1-A5-001, CV-P1-002
**Phases:** CC, CV
**Also identified by:** CV-P1-002

The file header comment says "Total: 48 tests (15 + 12 + 15 + 6)" but the actual test counts are 56 (20 + 16 + 14 + 6). The per-module counts are also stale.

**Fix:** Update header comment to "Total: 56 tests (20 + 16 + 14 + 6)" and update the per-module test counts.

---

### [SF-023] Stale test count in file header comment (governance-endpoint-auth.test.ts)
**File:** tests/governance-endpoint-auth.test.ts:8
**Original IDs:** CC-P1-A5-002
**Phases:** CC

The file header says "Coverage: 13 tests across 3 security fixes" but there are only 12 actual `it()` blocks.

**Fix:** Update header to "Coverage: 12 tests across 3 security fixes".

---

### [SF-024] Pending/approved/refused UI indicators: `type` query parameter silently ignored by API
**File:** hooks/useGovernance.ts:96, lib/governance-request-registry.ts:82-98, services/headless-router.ts:1367-1372, app/api/v1/governance/requests/route.ts:78-100
**Original IDs:** CV-P1-003, CV-P1-005
**Phases:** CV
**Also identified by:** CV-P1-005

The `type=configure-agent` query parameter sent by `useGovernance.ts:96` is silently ignored by both the Next.js route and headless router. The `listGovernanceRequests` function only supports `status`, `hostId`, and `agentId` filters -- NOT `type`. This means `pendingConfigRequests` will contain ALL pending governance requests, not just configure-agent ones. The AgentSkillEditor then displays them all under "Pending Configuration Changes."

**Fix:** Add `type` to the `GovernanceRequestFilter` interface and implement filtering in `listGovernanceRequests`.

---

### [SF-025] COS auto-rejection missing in headless mode
**File:** services/headless-router.ts (missing endpoint), app/api/teams/[id]/chief-of-staff/route.ts:60-77
**Original IDs:** CV-P1-004
**Phases:** CV

The Next.js API route correctly implements auto-rejection of pending configure-agent requests when COS is set to null. But the headless router does NOT have a `/api/teams/:id/chief-of-staff` endpoint. The 11a safeguard only works in full Next.js server mode, not in headless mode.

**Fix:** Add a `/api/teams/:id/chief-of-staff` endpoint to the headless router with the same auto-reject logic.

---

## Nits & Suggestions

### [NT-001] `ConfigurationPayload` fields are all optional with no discriminated union
**File:** types/governance-request.ts:62-71
**Original IDs:** CC-P1-A0-004
**Phases:** CC

`ConfigurationPayload` has an `operation` field that determines which other fields are relevant, but all other fields are optional. A discriminated union would provide compile-time safety. The deploy service does runtime validation, so this is not a functional bug.

**Fix:** Consider a discriminated union for stricter type safety. Low priority since runtime validation exists.

---

### [NT-002] Lock ordering comment does not mention `governance-requests` lock name
**File:** lib/file-lock.ts:13-22
**Original IDs:** CC-P1-A0-005
**Phases:** CC

The lock ordering invariant comment lists `'teams'`, `'transfers'`, `'governance'` but the governance request registry uses `'governance-requests'` (a different lock name). The comment should document it.

**Fix:** Add `'governance-requests'` to the lock ordering documentation in `file-lock.ts`.

---

### [NT-003] `approveGovernanceRequest` silently returns request unchanged for terminal states
**File:** lib/governance-request-registry.ts:156
**Original IDs:** CC-P1-A0-006
**Phases:** CC

When called on an already-rejected or already-executed request, it returns the request unchanged without any indication that the approval was not applied. Callers have no way to distinguish "approval was applied" from "request was already terminal."

**Fix:** Consider returning a result type like `{ applied: boolean; request: GovernanceRequest }`.

---

### [NT-004] `getStartupInfo` return type is `ServiceResult<any>`
**File:** services/agents-core-service.ts:1577
**Original IDs:** CC-P1-A1-010
**Phases:** CC

The return type uses `any` instead of a properly typed interface.

**Fix:** Define a proper interface for the startup status data.

---

### [NT-005] `proxyHealthCheck` return type is `ServiceResult<any>`
**File:** services/agents-core-service.ts:1594
**Original IDs:** CC-P1-A1-011
**Phases:** CC

Same as NT-004 -- the return type uses `any`.

**Fix:** Type the expected health check response shape.

---

### [NT-006] `RegisterAgentParams` uses `[key: string]: any` index signature
**File:** services/agents-core-service.ts:124
**Original IDs:** CC-P1-A1-012
**Phases:** CC

The `[key: string]: any` index signature defeats TypeScript's type narrowing for the entire interface.

**Fix:** Use a separate `cloudConfig?: Record<string, unknown>` field instead of an index signature.

---

### [NT-007] Dead code: memory settings check in `saveSkillSettings` does nothing
**File:** services/agents-skills-service.ts:301-306
**Original IDs:** CC-P1-A1-013
**Phases:** CC

The code checks if `settings.memory` is truthy and retrieves the subconscious, but does nothing with it besides logging. Appears to be an unfinished placeholder.

**Fix:** Either implement the intended behavior or remove this dead code block.

---

### [NT-008] Inconsistent agent registry API usage: sync `getAgent` vs async `agentRegistry.getAgent`
**File:** services/agents-skills-service.ts:95 vs 251
**Original IDs:** CC-P1-A1-014
**Phases:** CC

The file uses two different agent lookup methods: the synchronous `getAgent` from `@/lib/agent-registry` and the async `agentRegistry.getAgent` from `@/lib/agent`. These are two different agent systems (file-based vs in-memory) that may return different results.

**Fix:** Standardize on one agent lookup method within the file, or document why different methods are used for different functions.

---

### [NT-009] `agents-config-deploy-service.ts` does not validate `agentId` as UUID
**File:** services/agents-config-deploy-service.ts:38-44
**Original IDs:** CC-P1-A1-015
**Phases:** CC

Unlike `agents-skills-service.ts` (which validates UUID format), the `deployConfigToAgent` function does not validate `agentId`. The `agentId` is not used directly in filesystem paths here (the `workingDirectory` from the registry is used instead), so this is lower severity.

**Fix:** Add UUID validation consistent with other service files.

---

### [NT-010] Lazy import of deployConfigToAgent in performRequestExecution is unnecessary
**File:** services/cross-host-governance-service.ts:491
**Original IDs:** CC-P1-A2-005
**Phases:** CC

The comment says "lazy import to avoid circular deps," but `deployConfigToAgent` is already statically imported in `headless-router.ts` from the same module. The lazy import adds unnecessary async overhead.

**Fix:** Verify whether circular dependency is real for this specific import graph. If not, switch to a static import.

---

### [NT-011] Lazy import of notifyConfigRequestOutcome is duplicated in three places
**File:** services/cross-host-governance-service.ts:213,293,348
**Original IDs:** CC-P1-A2-006
**Phases:** CC

The dynamic `import('@/services/config-notification-service')` call with its try/catch wrapper is repeated identically in three places.

**Fix:** Extract a helper function like `async function safeNotifyConfigOutcome(request, outcome)`.

---

### [NT-012] Inconsistent error response shape across routes
**File:** app/api/agents/[id]/skills/settings/route.ts:22,28 vs app/api/agents/[id]/skills/route.ts:25,31
**Original IDs:** CC-P1-A3-005
**Phases:** CC

The skills/settings route wraps errors in `{ success: false, error: ... }` while the skills route uses `{ error: ... }` (no `success` field).

**Fix:** Standardize error response shape across the skills API family.

---

### [NT-013] `body.configuration || body` fallback could mask malformed payloads (Next.js route)
**File:** app/api/agents/[id]/config/deploy/route.ts:34
**Original IDs:** CC-P1-A3-006
**Phases:** CC

The expression `body.configuration || body` silently accepts two payload shapes. Not harmful because the downstream service validates `config.operation`.

**Fix:** Consider explicit documentation or a clear comment explaining the two accepted payload shapes.

---

### [NT-014] Unused atomic rate limit function -- non-atomic pattern used instead
**File:** app/api/teams/[id]/chief-of-staff/route.ts:31-45
**Original IDs:** CC-P1-A3-007
**Phases:** CC

The rate-limit module provides `checkAndRecordAttempt()` but the code uses separate check/record pattern. The separate pattern is actually **correct** for the COS use case: it only penalizes failed attempts, not successful ones.

**Fix:** Add a comment explaining why `checkAndRecordAttempt` is not used.

---

### [NT-015] Accessibility: approve/reject buttons lack `aria-label`
**File:** components/marketplace/AgentSkillEditor.tsx:294-309
**Original IDs:** CC-P1-A4-005
**Phases:** CC

The approve and reject icon buttons have `title` attributes but no `aria-label`. Screen readers may not announce the purpose clearly.

**Fix:** Add `aria-label="Approve configuration request"` and `aria-label="Reject configuration request"`.

---

### [NT-016] Pending config display shows raw `req.type` as fallback
**File:** components/marketplace/AgentSkillEditor.tsx:286-287
**Original IDs:** CC-P1-A4-006
**Phases:** CC

If `configuration` is undefined, it falls back to `req.type` which is a value like `'configure-agent'` -- a machine-readable string, not user-friendly.

**Fix:** Map `req.type` to a human-readable label.

---

### [NT-017] `useGovernance` instantiated twice for same agentId in AgentProfile flow
**File:** components/AgentProfile.tsx:55 and components/marketplace/AgentSkillEditor.tsx:70
**Original IDs:** CC-P1-A4-007
**Phases:** CC

When the Skills section is expanded in AgentProfile, `useGovernance(agentId)` runs in both components, causing two independent sets of fetch calls to the same 4 API endpoints, doubling network traffic.

**Fix:** Consider passing governance data as props or creating a GovernanceContext provider.

---

### [NT-018] Module 4 tests test mock call patterns rather than actual notification service logic
**File:** tests/agent-config-governance-extended.test.ts:1220-1230
**Original IDs:** CC-P1-A5-003
**Phases:** CC

All 6 tests in Module 4 verify that the cross-host-governance-service calls the mocked `notifyConfigRequestOutcome` with correct arguments, providing zero coverage of the actual `config-notification-service.ts` logic.

**Fix:** Consider creating a separate `config-notification-service.test.ts` that tests the function directly.

---

### [NT-019] `makeAgentWithSubconscious` factory return type is `Record<string, unknown>`
**File:** tests/agent-config-governance-extended.test.ts:303-310
**Original IDs:** CC-P1-A5-004
**Phases:** CC

The factory function returns `Record<string, unknown>`, which loses all type information. A more specific return type would catch mismatches if the Agent class interface changes.

**Fix:** Use a more specific interface as the return type.

---

### [NT-020] `vi.waitFor` used for fire-and-forget fetch assertion relies on timing
**File:** tests/governance-endpoint-auth.test.ts:220,255
**Original IDs:** CC-P1-A5-005
**Phases:** CC

The tests use `vi.waitFor(() => expect(mockFetch).toHaveBeenCalledTimes(1))` to wait for fire-and-forget fetch to complete. The `vi.waitFor` approach is the best available option for testing fire-and-forget patterns.

**Fix:** No immediate action needed. If flakiness is observed in CI, consider adding an explicit `timeout` option to `vi.waitFor`.

---

## Deduplication Log

| Final ID | Original IDs | Duplicates Removed | Reason |
|----------|-------------|-------------------|--------|
| MF-001 | CC-P1-A1-001 | 0 | Unique finding |
| MF-002 | CC-P1-A1-002 | 0 | Unique finding |
| MF-003 | CC-P1-A2-001 | 0 | Unique finding |
| MF-004 | CV-P1-001 | 0 | Unique finding |
| SF-001 | CC-P1-A0-001 | 0 | Unique finding |
| SF-002 | CC-P1-A0-002 | 0 | Unique finding |
| SF-003 | CC-P1-A0-003 | 0 | Unique finding |
| SF-004 | CC-P1-A1-003 | 0 | Unique finding |
| SF-005 | CC-P1-A1-004 | 0 | Unique finding |
| SF-006 | CC-P1-A1-005 | 0 | Unique finding |
| SF-007 | CC-P1-A1-006 | 0 | Unique finding |
| SF-008 | CC-P1-A1-007 | 0 | Unique finding |
| SF-009 | CC-P1-A1-008 | 0 | Unique finding |
| SF-010 | CC-P1-A1-009 | 0 | Unique finding |
| SF-011 | CC-P1-A2-002 | 0 | Unique finding |
| SF-012 | CC-P1-A2-003 | 0 | Unique finding |
| SF-013 | CC-P1-A2-004 | 0 | Unique finding |
| SF-014 | CC-P1-A3-001 | 0 | Unique finding |
| SF-015 | CC-P1-A3-002 | 0 | Unique finding |
| SF-016 | CC-P1-A3-003 | 0 | Unique finding |
| SF-017 | CC-P1-A3-004 | 0 | Unique finding |
| SF-018 | CC-P1-A4-001 | 0 | Unique finding |
| SF-019 | CC-P1-A4-002 | 0 | Unique finding |
| SF-020 | CC-P1-A4-003 | 0 | Unique finding |
| SF-021 | CC-P1-A4-004 | 0 | Unique finding |
| SF-022 | CC-P1-A5-001, CV-P1-002 | 1 | Same file+line (test.ts:26), both identify stale test count comment; CC-P1-A5-001 is more detailed, SHOULD-FIX kept as highest severity |
| SF-023 | CC-P1-A5-002 | 0 | Unique finding |
| SF-024 | CV-P1-003, CV-P1-005 | 1 | Both identify missing `type` query filter for governance requests API; CV-P1-005 is subset of CV-P1-003; same root cause, same fix |
| SF-025 | CV-P1-004 | 0 | Unique finding |
| NT-001 | CC-P1-A0-004 | 0 | Unique finding |
| NT-002 | CC-P1-A0-005 | 0 | Unique finding |
| NT-003 | CC-P1-A0-006 | 0 | Unique finding |
| NT-004 | CC-P1-A1-010 | 0 | Unique finding |
| NT-005 | CC-P1-A1-011 | 0 | Unique finding |
| NT-006 | CC-P1-A1-012 | 0 | Unique finding |
| NT-007 | CC-P1-A1-013 | 0 | Unique finding |
| NT-008 | CC-P1-A1-014 | 0 | Unique finding |
| NT-009 | CC-P1-A1-015 | 0 | Unique finding |
| NT-010 | CC-P1-A2-005 | 0 | Unique finding |
| NT-011 | CC-P1-A2-006 | 0 | Unique finding |
| NT-012 | CC-P1-A3-005 | 0 | Unique finding |
| NT-013 | CC-P1-A3-006 | 0 | Unique finding |
| NT-014 | CC-P1-A3-007 | 0 | Unique finding |
| NT-015 | CC-P1-A4-005 | 0 | Unique finding |
| NT-016 | CC-P1-A4-006 | 0 | Unique finding |
| NT-017 | CC-P1-A4-007 | 0 | Unique finding |
| NT-018 | CC-P1-A5-003 | 0 | Unique finding |
| NT-019 | CC-P1-A5-004 | 0 | Unique finding |
| NT-020 | CC-P1-A5-005 | 0 | Unique finding |

**P10 findings discarded (19):** CC-P10-A0-001 through CC-P10-A0-014, CC-P10-A1-001 through CC-P10-A1-004, plus CV-P10/SR-P10 findings from prior review cycle source reports. These were from an already-resolved review cycle and excluded per orchestrator instruction.

---

## Self-Verification

- [x] I parsed ALL findings from the intermediate report (none skipped or missed)
- [x] I grouped findings by file path before comparing
- [x] For each potential duplicate pair, I checked ALL THREE conditions:
      (1) same file path + (2) overlapping lines within 5 + (3) same root cause
- [x] I did NOT merge findings at the same line that describe DIFFERENT bugs
      (e.g., CC-P1-A2-001 vs CC-P1-A2-002 vs CC-P1-A2-003 all at headless-router.ts:827-835 -- kept all three as they are different bugs: missing auth check, wrong HTTP status code, body validation bypass)
      (e.g., CC-P1-A4-002 vs CC-P1-A4-003 at AgentSkillEditor.tsx:294-309 -- kept both as they are different issues: missing confirmation vs missing disabled state)
- [x] I did NOT merge findings from different files even if they describe the same pattern
      (e.g., CC-P1-A3-001, CC-P1-A3-002, CC-P1-A3-003 -- same missing UUID validation pattern but different route files)
- [x] For merged findings: I kept the HIGHEST severity
      (SF-022: CC-P1-A5-001 SHOULD-FIX kept over CV-P1-002 NIT)
      (SF-024: both CV-P1-003 and CV-P1-005 were SHOULD-FIX, kept SHOULD-FIX)
- [x] For merged findings: I preserved ALL original IDs in "Also identified by" annotation
- [x] For merged findings: I noted ALL source phases (CC, CV, SR)
- [x] My final IDs use sequential numbering: MF-001..MF-004, SF-001..SF-025, NT-001..NT-020
- [x] My deduplication log has an entry for EVERY final finding (including unique ones)
- [x] Each dedup log entry includes the merge reasoning
- [x] My verdict follows the rules: MUST-FIX>0 → REQUEST CHANGES
- [x] Math check: 51 raw - 2 duplicates removed = 49 final (4 + 25 + 20 = 49)
- [x] The source reports section is preserved below
- [x] My return message to the orchestrator is exactly 1-2 lines

---

## Source Reports

- `epcp-correctness-P1-3408eab8-f0d4-4114-bee4-9d87f39b4dc0.md`
- `epcp-correctness-P1-3d927447-1513-4cb4-8989-cb94d384ac69.md`
- `epcp-correctness-P1-43d4b737-5df7-44d2-a2d7-5b31acfb03cb.md`
- `epcp-correctness-P1-525764f4-177f-4ada-8b4e-575384f68cb8.md`
- `epcp-correctness-P1-86e11eb0-e872-4e12-9d0d-2ac06f44ebf7.md`
- `epcp-correctness-P1-f3a6cd0f-28b9-4aa9-bf8a-5e7f356a34ac.md`
- `epcp-correctness-P10-4eb0b463-6a5f-4c16-8bc0-5acded1c514d.md` (discarded: P10)
- `epcp-correctness-P10-c1adbedc-06b2-49ff-b7ae-c24515a2b2a5.md` (discarded: P10)
- `epcp-claims-P1-c703d919-4f86-4e24-947b-8ef62d416552.md`
- `epcp-claims-P10-282c7b23-2b20-4293-9867-e933cb7551f4.md` (discarded: P10)
- `epcp-review-P1-2fe039a3-e6e0-4289-aef2-76d0e633622b.md`
- `epcp-review-P10-1d1655d9-89e3-47fc-a143-dcf35ca5ad6a.md` (discarded: P10)

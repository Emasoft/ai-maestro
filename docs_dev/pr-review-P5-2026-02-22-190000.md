# EPCP Final PR Review Report

**Generated:** 2026-02-22T19:00:00Z
**Pass:** 5
**Pipeline:** Code Correctness -> Claim Verification -> Skeptical Review
**Dedup:** 146 raw findings -> 142 unique findings (4 duplicates/invalid removed)

---

## Summary

| Severity | Count |
|----------|-------|
| **MUST-FIX** | 27 |
| **SHOULD-FIX** | 70 |
| **NIT** | 45 |
| **Total findings** | 142 |

**Verdict: REQUEST CHANGES -- 27 must-fix issue(s) found.**

---

## MUST-FIX Issues

### [MF-001] G4 revocation skipped when team type changes from open to closed
**File:** lib/team-registry.ts:347-369
**Original IDs:** CC-P5-A2-001
**Phases:** CC

When `updateTeam()` changes a team's type from `open` to `closed` without modifying `agentIds`, the G4 open-team revocation code does NOT run for existing members. The comment at line 349-350 says "MF-06: Always check when team is closed (not just when agentIds was explicitly provided), so that a type change to 'closed' also triggers revocation for existing members." However, the implementation at line 353 only processes "newly added" agents: `result2.agentIds.filter(aid => !previousAgentIds.includes(aid))`. When only the type changes (no agents added), `previousAgentIds` equals `result2.agentIds`, so `newlyAdded` is empty, and the `if (newlyAdded.length > 0)` guard at line 354 prevents any revocation.

**Fix:** When the type is changed to `closed`, revoke open-team memberships for ALL members of the team (not just newly added ones). Capture `previousType` before the update and check if type changed to `closed`.

---

### [MF-002] Double-release of index throttle slot on early-return paths
**File:** lib/index-delta.ts:434, 464, 585, 644, 655
**Original IDs:** CC-P5-A3-001 (index-delta)
**Phases:** CC

`runIndexDelta` calls `releaseSlot()` explicitly on 4 early-return paths AND in the catch block. The pattern is fragile: 4 separate explicit `releaseSlot()` calls plus the catch handler makes it easy for a future edit to miss one path or double-release. The correct pattern is try/finally for exactly-once release.

**Fix:** Replace try/catch with try/finally for slot release, remove all manual `releaseSlot()` calls.

---

### [MF-003] agent-registry read-modify-write race conditions: no file locking
**File:** lib/agent-registry.ts (multiple functions)
**Original IDs:** CC-P5-A3-002
**Phases:** CC

Unlike `task-registry.ts` and `document-registry.ts` which use `withLock()` from `file-lock.ts` for all mutations, `agent-registry.ts` has NO locking for ANY of its read-modify-write operations. Functions like `createAgent`, `updateAgent`, `deleteAgent`, `linkSession`, etc. all follow: `loadAgents()` -> modify -> `saveAgents()`. Concurrent API requests can lose updates.

**Fix:** Wrap all mutating functions in `withLock('agents', () => { ... })`.

---

### [MF-004] Unsafe type assertion on dynamic route params
**File:** app/teams/[id]/page.tsx:23
**Original IDs:** CC-P5-A7-001
**Phases:** CC

`params.id` from `useParams()` has type `string | string[] | undefined` in Next.js App Router, but is blindly cast via `as string`.

**Fix:** Use a runtime guard: `const teamId = typeof params.id === 'string' ? params.id : ''`.

---

### [MF-005] Missing AbortController in useTeam fetch -- stale state updates on unmount
**File:** hooks/useTeam.ts:33-41
**Original IDs:** CC-P5-A7-002
**Phases:** CC

`useTeam` does not use an AbortController for fetch cleanup. When `teamId` changes rapidly or the component unmounts, `setTeam`/`setError` are called on an unmounted component. The sibling hook `useGovernance` correctly uses AbortController.

**Fix:** Add AbortController to the useEffect, pass signal to fetch calls, abort on cleanup.

---

### [MF-006] Route ordering: DELETE /api/sessions/restore shadowed by parameterized route
**File:** services/headless-router.ts:519, 542
**Original IDs:** CC-P5-A3-001 (headless-router)
**Phases:** CC

The parameterized route `DELETE /api/sessions/([^/]+)$` at line 519 appears before the static route `DELETE /api/sessions/restore$` at line 542. First-match-wins means `DELETE /api/sessions/restore` calls `deleteSession("restore")` instead of `deletePersistedSession()`.

**Fix:** Move static sub-paths (`restore`, `activity`) before the parameterized route.

---

### [MF-007] cross-host-governance test missing mock for @/lib/host-keys
**File:** tests/cross-host-governance.test.ts
**Original IDs:** CC-P5-A6-001
**Phases:** CC

The source imports `signHostAttestation` from `@/lib/host-keys` but the test does not mock it. Real `host-keys` module reads Ed25519 key files from disk. Tests pass by coincidence (fire-and-forget catch).

**Fix:** Add `vi.mock('@/lib/host-keys', ...)`.

---

### [MF-008] cross-host-governance test missing mock for @/lib/manager-trust
**File:** tests/cross-host-governance.test.ts
**Original IDs:** CC-P5-A6-002
**Phases:** CC

The source imports `shouldAutoApprove` from `@/lib/manager-trust` but the test does not mock it. Real module reads `~/.aimaestro/manager-trust.json` from disk.

**Fix:** Add `vi.mock('@/lib/manager-trust', ...)`.

---

### [MF-009] cross-host-governance test missing mock for @/lib/rate-limit
**File:** tests/cross-host-governance.test.ts
**Original IDs:** CC-P5-A6-003
**Phases:** CC

The rate-limit module uses an in-memory Map that persists across tests. `vi.clearAllMocks()` doesn't clear it. After enough failed password tests, subsequent tests could get rate-limited (429 instead of expected 401).

**Fix:** Either mock `@/lib/rate-limit` or add `resetRateLimit` calls in `beforeEach`.

---

### [MF-010] CozoDB injection via wrong escaping convention in agents-graph-service.ts
**File:** services/agents-graph-service.ts:68-69
**Original IDs:** CC-P5-A5-001
**Phases:** CC

Local `escapeString()` uses SQL-style double-single-quote escaping (`''`) for CozoDB queries. The canonical `escapeForCozo()` in `lib/cozo-utils.ts` uses backslash escaping (`\'`). Used at 25+ locations.

**Fix:** Replace all uses of local `escapeString()` with canonical `escapeForCozo()` from `@/lib/cozo-utils`.

---

### [MF-011] CozoDB injection via wrong escaping in agents-docs-service.ts list query
**File:** services/agents-docs-service.ts:116
**Original IDs:** CC-P5-A5-002
**Phases:** CC

Same wrong escaping convention as MF-010 but in the docs service. The `project` parameter is interpolated with `replace(/'/g, "''")`.

**Fix:** Use `escapeForCozo(project)` from `@/lib/cozo-utils`.

---

### [MF-012] CozoDB injection via wrong escaping in agents-graph-service.ts files query
**File:** services/agents-graph-service.ts:950
**Original IDs:** CC-P5-A5-003
**Phases:** CC

Same wrong escaping as MF-010 but in the `files` case of `queryCodeGraph()`. The `projectFilter` parameter uses `''` instead of `\'`.

**Fix:** Use `escapeForCozo(projectFilter)`.

---

### [MF-013] Shell injection risk via execSync with user-controlled cwd
**File:** services/agents-repos-service.ts:32-33
**Original IDs:** CC-P5-A5-004
**Phases:** CC

`getGitRepoInfo()` calls `execSync('git config --get remote.origin.url', { cwd: dirPath })` where `dirPath` comes from `agent.workingDirectory`. If it contains shell metacharacters, the shell invocations could be exploited. Line 48 explicitly uses `shell: '/bin/bash'`.

**Fix:** Use `execFileSync` with array arguments instead of `execSync`. Validate `dirPath` against a safe pattern.

---

### [MF-014] Missing JSON body guard in POST /api/webhooks
**File:** app/api/webhooks/route.ts:22
**Original IDs:** CC-P5-A1-001
**Phases:** CC

The `POST` handler calls `await request.json()` without try/catch. Malformed JSON returns generic 500 instead of 400.

**Fix:** Wrap in try/catch returning 400.

---

### [MF-015] Missing JSON body guard in PATCH /api/meetings/[id]
**File:** app/api/meetings/[id]/route.ts:20
**Original IDs:** CC-P5-A1-002
**Phases:** CC

Same pattern: `request.json()` without try/catch.

**Fix:** Wrap in try/catch returning 400.

---

### [MF-016] Missing JSON body guard in POST /api/meetings
**File:** app/api/meetings/route.ts:12
**Original IDs:** CC-P5-A1-003
**Phases:** CC

Same pattern: `request.json()` without try/catch.

**Fix:** Wrap in try/catch returning 400.

---

### [MF-017] Missing JSON body guard in POST /api/sessions/create
**File:** app/api/sessions/create/route.ts:6
**Original IDs:** CC-P5-A1-004
**Phases:** CC

`request.json()` without try/catch. Outer catch returns generic 500 instead of 400.

**Fix:** Add inner try/catch returning 400 for malformed JSON.

---

### [MF-018] Missing JSON body guard in POST /api/sessions/restore
**File:** app/api/sessions/restore/route.ts:24
**Original IDs:** CC-P5-A1-005
**Phases:** CC

Same pattern.

**Fix:** Add inner try/catch returning 400.

---

### [MF-019] Missing JSON body guard in POST /api/organization
**File:** app/api/organization/route.ts:20
**Original IDs:** CC-P5-A1-006
**Phases:** CC

Same pattern.

**Fix:** Add inner try/catch returning 400.

---

### [MF-020] Missing JSON body guard in POST /api/sessions/[id]/command
**File:** app/api/sessions/[id]/command/route.ts:24
**Original IDs:** CC-P5-A1-007
**Phases:** CC

Same pattern.

**Fix:** Add inner try/catch returning 400.

---

### [MF-021] Missing JSON body guard in PATCH /api/sessions/[id]/rename
**File:** app/api/sessions/[id]/rename/route.ts:21
**Original IDs:** CC-P5-A1-008
**Phases:** CC

Same pattern, destructures `request.json()` via `Promise.all`.

**Fix:** Add inner try/catch for `request.json()` returning 400.

---

### [MF-022] Missing JSON body guard in POST /api/sessions/activity/update
**File:** app/api/sessions/activity/update/route.ts:13
**Original IDs:** CC-P5-A1-009
**Phases:** CC

Same pattern.

**Fix:** Add inner try/catch returning 400.

---

### [MF-023] Missing JSON body guard in POST /api/conversations/parse
**File:** app/api/conversations/parse/route.ts:10
**Original IDs:** CC-P5-A1-010
**Phases:** CC

Same pattern.

**Fix:** Add inner try/catch returning 400.

---

### [MF-024] Missing try/catch in POST and GET handlers for cross-host governance requests
**File:** app/api/v1/governance/requests/route.ts:56-57, 75-80
**Original IDs:** CC-P5-A0-001
**Phases:** CC

The `POST` handler calls `submitCrossHostRequest(body)` and the `GET` handler calls `listCrossHostRequests(...)` without try/catch. Unhandled exceptions return generic 500 without structured `{ error }` JSON.

**Fix:** Wrap in try/catch matching pattern in approve/reject routes.

---

### [MF-025] Missing error check in exchange-peers route before accessing result.data
**File:** app/api/hosts/exchange-peers/route.ts:17
**Original IDs:** CC-P5-A0-002
**Phases:** CC

The route passes `result.data` directly without checking `result.error`. If the service is refactored to use the standard ServiceResult pattern, `result.data` would be `undefined`, crashing `NextResponse.json()`.

**Fix:** Add standard `if (result.error)` check before success path.

---

### [MF-026] `resolveAgent` uses destructuring `split('@')` which silently drops segments for multi-@ inputs
**File:** lib/messageQueue.ts:488
**Original IDs:** CC-P5-A4-001
**Phases:** CC

`const [name, hostId] = identifier.split('@')` silently drops extra segments. The sibling function `parseQualifiedName()` in `message-send.ts` uses `indexOf('@')` correctly.

**Fix:** Replace with `indexOf`-based parsing consistent with `parseQualifiedName()`.

---

### [MF-027] `convertAMPToMessage` never populates `fromVerified` field
**File:** lib/messageQueue.ts:192-250
**Original IDs:** CC-P5-A4-002
**Phases:** CC

The `Message` interface defines `fromVerified?: boolean` but `convertAMPToMessage()` never sets it. AMP-format messages always have `fromVerified: undefined`, misclassifying verification status.

**Fix:** Add `fromVerified` to the return object, derive from AMP message metadata.

---

## SHOULD-FIX Issues

### [SF-001] Rate-limit check/record is not atomic in password-gated functions
**File:** services/cross-host-governance-service.ts:71-82, 254-266, 343-355
**Original IDs:** CC-P5-A2-001, CC-P5-A3-010
**Phases:** CC
Also identified by: CC-P5-A3-010 (rate-limit module design NIT merged here)

All three password-gated functions use `checkRateLimit()` -> `verifyPassword()` -> `recordFailure()`. A `checkAndRecordAttempt()` atomic helper exists but is not used. Since `verifyPassword` is async (bcrypt.compare), concurrent requests can both pass check before either records failure.

**Fix:** Replace with `checkAndRecordAttempt` for each function, call `resetRateLimit` on success.

---

### [SF-002] `expirePendingRequestsInPlace` only expires 'pending' status, not intermediate approval statuses
**File:** lib/governance-request-registry.ts:264-278
**Original IDs:** CC-P5-A2-002 (governance-request-registry)
**Phases:** CC

Requests in 'remote-approved', 'local-approved', or 'dual-approved' states are NOT expired even if older than 7 days. They can accumulate indefinitely.

**Fix:** Include intermediate approval statuses in the TTL check.

---

### [SF-003] `receiveCrossHostRequest` validates `requestedByRole` but not `requestedBy`
**File:** services/cross-host-governance-service.ts:164-166
**Original IDs:** CC-P5-A2-003 (cross-host-governance)
**Phases:** CC

The function validates `request.id`, `request.type`, `request.payload.agentId`, and `requestedByRole` but not `requestedBy` (the requester's agent ID). Empty string could lead to confusing audit trails.

**Fix:** Add `!request.requestedBy` to the validation check.

---

### [SF-004] `deployUpdateSettings` performs non-atomic write to settings.json
**File:** services/agents-config-deploy-service.ts:331-351
**Original IDs:** CC-P5-A2-004 (agents-config-deploy)
**Phases:** CC

Writes directly with `fs.writeFile()`. Crash mid-write could corrupt the file. No file-level locking for concurrent deployments.

**Fix:** Use atomic temp-file-then-rename pattern.

---

### [SF-005] recipientHostId not validated by verifier -- cross-target replay prevention ineffective
**File:** lib/role-attestation.ts:68-81
**Original IDs:** CC-P5-A2-002 (role-attestation)
**Phases:** CC

`verifyRoleAttestation()` does not check that `attestation.recipientHostId` matches the verifying host's own ID. Signature verification succeeds regardless of which host receives the attestation.

**Fix:** Add optional `expectedRecipientHostId` parameter to `verifyRoleAttestation()`.

---

### [SF-006] Non-atomic write in saveTransfers -- crash can corrupt governance-transfers.json
**File:** lib/transfer-registry.ts:56-60
**Original IDs:** CC-P5-A2-003 (transfer-registry)
**Phases:** CC

`saveTransfers()` writes directly to target file instead of using atomic temp-file-then-rename pattern used everywhere else.

**Fix:** Use write-to-tmp then renameSync pattern.

---

### [SF-007] handleGovernanceSyncMessage does not validate payload fields before type assertion
**File:** lib/governance-sync.ts:185-189
**Original IDs:** CC-P5-A2-004 (governance-sync)
**Phases:** CC

Uses `as` to type-assert `message.payload` without validating expected fields. Malicious peer could send payload with `undefined` values stored to disk.

**Fix:** Add runtime validation for extracted fields.

---

### [SF-008] amp-auth loadApiKeys/saveApiKeys has no file locking
**File:** lib/amp-auth.ts:50-90
**Original IDs:** CC-P5-A3-003
**Phases:** CC

Functions like `createApiKey`, `validateApiKey`, `rotateApiKey`, `revokeApiKey` all load, modify, and save without locking. Concurrent operations could lose data.

**Fix:** Use `withLock('amp-api-keys', ...)` around mutating operations.

---

### [SF-009] validateApiKey timing-safe comparison is partially defeated
**File:** lib/amp-auth.ts:191-199
**Original IDs:** CC-P5-A3-004
**Phases:** CC

Uses `timingSafeEqual` for hash comparison but reveals how many keys were checked before finding a match via timing of the `find` call. Minor information leak.

**Fix:** Iterate all keys regardless of match, or accept as Phase 1 limitation.

---

### [SF-010] saveAgents invalidates cache but concurrent loadAgents may use stale mtime
**File:** lib/agent-registry.ts:192-208, 145-187
**Original IDs:** CC-P5-A3-005
**Phases:** CC

After write, filesystem mtime granularity might not advance, causing the next `loadAgents()` to return stale cached result.

**Fix:** After invalidating cache, eagerly populate with the just-written agents.

---

### [SF-011] resolveAlias does not handle multiple @ signs in name@host format
**File:** lib/agent-registry.ts:998-999
**Original IDs:** CC-P5-A3-006
**Phases:** CC

`nameOrId.split('@')` for `user@host@extra` produces 3 elements, destructuring takes only first two.

**Fix:** Use `split('@', 2)` or `indexOf('@')`.

---

### [SF-012] index-delta extractConversationMetadata reads entire file into memory
**File:** lib/index-delta.ts:277-278
**Original IDs:** CC-P5-A3-007
**Phases:** CC

`fs.readFileSync` reads entire conversation file (can be hundreds of MB) but only uses first 50 and last 20 lines.

**Fix:** Use streaming reader or read only first/last N kilobytes.

---

### [SF-013] use-governance-hook test replicas diverge from real hook on refresh() call
**File:** tests/use-governance-hook.test.ts:29-100
**Original IDs:** CC-P5-A4-001 (test)
**Phases:** CC

Standalone function replicas omit `refresh()` call that the real hook makes after successful operations. TODO comment acknowledges this.

**Fix:** Add minimal React hook test wrapper or document the gap.

---

### [SF-014] agent-config-governance-extended test relies on default mockFsAccess from outer scope
**File:** tests/agent-config-governance-extended.test.ts:744-760
**Original IDs:** CC-P5-A4-002 (test)
**Phases:** CC

Test comment at line 747 is misleading about which mock is active.

**Fix:** Update comment to clarify mock behavior comes from describe-level `beforeEach`.

---

### [SF-015] agent-config-governance-extended test over-broad mockFsAccess
**File:** tests/agent-config-governance-extended.test.ts:724-742
**Original IDs:** CC-P5-A4-003 (test)
**Phases:** CC

`mockFsAccess.mockResolvedValue(undefined)` makes ALL `fs.access` calls succeed instead of just the skill directory check.

**Fix:** Use `mockImplementation` for specific paths only.

---

### [SF-016] Unsafe `error as Error` cast in useWebSocket
**File:** hooks/useWebSocket.ts:170
**Original IDs:** CC-P5-A7-003
**Phases:** CC

Catch block casts `error as Error`, but WebSocket can throw non-Error types (DOMException, string).

**Fix:** Use `error instanceof Error ? error : new Error(String(error))`.

---

### [SF-017] useTerminal returns `terminalRef.current` which does not trigger re-renders
**File:** hooks/useTerminal.ts:341
**Original IDs:** CC-P5-A7-004
**Phases:** CC

Refs do not trigger re-renders. Consumers depending on `terminal` being non-null won't see it until something else re-renders. Mitigated by `isReady` in TerminalView.

**Fix:** Add `terminal` state variable or document clearly that callers must trigger own re-render.

---

### [SF-018] No exponential backoff for WebSocket reconnection
**File:** hooks/useWebSocket.ts:154-159
**Original IDs:** CC-P5-A7-005
**Phases:** CC

Uses fixed 3-second delay for all attempts. CLAUDE.md documents exponential backoff but implementation doesn't follow.

**Fix:** Implement exponential backoff as documented.

---

### [SF-019] Modal dialogs in teams/page.tsx lack role="dialog" and aria-modal
**File:** app/teams/page.tsx:241-293, 296-317
**Original IDs:** CC-P5-A7-006
**Phases:** CC

Create Team and Delete Confirmation dialogs lack `role="dialog"`, `aria-modal="true"`, `aria-labelledby`, and focus trap.

**Fix:** Add ARIA attributes and implement focus trap.

---

### [SF-020] Create Team input lacks explicit label element
**File:** app/teams/page.tsx:252-262
**Original IDs:** CC-P5-A7-007
**Phases:** CC

Team name `<input>` uses only `placeholder` as label. Screen readers need explicit `<label>` or `aria-label`.

**Fix:** Add `aria-label="Team name"` or `<label htmlFor="...">`.

---

### [SF-021] Connection status indicator uses color-only differentiation
**File:** components/TerminalView.tsx:631-635
**Original IDs:** CC-P5-A7-008
**Phases:** CC

Colored dot (green/red) with no text label or `aria-label`. Color-blind users and screen readers have no information.

**Fix:** Add `aria-label` and `role="status"`.

---

### [SF-022] Emoji-only buttons lack aria-labels in TerminalView header
**File:** components/TerminalView.tsx:651-716
**Original IDs:** CC-P5-A7-009
**Phases:** CC

Several buttons use emoji as visible label on mobile (text hidden via `hidden md:inline`). No `aria-label`.

**Fix:** Add `aria-label` matching `title` attribute to each button.

---

### [SF-023] N+1 fetch pattern in teams list page
**File:** app/teams/page.tsx:37-51
**Original IDs:** CC-P5-A7-010
**Phases:** CC

Fetches all teams, then for each makes 2 additional fetches (tasks + docs) = 1 + 2N requests.

**Fix:** Implement bulk `/api/teams/stats` endpoint.

---

### [SF-024] Floating promise in onSkillsChange callback
**File:** components/marketplace/AgentSkillEditor.tsx:591-593
**Original IDs:** CC-P5-A0-001 (marketplace)
**Phases:** CC

`loadSkills()` called without awaiting. If it fails, error is silently swallowed.

**Fix:** Make callback async and await `loadSkills()`.

---

### [SF-025] Potential duplicate skill addition (no client-side guard)
**File:** components/marketplace/AgentSkillEditor.tsx:142-166
**Original IDs:** CC-P5-A0-002 (marketplace)
**Phases:** CC

`handleAddSkill` sends PATCH without checking if skill is already installed.

**Fix:** Add early return if skill already in `skills.marketplace`.

---

### [SF-026] handleGovernanceSyncMessage return value ignored -- always reports success
**File:** services/headless-router.ts:1322-1323
**Original IDs:** CC-P5-A3-002 (headless-router)
**Phases:** CC

The handler calls `handleGovernanceSyncMessage(body.fromHostId, body)` but ignores its boolean return, always responding `{ ok: true }`. Invalid payloads are silently accepted.

**Fix:** Check return value and send 400 when false.

---

### [SF-027] governance-endpoint-auth test: broadcastGovernanceSync passthrough mock fragile
**File:** tests/governance-endpoint-auth.test.ts:87-93
**Original IDs:** CC-P5-A6-004
**Phases:** CC

Passthrough mock for governance-sync creates maintenance burden if new imports are added.

**Fix:** Add smoke test verifying mock list matches actual imports.

---

### [SF-028] cross-host-governance test: no coverage for auto-approve path
**File:** tests/cross-host-governance.test.ts
**Original IDs:** CC-P5-A6-005
**Phases:** CC

`receiveCrossHostRequest` auto-approve path (shouldAutoApprove returns true) has no test coverage. Critical governance behavior untested.

**Fix:** Add tests for auto-approve with and without local manager.

---

### [SF-029] cross-host-governance test: no coverage for assign-cos, remove-cos, transfer-agent execution
**File:** tests/cross-host-governance.test.ts:612-678
**Original IDs:** CC-P5-A6-006
**Phases:** CC

Only `add-to-team` and `remove-from-team` are tested in `performRequestExecution`. Three operation types untested.

**Fix:** Add tests for assign-cos (R1.8, G3), remove-cos, and transfer-agent.

---

### [SF-030] cross-host-governance test: no CC-P1-002 sanitization test coverage
**File:** tests/cross-host-governance.test.ts:330-396
**Original IDs:** CC-P5-A6-007
**Phases:** CC

Critical security validation (type whitelist, role whitelist, sourceHostId match, status/approvals stripping) has no dedicated test coverage.

**Fix:** Add tests for each CC-P1-002 guard.

---

### [SF-031] cross-host-governance test: no CC-010 (neither source nor target) test
**File:** tests/cross-host-governance.test.ts:402-493
**Original IDs:** CC-P5-A6-008
**Phases:** CC

Guard at line 241-243 "Reject if this host is neither source nor target" has no test.

**Fix:** Add test where both sourceHostId and targetHostId differ from getSelfHostId().

---

### [SF-032] role-attestation test: no coverage for recipientHostId parameter
**File:** tests/role-attestation.test.ts
**Original IDs:** CC-P5-A6-009
**Phases:** CC

No test calls `createRoleAttestation` with a `recipientHostId` argument.

**Fix:** Add tests for recipientHostId inclusion and verification.

---

### [SF-033] message-filter test: no test for Step 5b (open-world sender to MANAGER/COS)
**File:** tests/message-filter.test.ts
**Original IDs:** CC-P5-A6-010
**Phases:** CC

Step 5b (open-world agent can reach MANAGER and COS of closed teams) has no ALLOW test.

**Fix:** Add tests for open-world sender to MANAGER and COS.

---

### [SF-034] governance-sync test: no coverage for requestPeerSync Ed25519 signing
**File:** tests/governance-sync.test.ts
**Original IDs:** CC-P5-A6-011
**Phases:** CC

Tests verify URL and response but not that signature headers are sent.

**Fix:** Add assertions for signHostAttestation call and headers.

---

### [SF-035] agent-config-governance test: missing mock for @/lib/governance-sync
**File:** tests/agent-config-governance.test.ts
**Original IDs:** CC-P5-A6-012
**Phases:** CC

Source governance.ts imports broadcastGovernanceSync but test doesn't mock governance-sync. Currently safe because governance is fully mocked, but fragile.

**Fix:** Add defensive `vi.mock('@/lib/governance-sync', ...)`.

---

### [SF-036] No authorization checks in agents-messaging-service.ts
**File:** services/agents-messaging-service.ts
**Original IDs:** CC-P5-A5-005
**Phases:** CC

No messaging function verifies caller is authorized to act on behalf of specified agentId. Any caller can read/send/delete messages for any agent.

**Fix:** Add agent identity verification at service or router layer.

---

### [SF-037] updateMetrics accepts arbitrary keys via rest spread
**File:** services/agents-memory-service.ts:1119
**Original IDs:** CC-P5-A5-006
**Phases:** CC

`...metrics` rest spread forwards any arbitrary keys from request body to `updateAgentMetrics()`.

**Fix:** Explicitly whitelist allowed metric fields.

---

### [SF-038] Missing null check on agent before accessing subconscious
**File:** services/agents-subconscious-service.ts:73
**Original IDs:** CC-P5-A5-007
**Phases:** CC

`getAgent(agentId)` result not checked for null before calling `.getSubconscious()`. Other functions in same file do check.

**Fix:** Add null check and return 404 ServiceResult.

---

### [SF-039] connectionString exposed in error messages
**File:** services/agents-graph-service.ts:314-319
**Original IDs:** CC-P5-A5-008
**Phases:** CC

PostgreSQL error messages may include connection credentials passed through to API response.

**Fix:** Return generic error message, log full error server-side only.

---

### [SF-040] Missing path validation for skill settings file access
**File:** services/agents-skills-service.ts:189
**Original IDs:** CC-P5-A5-009
**Phases:** CC

`agentId` used directly in path construction without traversal validation. Could read/write arbitrary files if `agentId` contains `../`.

**Fix:** Validate `agentId` against UUID pattern before path construction.

---

### [SF-041] Potential infinite loop in background delta indexing
**File:** services/agents-docs-service.ts:76-78
**Original IDs:** CC-P5-A5-010
**Phases:** CC

`triggerBackgroundDocsDeltaIndexing()` calls the same endpoint it's triggered from. Could create infinite recursive HTTP requests under certain conditions.

**Fix:** Add guard header `X-No-Background-Trigger: true`.

---

### [SF-042] amount=0 silently defaults to 1 in updateMetrics increment
**File:** services/agents-memory-service.ts:1122
**Original IDs:** CC-P5-A5-011
**Phases:** CC

`amount || 1` converts intentional 0 to 1. Same bug pattern as CC-P4-009 fixed elsewhere.

**Fix:** Use `amount ?? 1` to preserve intentional zero.

---

### [SF-043] status parameter typed as `any` without validation
**File:** services/agents-messaging-service.ts:196
**Original IDs:** CC-P5-A5-012
**Phases:** CC

`status` and `priority` typed as `any`, passed directly without validation.

**Fix:** Type to actual allowed values and validate.

---

### [SF-044] Skill settings PUT does not validate body.settings is an object
**File:** app/api/agents/[id]/skills/settings/route.ts:64
**Original IDs:** CC-P5-A1-001 (skills)
**Phases:** CC

Service's `if (!settings)` check catches undefined/null but not arrays or strings.

**Fix:** Add type guard: `typeof body.settings !== 'object' || Array.isArray(body.settings)`.

---

### [SF-045] Silent swallow of malformed JSON in POST /api/agents/[id]/docs
**File:** app/api/agents/[id]/docs/route.ts:53-60
**Original IDs:** CC-P5-A1-011
**Phases:** CC

Invalid non-empty JSON body is silently treated as `{}` instead of returning 400.

**Fix:** If `text.trim()` is non-empty and `JSON.parse` fails, return 400.

---

### [SF-046] Silent swallow of malformed JSON in POST /api/agents/[id]/graph/code
**File:** app/api/agents/[id]/graph/code/route.ts:42-50
**Original IDs:** CC-P5-A1-012
**Phases:** CC

Same pattern as SF-045.

**Fix:** Distinguish empty body (allowed) from malformed non-empty JSON (return 400).

---

### [SF-047] NaN propagation risk in parseInt for depth parameter
**File:** app/api/agents/[id]/graph/code/route.ts:22
**Original IDs:** CC-P5-A1-013
**Phases:** CC

`parseInt(searchParams.get('depth') || '1', 10)` yields NaN for non-numeric strings.

**Fix:** Add NaN guard: `parseInt(...) || 1`.

---

### [SF-048] NaN propagation in parseInt for limit parameter
**File:** app/api/agents/[id]/docs/route.ts:28
**Original IDs:** CC-P5-A1-014
**Phases:** CC

Same pattern for `limit`.

**Fix:** Add NaN guard: `parseInt(...) || 10`.

---

### [SF-049] NaN propagation in parseInt for batchSize parameter
**File:** app/api/agents/[id]/index-delta/route.ts:22
**Original IDs:** CC-P5-A1-015
**Phases:** CC

Same pattern for `batchSize`.

**Fix:** Add explicit NaN guard.

---

### [SF-050] NaN propagation in parseInt for timeout parameter
**File:** app/api/agents/unified/route.ts:19
**Original IDs:** CC-P5-A1-016
**Phases:** CC

Same pattern for `timeout`.

**Fix:** Add NaN guard: `parseInt(...) || 3000`.

---

### [SF-051] Error result not checked in GET /api/agents/unified
**File:** app/api/agents/unified/route.ts:22
**Original IDs:** CC-P5-A1-017
**Phases:** CC

Returns `result.data` without checking `result.error`, giving 200 status for errors.

**Fix:** Add error check consistent with other routes.

---

### [SF-052] Unchecked JSON parse in agent import options
**File:** app/api/agents/import/route.ts:23
**Original IDs:** CC-P5-A1-018
**Phases:** CC

`JSON.parse(optionsStr)` without try/catch returns 500 instead of 400.

**Fix:** Wrap in try/catch returning 400.

---

### [SF-053] Logging user-controlled data to console
**File:** app/api/conversations/parse/route.ts:13
**Original IDs:** CC-P5-A1-019
**Phases:** CC

Logs `conversationFile` from request body directly. Could contain sensitive paths.

**Fix:** Remove or reduce to debug-level logging.

---

### [SF-054] Inconsistent error response shape across API routes
**File:** Multiple files
**Original IDs:** CC-P5-A1-020, CC-P5-A0-009
**Phases:** CC
Also identified by: CC-P5-A0-009 (NIT merged here -- same root cause)

No consistent error response shape. Some return `{ error }`, some `{ success: false, error }`, some `{ error, details }`, meetings use `result.data ?? { error }`.

**Fix:** Standardize on explicit `if (result.error)` pattern.

---

### [SF-055] Missing error check in register-peer route
**File:** app/api/hosts/register-peer/route.ts:17
**Original IDs:** CC-P5-A0-003 (register-peer)
**Phases:** CC

Same pattern as MF-025 but for `registerPeer`. No `if (result.error)` check.

**Fix:** Add standard error check.

---

### [SF-056] SSRF potential in hosts/health endpoint
**File:** app/api/hosts/health/route.ts:12-13
**Original IDs:** CC-P5-A0-004
**Phases:** CC

`GET /api/hosts/health?url=<hostUrl>` proxies to any user-supplied URL. Could scan internal networks. Mitigated by Phase 1 localhost-only.

**Fix:** Add URL destination validation: reject private IPs, file://, restrict to configured hosts.

---

### [SF-057] Type assertion `as UpdateTaskParams` bypasses type safety
**File:** app/api/teams/[id]/tasks/[taskId]/route.ts:27
**Original IDs:** CC-P5-A0-005
**Phases:** CC

Unvalidated request body spread into type-asserted `UpdateTaskParams`.

**Fix:** Validate body fields before casting or ensure service validates.

---

### [SF-058] Type assertion `as CreateTaskParams` bypasses type safety
**File:** app/api/teams/[id]/tasks/route.ts:50
**Original IDs:** CC-P5-A0-006
**Phases:** CC

Same pattern as SF-057 for `createTeamTask`.

**Fix:** Validate body fields or ensure service validates.

---

### [SF-059] `any` type annotation on hosts.find callback
**File:** app/api/v1/governance/requests/route.ts:36
**Original IDs:** CC-P5-A0-007
**Phases:** CC

`hosts.find((h: any) => ...)` uses unnecessary `any` when `Host` type has `id` property.

**Fix:** Remove `: any` annotation.

---

### [SF-060] hostId in error message leaks identifiers
**File:** app/api/v1/governance/requests/route.ts:38
**Original IDs:** CC-P5-A0-008
**Phases:** CC

Error message includes user-supplied hostId: `Unknown host: ${hostId}`. Helps enumerate valid host IDs.

**Fix:** Use generic message: `'Unknown host'`.

---

### [SF-061] `convertAMPToMessage` uses `any` type for ampMsg parameter
**File:** lib/messageQueue.ts:192
**Original IDs:** CC-P5-A4-003 (message-any)
**Phases:** CC

Untyped parameter eliminates compile-time type checking for all field accesses.

**Fix:** Define interface for on-disk AMP message format.

---

### [SF-062] `triggerOldDuplicateCleanup` deletes files matching overly broad `msg-*.json` pattern
**File:** lib/messageQueue.ts:138
**Original IDs:** CC-P5-A4-004 (cleanup)
**Phases:** CC

Pattern `msg-*.json` catches both old-format and new-format messages.

**Fix:** Use more specific pattern or check message content.

---

### [SF-063] `markMessageAsRead` and `archiveMessage` have TOCTOU race
**File:** lib/messageQueue.ts:709-735, 757-782
**Original IDs:** CC-P5-A4-005
**Phases:** CC

Read-modify-write without locking on same file.

**Fix:** Use atomic write or file-lock mechanism.

---

### [SF-064] `cleanupAgentCacheSweep` exported but never called
**File:** lib/messageQueue.ts:457-460
**Original IDs:** CC-P5-A4-006
**Phases:** CC

Exported function is dead code. Leaked interval could affect test runners.

**Fix:** Wire into shutdown handler and test teardown, or remove export.

---

### [SF-065] `hashApiKey` uses unsalted SHA-256
**File:** lib/amp-auth.ts:100-102
**Original IDs:** CC-P5-A4-007
**Phases:** CC

Plain SHA-256 with no salt. API keys have high entropy so brute force is infeasible, but defense-in-depth suggests salted hash.

**Fix:** Consider HMAC-SHA256 with per-record salt.

---

### [SF-066] `sendFromUI` empty agentId edge case
**File:** lib/message-send.ts:193
**Original IDs:** CC-P5-A4-008
**Phases:** CC

Empty string `agentId` is handled accidentally via `'' || null` but inconsistently across functions.

**Fix:** Add explicit empty-string check.

---

### [SF-067] Claim: "SF-003: TOCTOU guard added after approveGovernanceRequest" -- partially implemented
**File:** services/cross-host-governance-service.ts:269, 306, 316-317
**Original IDs:** CV-P5-001 (TOCTOU)
**Phases:** CV

The pre/post pattern works in practice only because `approveGovernanceRequest` is internally locked. The "TOCTOU guard" label slightly overstates what the pattern alone achieves.

**Impact:** Low. Guard works correctly due to internal lock.

---

### [SF-068] Claim: "agentHostMap field on Team type" -- dead type declaration
**File:** types/team.ts:29-30
**Original IDs:** CV-P5-001 (agentHostMap)
**Phases:** CV

CHANGELOG implies field enables "multi-host team membership tracking" but it is a dead type stub with zero implementation. Field is `@planned Layer 3`.

**Impact:** Low. Optional field, code comment is honest.

---

### [SF-069] Claim: "configure-agent" as cross-host request type -- scaffolded but not wired
**File:** types/governance-request.ts:20, services/cross-host-governance-service.ts:94-96
**Original IDs:** CV-P5-002
**Phases:** CV

Type defined, can be received, but explicitly rejected by submit. Execution is a no-op with warning log.

**Impact:** Low. Scaffolded for future use.

---

### [SF-070] Claim: "Layer 5 agent configuration governance enforcement" -- optional, not mandatory
**File:** services/agents-core-service.ts:602-714
**Original IDs:** CV-P5-003
**Phases:** CV

Enforcement is wrapped in `if (requestingAgentId) {` -- skipped entirely when no auth header present. CHANGELOG doesn't mention the "optional" qualifier.

**Impact:** Low. By design for Phase 1.

---

## Nits & Suggestions

### [NT-001] `performRequestExecution` swallows execution failures silently
**File:** services/cross-host-governance-service.ts:398-400, 546-549
**Original IDs:** CC-P5-A2-005 (cross-host-governance)
**Phases:** CC

Documented as Phase 2 concern. No action needed now.

---

### [NT-002] `loadGovernanceRequests` does not validate version field
**File:** lib/governance-request-registry.ts:42-45
**Original IDs:** CC-P5-A2-006 (governance-request-registry)
**Phases:** CC

No validation of `version === 1` or `Array.isArray(parsed.requests)`.

**Fix:** Add version guard.

---

### [NT-003] `receiveCrossHostRequest` uses spread that could carry unexpected fields
**File:** services/cross-host-governance-service.ts:207-212
**Original IDs:** CC-P5-A2-007 (cross-host-governance)
**Phases:** CC

Any extra fields from malicious peer persist to disk via `...request` spread.

**Fix:** Destructure only known fields.

---

### [NT-004] No test coverage for agents-config-deploy-service.ts
**File:** services/agents-config-deploy-service.ts
**Original IDs:** CC-P5-A2-008
**Phases:** CC

Config deployment operations have no automated test coverage.

**Fix:** Create test file covering skill/plugin add/remove, settings merge, bulk-config.

---

### [NT-005] isChiefOfStaffAnywhere null check pattern subtlety
**File:** lib/governance.ts:147-152
**Original IDs:** CC-P5-A2-005 (governance)
**Phases:** CC

No actual bug; `null === agentId` is always false. Pattern subtly differs from `isManager`.

**Fix:** Add comment noting safety.

---

### [NT-006] GovernanceRole type alias adds indirection without value
**File:** types/governance.ts:19
**Original IDs:** CC-P5-A2-006 (governance-types)
**Phases:** CC

`GovernanceRole` is just a type alias for `AgentRole`.

**Fix:** Consider using `AgentRole` directly.

---

### [NT-007] Missing renameSync import for atomic write fix
**File:** lib/transfer-registry.ts:9
**Original IDs:** CC-P5-A2-007 (transfer-registry)
**Phases:** CC

Note for when SF-006 fix is applied: add `renameSync` to the import.

---

### [NT-008] Inconsistent error return patterns across registries
**File:** lib/document-registry.ts:47, lib/task-registry.ts:48
**Original IDs:** CC-P5-A3-008
**Phases:** CC

`saveDocuments`/`saveTasks` return boolean (often ignored), `saveApiKeys` throws. Inconsistent.

**Fix:** Standardize on throw-on-failure (fail-fast).

---

### [NT-009] generateUniquePersonaName can return duplicate when all names used
**File:** lib/agent-registry.ts:96-101
**Original IDs:** CC-P5-A3-009
**Phases:** CC

Falls through and returns a duplicate when all persona names are used.

**Fix:** Append numeric suffix for uniqueness.

---

### [NT-010] file-lock has no deadlock detection or timeout
**File:** lib/file-lock.ts:74
**Original IDs:** CC-P5-A3-011
**Phases:** CC

Stuck lock holder causes indefinite hang for all queued waiters.

**Fix:** Add optional timeout parameter. Low priority for Phase 1.

---

### [NT-011] governance-request-registry test: requestedByRole value may not match AgentRole type
**File:** tests/governance-request-registry.test.ts:450
**Original IDs:** CC-P5-A4-004 (test-role)
**Phases:** CC

Test uses `'chief-of-staff'` which may or may not be a valid AgentRole literal.

**Fix:** Verify against actual type definition.

---

### [NT-012] governance-request-registry test: makeRequest helper has stale defaults
**File:** tests/governance-request-registry.test.ts:130-153
**Original IDs:** CC-P5-A4-005 (test-helper)
**Phases:** CC

`updatedAt` defaults to old date. Could be confusing for future readers.

**Fix:** Override `updatedAt` in test fixtures for clarity.

---

### [NT-013] `as any` type assertions for negative tests are correct
**File:** tests/agent-config-governance-extended.test.ts:874, 1052
**Original IDs:** CC-P5-A4-006 (test-any)
**Phases:** CC

Correctly uses `as any` for negative test cases. No fix needed.

---

### [NT-014] use-governance-hook test: afterEach restoreAllMocks doesn't restore direct assignment
**File:** tests/use-governance-hook.test.ts:108-115
**Original IDs:** CC-P5-A4-007 (test-mock)
**Phases:** CC

`vi.restoreAllMocks()` doesn't restore direct `global.fetch` assignment. Harmless because `beforeEach` re-assigns.

**Fix:** Cosmetic only.

---

### [NT-015] screenReaderMode explicitly disabled in useTerminal
**File:** hooks/useTerminal.ts:107
**Original IDs:** CC-P5-A7-011
**Phases:** CC

Reasonable default but removes terminal content from accessibility tree. Comment partially explains trade-off.

**Fix:** Expand comment about the trade-off.

---

### [NT-016] GovernancePasswordDialog has redundant state reset on close
**File:** components/governance/GovernancePasswordDialog.tsx:26-33, 36-43
**Original IDs:** CC-P5-A7-012
**Phases:** CC

State reset in both useEffect and handleClose. Defensive and acceptable.

**Fix:** Add clarifying comment.

---

### [NT-017] localStorage reads use mount-only effects without storageId in deps
**File:** components/TerminalView.tsx:498-508, 510-519
**Original IDs:** CC-P5-A7-013
**Phases:** CC

Correct for tab-based architecture. eslint-disable comments acknowledge choice.

**Fix:** No action needed.

---

### [NT-018] RoleBadge default case uses String() conversion on exhausted type
**File:** components/governance/RoleBadge.tsx:64-72
**Original IDs:** CC-P5-A7-014
**Phases:** CC

Good defensive coding. CC-P1-708 comment explains.

**Fix:** No change needed.

---

### [NT-019] TeamOverviewSection re-fetches repos on every section expand
**File:** components/zoom/AgentProfileTab.tsx:74-93
**Original IDs:** CC-P5-A7-015
**Phases:** CC

No `reposLoaded` guard unlike AgentProfile.tsx.

**Fix:** Add `reposLoaded` state guard.

---

### [NT-020] Checkbox `checked` prop receives `undefined` when skills is null
**File:** components/marketplace/AgentSkillEditor.tsx:450
**Original IDs:** CC-P5-A0-003 (checkbox)
**Phases:** CC

Could make checkbox uncontrolled in theory.

**Fix:** Use `?? false` for explicitness.

---

### [NT-021] Unused selectedSkill state (dead code)
**File:** components/marketplace/AgentSkillEditor.tsx:71
**Original IDs:** CC-P5-A0-004 (dead-code)
**Phases:** CC

`selectedSkill` is never set to non-null. SkillDetailModal never opens.

**Fix:** Remove dead code or wire up click handler.

---

### [NT-022] Custom skill key uses `skill.name` instead of unique identifier
**File:** components/marketplace/AgentSkillEditor.tsx:529
**Original IDs:** CC-P5-A0-005 (react-key)
**Phases:** CC

Duplicate names would cause React key warnings.

**Fix:** Use `skill.path || skill.name` for key.

---

### [NT-023] Duplicated host signature verification boilerplate across 4 endpoints
**File:** services/headless-router.ts:1294-1321, 1327-1354, 1370-1401, 1436-1449
**Original IDs:** CC-P5-A3-003 (headless-router)
**Phases:** CC

~80 lines of duplicated verification logic with minor variations.

**Fix:** Extract `verifyHostSignature(req, expectedPrefix)` helper.

---

### [NT-024] query parameter cast to `any` loses type safety in multiple handlers
**File:** services/headless-router.ts:781, 793, 805, 990, 1177, 1184
**Original IDs:** CC-P5-A3-004 (headless-router)
**Phases:** CC

Multiple handlers cast `query as any` bypassing TypeScript.

**Fix:** Define typed query parameter interfaces.

---

### [NT-025] governance-endpoint-auth test: unused buildLocalGovernanceSnapshot import
**File:** tests/governance-endpoint-auth.test.ts:105
**Original IDs:** CC-P5-A6-013
**Phases:** CC

Dead import.

**Fix:** Remove unused import.

---

### [NT-026] role-attestation test: buildValidAttestation helper does not include recipientHostId
**File:** tests/role-attestation.test.ts:90
**Original IDs:** CC-P5-A6-014
**Phases:** CC

Data string construction would be wrong if recipientHostId were tested.

**Fix:** Update helper to include recipientHostId when present.

---

### [NT-027] cross-host-governance test: unused mockExecuteGovernanceRequest mock
**File:** tests/cross-host-governance.test.ts:54, 65, 156
**Original IDs:** CC-P5-A6-015
**Phases:** CC

Dead mock code after CC-003 fix removed `executeGovernanceRequest`.

**Fix:** Remove unused mock.

---

### [NT-028] governance-endpoint-auth test: SR-007 tests only use manager role
**File:** tests/governance-endpoint-auth.test.ts:258-343
**Original IDs:** CC-P5-A6-016
**Phases:** CC

All tests use `'manager'`; COS role-independence not demonstrated.

**Fix:** Add one test with `'chief-of-staff'`.

---

### [NT-029] agent-registry test: large test file could benefit from nesting
**File:** tests/agent-registry.test.ts (986 lines)
**Original IDs:** CC-P5-A6-017
**Phases:** CC

60+ tests in relatively flat structure. Readability concern.

**Fix:** Consider nesting describe blocks.

---

### [NT-030] test-utils/fixtures.ts: unused makeServiceResult helper
**File:** tests/test-utils/fixtures.ts:150-155
**Original IDs:** CC-P5-A6-018
**Phases:** CC

Exported but never imported anywhere.

**Fix:** Remove or document for future use.

---

### [NT-031] Inconsistent use of `any` type throughout services
**File:** Multiple service files
**Original IDs:** CC-P5-A5-014
**Phases:** CC

Many services use `ServiceResult<any>` instead of proper return types.

**Fix:** Define specific result type interfaces.

---

### [NT-032] `readyState === 1` magic number in shared-state.ts
**File:** services/shared-state.ts
**Original IDs:** CC-P5-A5-015
**Phases:** CC

Should use `WebSocket.OPEN` named constant.

**Fix:** Replace with `WebSocket.OPEN`.

---

### [NT-033] Console.log in production code for playback service
**File:** services/agents-playback-service.ts:47-49, 104-106
**Original IDs:** CC-P5-A5-016
**Phases:** CC

Placeholder code logs on every request.

**Fix:** Gate behind debug flag or remove.

---

### [NT-034] Governance requests POST body passes potentially extra fields downstream
**File:** app/api/v1/governance/requests/route.ts:71-72, 96
**Original IDs:** CC-P5-A1-002 (governance-password)
**Phases:** CC

Entire untyped `body` (including unexpected fields) forwarded to service.

**Fix:** Destructure only expected fields before passing.

---

### [NT-035] Status param reflected in error message
**File:** app/api/v1/governance/requests/route.ts:130
**Original IDs:** CC-P5-A1-003 (reflected-input)
**Phases:** CC

User input reflected in JSON error. Safe for JSON API, minor code smell.

**Fix:** No action needed for Phase 1.

---

### [NT-036] Redundant `details: result.error` in health proxy response
**File:** app/api/agents/health/route.ts:29
**Original IDs:** CC-P5-A1-021
**Phases:** CC

`error` and `details` contain same value.

**Fix:** Remove redundant `details`.

---

### [NT-037] Redundant `details: result.error` in register response
**File:** app/api/agents/register/route.ts:19
**Original IDs:** CC-P5-A1-022
**Phases:** CC

Same redundancy.

**Fix:** Remove redundant `details`.

---

### [NT-038] Unused import `NextRequest` in several files
**File:** app/api/agents/[id]/database/route.ts:1, app/api/agents/[id]/tracking/route.ts:1
**Original IDs:** CC-P5-A1-023
**Phases:** CC

Very minor. Import used for typing but request-specific features unused.

**Fix:** Use `_request: NextRequest` or `Request` type.

---

### [NT-039] Missing top-level try/catch in several agent route handlers
**File:** Multiple agent route files
**Original IDs:** CC-P5-A1-024
**Phases:** CC

Thin-wrapper routes lack defense-in-depth try/catch.

**Fix:** Add top-level try/catch.

---

### [NT-040] Missing UUID validation on path parameters in several routes
**File:** Multiple team/host routes
**Original IDs:** CC-P5-A0-010
**Phases:** CC

Several routes accept `id`, `docId`, `taskId` without UUID format validation. Other routes correctly use `isValidUuid`.

**Fix:** Add `isValidUuid` checks for consistency.

---

### [NT-041] Missing `export const dynamic = 'force-dynamic'` on routes reading runtime state
**File:** app/api/governance/route.ts, app/api/governance/reachable/route.ts, app/api/teams/names/route.ts
**Original IDs:** CC-P5-A0-011
**Phases:** CC

GET routes read runtime filesystem state but lack explicit dynamic marking.

**Fix:** Add `export const dynamic = 'force-dynamic'`.

---

### [NT-042] Emoji in notification format may render incorrectly in some terminals
**File:** lib/notification-service.ts:74-76
**Original IDs:** CC-P5-A4-009
**Phases:** CC

Hardcoded emoji for priority indicators.

**Fix:** Make configurable or use plain text alternatives.

---

### [NT-043] Duplicate `ResolvedAgent` interface definitions across files
**File:** lib/messageQueue.ts:90-97, lib/message-send.ts:42-49
**Original IDs:** CC-P5-A4-010
**Phases:** CC

Nearly identical interfaces in two files.

**Fix:** Export from one and import in the other.

---

### [NT-044] `getSelfHostName` duplicated between messageQueue.ts and message-send.ts
**File:** lib/messageQueue.ts:14-21, lib/message-send.ts:78-85
**Original IDs:** CC-P5-A4-011
**Phases:** CC

Identical implementations in two files.

**Fix:** Extract to shared utility.

---

### [NT-045] `saveApiKeys` writes JSON without atomic write
**File:** lib/amp-auth.ts:82
**Original IDs:** CC-P5-A4-012
**Phases:** CC

Not atomic. Crash mid-write corrupts file.

**Fix:** Write to tmp then rename.

---

## Deduplication Log

| Final ID | Original IDs | Duplicates Removed | Reason |
|----------|-------------|-------------------|--------|
| MF-001 | CC-P5-A2-001 | 0 | Unique finding |
| MF-002 | CC-P5-A3-001 (index-delta) | 0 | Unique finding |
| MF-003 | CC-P5-A3-002 | 0 | Unique finding |
| MF-004 | CC-P5-A7-001 | 0 | Unique finding |
| MF-005 | CC-P5-A7-002 | 0 | Unique finding |
| MF-006 | CC-P5-A3-001 (headless-router) | 0 | Same raw ID as MF-002 but different file+issue (route ordering vs slot release) |
| MF-007 | CC-P5-A6-001 | 0 | Unique finding |
| MF-008 | CC-P5-A6-002 | 0 | Unique finding |
| MF-009 | CC-P5-A6-003 | 0 | Unique finding |
| MF-010 | CC-P5-A5-001 | 0 | Unique finding |
| MF-011 | CC-P5-A5-002 | 0 | Different file from MF-010 (docs-service vs graph-service) |
| MF-012 | CC-P5-A5-003 | 0 | Different location in graph-service from MF-010 (line 950 vs 68) |
| MF-013 | CC-P5-A5-004 | 0 | Unique finding |
| MF-014 | CC-P5-A1-001 | 0 | Unique finding |
| MF-015 | CC-P5-A1-002 | 0 | Unique finding (different file from each other) |
| MF-016 | CC-P5-A1-003 | 0 | Unique finding |
| MF-017 | CC-P5-A1-004 | 0 | Unique finding |
| MF-018 | CC-P5-A1-005 | 0 | Unique finding |
| MF-019 | CC-P5-A1-006 | 0 | Unique finding |
| MF-020 | CC-P5-A1-007 | 0 | Unique finding |
| MF-021 | CC-P5-A1-008 | 0 | Unique finding |
| MF-022 | CC-P5-A1-009 | 0 | Unique finding |
| MF-023 | CC-P5-A1-010 | 0 | Unique finding |
| MF-024 | CC-P5-A0-001 | 0 | Unique finding |
| MF-025 | CC-P5-A0-002 | 0 | Unique finding |
| MF-026 | CC-P5-A4-001 | 0 | Unique finding |
| MF-027 | CC-P5-A4-002 | 0 | Unique finding |
| SF-001 | CC-P5-A2-001, CC-P5-A3-010 | 1 | Same root cause: rate-limit atomicity (NIT merged into SF) |
| SF-002 | CC-P5-A2-002 (gov-request-reg) | 0 | Unique finding |
| SF-003 | CC-P5-A2-003 (cross-host-gov) | 0 | Unique finding |
| SF-004 | CC-P5-A2-004 (config-deploy) | 0 | Unique finding |
| SF-005 | CC-P5-A2-002 (role-attestation) | 0 | Different file from SF-002 |
| SF-006 | CC-P5-A2-003 (transfer-registry) | 0 | Different file from SF-003 |
| SF-007 | CC-P5-A2-004 (governance-sync) | 0 | Different file from SF-004 |
| SF-008 | CC-P5-A3-003 | 0 | Unique finding |
| SF-009 | CC-P5-A3-004 | 0 | Unique finding |
| SF-010 | CC-P5-A3-005 | 0 | Unique finding |
| SF-011 | CC-P5-A3-006 | 0 | Unique finding |
| SF-012 | CC-P5-A3-007 | 0 | Unique finding |
| SF-013 | CC-P5-A4-001 (test) | 0 | Unique finding |
| SF-014 | CC-P5-A4-002 (test) | 0 | Unique finding |
| SF-015 | CC-P5-A4-003 (test) | 0 | Unique finding |
| SF-016 | CC-P5-A7-003 | 0 | Unique finding |
| SF-017 | CC-P5-A7-004 | 0 | Unique finding |
| SF-018 | CC-P5-A7-005 | 0 | Unique finding |
| SF-019 | CC-P5-A7-006 | 0 | Unique finding |
| SF-020 | CC-P5-A7-007 | 0 | Unique finding |
| SF-021 | CC-P5-A7-008 | 0 | Unique finding |
| SF-022 | CC-P5-A7-009 | 0 | Unique finding |
| SF-023 | CC-P5-A7-010 | 0 | Unique finding |
| SF-024 | CC-P5-A0-001 (marketplace) | 0 | Unique finding |
| SF-025 | CC-P5-A0-002 (marketplace) | 0 | Unique finding |
| SF-026 | CC-P5-A3-002 (headless-router) | 0 | Unique finding |
| SF-027 | CC-P5-A6-004 | 0 | Unique finding |
| SF-028 | CC-P5-A6-005 | 0 | Unique finding |
| SF-029 | CC-P5-A6-006 | 0 | Unique finding |
| SF-030 | CC-P5-A6-007 | 0 | Unique finding |
| SF-031 | CC-P5-A6-008 | 0 | Unique finding |
| SF-032 | CC-P5-A6-009 | 0 | Unique finding |
| SF-033 | CC-P5-A6-010 | 0 | Unique finding |
| SF-034 | CC-P5-A6-011 | 0 | Unique finding |
| SF-035 | CC-P5-A6-012 | 0 | Unique finding |
| SF-036 | CC-P5-A5-005 | 0 | Unique finding |
| SF-037 | CC-P5-A5-006 | 0 | Unique finding |
| SF-038 | CC-P5-A5-007 | 0 | Unique finding |
| SF-039 | CC-P5-A5-008 | 0 | Unique finding |
| SF-040 | CC-P5-A5-009 | 0 | Unique finding |
| SF-041 | CC-P5-A5-010 | 0 | Unique finding |
| SF-042 | CC-P5-A5-011 | 0 | Unique finding |
| SF-043 | CC-P5-A5-012 | 0 | Unique finding |
| SF-044 | CC-P5-A1-001 (skills) | 0 | Unique finding |
| SF-045 | CC-P5-A1-011 | 0 | Unique finding |
| SF-046 | CC-P5-A1-012 | 0 | Unique finding |
| SF-047 | CC-P5-A1-013 | 0 | Unique finding |
| SF-048 | CC-P5-A1-014 | 0 | Unique finding |
| SF-049 | CC-P5-A1-015 | 0 | Unique finding |
| SF-050 | CC-P5-A1-016 | 0 | Unique finding |
| SF-051 | CC-P5-A1-017 | 0 | Unique finding |
| SF-052 | CC-P5-A1-018 | 0 | Unique finding |
| SF-053 | CC-P5-A1-019 | 0 | Unique finding |
| SF-054 | CC-P5-A1-020, CC-P5-A0-009 | 1 | Same root cause: inconsistent error response patterns (NIT merged into SF) |
| SF-055 | CC-P5-A0-003 (register-peer) | 0 | Unique finding |
| SF-056 | CC-P5-A0-004 | 0 | Unique finding |
| SF-057 | CC-P5-A0-005 | 0 | Unique finding |
| SF-058 | CC-P5-A0-006 | 0 | Unique finding |
| SF-059 | CC-P5-A0-007 | 0 | Unique finding |
| SF-060 | CC-P5-A0-008 | 0 | Unique finding |
| SF-061 | CC-P5-A4-003 (message-any) | 0 | Unique finding |
| SF-062 | CC-P5-A4-004 (cleanup) | 0 | Unique finding |
| SF-063 | CC-P5-A4-005 | 0 | Unique finding |
| SF-064 | CC-P5-A4-006 | 0 | Unique finding |
| SF-065 | CC-P5-A4-007 | 0 | Unique finding |
| SF-066 | CC-P5-A4-008 | 0 | Unique finding |
| SF-067 | CV-P5-001 (TOCTOU) | 0 | Unique finding |
| SF-068 | CV-P5-001 (agentHostMap) | 0 | Unique finding |
| SF-069 | CV-P5-002 | 0 | Unique finding |
| SF-070 | CV-P5-003 | 0 | Unique finding |
| NT-001 | CC-P5-A2-005 (cross-host-gov) | 0 | Unique finding |
| NT-002 | CC-P5-A2-006 (gov-request-reg) | 0 | Unique finding |
| NT-003 | CC-P5-A2-007 (cross-host-gov) | 0 | Unique finding |
| NT-004 | CC-P5-A2-008 | 0 | Unique finding |
| NT-005 | CC-P5-A2-005 (governance) | 0 | Different file from NT-001 |
| NT-006 | CC-P5-A2-006 (governance-types) | 0 | Different from NT-002 |
| NT-007 | CC-P5-A2-007 (transfer-registry) | 0 | Different from NT-003 |
| NT-008 | CC-P5-A3-008 | 0 | Unique finding |
| NT-009 | CC-P5-A3-009 | 0 | Unique finding |
| NT-010 | CC-P5-A3-011 | 0 | Unique finding |
| NT-011 | CC-P5-A4-004 (test-role) | 0 | Unique finding |
| NT-012 | CC-P5-A4-005 (test-helper) | 0 | Unique finding |
| NT-013 | CC-P5-A4-006 (test-any) | 0 | Unique finding |
| NT-014 | CC-P5-A4-007 (test-mock) | 0 | Unique finding |
| NT-015 | CC-P5-A7-011 | 0 | Unique finding |
| NT-016 | CC-P5-A7-012 | 0 | Unique finding |
| NT-017 | CC-P5-A7-013 | 0 | Unique finding |
| NT-018 | CC-P5-A7-014 | 0 | Unique finding |
| NT-019 | CC-P5-A7-015 | 0 | Unique finding |
| NT-020 | CC-P5-A0-003 (checkbox) | 0 | Unique finding |
| NT-021 | CC-P5-A0-004 (dead-code) | 0 | Unique finding |
| NT-022 | CC-P5-A0-005 (react-key) | 0 | Unique finding |
| NT-023 | CC-P5-A3-003 (headless-router) | 0 | Unique finding |
| NT-024 | CC-P5-A3-004 (headless-router) | 0 | Unique finding |
| NT-025 | CC-P5-A6-013 | 0 | Unique finding |
| NT-026 | CC-P5-A6-014 | 0 | Unique finding |
| NT-027 | CC-P5-A6-015 | 0 | Unique finding |
| NT-028 | CC-P5-A6-016 | 0 | Unique finding |
| NT-029 | CC-P5-A6-017 | 0 | Unique finding |
| NT-030 | CC-P5-A6-018 | 0 | Unique finding |
| NT-031 | CC-P5-A5-014 | 0 | Unique finding |
| NT-032 | CC-P5-A5-015 | 0 | Unique finding |
| NT-033 | CC-P5-A5-016 | 0 | Unique finding |
| NT-034 | CC-P5-A1-002 (gov-password) | 0 | Unique finding |
| NT-035 | CC-P5-A1-003 (reflected-input) | 0 | Unique finding |
| NT-036 | CC-P5-A1-021 | 0 | Unique finding |
| NT-037 | CC-P5-A1-022 | 0 | Unique finding |
| NT-038 | CC-P5-A1-023 | 0 | Unique finding |
| NT-039 | CC-P5-A1-024 | 0 | Unique finding |
| NT-040 | CC-P5-A0-010 | 0 | Unique finding |
| NT-041 | CC-P5-A0-011 | 0 | Unique finding |
| NT-042 | CC-P5-A4-009 | 0 | Unique finding |
| NT-043 | CC-P5-A4-010 | 0 | Unique finding |
| NT-044 | CC-P5-A4-011 | 0 | Unique finding |
| NT-045 | CC-P5-A4-012 | 0 | Unique finding |
| -- | CC-P5-A3-010 | merged into SF-001 | Same root cause: rate-limit atomicity concern (NIT absorbed into SHOULD-FIX) |
| -- | CC-P5-A0-009 | merged into SF-054 | Same root cause: inconsistent error response patterns (NIT absorbed into SHOULD-FIX) |
| -- | CC-P5-A5-013 | removed | Retracted by reviewing agent after verification |
| -- | CV-P5-004 | removed | Informational (verified correct), not a finding |

## Removed Findings Detail

1. **CC-P5-A3-010** (NIT: rate-limit not atomic) -- Merged into SF-001. The NIT described the same root cause (rate-limit check/record non-atomicity) as the SHOULD-FIX about callers not using the atomic helper. Fixing the callers renders the module-level NIT moot.

2. **CC-P5-A0-009** (NIT: inconsistent error response patterns across routes) -- Merged into SF-054. Both describe the same systemic issue (inconsistent error response shapes) across different parts of the API. A single standardization effort addresses both.

3. **CC-P5-A5-013** (NIT: dead escapeForCozo import in config-service.ts) -- Retracted by reviewing agent after re-reading the file and finding `escapeForCozo` IS used at line 787.

4. **CV-P5-004** (NIT section: CHANGELOG test count) -- Informational verification that the count is correct (169 tests across 9 files). Not a finding.

---

## Self-Verification

- [x] I parsed ALL findings from the intermediate report (none skipped or missed)
- [x] I grouped findings by file path before comparing
- [x] For each potential duplicate pair, I checked ALL THREE conditions:
      (1) same file path + (2) overlapping lines within 5 + (3) same root cause
- [x] I did NOT merge findings at the same line that describe DIFFERENT bugs
- [x] I did NOT merge findings from different files even if they describe the same pattern
- [x] For merged findings: I kept the HIGHEST severity (SF-001 kept SHOULD-FIX over NIT; SF-054 kept SHOULD-FIX over NIT)
- [x] For merged findings: I preserved ALL original IDs in "Also identified by" annotation
- [x] For merged findings: I noted ALL source phases (CC for both merges)
- [x] My final IDs use sequential numbering: MF-001 through MF-027, SF-001 through SF-070, NT-001 through NT-045
- [x] My deduplication log has an entry for EVERY final finding (including unique ones)
- [x] Each dedup log entry includes the merge reasoning
- [x] My verdict follows the rules: MUST-FIX>0 -> REQUEST CHANGES
- [x] Math check: 142 = 146 - 4 (2 merges + 1 retracted + 1 informational)
- [x] The source reports section is included below
- [x] My return message to the orchestrator is exactly 1-2 lines

---

## Source Reports

- `epcp-correctness-P5-3e1c8815-3a1f-45cb-941c-675041d38a9e.md`
- `epcp-correctness-P5-3f94a374-3aa9-4cfb-9ef4-23f7b448906c.md`
- `epcp-correctness-P5-49bbb7ff-62db-4c4b-b71d-de8f7f6bb772.md`
- `epcp-correctness-P5-68072ed1-8666-4da8-82a4-425f59691dcc.md`
- `epcp-correctness-P5-6c2565dd-5da0-477e-8b87-6190e8270403.md`
- `epcp-correctness-P5-82b5bc3b-ebdd-4f04-be79-6d1fc85e5820.md`
- `epcp-correctness-P5-8f69c296-73f7-458d-9991-df5d5bbaf5e5.md`
- `epcp-correctness-P5-95d3ae7b-07e6-4eef-8223-205835b28305.md`
- `epcp-correctness-P5-bcd48980-0bdd-4634-86fa-ae9e702ead6e.md`
- `epcp-correctness-P5-c082ace4-b527-4ab7-b9e6-53cb67f91fb8.md`
- `epcp-correctness-P5-c740d838-4df0-4c2f-b837-2ecf81c9b94f.md`
- `epcp-correctness-P5-d47d0772-90c1-45bc-a972-3fbc8f09d84d.md`
- `epcp-correctness-P5-ea27aa6a-a6b4-4412-863c-c19ea713ca80.md`
- `epcp-claims-P5-972fb328-bf68-4948-af2b-c54f6f141ef7.md`
- `epcp-claims-P5-e78553ca-bf52-4fba-b560-da5a24ad2307.md`
- `epcp-review-P5-44035da6-a7b1-4c19-a7a8-56a9dedb8ba7.md`
- `epcp-review-P5-560f7159-44ba-4c6a-8658-8141714272e3.md`

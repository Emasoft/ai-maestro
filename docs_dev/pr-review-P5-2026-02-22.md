# EPCP Final PR Review Report

**Generated:** 2026-02-22T05:15:00Z
**Pass:** 5
**Pipeline:** Code Correctness -> Claim Verification -> Skeptical Review
**Dedup:** 118 raw findings -> 114 unique findings (4 removed: 2 invalid/retracted, 2 duplicates merged)

---

## Summary

| Severity | Count |
|----------|-------|
| **MUST-FIX** | 25 |
| **SHOULD-FIX** | 58 |
| **NIT** | 31 |
| **Total findings** | 114 |

**Verdict: REQUEST CHANGES -- 25 must-fix issue(s) found.**

---

## MUST-FIX Issues

### [MF-001] G4 revocation skipped when team type changes from open to closed
**File:** lib/team-registry.ts:347-369
**Original IDs:** CC-P5-A2-001
**Phases:** CC
When `updateTeam()` changes a team's type from `open` to `closed` without modifying `agentIds`, the G4 open-team revocation code does NOT run for existing members. The comment at line 349-350 says "MF-06: Always check when team is closed (not just when agentIds was explicitly provided), so that a type change to 'closed' also triggers revocation for existing members." However, the implementation at line 353 only processes "newly added" agents: `result2.agentIds.filter(aid => !previousAgentIds.includes(aid))`. When only the type changes (no agents added), `previousAgentIds` equals `result2.agentIds`, so `newlyAdded` is empty, and the `if (newlyAdded.length > 0)` guard at line 354 prevents any revocation.

**Fix:** When the type is changed to `closed`, revoke open-team memberships for ALL members of the team (not just newly added ones). Check if the type changed from non-closed to closed, and if so, iterate over all `result2.agentIds` instead of just `newlyAdded`.

---

### [MF-002] Double-release of index throttle slot on early-return paths
**File:** lib/index-delta.ts:434, 464, 585, 644, 655
**Original IDs:** CC-P5-A3-001
**Phases:** CC
`runIndexDelta` calls `releaseSlot()` explicitly on early-return paths (lines 434, 464, 585, 644) AND in the catch block (line 655). The function has 4 separate explicit `releaseSlot()` calls plus the catch handler, making it easy for a future edit to miss one path or double-release. The correct pattern is to use try/finally to guarantee exactly-once release.

**Fix:** Replace the try/catch with try/finally for the slot release, and remove all manual `releaseSlot()` calls.

---

### [MF-003] agent-registry read-modify-write race conditions: no file locking
**File:** lib/agent-registry.ts (multiple functions)
**Original IDs:** CC-P5-A3-002
**Phases:** CC
Unlike `task-registry.ts` and `document-registry.ts` which use `withLock()` from `file-lock.ts` for all mutations, `agent-registry.ts` has NO locking for ANY of its read-modify-write operations. Functions like `createAgent`, `updateAgent`, `deleteAgent`, `linkSession`, `unlinkSession`, `renameAgent`, `updateAgentStatus`, `incrementAgentMetric`, etc. all follow the pattern: `loadAgents()` -> modify in-memory array -> `saveAgents()`. Under concurrent API requests, two requests can load the same state, both modify it, and one overwrites the other's changes (lost update).

**Fix:** Wrap all mutating functions in `agent-registry.ts` with `withLock('agents', () => { ... })`.

---

### [MF-004] Unsafe type assertion on dynamic route params
**File:** app/teams/[id]/page.tsx:23
**Original IDs:** CC-P5-A7-001
**Phases:** CC
`params.id` from `useParams()` has type `string | string[] | undefined` in Next.js App Router, but is blindly cast via `as string`. If `params` is somehow undefined, this would silently produce `undefined` where a string is expected, leading to malformed API URLs like `/api/teams/undefined`.

**Fix:** Use a runtime guard: `const teamId = typeof params.id === 'string' ? params.id : ''` and render an error state if empty.

---

### [MF-005] Missing AbortController in useTeam fetch -- stale state updates on unmount
**File:** hooks/useTeam.ts:33-41
**Original IDs:** CC-P5-A7-002
**Phases:** CC
`useTeam` does not use an AbortController for fetch cleanup. When `teamId` changes rapidly or the component unmounts, the `fetchTeam` call completes and calls `setTeam` / `setError` on an unmounted component. The sibling hook `useGovernance` correctly uses AbortController (line 125-128).

**Fix:** Add an AbortController to the useEffect, pass `signal` to `fetchTeam`'s fetch calls, and abort on cleanup.

---

### [MF-006] cross-host-governance test missing mock for `@/lib/host-keys`
**File:** tests/cross-host-governance.test.ts (entire file)
**Original IDs:** CC-P5-A6-001
**Phases:** CC
The source file `services/cross-host-governance-service.ts` imports `signHostAttestation` from `@/lib/host-keys` (line 30). The test file does NOT mock `@/lib/host-keys`. The real module reads Ed25519 key files from `~/.aimaestro/host-keys/`. Tests pass because `sendRequestToRemoteHost` is fire-and-forget, but if `~/.aimaestro/host-keys/` does not exist on CI, the test will generate real keys and write to disk (side effect).

**Fix:** Add `vi.mock('@/lib/host-keys', () => ({ signHostAttestation: vi.fn(() => 'mock-sig'), getHostPublicKeyHex: vi.fn(() => 'mock-pubkey'), verifyHostAttestation: vi.fn(() => true) }))`.

---

### [MF-007] cross-host-governance test missing mock for `@/lib/manager-trust`
**File:** tests/cross-host-governance.test.ts (entire file)
**Original IDs:** CC-P5-A6-002
**Phases:** CC
The source imports `shouldAutoApprove` from `@/lib/manager-trust` (line 34). The test does NOT mock it. The real module reads `~/.aimaestro/manager-trust.json` from the real filesystem. Tests pass by coincidence (default behavior returns `false`).

**Fix:** Add `vi.mock('@/lib/manager-trust', () => ({ shouldAutoApprove: vi.fn().mockReturnValue(false) }))`.

---

### [MF-008] cross-host-governance test missing mock for `@/lib/rate-limit`
**File:** tests/cross-host-governance.test.ts (entire file)
**Original IDs:** CC-P5-A6-003
**Phases:** CC
The source imports `checkRateLimit`, `recordFailure`, `resetRateLimit` from `@/lib/rate-limit`. The rate-limit module uses an in-memory `Map` that persists across tests. After 5 failed password attempts across tests, subsequent tests could get rate-limited (429 instead of expected 401).

**Fix:** Mock `@/lib/rate-limit` or add a `resetRateLimit` call in `beforeEach`.

---

### [MF-009] CozoDB injection via wrong escaping convention in agents-graph-service.ts
**File:** services/agents-graph-service.ts:68-69 and 950
**Original IDs:** CC-P5-A5-001, CC-P5-A5-003
**Also identified by:** CC-P5-A5-003
**Phases:** CC
The local `escapeString()` function uses SQL-style double-single-quote escaping (`''`) for CozoDB queries. However, the canonical `escapeForCozo()` in `lib/cozo-utils.ts` uses backslash escaping (`\'`). This means `escapeString` does NOT properly escape single quotes for CozoDB. Used at 25+ locations throughout the file, including the `files` query at line 950. A user-supplied `name` parameter containing a single quote followed by CozoScript syntax could break out of the string literal and inject arbitrary CozoScript.

**Fix:** Replace all uses of the local `escapeString()` with the canonical `escapeForCozo()` from `@/lib/cozo-utils`.

---

### [MF-010] CozoDB injection via wrong escaping in agents-docs-service.ts list query
**File:** services/agents-docs-service.ts:116
**Original IDs:** CC-P5-A5-002
**Phases:** CC
The `list` action in `queryDocs()` builds a CozoDB query using SQL-style single-quote escaping (`replace(/'/g, "''")`), which is the wrong convention for CozoDB. The `project` parameter comes from user input and is interpolated directly into the query string.

**Fix:** Use `escapeForCozo(project)` from `@/lib/cozo-utils` instead of manual escaping.

---

### [MF-011] Shell injection risk via execSync with user-controlled cwd in agents-repos-service.ts
**File:** services/agents-repos-service.ts:32-33
**Original IDs:** CC-P5-A5-004
**Phases:** CC
`getGitRepoInfo()` calls `execSync('git config --get remote.origin.url', { cwd: dirPath })` where `dirPath` comes from `agent.workingDirectory`. Line 48 explicitly uses `shell: '/bin/bash'`. If `dirPath` contains shell metacharacters, the shell invocations could be exploited.

**Fix:** Use `execFileSync` with array arguments instead of `execSync`. Validate `dirPath` against a safe pattern before use.

---

### [MF-012] Missing JSON body guard in POST /api/webhooks
**File:** app/api/webhooks/route.ts:22
**Original IDs:** CC-P5-A1-001
**Phases:** CC
The `POST` handler calls `await request.json()` without a try/catch guard. If the client sends malformed JSON, this will throw an unhandled exception and return a generic 500 error instead of a proper 400 response.

**Fix:** Wrap in try/catch returning 400 for invalid JSON.

---

### [MF-013] Missing JSON body guard in PATCH /api/meetings/[id]
**File:** app/api/meetings/[id]/route.ts:20
**Original IDs:** CC-P5-A1-002
**Phases:** CC
The `PATCH` handler calls `await request.json()` without a try/catch.

**Fix:** Wrap in try/catch consistent with rest of codebase.

---

### [MF-014] Missing JSON body guard in POST /api/meetings
**File:** app/api/meetings/route.ts:12
**Original IDs:** CC-P5-A1-003
**Phases:** CC
`await request.json()` without try/catch in the POST handler for meeting creation.

**Fix:** Wrap in try/catch consistent with rest of codebase.

---

### [MF-015] Missing JSON body guard in POST /api/sessions/create
**File:** app/api/sessions/create/route.ts:6
**Original IDs:** CC-P5-A1-004
**Phases:** CC
The POST handler calls `request.json()` without try/catch. The outer catch returns generic 500 instead of 400.

**Fix:** Add inner try/catch around `request.json()` returning 400.

---

### [MF-016] Missing JSON body guard in POST /api/sessions/restore
**File:** app/api/sessions/restore/route.ts:24
**Original IDs:** CC-P5-A1-005
**Phases:** CC
The POST handler calls `request.json()` without specific try/catch for malformed JSON. Caught by outer try/catch but returns generic 500.

**Fix:** Add inner try/catch returning 400 for invalid JSON.

---

### [MF-017] Missing JSON body guard in POST /api/organization
**File:** app/api/organization/route.ts:20
**Original IDs:** CC-P5-A1-006
**Phases:** CC
The POST handler calls `request.json()` without a specific guard. On malformed JSON, the outer catch returns 500 instead of 400.

**Fix:** Add inner try/catch returning 400 for invalid JSON.

---

### [MF-018] Missing JSON body guard in POST /api/sessions/[id]/command
**File:** app/api/sessions/[id]/command/route.ts:24
**Original IDs:** CC-P5-A1-007
**Phases:** CC
The POST handler calls `request.json()` without a specific guard. On malformed JSON, the outer catch returns 500 with generic message instead of 400.

**Fix:** Add inner try/catch returning 400.

---

### [MF-019] Missing JSON body guard in PATCH /api/sessions/[id]/rename
**File:** app/api/sessions/[id]/rename/route.ts:21
**Original IDs:** CC-P5-A1-008
**Phases:** CC
The PATCH handler destructures `request.json()` directly via `Promise.all`. On malformed JSON, the outer catch returns 500.

**Fix:** Add inner try/catch for `request.json()` returning 400.

---

### [MF-020] Missing JSON body guard in POST /api/sessions/activity/update
**File:** app/api/sessions/activity/update/route.ts:13
**Original IDs:** CC-P5-A1-009
**Phases:** CC
The POST handler destructures `request.json()` directly. On malformed JSON, the outer catch returns 500.

**Fix:** Add inner try/catch returning 400.

---

### [MF-021] Missing JSON body guard in POST /api/conversations/parse
**File:** app/api/conversations/parse/route.ts:10
**Original IDs:** CC-P5-A1-010
**Phases:** CC
The POST handler calls `request.json()` directly. On malformed JSON the outer catch returns 500.

**Fix:** Add inner try/catch returning 400.

---

### [MF-022] Missing try/catch in POST and GET handlers for cross-host governance requests
**File:** app/api/v1/governance/requests/route.ts:56-57 and :75-80
**Original IDs:** CC-P5-A0-001
**Phases:** CC
The `POST` handler calls `submitCrossHostRequest(body)` and `receiveCrossHostRequest(body.fromHostId, body.request)` without a surrounding try/catch. Similarly, the `GET` handler calls `listCrossHostRequests(...)` without a try/catch. If these throw, the exception propagates unhandled, returning a generic 500 instead of structured JSON.

**Fix:** Wrap both handlers in try/catch blocks matching the pattern in `approve/route.ts` and `reject/route.ts`.

---

### [MF-023] Missing error check in exchange-peers route before accessing result.data
**File:** app/api/hosts/exchange-peers/route.ts:17
**Original IDs:** CC-P5-A0-002
**Phases:** CC
The route handler directly passes `result.data` without checking `result.error`. If the service function is refactored to use the standard ServiceResult pattern (returning `{ error: '...', status: 400 }` without `data`), `result.data` would be `undefined`, crashing `NextResponse.json()`.

**Fix:** Add the standard `if (result.error)` check before the success path.

---

### [MF-024] `resolveAgent` uses destructuring `split('@')` which silently drops segments for multi-@ addresses
**File:** lib/messageQueue.ts:488
**Original IDs:** CC-P5-A4-001
**Phases:** CC
In `resolveAgent()`, the `name@host` parsing uses `const [name, hostId] = identifier.split('@')`. If the identifier contains more than one `@`, `split('@')` returns 3+ elements but destructuring only captures the first two. The sibling function `parseQualifiedName()` in `message-send.ts:56-63` uses `indexOf('@')` correctly.

**Fix:** Replace with `indexOf`-based parsing consistent with `parseQualifiedName()`.

---

### [MF-025] `convertAMPToMessage` never populates `fromVerified` field
**File:** lib/messageQueue.ts:192-250
**Original IDs:** CC-P5-A4-002
**Phases:** CC
The `Message` interface defines `fromVerified?: boolean` which is expected to be `true` for registered agents and `false` for external/unverified agents. The `convertAMPToMessage()` function never sets this field. Meanwhile, the old flat-format path in `collectMessagesFromAMPDir` does pass through `fromVerified: ampMsg.fromVerified` (line 338). This means AMP-format messages always have `fromVerified: undefined`.

**Fix:** Add `fromVerified` to the return object in `convertAMPToMessage()`. Also add `fromVerified: msg.fromVerified` to the summary block at line 311.

---

## SHOULD-FIX Issues

### [SF-001] recipientHostId not validated by verifier -- cross-target replay prevention ineffective
**File:** lib/role-attestation.ts:68-81
**Original IDs:** CC-P5-A2-002
**Phases:** CC
`verifyRoleAttestation()` does not check that `attestation.recipientHostId` matches the verifying host's own ID. The signature verification succeeds regardless of which host receives the attestation, because the signed data includes the recipientHostId but the verifier never compares it against itself.

**Fix:** Add an optional `expectedRecipientHostId` parameter to `verifyRoleAttestation()`. When provided, verify that `attestation.recipientHostId === expectedRecipientHostId`.

---

### [SF-002] Non-atomic write in saveTransfers -- crash can corrupt governance-transfers.json
**File:** lib/transfer-registry.ts:56-60
**Original IDs:** CC-P5-A2-003
**Phases:** CC
`saveTransfers()` writes directly to the target file using `writeFileSync()` instead of using the atomic temp-file-then-rename pattern used by every other save function in this domain.

**Fix:** Use the atomic write pattern: write to `.tmp` then `renameSync`.

---

### [SF-003] handleGovernanceSyncMessage does not validate payload fields before type assertion
**File:** lib/governance-sync.ts:185-189
**Original IDs:** CC-P5-A2-004
**Phases:** CC
`handleGovernanceSyncMessage()` uses `as` to type-assert `message.payload` without validating that the expected fields (`managerId`, `managerName`, `teams`) actually exist and have the correct types. A malicious or buggy peer could send a payload missing these fields.

**Fix:** Add runtime validation for the extracted fields.

---

### [SF-004] amp-auth loadApiKeys/saveApiKeys has no file locking
**File:** lib/amp-auth.ts:50-90
**Original IDs:** CC-P5-A3-003
**Phases:** CC
`loadApiKeys()` and `saveApiKeys()` perform unprotected read-modify-write sequences. Functions like `createApiKey`, `validateApiKey`, `rotateApiKey`, and `revokeApiKey` all load, modify, and save without any locking.

**Fix:** Use `withLock('amp-api-keys', ...)` around mutating operations.

---

### [SF-005] validateApiKey timing-safe comparison is partially defeated
**File:** lib/amp-auth.ts:191-199
**Original IDs:** CC-P5-A3-004
**Phases:** CC
The `validateApiKey` function uses `timingSafeEqual` for hash comparison, but iterates through ALL keys with `Array.find()`. The overall pattern reveals how many keys were checked before finding a match. Minor for Phase 1 localhost.

**Fix:** Consider iterating all keys regardless of match to prevent timing leaks in future phases.

---

### [SF-006] saveAgents invalidates cache but concurrent loadAgents may use stale mtime
**File:** lib/agent-registry.ts:192-208, 145-187
**Original IDs:** CC-P5-A3-005
**Phases:** CC
`saveAgents()` sets `_cachedAgents = null` to invalidate the cache. If two `loadAgents()` calls happen in rapid succession within the same filesystem tick where mtime doesn't change, the second call might get stale cached data.

**Fix:** After invalidating cache in `saveAgents`, eagerly populate the cache with the agents just saved.

---

### [SF-007] resolveAlias does not handle multiple @ signs in name@host format
**File:** lib/agent-registry.ts:998-999
**Original IDs:** CC-P5-A3-006
**Phases:** CC
`resolveAlias` splits on `@` using `nameOrId.split('@')`, which for `user@host@extra` would silently ignore the rest.

**Fix:** Use `nameOrId.split('@', 2)` or `nameOrId.indexOf('@')` to split at the first `@` only.

---

### [SF-008] index-delta extractConversationMetadata reads entire file into memory
**File:** lib/index-delta.ts:277-278
**Original IDs:** CC-P5-A3-007
**Phases:** CC
`extractConversationMetadata` calls `fs.readFileSync(jsonlPath, 'utf-8')` which reads the entire file into memory. For large conversation files (hundreds of MB), this can cause memory pressure or OOM. The function only needs the first 50 and last 20 lines.

**Fix:** Use a streaming reader or read only the first and last N kilobytes of the file.

---

### [SF-009] Unsafe `error as Error` cast in useWebSocket
**File:** hooks/useWebSocket.ts:170
**Original IDs:** CC-P5-A7-003
**Phases:** CC
The catch block casts `error as Error`, but `new WebSocket()` can throw non-Error types. This would result in `connectionError.message` being `undefined`.

**Fix:** Use `setConnectionError(error instanceof Error ? error : new Error(String(error)))`.

---

### [SF-010] useTerminal returns `terminalRef.current` which does not trigger re-renders
**File:** hooks/useTerminal.ts:341
**Original IDs:** CC-P5-A7-004
**Phases:** CC
The hook returns `terminal: terminalRef.current`. Since refs do not trigger re-renders, consumers that depend on `terminal` being non-null will not see it until something else causes a re-render.

**Fix:** Consider adding a `terminal` state variable, or document that callers must trigger their own re-render.

---

### [SF-011] No exponential backoff for WebSocket reconnection
**File:** hooks/useWebSocket.ts:154-159
**Original IDs:** CC-P5-A7-005
**Phases:** CC
The reconnection uses a fixed 3-second delay for all attempts. CLAUDE.md documents an exponential backoff strategy (`[100, 500, 1000, 2000, 5000]`) but the implementation does not follow it.

**Fix:** Implement exponential backoff as documented.

---

### [SF-012] Modal dialogs in teams/page.tsx lack role="dialog" and aria-modal
**File:** app/teams/page.tsx:241-293 and 296-317
**Original IDs:** CC-P5-A7-006
**Phases:** CC
The Create Team dialog and Delete Confirmation dialog are rendered as plain `<div>` elements. They lack `role="dialog"`, `aria-modal="true"`, `aria-labelledby`, and focus trap behavior.

**Fix:** Add `role="dialog"` and `aria-modal="true"`. Implement focus trap.

---

### [SF-013] Create Team input lacks explicit `<label>` element
**File:** app/teams/page.tsx:252-262
**Original IDs:** CC-P5-A7-007
**Phases:** CC
The team name `<input>` in the Create Team dialog uses `placeholder="Team name..."` as its only label. Screen readers need an explicit `<label htmlFor="...">` or `aria-label`.

**Fix:** Add `aria-label="Team name"` or a proper `<label>` element.

---

### [SF-014] Connection status indicator uses color-only differentiation
**File:** components/TerminalView.tsx:631-635
**Original IDs:** CC-P5-A7-008
**Phases:** CC
The connection status indicator is a small colored dot with no text label or `aria-label`. Users with color blindness cannot distinguish the states.

**Fix:** Add `aria-label={isConnected ? 'Connected' : 'Disconnected'}` and `role="status"`.

---

### [SF-015] Emoji-only buttons lack aria-labels in TerminalView header
**File:** components/TerminalView.tsx:651-716
**Original IDs:** CC-P5-A7-009
**Phases:** CC
Several buttons use emoji as their visible label on mobile (text hidden via `hidden md:inline`). Without an `aria-label`, screen readers read the raw emoji unicode or nothing.

**Fix:** Add `aria-label` to each button matching its `title` attribute.

---

### [SF-016] N+1 fetch pattern in teams list page
**File:** app/teams/page.tsx:37-51
**Original IDs:** CC-P5-A7-010
**Phases:** CC
The teams list page fetches all teams, then for each team makes 2 additional fetches (tasks + documents) resulting in 1 + 2N requests. With 10 teams, this is 21 HTTP requests on page load.

**Fix:** Implement a bulk `/api/teams/stats` endpoint that returns all counts in a single request.

---

### [SF-017] governance-endpoint-auth test: broadcastGovernanceSync passthrough mock may break on new imports
**File:** tests/governance-endpoint-auth.test.ts:87-93
**Original IDs:** CC-P5-A6-004
**Phases:** CC
The passthrough mock for `@/lib/governance-sync` keeps all real exports. If governance-sync.ts adds a new dependency, these tests will fail with cryptic import errors.

**Fix:** Consider adding a smoke test that verifies the mock list matches the actual imports.

---

### [SF-018] cross-host-governance test: no coverage for `receiveCrossHostRequest` auto-approve path
**File:** tests/cross-host-governance.test.ts
**Original IDs:** CC-P5-A6-005
**Phases:** CC
The source has an auto-approve path when `shouldAutoApprove(request)` returns true. No test covers this path. Auto-approval bypasses the dual-manager approval workflow.

**Fix:** Add tests that verify auto-approve behavior.

---

### [SF-019] cross-host-governance test: no coverage for `assign-cos`, `remove-cos`, `transfer-agent` execution paths
**File:** tests/cross-host-governance.test.ts:612-678
**Original IDs:** CC-P5-A6-006
**Phases:** CC
`performRequestExecution` handles 5 operation types but only `add-to-team` and `remove-from-team` are tested. The `assign-cos` path has complex logic (COS-only-on-closed-teams, G3 one-COS-per-agent).

**Fix:** Add tests for `assign-cos`, `remove-cos`, and `transfer-agent`.

---

### [SF-020] cross-host-governance test: `receiveCrossHostRequest` doesn't test CC-P1-002 sanitization
**File:** tests/cross-host-governance.test.ts:330-396
**Original IDs:** CC-P5-A6-007
**Phases:** CC
The source has critical security validation (type whitelist, role whitelist, sourceHostId match, forced pending status). None of these have dedicated test coverage.

**Fix:** Add tests for each security guard: invalid request type returns 400, invalid role returns 400, mismatched sourceHostId returns 400, pre-filled status/approvals are stripped.

---

### [SF-021] cross-host-governance test: `approveCrossHostRequest` doesn't test CC-010 (neither source nor target)
**File:** tests/cross-host-governance.test.ts:402-493
**Original IDs:** CC-P5-A6-008
**Phases:** CC
The source has a guard rejecting requests where the host is neither source nor target. No test covers this path.

**Fix:** Add a test where both `sourceHostId` and `targetHostId` are different from the local host.

---

### [SF-022] role-attestation test: no coverage for `recipientHostId` parameter in `createRoleAttestation`
**File:** tests/role-attestation.test.ts
**Original IDs:** CC-P5-A6-009
**Phases:** CC
`createRoleAttestation` accepts an optional `recipientHostId` parameter. No test calls it with this argument.

**Fix:** Add tests for `createRoleAttestation` with `recipientHostId` and verify it's included in attestation data.

---

### [SF-023] message-filter test: no test for Step 5b (open-world sender to MANAGER/COS recipient)
**File:** tests/message-filter.test.ts
**Original IDs:** CC-P5-A6-010
**Phases:** CC
Source lines 174-181 implement Step 5b: an open-world agent can reach MANAGER and COS of closed teams. No test covers the ALLOW case.

**Fix:** Add two tests: open-world sender to MANAGER (allowed) and open-world sender to COS (allowed).

---

### [SF-024] governance-sync test: no coverage for `requestPeerSync` Ed25519 signing
**File:** tests/governance-sync.test.ts
**Original IDs:** CC-P5-A6-011
**Phases:** CC
The source `requestPeerSync` function includes Ed25519 signature headers. The test verifies URL and response but does NOT verify that the signature headers are sent.

**Fix:** Add assertions that `signHostAttestation` was called and that fetch headers include `X-Host-Id`, `X-Host-Timestamp`, `X-Host-Signature`.

---

### [SF-025] agent-config-governance test: missing mock for `@/lib/governance-sync`
**File:** tests/agent-config-governance.test.ts
**Original IDs:** CC-P5-A6-012
**Phases:** CC
The test mocks `@/lib/governance` but governance.ts imports `broadcastGovernanceSync` from `@/lib/governance-sync`. If the mock structure changes to passthrough, the real governance-sync would load and fail.

**Fix:** Add `vi.mock('@/lib/governance-sync', () => ({ broadcastGovernanceSync: vi.fn() }))` for defensive robustness.

---

### [SF-026] No authorization checks in agents-messaging-service.ts
**File:** services/agents-messaging-service.ts (entire file)
**Original IDs:** CC-P5-A5-005
**Phases:** CC
None of the messaging functions verify that the caller is authorized to act on behalf of the specified `agentId`. Any caller can read, send, update, or delete messages for any agent.

**Fix:** Add agent identity verification, either at the service layer or the router layer.

---

### [SF-027] updateMetrics accepts arbitrary keys via rest spread
**File:** services/agents-memory-service.ts:1119
**Original IDs:** CC-P5-A5-006
**Phases:** CC
`updateMetrics()` destructures `body` with a rest spread `...metrics`, then passes the rest to `updateAgentMetrics()`. Any arbitrary keys from the request body are forwarded as metric updates.

**Fix:** Explicitly whitelist allowed metric fields instead of using rest spread.

---

### [SF-028] Missing null check on agent before accessing subconscious in triggerSubconsciousAction
**File:** services/agents-subconscious-service.ts:73
**Original IDs:** CC-P5-A5-007
**Phases:** CC
`agentRegistry.getAgent(agentId)` is called but there is no null check before calling `agent.getSubconscious()`. Compare with `getSubconsciousStatus()` which does check.

**Fix:** Add a null check and return a 404 `ServiceResult` if agent is not found.

---

### [SF-029] connectionString exposed in error messages in agents-graph-service.ts
**File:** services/agents-graph-service.ts:314-319
**Original IDs:** CC-P5-A5-008
**Phases:** CC
When database introspection fails, the error message (which may contain connection credentials) is passed through to the API response.

**Fix:** Return a generic error message like `'Failed to connect to database'`. Log the full error server-side only.

---

### [SF-030] Missing path validation for skill settings file access
**File:** services/agents-skills-service.ts:189
**Original IDs:** CC-P5-A5-009
**Phases:** CC
The `agentId` parameter is used directly in file path construction. If `agentId` contains path traversal characters (e.g., `../../../etc/passwd`), it could read or write arbitrary files.

**Fix:** Validate `agentId` against a UUID pattern before constructing the file path.

---

### [SF-031] Potential infinite loop in background delta indexing
**File:** services/agents-docs-service.ts:76-78
**Original IDs:** CC-P5-A5-010
**Phases:** CC
`triggerBackgroundDocsDeltaIndexing()` calls the same `/api/agents/${agentId}/docs` POST endpoint it is triggered from. If any error handling re-routes this, infinite recursion is possible.

**Fix:** Add a guard to prevent re-triggering delta indexing when the request is already a delta indexing request.

---

### [SF-032] amount=0 silently defaults to 1 in updateMetrics increment
**File:** services/agents-memory-service.ts:1122
**Original IDs:** CC-P5-A5-011
**Phases:** CC
When `action === 'increment'`, the amount defaults to 1 via `amount || 1`. If the caller passes `amount: 0`, it silently becomes 1.

**Fix:** Use `amount ?? 1` to preserve intentional zero values.

---

### [SF-033] status parameter typed as `any` without validation in messages-service.ts
**File:** services/agents-messaging-service.ts:196
**Original IDs:** CC-P5-A5-012
**Phases:** CC
In `listMessages()`, `status` and `priority` are typed as `any` and passed directly without validation.

**Fix:** Type to actual allowed values and validate before passing through.

---

### [SF-034] Silent swallow of malformed JSON in POST /api/agents/[id]/docs
**File:** app/api/agents/[id]/docs/route.ts:53-60
**Original IDs:** CC-P5-A1-011
**Phases:** CC
The POST handler parses the body with `JSON.parse(text)` but catches parse errors silently and uses `{}`. Invalid (non-empty) JSON should return 400.

**Fix:** If `text.trim()` is non-empty and `JSON.parse` fails, return 400.

---

### [SF-035] Silent swallow of malformed JSON in POST /api/agents/[id]/graph/code
**File:** app/api/agents/[id]/graph/code/route.ts:42-50
**Original IDs:** CC-P5-A1-012
**Phases:** CC
Same pattern as SF-034 -- invalid non-empty JSON body is silently treated as `{}`.

**Fix:** Distinguish between empty body (allowed) and malformed non-empty JSON (return 400).

---

### [SF-036] NaN propagation risk in parseInt for depth parameter
**File:** app/api/agents/[id]/graph/code/route.ts:22
**Original IDs:** CC-P5-A1-013
**Phases:** CC
`parseInt(searchParams.get('depth') || '1', 10)` will yield `NaN` for non-numeric strings like `?depth=abc`.

**Fix:** Add NaN guard: `parseInt(...) || 1`.

---

### [SF-037] Potential NaN propagation in parseInt for limit in docs route
**File:** app/api/agents/[id]/docs/route.ts:28
**Original IDs:** CC-P5-A1-014
**Phases:** CC
Same pattern: `parseInt(searchParams.get('limit') || '10', 10)` will yield NaN for non-numeric strings.

**Fix:** Add NaN guard: `parseInt(...) || 10`.

---

### [SF-038] Potential NaN propagation in parseInt for batchSize
**File:** app/api/agents/[id]/index-delta/route.ts:22
**Original IDs:** CC-P5-A1-015
**Phases:** CC
`parseInt(searchParams.get('batchSize')!)` can return NaN for non-numeric strings.

**Fix:** Add explicit NaN guard.

---

### [SF-039] Potential NaN propagation in timeout parameter
**File:** app/api/agents/unified/route.ts:19
**Original IDs:** CC-P5-A1-016
**Phases:** CC
`parseInt(searchParams.get('timeout') || '3000', 10)` can return NaN for non-numeric strings.

**Fix:** Add NaN guard: `parseInt(...) || 3000`.

---

### [SF-040] Error result not checked in GET /api/agents/unified
**File:** app/api/agents/unified/route.ts:22
**Original IDs:** CC-P5-A1-017
**Phases:** CC
The GET handler does not check `result.error` before returning. If the service returns an error, it will be returned with status 200.

**Fix:** Add error check consistent with other routes.

---

### [SF-041] Unchecked JSON parse in agent import options
**File:** app/api/agents/import/route.ts:23
**Original IDs:** CC-P5-A1-018
**Phases:** CC
The `optionsStr` from `formData.get('options')` is parsed with `JSON.parse(optionsStr)` without try/catch.

**Fix:** Wrap in try/catch returning 400 with descriptive error about malformed options JSON.

---

### [SF-042] Logging user-controlled data to console
**File:** app/api/conversations/parse/route.ts:13
**Original IDs:** CC-P5-A1-019
**Phases:** CC
The endpoint logs `conversationFile` from the request body directly to console. Could contain sensitive file paths.

**Fix:** Remove or reduce to debug-level logging.

---

### [SF-043] Inconsistent error response shape across API routes
**File:** Multiple files
**Original IDs:** CC-P5-A1-020, CC-P5-A0-009
**Also identified by:** CC-P5-A0-009
**Phases:** CC
Three different error response patterns exist across the routes:
1. **Pattern A** (governance/trust, etc.): Direct `if (result.error)` check -- clean and explicit.
2. **Pattern B** (v1/governance/requests): `result.data ?? { error: result.error }` -- compact but hides branching.
3. **Pattern C** (hosts/register-peer, hosts/exchange-peers): No error check at all.

This inconsistency makes maintenance error-prone. Pattern A is the clearest and most defensive.

**Fix:** Standardize on Pattern A across all routes.

---

### [SF-044] Missing error check in register-peer route before accessing result.data
**File:** app/api/hosts/register-peer/route.ts:17
**Original IDs:** CC-P5-A0-003
**Phases:** CC
Same issue as MF-023 (exchange-peers) but for `registerPeer`. The service function always returns with `data` set, but deviates from the `ServiceResult` contract.

**Fix:** Add the standard `if (result.error)` check.

---

### [SF-045] SSRF potential in hosts/health endpoint -- no destination URL validation
**File:** app/api/hosts/health/route.ts:12-13
**Original IDs:** CC-P5-A0-004
**Phases:** CC
The `GET /api/hosts/health?url=<hostUrl>` endpoint proxies a health check to any user-supplied URL. Could be used to scan internal networks. Phase 1 is localhost-only which mitigates this.

**Fix:** Add URL destination validation: reject `file://`, private IPs, `localhost`. Restrict to configured hosts only.

---

### [SF-046] Type assertion `as UpdateTaskParams` bypasses type safety
**File:** app/api/teams/[id]/tasks/[taskId]/route.ts:27
**Original IDs:** CC-P5-A0-005
**Phases:** CC
The route spreads unvalidated request body into a type-asserted `UpdateTaskParams`. The cast bypasses TypeScript's type checker.

**Fix:** Validate the body fields before casting, or ensure the service validates.

---

### [SF-047] Type assertion `as CreateTaskParams` bypasses type safety
**File:** app/api/teams/[id]/tasks/route.ts:50
**Original IDs:** CC-P5-A0-006
**Phases:** CC
Same pattern as SF-046 but for `createTeamTask`.

**Fix:** Validate body fields or ensure the service validates.

---

### [SF-048] `any` type annotation on hosts.find callback parameter
**File:** app/api/v1/governance/requests/route.ts:36
**Original IDs:** CC-P5-A0-007
**Phases:** CC
`.find((h: any) => h.id === hostId)` uses an explicit `any` annotation. Since `getHosts()` returns `Host[]`, the `any` is unnecessary.

**Fix:** Remove the `: any` annotation.

---

### [SF-049] hostId in error message could leak host identifiers to unauthenticated callers
**File:** app/api/v1/governance/requests/route.ts:38
**Original IDs:** CC-P5-A0-008
**Phases:** CC
The error message includes the user-supplied `hostId` value: `Unknown host: ${hostId}`. This confirms to an attacker that the host ID they tried is not in the system.

**Fix:** Use a generic message: `{ error: 'Unknown host' }` without echoing the ID.

---

### [SF-050] `convertAMPToMessage` uses `any` type for `ampMsg` parameter
**File:** lib/messageQueue.ts:192
**Original IDs:** CC-P5-A4-003
**Phases:** CC
`convertAMPToMessage(ampMsg: any)` takes an untyped parameter. All field accesses are unchecked at compile time.

**Fix:** Define an interface for the on-disk AMP message format and use it instead of `any`.

---

### [SF-051] `triggerOldDuplicateCleanup` deletes files matching `msg-*.json` which could catch legitimate messages
**File:** lib/messageQueue.ts:138
**Original IDs:** CC-P5-A4-004
**Phases:** CC
The cleanup function deletes ALL files matching `msg-*.json` pattern. Files created by the current system also match this pattern.

**Fix:** Use a more specific pattern or check that files lack certain new-format fields before deleting.

---

### [SF-052] `markMessageAsRead` and `archiveMessage` have TOCTOU race on file read/write
**File:** lib/messageQueue.ts:709-735, 757-782
**Original IDs:** CC-P5-A4-005
**Phases:** CC
Both functions read, parse, modify status, and write without any locking. Concurrent requests could overwrite each other.

**Fix:** Use atomic write (write to temp + rename) or file-lock mechanism.

---

### [SF-053] `cleanupAgentCacheSweep` is exported but never called outside the module
**File:** lib/messageQueue.ts:457-460
**Original IDs:** CC-P5-A4-006
**Phases:** CC
The function is exported for shutdown/test cleanup but is never imported or called anywhere. Dead code.

**Fix:** Wire into the server shutdown handler and test teardown, or remove the export.

---

### [SF-054] `hashApiKey` uses unsalted SHA-256
**File:** lib/amp-auth.ts:100-102
**Original IDs:** CC-P5-A4-007
**Phases:** CC
API key hashes are stored using plain SHA-256 with no salt. For 256-bit keys, brute force is infeasible, but defense-in-depth suggests adding a per-record salt.

**Fix:** Consider `HMAC-SHA256(salt, apiKey)` where salt is stored alongside the hash.

---

### [SF-055] `sendFromUI` passes potentially empty agentId without explicit guard
**File:** lib/message-send.ts:193
**Original IDs:** CC-P5-A4-008
**Phases:** CC
When the sender resolves but has no UUID, `fromAgent?.agentId` could be an empty string. The pattern is handled accidentally via `'' || null`, but inconsistently across usage sites.

**Fix:** Add explicit empty-string check: `senderAgentId: fromAgent?.agentId?.length ? fromAgent.agentId : null`.

---

### [SF-056] Claim: "agentHostMap field on Team type" is purely a type stub with zero implementation
**File:** types/team.ts:30
**Original IDs:** CV-P5-001
**Phases:** CV
The `agentHostMap?: Record<string, string>` field is declared but has a JSDoc comment `@planned Layer 3 -- not yet populated or consumed anywhere`. The CHANGELOG implies it enables "multi-host team membership tracking" but it is purely a type stub.

**Fix:** Either implement the field or add a note to the CHANGELOG clarifying its planned status.

---

### [SF-057] Claim: "configure-agent" as a cross-host governance request type is not fully implemented
**File:** types/governance-request.ts:20, services/cross-host-governance-service.ts
**Original IDs:** CV-P5-002
**Phases:** CV
The `configure-agent` type is defined and `receiveCrossHostRequest` accepts it, but `submitCrossHostRequest` explicitly rejects it and `performRequestExecution` has a no-op default case.

**Fix:** Either implement the type end-to-end or document it as scaffolded/planned.

---

### [SF-058] Claim: "Layer 5 agent configuration governance enforcement" is optional, not mandatory
**File:** services/agents-core-service.ts, CHANGELOG.md
**Original IDs:** CV-P5-003
**Phases:** CV
The CHANGELOG says "Agent configuration governance (Layer 5): MANAGER/COS role enforcement on agent CRUD operations". The enforcement IS implemented but is optional -- if no `X-Agent-Id` / `Authorization` header is present, governance checks are skipped entirely. The CHANGELOG doesn't mention this.

**Fix:** Clarify in the CHANGELOG that governance enforcement is optional when no auth is provided (Phase 1 behavior).

---

## Nits & Suggestions

### [NT-001] isChiefOfStaffAnywhere null guard pattern is safe but could be more explicit
**File:** lib/governance.ts:147-152
**Original IDs:** CC-P5-A2-005
**Phases:** CC
No actual bug, but for consistency with the defensive pattern in `isManager()`, a comment noting why the comparison is safe would improve clarity.

---

### [NT-002] GovernanceRole type alias adds indirection without value
**File:** types/governance.ts:19
**Original IDs:** CC-P5-A2-006
**Phases:** CC
`GovernanceRole` is a type alias for `AgentRole` with no additional semantic distinction. Consider removing it.

---

### [NT-003] renameSync missing from transfer-registry.ts imports (needed for atomic write fix)
**File:** lib/transfer-registry.ts:9
**Original IDs:** CC-P5-A2-007
**Phases:** CC
Note for the SF-002 fix implementation: `renameSync` needs to be added to the import.

---

### [NT-004] Inconsistent error return patterns across registries
**File:** lib/document-registry.ts:47, lib/task-registry.ts:48
**Original IDs:** CC-P5-A3-008
**Phases:** CC
`saveDocuments` and `saveTasks` return `boolean`, `saveAgents` also returns boolean, but `saveApiKeys` throws on failure. Callers ignore the return value in most cases.

**Fix:** Choose a consistent pattern. Given the fail-fast preference, throwing is preferred.

---

### [NT-005] generateUniquePersonaName can return a duplicate when all names are used
**File:** lib/agent-registry.ts:96-101
**Original IDs:** CC-P5-A3-009
**Phases:** CC
When all persona names are used, the function returns a duplicate name.

**Fix:** Append a numeric suffix to guarantee uniqueness.

---

### [NT-006] rate-limit check and record are not atomic
**File:** lib/rate-limit.ts:12-13
**Original IDs:** CC-P5-A3-010
**Phases:** CC
Between `checkRateLimit` returning `{allowed: true}` and `recordFailure` being called, another request could get through.

**Fix:** Combine into a single `checkAndRecord` function.

---

### [NT-007] file-lock has no deadlock detection or timeout
**File:** lib/file-lock.ts:74
**Original IDs:** CC-P5-A3-011
**Phases:** CC
If a lock holder crashes, all queued waiters will wait forever.

**Fix:** Add an optional timeout parameter to `acquireLock` / `withLock`.

---

### [NT-008] screenReaderMode explicitly disabled in useTerminal
**File:** hooks/useTerminal.ts:107
**Original IDs:** CC-P5-A7-011
**Phases:** CC
`screenReaderMode: false` removes terminal content from the accessibility tree. A more explicit comment about the trade-off would be helpful.

---

### [NT-009] GovernancePasswordDialog has redundant state reset on close
**File:** components/governance/GovernancePasswordDialog.tsx:26-33 and 36-43
**Original IDs:** CC-P5-A7-012
**Phases:** CC
State is reset both in the `useEffect` when `isOpen` becomes true and in `handleClose`. The duplication is defensive and acceptable.

---

### [NT-010] localStorage reads use mount-only effects without storageId in deps
**File:** components/TerminalView.tsx:498-508, 510-519
**Original IDs:** CC-P5-A7-013
**Phases:** CC
No action needed for current tab-based architecture. The comment adequately explains the design choice.

---

### [NT-011] RoleBadge default case uses String() conversion on exhausted type
**File:** components/governance/RoleBadge.tsx:64-72
**Original IDs:** CC-P5-A7-014
**Phases:** CC
The defensive pattern is correct. No change needed.

---

### [NT-012] TeamOverviewSection re-fetches repos on every section expand
**File:** components/zoom/AgentProfileTab.tsx:74-93
**Original IDs:** CC-P5-A7-015
**Phases:** CC
Unlike `AgentProfile.tsx` which has a `reposLoaded` guard, this component will re-fetch repos every time the section is expanded.

**Fix:** Add a `reposLoaded` state guard.

---

### [NT-013] governance-endpoint-auth test: `buildLocalGovernanceSnapshot` imported but not tested directly
**File:** tests/governance-endpoint-auth.test.ts:105
**Original IDs:** CC-P5-A6-013
**Phases:** CC
Dead import. The function is tested in `governance-sync.test.ts`.

**Fix:** Remove the unused import.

---

### [NT-014] role-attestation test: `buildValidAttestation` does not include `recipientHostId` in data string
**File:** tests/role-attestation.test.ts:90
**Original IDs:** CC-P5-A6-014
**Phases:** CC
If a test passes `recipientHostId` in overrides, the binding data would be wrong and verification would fail for the wrong reason.

**Fix:** Update `buildValidAttestation` to include recipientHostId when present.

---

### [NT-015] cross-host-governance test: unused `mockExecuteGovernanceRequest` mock
**File:** tests/cross-host-governance.test.ts:54,65,156
**Original IDs:** CC-P5-A6-015
**Phases:** CC
`mockExecuteGovernanceRequest` is declared and wired but never asserted against. Dead code.

**Fix:** Remove the unused mock.

---

### [NT-016] governance-endpoint-auth test: SR-007 tests only use `requestedByRole: 'manager'`
**File:** tests/governance-endpoint-auth.test.ts:258-343
**Original IDs:** CC-P5-A6-016
**Phases:** CC
All SR-007 type whitelist tests use `'manager'` role. The source also accepts `'chief-of-staff'`. Testing only with `'manager'` means no coverage for COS triggering the whitelist check.

**Fix:** Consider adding one test with `requestedByRole: 'chief-of-staff'`.

---

### [NT-017] agent-registry test: large test file with 60+ tests could benefit from describe nesting
**File:** tests/agent-registry.test.ts (986 lines)
**Original IDs:** CC-P5-A6-017
**Phases:** CC
Purely a readability/maintenance concern. No functional change needed.

---

### [NT-018] test-utils/fixtures.ts: `makeServiceResult` helper is unused
**File:** tests/test-utils/fixtures.ts:150-155
**Original IDs:** CC-P5-A6-018
**Phases:** CC
Exported but never imported or used in any test file.

**Fix:** Remove or note for future use.

---

### [NT-019] Inconsistent use of `any` type throughout services
**File:** Multiple service files
**Original IDs:** CC-P5-A5-014
**Phases:** CC
Many service functions use `ServiceResult<any>` instead of defining proper return types. Eliminates compile-time type checking.

**Fix:** Define specific result type interfaces.

---

### [NT-020] Redundant pattern: `readyState === 1` magic number in shared-state.ts
**File:** services/shared-state.ts
**Original IDs:** CC-P5-A5-015
**Phases:** CC
Uses magic number 1 instead of `WebSocket.OPEN`.

**Fix:** Use `WebSocket.OPEN`.

---

### [NT-021] Console.log in production code for playback service
**File:** services/agents-playback-service.ts:47-49, 104-106
**Original IDs:** CC-P5-A5-016
**Phases:** CC
The playback service logs on every request. Noisy in production.

**Fix:** Gate behind a debug flag or remove from placeholder implementations.

---

### [NT-022] Redundant `details: result.error` in health proxy response
**File:** app/api/agents/health/route.ts:29
**Original IDs:** CC-P5-A1-021
**Phases:** CC
The error response includes both `error` and `details` with the same value.

**Fix:** Remove `details` or provide actual details.

---

### [NT-023] Redundant `details: result.error` in register response
**File:** app/api/agents/register/route.ts:19
**Original IDs:** CC-P5-A1-022
**Phases:** CC
Same issue: both `error` and `details` contain `result.error`.

**Fix:** Remove redundant `details` field.

---

### [NT-024] Unused import `NextRequest` in several files
**File:** app/api/agents/[id]/database/route.ts:1, app/api/agents/[id]/tracking/route.ts:1
**Original IDs:** CC-P5-A1-023
**Phases:** CC
`NextRequest` is imported but `request` is never accessed for its NextRequest-specific features.

**Fix:** Use `_request: NextRequest` or just `Request` type. Very minor.

---

### [NT-025] Missing top-level try/catch in several agent route handlers
**File:** app/api/agents/[id]/amp/addresses/route.ts, and 4 more
**Original IDs:** CC-P5-A1-024
**Phases:** CC
These thin-wrapper routes delegate to service functions but do not have top-level try/catch. Minor since service functions are designed to not throw.

**Fix:** Add top-level try/catch for defense-in-depth.

---

### [NT-026] Missing UUID validation on path parameters in several routes
**File:** app/api/teams/[id]/route.ts, and 5 more
**Original IDs:** CC-P5-A0-010
**Phases:** CC
Several routes accept path parameters without UUID format validation, inconsistent with other routes that validate.

**Fix:** Add `isValidUuid(id)` checks.

---

### [NT-027] Missing `export const dynamic = 'force-dynamic'` on several routes
**File:** app/api/governance/route.ts, and 2 more
**Original IDs:** CC-P5-A0-011
**Phases:** CC
Some GET routes read runtime state but don't export `dynamic = 'force-dynamic'`. Next.js may cache these.

**Fix:** Add `export const dynamic = 'force-dynamic'`.

---

### [NT-028] notification-service.ts uses emoji in notification format
**File:** lib/notification-service.ts:74-76
**Original IDs:** CC-P5-A4-009
**Phases:** CC
Hardcoded emoji for priority indicators may not render correctly in minimal terminal environments.

**Fix:** Consider configurable or plain text alternatives.

---

### [NT-029] Duplicate `ResolvedAgent` interface definitions across files
**File:** lib/messageQueue.ts:90-97, lib/message-send.ts:42-49
**Original IDs:** CC-P5-A4-010
**Phases:** CC
The interface is defined in both files with nearly identical definitions. Fragile if one is updated without the other.

**Fix:** Export from `messageQueue.ts` and import in `message-send.ts`.

---

### [NT-030] `getSelfHostName` is duplicated between messageQueue.ts and message-send.ts
**File:** lib/messageQueue.ts:14-21, lib/message-send.ts:78-85
**Original IDs:** CC-P5-A4-011
**Phases:** CC
Identical implementations in two files. Violates DRY.

**Fix:** Extract to a shared utility.

---

### [NT-031] `saveApiKeys` writes JSON without atomic write
**File:** lib/amp-auth.ts:82
**Original IDs:** CC-P5-A4-012
**Phases:** CC
Uses `fs.writeFileSync()` which is not atomic. Process crash mid-write could corrupt the file.

**Fix:** Write to temp file then `fs.renameSync`.

---

## Deduplication Log

| Final ID | Original IDs | Duplicates Removed | Reason |
|----------|-------------|-------------------|--------|
| MF-001 | CC-P5-A2-001 | 0 | Unique finding |
| MF-002 | CC-P5-A3-001 | 0 | Unique finding |
| MF-003 | CC-P5-A3-002 | 0 | Unique finding |
| MF-004 | CC-P5-A7-001 | 0 | Unique finding |
| MF-005 | CC-P5-A7-002 | 0 | Unique finding |
| MF-006 | CC-P5-A6-001 | 0 | Unique finding |
| MF-007 | CC-P5-A6-002 | 0 | Unique finding |
| MF-008 | CC-P5-A6-003 | 0 | Unique finding |
| MF-009 | CC-P5-A5-001, CC-P5-A5-003 | 1 | Same file (agents-graph-service.ts), same root cause (local escapeString function at line 68 uses wrong escaping convention), fixing escapeString fixes both line 68 usage and line 950 usage |
| MF-010 | CC-P5-A5-002 | 0 | Unique finding (different file: agents-docs-service.ts) |
| MF-011 | CC-P5-A5-004 | 0 | Unique finding |
| MF-012 | CC-P5-A1-001 | 0 | Unique finding |
| MF-013 | CC-P5-A1-002 | 0 | Unique finding |
| MF-014 | CC-P5-A1-003 | 0 | Unique finding |
| MF-015 | CC-P5-A1-004 | 0 | Unique finding |
| MF-016 | CC-P5-A1-005 | 0 | Unique finding |
| MF-017 | CC-P5-A1-006 | 0 | Unique finding |
| MF-018 | CC-P5-A1-007 | 0 | Unique finding |
| MF-019 | CC-P5-A1-008 | 0 | Unique finding |
| MF-020 | CC-P5-A1-009 | 0 | Unique finding |
| MF-021 | CC-P5-A1-010 | 0 | Unique finding |
| MF-022 | CC-P5-A0-001 | 0 | Unique finding |
| MF-023 | CC-P5-A0-002 | 0 | Unique finding |
| MF-024 | CC-P5-A4-001 | 0 | Unique finding |
| MF-025 | CC-P5-A4-002 | 0 | Unique finding |
| SF-001 | CC-P5-A2-002 | 0 | Unique finding |
| SF-002 | CC-P5-A2-003 | 0 | Unique finding |
| SF-003 | CC-P5-A2-004 | 0 | Unique finding |
| SF-004 | CC-P5-A3-003 | 0 | Unique finding |
| SF-005 | CC-P5-A3-004 | 0 | Unique finding |
| SF-006 | CC-P5-A3-005 | 0 | Unique finding |
| SF-007 | CC-P5-A3-006 | 0 | Unique finding |
| SF-008 | CC-P5-A3-007 | 0 | Unique finding |
| SF-009 | CC-P5-A7-003 | 0 | Unique finding |
| SF-010 | CC-P5-A7-004 | 0 | Unique finding |
| SF-011 | CC-P5-A7-005 | 0 | Unique finding |
| SF-012 | CC-P5-A7-006 | 0 | Unique finding |
| SF-013 | CC-P5-A7-007 | 0 | Unique finding |
| SF-014 | CC-P5-A7-008 | 0 | Unique finding |
| SF-015 | CC-P5-A7-009 | 0 | Unique finding |
| SF-016 | CC-P5-A7-010 | 0 | Unique finding |
| SF-017 | CC-P5-A6-004 | 0 | Unique finding |
| SF-018 | CC-P5-A6-005 | 0 | Unique finding |
| SF-019 | CC-P5-A6-006 | 0 | Unique finding |
| SF-020 | CC-P5-A6-007 | 0 | Unique finding |
| SF-021 | CC-P5-A6-008 | 0 | Unique finding |
| SF-022 | CC-P5-A6-009 | 0 | Unique finding |
| SF-023 | CC-P5-A6-010 | 0 | Unique finding |
| SF-024 | CC-P5-A6-011 | 0 | Unique finding |
| SF-025 | CC-P5-A6-012 | 0 | Unique finding |
| SF-026 | CC-P5-A5-005 | 0 | Unique finding |
| SF-027 | CC-P5-A5-006 | 0 | Unique finding |
| SF-028 | CC-P5-A5-007 | 0 | Unique finding |
| SF-029 | CC-P5-A5-008 | 0 | Unique finding |
| SF-030 | CC-P5-A5-009 | 0 | Unique finding |
| SF-031 | CC-P5-A5-010 | 0 | Unique finding |
| SF-032 | CC-P5-A5-011 | 0 | Unique finding |
| SF-033 | CC-P5-A5-012 | 0 | Unique finding |
| SF-034 | CC-P5-A1-011 | 0 | Unique finding |
| SF-035 | CC-P5-A1-012 | 0 | Unique finding |
| SF-036 | CC-P5-A1-013 | 0 | Unique finding |
| SF-037 | CC-P5-A1-014 | 0 | Unique finding |
| SF-038 | CC-P5-A1-015 | 0 | Unique finding |
| SF-039 | CC-P5-A1-016 | 0 | Unique finding |
| SF-040 | CC-P5-A1-017 | 0 | Unique finding |
| SF-041 | CC-P5-A1-018 | 0 | Unique finding |
| SF-042 | CC-P5-A1-019 | 0 | Unique finding |
| SF-043 | CC-P5-A1-020, CC-P5-A0-009 | 1 | Same root cause: inconsistent error response patterns across API routes. A1-020 (SHOULD-FIX) describes shape inconsistency, A0-009 (NIT) describes pattern inconsistency. Merged at SHOULD-FIX severity. |
| SF-044 | CC-P5-A0-003 | 0 | Unique finding |
| SF-045 | CC-P5-A0-004 | 0 | Unique finding |
| SF-046 | CC-P5-A0-005 | 0 | Unique finding |
| SF-047 | CC-P5-A0-006 | 0 | Unique finding |
| SF-048 | CC-P5-A0-007 | 0 | Unique finding |
| SF-049 | CC-P5-A0-008 | 0 | Unique finding |
| SF-050 | CC-P5-A4-003 | 0 | Unique finding |
| SF-051 | CC-P5-A4-004 | 0 | Unique finding |
| SF-052 | CC-P5-A4-005 | 0 | Unique finding |
| SF-053 | CC-P5-A4-006 | 0 | Unique finding |
| SF-054 | CC-P5-A4-007 | 0 | Unique finding |
| SF-055 | CC-P5-A4-008 | 0 | Unique finding |
| SF-056 | CV-P5-001 | 0 | Unique finding |
| SF-057 | CV-P5-002 | 0 | Unique finding |
| SF-058 | CV-P5-003 | 0 | Unique finding |
| NT-001 | CC-P5-A2-005 | 0 | Unique finding |
| NT-002 | CC-P5-A2-006 | 0 | Unique finding |
| NT-003 | CC-P5-A2-007 | 0 | Unique finding |
| NT-004 | CC-P5-A3-008 | 0 | Unique finding |
| NT-005 | CC-P5-A3-009 | 0 | Unique finding |
| NT-006 | CC-P5-A3-010 | 0 | Unique finding |
| NT-007 | CC-P5-A3-011 | 0 | Unique finding |
| NT-008 | CC-P5-A7-011 | 0 | Unique finding |
| NT-009 | CC-P5-A7-012 | 0 | Unique finding |
| NT-010 | CC-P5-A7-013 | 0 | Unique finding |
| NT-011 | CC-P5-A7-014 | 0 | Unique finding |
| NT-012 | CC-P5-A7-015 | 0 | Unique finding |
| NT-013 | CC-P5-A6-013 | 0 | Unique finding |
| NT-014 | CC-P5-A6-014 | 0 | Unique finding |
| NT-015 | CC-P5-A6-015 | 0 | Unique finding |
| NT-016 | CC-P5-A6-016 | 0 | Unique finding |
| NT-017 | CC-P5-A6-017 | 0 | Unique finding |
| NT-018 | CC-P5-A6-018 | 0 | Unique finding |
| NT-019 | CC-P5-A5-014 | 0 | Unique finding |
| NT-020 | CC-P5-A5-015 | 0 | Unique finding |
| NT-021 | CC-P5-A5-016 | 0 | Unique finding |
| NT-022 | CC-P5-A1-021 | 0 | Unique finding |
| NT-023 | CC-P5-A1-022 | 0 | Unique finding |
| NT-024 | CC-P5-A1-023 | 0 | Unique finding |
| NT-025 | CC-P5-A1-024 | 0 | Unique finding |
| NT-026 | CC-P5-A0-010 | 0 | Unique finding |
| NT-027 | CC-P5-A0-011 | 0 | Unique finding |
| NT-028 | CC-P5-A4-009 | 0 | Unique finding |
| NT-029 | CC-P5-A4-010 | 0 | Unique finding |
| NT-030 | CC-P5-A4-011 | 0 | Unique finding |
| NT-031 | CC-P5-A4-012 | 0 | Unique finding |
| -- | CC-P5-A5-013 | -- | REMOVED: Self-retracted by reviewer (escapeForCozo IS used at line 787) |
| -- | CV-P5-004 | -- | REMOVED: Informational only (severity N/A, verified CHANGELOG count is correct) |

## Removed Findings

| Original ID | Reason |
|-------------|--------|
| CC-P5-A5-013 | Self-retracted by reviewer: after re-reading the file, `escapeForCozo` IS used at line 787. Not a dead import. |
| CV-P5-004 | Informational note confirming CHANGELOG test count (169) is correct. Not a finding. |

## Self-Verification

- [x] I parsed ALL findings from the intermediate report (none skipped or missed)
- [x] I grouped findings by file path before comparing
- [x] For each potential duplicate pair, I checked ALL THREE conditions:
      (1) same file path + (2) overlapping lines within 5 + (3) same root cause
- [x] I did NOT merge findings at the same line that describe DIFFERENT bugs
- [x] I did NOT merge findings from different files even if they describe the same pattern
- [x] For merged findings: I kept the HIGHEST severity (MF-009 kept MUST-FIX; SF-043 kept SHOULD-FIX over NIT)
- [x] For merged findings: I preserved ALL original IDs in "Also identified by" annotation
- [x] For merged findings: I noted ALL source phases (CC for both merges)
- [x] My final IDs use sequential numbering: MF-001 through MF-025, SF-001 through SF-058, NT-001 through NT-031
- [x] My deduplication log has an entry for EVERY final finding (including unique ones)
- [x] Each dedup log entry includes the merge reasoning
- [x] My verdict follows the rules: MUST-FIX>0 -> REQUEST CHANGES; 25 must-fix issues found
- [x] Math check: 118 raw - 2 invalid - 2 duplicates = 114 unique findings (25 + 58 + 31 = 114)
- [x] The source reports section is copied from the intermediate report
- [x] My return message to the orchestrator is exactly 1-2 lines (no code blocks, no verbose output)

## Source Reports

- `epcp-correctness-P5-3f94a374-3aa9-4cfb-9ef4-23f7b448906c.md`
- `epcp-correctness-P5-49bbb7ff-62db-4c4b-b71d-de8f7f6bb772.md`
- `epcp-correctness-P5-6c2565dd-5da0-477e-8b87-6190e8270403.md`
- `epcp-correctness-P5-95d3ae7b-07e6-4eef-8223-205835b28305.md`
- `epcp-correctness-P5-bcd48980-0bdd-4634-86fa-ae9e702ead6e.md`
- `epcp-correctness-P5-c740d838-4df0-4c2f-b837-2ecf81c9b94f.md`
- `epcp-correctness-P5-d47d0772-90c1-45bc-a972-3fbc8f09d84d.md`
- `epcp-correctness-P5-ea27aa6a-a6b4-4412-863c-c19ea713ca80.md`
- `epcp-claims-P5-e78553ca-bf52-4fba-b560-da5a24ad2307.md`
- `epcp-review-P5-560f7159-44ba-4c6a-8658-8141714272e3.md`

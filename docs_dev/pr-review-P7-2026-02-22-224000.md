# EPCP Final PR Review Report

**Generated:** 2026-02-22T22:40:00
**Pass:** 7
**Pipeline:** Code Correctness -> Claim Verification -> Skeptical Review
**Dedup:** 23 raw findings -> 17 unique findings (6 removed: 4 duplicates merged, 2 retracted)

---

## Summary

| Severity | Count |
|----------|-------|
| **MUST-FIX** | 3 |
| **SHOULD-FIX** | 8 |
| **NIT** | 6 |
| **Total findings** | 17 |

**Verdict: REQUEST CHANGES -- 3 must-fix issue(s) found.**

---

## MUST-FIX Issues

### [MF-001] Missing `await` on async `updateAgentById` in PATCH /metadata route (headless-router.ts)
**File:** services/headless-router.ts:945
**Original IDs:** CC-P7-A0-001, CV-P7-002 (bottom, line 945 portion), CV-P7-001 (second, line 945 portion)
**Phases:** CC, CV
**Description:** `updateAgentById` is declared `async` and returns `Promise<ServiceResult<...>>`. At line 945 in the headless-router's PATCH `/api/agents/[id]/metadata` handler, the call is made without `await`. This means `result` is a Promise object, not the resolved value. Consequently, `result.error` is always `undefined` (Promise objects don't have an `.error` property), so the handler always falls through to the success branch and sends `{ metadata: undefined }` because `result.data` is also undefined on the Promise.
```typescript
// Line 943-951
{ method: 'PATCH', pattern: /^\/api\/agents\/([^/]+)\/metadata$/, paramNames: ['id'], handler: async (req, res, params) => {
  const metadata = await readJsonBody(req)
  const result = updateAgentById(params.id, { metadata })  // <-- MISSING await
  if (result.error) {
    sendJson(res, result.status, { error: result.error })
  } else {
    sendJson(res, 200, { metadata: result.data?.agent?.metadata })
  }
}},
```
**Fix:** Change line 945 to `const result = await updateAgentById(params.id, { metadata })`
**Also identified by:** CV-P7-002 (skeptical review) and CV-P7-001 (claim verification) both independently flagged this same missing await.

---

### [MF-002] Missing `await` on async `updateAgentById` in DELETE /metadata route (headless-router.ts)
**File:** services/headless-router.ts:953
**Original IDs:** CC-P7-A0-002, CV-P7-002 (bottom, line 953 portion), CV-P7-001 (second, line 953 portion)
**Phases:** CC, CV
**Description:** Same issue as MF-001 but in the DELETE `/api/agents/[id]/metadata` handler. The `updateAgentById` call at line 953 is missing `await`. The result is a Promise, so the handler always reaches the success branch and sends `{ success: true }` even if the underlying update failed.
```typescript
// Line 952-959
{ method: 'DELETE', pattern: /^\/api\/agents\/([^/]+)\/metadata$/, paramNames: ['id'], handler: async (_req, res, params) => {
  const result = updateAgentById(params.id, { metadata: {} })  // <-- MISSING await
  if (result.error) {
    sendJson(res, result.status, { error: result.error })
  } else {
    sendJson(res, 200, { success: true })
  }
}},
```
**Fix:** Change line 953 to `const result = await updateAgentById(params.id, { metadata: {} })`
**Also identified by:** CV-P7-002 and CV-P7-001 both independently flagged this same missing await.

---

### [MF-003] CozoDB injection via incorrect manual escaping in agents-graph-service.ts
**File:** services/agents-graph-service.ts:1033
**Original IDs:** CC-P7-A4-001
**Phases:** CC
**Description:** The `queryCodeGraph` function's `focus-node` action uses manual SQL-style escaping (`nodeId.replace(/'/g, "''")`) instead of the `escapeForCozo()` utility that is imported at line 22 and used correctly ~30 other times in the same file. CozoDB uses backslash escaping (`\'`), not SQL-style double-single-quote (`''`) escaping. The manual escaping also fails to handle backslashes, newlines, carriage returns, and tabs -- all of which `escapeForCozo` handles.

The vulnerable pattern appears at **4 locations**:
- Line 1033: `const escapedNodeId = nodeId.replace(/'/g, "''")`
- Line 1089: `const escapedId = id.replace(/'/g, "''")`
- Line 1100: `const escapedId = id.replace(/'/g, "''")`
- Line 1111: `const escapedId = id.replace(/'/g, "''")`

These escaped values are then interpolated into CozoDB queries with manual quote wrapping (e.g., `'${escapedNodeId}'`), creating an injection vector. For example, a `nodeId` containing a backslash followed by a single quote (`\'`) would pass through the manual escaping unchanged (it only doubles lone `'`), but in CozoDB the `\` would escape the closing quote, breaking out of the string literal.

The file even contains a comment at line 66-67 acknowledging this was supposed to be fixed:
```
// MF-009: Local escapeString removed -- use escapeForCozo from @/lib/cozo-utils
// escapeForCozo uses backslash escaping (correct for CozoDB) and wraps in quotes
```
But the `focus-node` action (lines 1013-1127) was missed during that fix.

**Fix:** Replace all 4 occurrences of manual escaping with `escapeForCozo()`:
- Line 1033: Replace `const escapedNodeId = nodeId.replace(/'/g, "''")` and all subsequent `'${escapedNodeId}'` with `${escapeForCozo(nodeId)}`
- Lines 1089, 1100, 1111: Same pattern. Note: `escapeForCozo` returns the value already wrapped in quotes, so the surrounding `'...'` must also be removed from the query strings.

---

## SHOULD-FIX Issues

### [SF-001] AgentProfileTab handleSave does not reset saving state on non-OK response
**File:** components/zoom/AgentProfileTab.tsx:169-176
**Original IDs:** CC-P7-A7-001
**Phases:** CC
**Description:** When the save API returns a non-OK response, the component does not call `setSaving(false)`. It only sets it to false after a 500ms delay on the success path. If the response is not OK and not a network error (caught by the catch block), `saving` remains `true` forever, leaving the Save button in a stuck spinner state.
```typescript
// lines 169-177
if (response.ok) {
  setHasChanges(false)
  setTimeout(() => setSaving(false), 500)
}
// NO else branch to setSaving(false) on non-OK response
```
**Fix:** Add an `else` branch that calls `setSaving(false)` on non-OK responses, matching the pattern already used in the sibling `AgentProfile.tsx` (annotated CC-P1-702 fix).

---

### [SF-002] AgentProfileTab repositories list uses array index as React key
**File:** components/zoom/AgentProfileTab.tsx:610-612
**Original IDs:** CC-P7-A7-002
**Phases:** CC
**Description:** The repositories list uses `key={idx}` (array index) as the React key for repository items. This can cause incorrect rendering when repositories are added, removed, or reordered. The sibling `AgentProfile.tsx` already uses a stable key (`repo.remoteUrl || repo.localPath || idx` with annotation SF-019), but this file was not updated.
**Fix:** Change `key={idx}` to `key={repo.remoteUrl || repo.localPath || idx}` to match the pattern in AgentProfile.tsx.

---

### [SF-003] TerminalView localStorage reads not wrapped in try/catch
**File:** components/TerminalView.tsx:507-526
**Original IDs:** CC-P7-A7-003
**Phases:** CC
**Description:** Multiple `localStorage.getItem` calls in TerminalView's useEffect hooks (lines 507-526) are not wrapped in try/catch, while the `notesCollapsed` initializer at line 57 IS wrapped (annotated SF-018). In private browsing or when localStorage is full, these unprotected reads will throw and crash the component during mount. The `localStorage.setItem` calls at lines 531-565 are also unprotected.
**Fix:** Wrap all `localStorage.getItem` and `localStorage.setItem` calls in try/catch blocks, consistent with the SF-018 pattern already used in the same file.

---

### [SF-004] Outer catch in plugin-builder routes misattributes service errors as "Invalid request body" (400)
**File:** app/api/plugin-builder/build/route.ts:13-31
**Original IDs:** CC-P7-A3-001
**Phases:** CC
**Description:** The `POST` handler wraps both `request.json()` and `buildPlugin(body)` in a single try/catch. If `buildPlugin` throws, the outer catch returns `{ error: 'Invalid request body' }` with status 400, which is misleading. The same pattern exists in `scan-repo/route.ts` and `push/route.ts`.
**Fix:** Separate the JSON parsing try/catch from the service call.

---

### [SF-005] PTY process may be accessed after cleanup in WebSocket message handler
**File:** server.mjs:1082-1097
**Original IDs:** CC-P7-A9-001
**Phases:** CC
**Description:** When a client sends a `resize` or raw input message, the handler accesses `sessionState.ptyProcess` without checking if the session has been cleaned up. If the PTY process exited between when the message was queued and when it's processed, `sessionState.ptyProcess.resize()` or `.write()` will throw. While the outer try-catch prevents a crash, the error message is misleading.
**Fix:** Add a guard: `if (sessionState.cleanedUp || !sessionState.ptyProcess) { return }` before accessing `ptyProcess`.

---

### [SF-006] saveTeams atomic write uses shared tmp filename without process.pid
**File:** lib/team-registry.ts:247
**Original IDs:** CC-P7-A5-001
**Phases:** CC
**Description:** `saveTeams()` uses `TEAMS_FILE + '.tmp'` as a fixed temp file path, unlike every other registry in the codebase which uses `+ '.tmp.' + process.pid`. If two concurrent callers invoke `saveTeams()` simultaneously, data loss can occur. `saveTeams` is also called WITHOUT a lock in the `loadTeams` migration path (line 233).
**Fix:** Change `TEAMS_FILE + '.tmp'` to `` TEAMS_FILE + `.tmp.${process.pid}` `` to match the codebase pattern.

---

### [SF-007] transfer-registry.ts atomic write uses shared tmp filename without process.pid
**File:** lib/transfer-registry.ts:60
**Original IDs:** CC-P7-A5-002
**Phases:** CC
**Description:** `saveTransfers()` uses `` `${TRANSFERS_FILE}.tmp` `` (no process.pid suffix), same issue as SF-006. Although `saveTransfers` is always called inside `withLock('transfers')`, the fixed temp filename is inconsistent with the codebase pattern and could cause issues if refactored.
**Fix:** Change to `` `${TRANSFERS_FILE}.tmp.${process.pid}` `` for consistency.

---

### [SF-008] Shared uuidCounter between makeDoc helper and uuid mock creates fragile test coupling
**File:** tests/document-registry.test.ts:33-38, 78-90
**Original IDs:** CC-P7-A8-001
**Phases:** CC
**Description:** The `makeDoc()` helper function uses `++uuidCounter` to generate document IDs, but this same `uuidCounter` variable is also used by the `uuid.v4()` mock. Both increment the same counter. Currently no test asserts on `makeDoc`-generated IDs after calling `createDocument`, so this doesn't cause failures -- but it makes the test infrastructure brittle for anyone adding new tests.
**Fix:** Give `makeDoc` its own separate counter (e.g. `let makeDocCounter = 0`), or use a fixed prefix that does not collide with `uuid-N` IDs.

---

## Nits & Suggestions

### [NT-001] Test description says "17 scenarios" but file has 27 tests
**File:** tests/message-filter.test.ts:69
**Original IDs:** CC-P7-A8-002
**Phases:** CC
**Description:** The comment at line 69 says `// Tests -- 17 scenarios covering all branches of checkMessageAllowed` but the file actually contains 27 test cases (17 Layer 1 + 10 Layer 2). The comment was not updated when Layer 2 tests were added.
**Fix:** Update the comment to say "27 scenarios" or remove the count.

---

### [NT-002] Non-null assertion on `result.data!` in AMP v1 routes
**File:** app/api/v1/health/route.ts:19 (and 6 other locations)
**Original IDs:** CC-P7-A3-002
**Phases:** CC
**Description:** Seven locations use `result.data!` non-null assertion after an `if (result.error)` early return. While runtime behavior is correct, the TypeScript compiler cannot narrow `data` from `T | undefined` to `T` after an `error` check because `ServiceResult<T>` uses optional fields. This is a known design limitation -- fixing it requires switching to a discriminated union `ServiceResult` type (codebase-wide refactor).
**Fix:** No immediate fix; would be resolved by a discriminated union `ServiceResult<T>` type refactor.

---

### [NT-003] Box-drawing banner width mismatch in install-messaging.sh
**File:** install-messaging.sh:65-71
**Original IDs:** CC-P7-A9-002
**Phases:** CC
**Description:** The ASCII box-drawing banner has inconsistent column widths. The top/bottom borders are 62 characters wide, but the middle lines use 64-character padding, causing visual misalignment in some terminal emulators.
**Fix:** Align border widths consistently.

---

### [NT-004] amp-auth.ts createApiKey body not indented under withLock
**File:** lib/amp-auth.ts:162-183
**Original IDs:** CC-P7-A5-003
**Phases:** CC
**Description:** The body of `createApiKey()` has the code inside `withLock()` not indented to the lock callback level. Lines 163-182 are at the same indentation as the `return withLock(...)` line, making it visually unclear that all logic runs under the lock. Functionally correct; cosmetic only.
**Fix:** Indent the body of the `withLock` callback by one additional level.

---

### [NT-005] TerminalView uses deprecated document.execCommand('copy')
**File:** components/TerminalView.tsx:161
**Original IDs:** CC-P7-A7-004
**Phases:** CC
**Description:** The `handleTerminalCopy` function has a fallback path that uses `document.execCommand('copy')`, which is deprecated by the Web API standard. Serves as a fallback for contexts where Clipboard API fails.
**Fix:** No immediate fix needed. Document for future removal.

---

### [NT-006] RepoScanner unmount does not abort in-flight scan
**File:** components/plugin-builder/RepoScanner.tsx:19
**Original IDs:** CC-P7-A7-005
**Phases:** CC
**Description:** The `RepoScanner` component stores an `AbortController` in a ref and aborts previous scans when starting a new one, but does not abort on component unmount. If the component unmounts during a scan, the fetch resolves and calls state setters on an unmounted component.
**Fix:** Add a cleanup effect: `useEffect(() => { return () => { abortRef.current?.abort() } }, [])`.

---

## Deduplication Log

| Final ID | Original IDs | Duplicates Removed | Reason |
|----------|-------------|-------------------|--------|
| MF-001 | CC-P7-A0-001, CV-P7-002 (line 945), CV-P7-001-second (line 945) | 2 | Same file+line (headless-router.ts:945), same missing await on updateAgentById. CC found independently, CV confirmed same issue via claim verification. |
| MF-002 | CC-P7-A0-002, CV-P7-002 (line 953), CV-P7-001-second (line 953) | 2 | Same file+line (headless-router.ts:953), same missing await on updateAgentById. CC found independently, CV confirmed same issue via claim verification. |
| MF-003 | CC-P7-A4-001 | 0 | Unique finding |
| SF-001 | CC-P7-A7-001 | 0 | Unique finding |
| SF-002 | CC-P7-A7-002 | 0 | Unique finding |
| SF-003 | CC-P7-A7-003 | 0 | Unique finding |
| SF-004 | CC-P7-A3-001 | 0 | Unique finding |
| SF-005 | CC-P7-A9-001 | 0 | Unique finding |
| SF-006 | CC-P7-A5-001 | 0 | Unique finding |
| SF-007 | CC-P7-A5-002 | 0 | Unique finding |
| SF-008 | CC-P7-A8-001 | 0 | Unique finding |
| NT-001 | CC-P7-A8-002 | 0 | Unique finding |
| NT-002 | CC-P7-A3-002 | 0 | Unique finding |
| NT-003 | CC-P7-A9-002 | 0 | Unique finding |
| NT-004 | CC-P7-A5-003 | 0 | Unique finding |
| NT-005 | CC-P7-A7-004 | 0 | Unique finding |
| NT-006 | CC-P7-A7-005 | 0 | Unique finding |

**Retracted findings (excluded from counts):**
- CC-P7-A0-001 (second, SHOULD-FIX, "parseFloat || undefined") -- Author explicitly retracted after re-reading code: "This finding is withdrawn after verification."
- CC-P7-A0-003 (SHOULD-FIX, "getMetrics sync") -- Author explicitly marked as "NIT (not a bug)" and "No action needed -- noting for completeness."

**Informational findings (excluded from counts):**
- CV-P7-001 (CHANGELOG "169 tests" claim) -- Documentation inaccuracy only; actual count exceeds claim (196 > 169). Low impact.
- CV-P7-002 (dual-manager claim) -- CHANGELOG claim is technically accurate per its wording. Non-implemented types are forward-looking placeholders. Low impact.

**Raw count breakdown:**
- 23 substantive raw findings parsed
- 4 duplicates merged (2 CV findings merged into MF-001, 2 CV findings merged into MF-002)
- 2 retracted by their own authors
- 17 unique findings remain

---

## Self-Verification

- [x] I parsed ALL findings from the intermediate report (none skipped or missed)
- [x] I grouped findings by file path before comparing
- [x] For each potential duplicate pair, I checked ALL THREE conditions:
      (1) same file path + (2) overlapping lines within 5 + (3) same root cause
- [x] I did NOT merge findings at the same line that describe DIFFERENT bugs
- [x] I did NOT merge findings from different files even if they describe the same pattern
- [x] For merged findings: I kept the HIGHEST severity (MUST-FIX from CC preserved over SHOULD-FIX from CV)
- [x] For merged findings: I preserved ALL original IDs in "Also identified by" annotation
- [x] For merged findings: I noted ALL source phases (CC, CV)
- [x] My final IDs use sequential numbering: MF-001..003, SF-001..008, NT-001..006
- [x] My deduplication log has an entry for EVERY final finding (including unique ones)
- [x] Each dedup log entry includes the merge reasoning
- [x] My verdict follows the rules: MUST-FIX>0 -> REQUEST CHANGES
- [x] Math check: 17 = 23 - 6 (4 duplicates + 2 retracted)
- [x] The source reports section is included below
- [x] My return message to the orchestrator is exactly 1-2 lines

---

## Source Reports

- `epcp-correctness-P7-2305aa91-9631-440f-82e1-17f8f9b44357.md`
- `epcp-correctness-P7-268dc241-STALE.md`
- `epcp-correctness-P7-3930fff6-9cdb-4ff9-8d42-b125b1ef4e05.md`
- `epcp-correctness-P7-752c6665-cfc2-436e-b291-09d815880b96.md`
- `epcp-correctness-P7-7ab743ca-9342-4c70-9b43-c456b4898f97.md`
- `epcp-correctness-P7-7ce9af35-81f0-47fc-b7be-4f45ed44a4e6.md`
- `epcp-correctness-P7-8c33016b-be54-42b0-9915-d3a950bab861.md`
- `epcp-correctness-P7-b3559652-053c-4427-89a8-5461446b3d5d.md`
- `epcp-correctness-P7-d9ea0473-5d18-4c24-b8ed-cb623bda65a3.md`
- `epcp-correctness-P7-eefd7535-7766-439e-b5fe-19348b575dbd.md`
- `epcp-correctness-P7-f660f3f4-e347-4dad-a92e-3df11c8ab80d.md`
- `epcp-claims-P7-55ff3976-eff0-433e-b47a-2013f4a67bf8.md`
- `epcp-claims-P7-6648438e-b892-460b-80ed-c82855a46b5e.md`
- `epcp-review-P7-70ecbca4-1565-48ff-bf1c-b4e852896722.md`
- `epcp-review-P7-b4183ea6-a20c-44da-9f37-c457bc154667.md`

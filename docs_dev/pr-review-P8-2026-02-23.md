# EPCP Final PR Review Report

**Generated:** 2026-02-23T03:15:00Z
**Pass:** 8
**Pipeline:** Code Correctness -> Claim Verification -> Skeptical Review
**Dedup:** 109 raw findings -> 102 unique findings (7 removed: 3 retracted/resolved, 4 duplicates merged)

---

## Summary

| Severity | Count |
|----------|-------|
| **MUST-FIX** | 8 |
| **SHOULD-FIX** | 54 |
| **NIT** | 40 |
| **Total findings** | 102 |

**Verdict: REQUEST CHANGES -- 8 must-fix issue(s) found.**

---

## MUST-FIX Issues

### [MF-001] SSRF allowlist only checks hostname, not port -- can reach internal services on known hosts
**File:** app/api/hosts/health/route.ts:26-46
**Original IDs:** CC-P8-A3-001
**Phases:** CC
The SSRF allowlist validates that the requested hostname matches a known host from `hosts.json`, but it does not validate the port. If a known host (e.g., `worker1.example.com`) is registered with URL `http://worker1.example.com:23000`, an attacker can craft a request to `http://worker1.example.com:6379` (Redis) or `http://worker1.example.com:5432` (PostgreSQL) and it will pass the allowlist check, allowing SSRF to arbitrary ports on known hosts.

**Evidence:**
```typescript
// Line 27-31: Only hostname is compared, port is ignored
const requestHostname = parsed.hostname.toLowerCase()
const isKnownHost = knownHosts.some(host => {
  try {
    const hostParsed = new URL(host.url)
    if (hostParsed.hostname.toLowerCase() === requestHostname) return true
  } catch { /* skip malformed host URLs */ }
```

**Fix:** Compare origin (protocol + hostname + port) instead of just hostname:
```typescript
if (hostParsed.origin.toLowerCase() === parsed.origin.toLowerCase()) return true
```

### [MF-002] Missing try-catch in agents/[id]/route.ts -- unhandled service throws crash the endpoint
**File:** app/api/agents/[id]/route.ts:10-76
**Original IDs:** CC-P8-A0-001
**Phases:** CC
The GET, PATCH, and DELETE handlers in this file have no outer try-catch. If `getAgentById()`, `updateAgentById()`, or `deleteAgentById()` throw an unexpected error (rather than returning a result with `.error`), the exception propagates uncaught to Next.js, resulting in a generic 500 with no structured JSON response. Other routes in this codebase (e.g., `chat/route.ts`, `docs/route.ts`, `export/route.ts`, `repos/route.ts`) consistently wrap their handlers in try-catch. This file is the primary CRUD endpoint for agents, making it high-traffic and high-impact.

**Fix:** Wrap each handler (GET, PATCH, DELETE) in a try-catch that returns a structured JSON 500 error, consistent with the pattern used in other routes.

### [MF-003] Missing try-catch in multiple graph/memory/tracking routes -- service throws go unhandled
**File:** Multiple files (20+ route files)
**Original IDs:** CC-P8-A0-002
**Phases:** CC
Several route files have handlers that lack outer try-catch blocks. If the delegated service function throws (e.g., database connection failure, file system error), the exception goes unhandled. Affected files include: `database/route.ts`, `graph/code/route.ts`, `graph/db/route.ts`, `graph/query/route.ts`, `hibernate/route.ts`, `index-delta/route.ts`, `memory/route.ts`, `memory/consolidate/route.ts`, `memory/long-term/route.ts`, `messages/route.ts`, `messages/[messageId]/route.ts`, `metrics/route.ts`, `search/route.ts`, `session/route.ts`, `tracking/route.ts`, `wake/route.ts`, `amp/addresses/[address]/route.ts`, `email/addresses/route.ts`, `email/addresses/[address]/route.ts`, `by-name/[name]/route.ts`, `unified/route.ts`.

**Fix:** Add outer try-catch to each handler that returns `{ error: 'Internal server error' }` with status 500. The routes that DO have try-catch (chat, docs, export, playback, repos, skills, subconscious, transfer, health, register, docker, import, metadata) demonstrate the intended pattern.

### [MF-004] Approve/reject routes return `{ error: undefined }` body on error paths instead of proper error object
**File:** app/api/v1/governance/requests/[id]/approve/route.ts:38, reject/route.ts:72,86
**Original IDs:** CC-P8-A1-001
**Phases:** CC
The approve and reject routes use `result.data ?? { error: result.error }` (nullish coalescing). This is fragile: if `result.data` is set AND `result.error` is also set (possible per the `ServiceResult<T>` type), the error is silently discarded. The governance trust routes correctly use explicit `if (result.error)` branching.

**Fix:** Replace the `??` pattern with explicit error branching:
```typescript
if (result.error) {
  return NextResponse.json({ error: result.error }, { status: result.status })
}
return NextResponse.json(result.data, { status: result.status })
```

### [MF-005] Missing error handling when `agentRegistry.getAgent()` throws in docs service
**File:** services/agents-docs-service.ts:60-61, 176-177, 227-228
**Original IDs:** CC-P8-A4-001
**Phases:** CC
`queryDocs` calls `agentRegistry.getAgent(agentId)` and immediately calls `.getDatabase()` on the result without checking if the agent exists. If `agentRegistry.getAgent()` throws (invalid/unknown ID), the error propagates as an unhandled exception with no 404 response. The same pattern exists in `indexDocs` (lines 176-177) and `clearDocs` (lines 227-228).

**Fix:** Add null check: `if (!agent) return { error: 'Agent not found', status: 404 }` before calling `getDatabase()`.

### [MF-006] Missing error handling when `agentRegistry.getAgent()` throws in `getConversationMessages`
**File:** services/config-service.ts:779-780
**Original IDs:** CC-P8-A4-002
**Phases:** CC
Same pattern as MF-005. `getConversationMessages` calls `agentRegistry.getAgent(agentId)` then immediately calls `.getDatabase()` without null-checking. While this is within a try/catch, the error message would be cryptic (e.g., "Cannot read properties of null") rather than a clean 404.

**Fix:** Add `if (!agent) return { error: 'Agent not found', status: 404 }` between lines 779 and 780.

### [MF-007] Potential CozoScript injection via unvalidated `limit` in docs list query
**File:** services/agents-docs-service.ts:121
**Original IDs:** CC-P8-A4-003
**Phases:** CC
The `listLimit` variable is interpolated directly into a CozoScript query string without escaping or type validation. While `limit` defaults to 10 and is typed as `number` in the interface, JavaScript allows passing strings that coerce. If a caller passes a string like `"10; :rm documents"`, it could be injected into the CozoScript.

**Evidence:**
```typescript
query += ` :order -updated_at :limit ${listLimit}`
```

**Fix:** Force integer coercion: `const safeLimit = Math.max(1, Math.min(1000, Math.floor(Number(listLimit) || 50)))` before interpolation.

### [MF-008] Claim: "ToxicSkills scan" not implemented despite commit message claiming it
**File:** services/agents-config-deploy-service.ts:162-165
**Original IDs:** CV-P8-001
**Phases:** CV
Commit 9e4e2cf Step 11 claims "ToxicSkills scan" as an edge case handled. The file contains a TODO comment: `// TODO(Phase 2): ToxicSkills scan not yet implemented. Deployed skills are NOT scanned for malicious content.` The file `lib/toxic-skills.ts` does not exist. No scan is performed. Skills deployed via governance config are NOT scanned for malicious content.

**Fix:** Either implement the ToxicSkills scan or correct the commit message/CHANGELOG to not claim this feature.

---

## SHOULD-FIX Issues

### [SF-001] task-registry.test.ts: makeTaskCounter not reset in beforeEach
**File:** tests/task-registry.test.ts:107-111
**Original IDs:** CC-P8-A8-001
**Phases:** CC
The `makeTaskCounter` variable starts at 1000 and is never reset in `beforeEach`, creating inconsistency with `makeDocCounter` reset in document-registry.test.ts and `makeTeamCounter` reset in team-registry.test.ts.

**Fix:** Add `makeTaskCounter = 1000` to the `beforeEach` block.

### [SF-002] use-governance-hook.test.ts: Testing standalone replicas instead of actual hook code
**File:** tests/use-governance-hook.test.ts:26-35
**Original IDs:** CC-P8-A8-002
**Phases:** CC
The test file tests standalone function replicas rather than the actual `useGovernance` hook. If the hook's implementation drifts from these replicas, tests will still pass while real code is broken.

**Fix:** Add `@testing-library/react` to test the real hook, or add a CI check that flags when `hooks/useGovernance.ts` changes without updating this test file.

### [SF-003] document-registry.test.ts: makeDoc helper does not match TeamDocument type for optional fields
**File:** tests/document-registry.test.ts:79-91
**Original IDs:** CC-P8-A8-003
**Phases:** CC
The `makeDoc` helper sets `pinned: false` and `tags: []` as defaults, but the `TeamDocument` type defines these as optional. Tests never exercise the code path where these fields are `undefined`.

**Fix:** Add a test case using `makeDoc({ pinned: undefined, tags: undefined })` to verify round-trip behavior.

### [SF-004] fixtures.ts: makeTask missing assigneeAgentId default
**File:** tests/test-utils/fixtures.ts:114-126
**Original IDs:** CC-P8-A8-004
**Phases:** CC
The `makeTask` factory does not set `assigneeAgentId`, resulting in `undefined` instead of `null`. Production code (`createTask` in task-registry.ts line 123) uses `data.assigneeAgentId ?? null`, so `undefined !== null` could cause inconsistencies.

**Fix:** Add `assigneeAgentId: null` to the fixtures.ts `makeTask` factory.

### [SF-005] blockedBy array elements not validated as strings at task create route level
**File:** app/api/teams/[id]/tasks/route.ts:65-73
**Original IDs:** CC-P8-A2-001
**Phases:** CC
The route validates that `body.blockedBy` is an array but does not validate that each element is a string. A client could send `blockedBy: [123, null, {}]`. The service layer does validate, so this is defense-in-depth.

**Fix:** Add element validation: `!body.blockedBy.every((v: unknown) => typeof v === 'string')`.

### [SF-006] blockedBy array elements not validated as strings at task update route level
**File:** app/api/teams/[id]/tasks/[taskId]/route.ts:46-57
**Original IDs:** CC-P8-A2-002
**Phases:** CC
Same issue as SF-005 but in the PUT task update route.

**Fix:** Add element validation matching SF-005 fix.

### [SF-007] String(null) converts null assigneeAgentId to literal "null" string in task create route
**File:** app/api/teams/[id]/tasks/route.ts:72
**Original IDs:** CC-P8-A2-003
**Phases:** CC
If a client sends `assigneeAgentId: null` (valid JSON to unassign), `String(null)` produces `"null"` literal string, which would be stored as an assignee ID.

**Fix:** Handle null explicitly: `body.assigneeAgentId === null ? null : String(body.assigneeAgentId)`.

### [SF-008] String(null) converts null assigneeAgentId to literal "null" string in task update route
**File:** app/api/teams/[id]/tasks/[taskId]/route.ts:56
**Original IDs:** CC-P8-A2-004
**Phases:** CC
Same issue as SF-007 but in the PUT task update route.

**Fix:** Same as SF-007.

### [SF-009] `result.data ?? { error: result.error }` pattern in meetings routes can swallow errors
**File:** app/api/meetings/[id]/route.ts:17,:43,:65
**Original IDs:** CC-P8-A3-002
**Phases:** CC
The meetings GET/PATCH/DELETE handlers use `result.data ?? { error: result.error }`. If the service returns `{ data: {}, error: 'something', status: 500 }`, the response will be `{}` with status 500 -- error message lost.

**Fix:** Use the standard `if (result.error)` guard pattern.

### [SF-010] Same `result.data ?? { error }` pattern in meetings list/create routes
**File:** app/api/meetings/route.ts:11,:29
**Original IDs:** CC-P8-A3-003
**Phases:** CC
Same issue as SF-009 in meetings list (GET) and create (POST) routes.

**Fix:** Use the standard `if (result.error)` guard pattern.

### [SF-011] Same `result.data ?? { error }` pattern in organization POST
**File:** app/api/organization/route.ts:27
**Original IDs:** CC-P8-A3-004
**Phases:** CC
Same issue as SF-009 in the organization POST handler.

**Fix:** Use the standard `if (result.error)` guard pattern.

### [SF-012] Non-null assertion on `result.data!` in multiple AMP v1 routes
**File:** app/api/v1/health/route.ts:19, app/api/v1/info/route.ts:19, app/api/v1/messages/pending/route.ts:28,:43,:63, app/api/v1/register/route.ts:28, app/api/v1/route/route.ts:37
**Original IDs:** CC-P8-A3-005
**Phases:** CC
Multiple AMP v1 routes use `result.data!` (non-null assertion) after checking `result.error`. If the service contract changes to return `{ data: undefined, error: undefined, status: 200 }`, these assertions would pass `undefined` to `NextResponse.json()`.

**Fix:** Use `result.data ?? {}` or add an explicit check.

### [SF-013] hosts/identity GET does not check for error in result
**File:** app/api/hosts/identity/route.ts:13-14
**Original IDs:** CC-P8-A3-006
**Phases:** CC
The `GET /api/hosts/identity` handler calls `getHostIdentity()` and returns `result.data` without checking `result.error`.

**Fix:** Add the standard error guard: `if (result.error) { return NextResponse.json({ error: result.error }, { status: result.status }) }`.

### [SF-014] Inconsistent auth enforcement: meetings GET is unauthenticated but PATCH/DELETE require auth
**File:** app/api/meetings/[id]/route.ts:7-18 vs :22-44
**Original IDs:** CC-P8-A3-007
**Phases:** CC
The `GET /api/meetings/[id]` handler does not require authentication, while `PATCH` and `DELETE` do. The PATCH/DELETE handlers reference "SF-013: Authenticate agent for write operations" suggesting read auth was an oversight.

**Fix:** Add `authenticateAgent` to the GET handlers, or document the rationale for open read access.

### [SF-015] SSRF risk in health proxy -- no URL validation before fetching
**File:** app/api/agents/health/route.ts:21-25
**Original IDs:** CC-P8-A0-003
**Phases:** CC
The `/api/agents/health` POST endpoint accepts a `url` parameter and proxies a fetch to it. It validates the URL is a non-empty string but does not validate the URL scheme (could be `file://`), destination (could target `http://169.254.169.254/`), or format.

**Fix:** Validate URL starts with `http://` or `https://`, optionally block internal IP ranges.

### [SF-016] minScore=0 silently treated as undefined in search route
**File:** app/api/agents/[id]/search/route.ts:37
**Original IDs:** CC-P8-A0-004
**Phases:** CC
`parseFloat('0') || undefined` evaluates to `undefined` because 0 is falsy.

**Fix:** Use `const val = parseFloat(str); return isNaN(val) ? undefined : val`.

### [SF-017] parseInt fallback `|| undefined` silently drops valid value 0 for limit/startTs/endTs in search route
**File:** app/api/agents/[id]/search/route.ts:36,40-41
**Original IDs:** CC-P8-A0-005
**Phases:** CC
Same falsy-zero pattern for `limit`, `startTs`, and `endTs`. `parseInt('0', 10) || undefined` yields `undefined`. For `startTs=0` (Unix epoch), the timestamp is silently dropped.

**Fix:** Use `const val = parseInt(str, 10); return isNaN(val) ? undefined : val`.

### [SF-018] maxConversations=0 silently becomes undefined in consolidate route
**File:** app/api/agents/[id]/memory/consolidate/route.ts:53-54
**Original IDs:** CC-P8-A0-006
**Phases:** CC
Same falsy-zero pattern. `parseInt(..., 10) || undefined` treats `0` as falsy.

**Fix:** Use the NaN-check pattern.

### [SF-019] Unsafe type assertion for `box` query parameter in messages route
**File:** app/api/agents/[id]/messages/[messageId]/route.ts:24
**Original IDs:** CC-P8-A0-007
**Phases:** CC
The `box` parameter is cast to `'inbox' | 'sent'` without validation. A caller could pass `?box=drafts`.

**Fix:** Validate the value against the allowed set before casting.

### [SF-020] Unsafe type assertion for `category` in long-term memory route
**File:** app/api/agents/[id]/memory/long-term/route.ts:38
**Original IDs:** CC-P8-A0-008
**Phases:** CC
The `category` parameter is cast to `MemoryCategory | null` without validation.

**Fix:** Validate against the known MemoryCategory enum values before casting.

### [SF-021] Unsafe type assertion for `tier` in long-term memory route
**File:** app/api/agents/[id]/memory/long-term/route.ts:42
**Original IDs:** CC-P8-A0-009
**Phases:** CC
The `tier` parameter is cast to `'warm' | 'long' | null` without validation.

**Fix:** Validate against the allowed values before casting.

### [SF-022] Unsafe type assertion for `roleFilter` in search route
**File:** app/api/agents/[id]/search/route.ts:38
**Original IDs:** CC-P8-A0-010
**Phases:** CC
The `role` parameter is cast to `'user' | 'assistant' | 'system' | null` without validation.

**Fix:** Validate against the allowed values before casting.

### [SF-023] Inconsistent response format -- some routes use `{ success: false }`, others use `{ error }`
**File:** Multiple files (42 route files)
**Original IDs:** CC-P8-A0-011
**Phases:** CC
No consistent error response format. Some routes return `{ error: '...' }`, others `{ success: false, error: '...' }`. Some mix both patterns within the same file.

**Fix:** Standardize on one error response format across all agent API routes.

### [SF-024] ServiceResult<T> allows simultaneous `data` and `error` -- no discriminated union
**File:** types/service.ts:10-15
**Original IDs:** CC-P8-A6-001
**Phases:** CC
`ServiceResult<T>` has both `data` and `error` as optional fields. TypeScript won't catch callers returning both, or neither.

**Fix:** Refactor to a discriminated union. Cross-cutting change affecting 15+ service files.

### [SF-025] GovernanceConfig version literal `1` not validated at runtime after JSON.parse
**File:** types/governance.ts:28, lib/governance.ts:41
**Original IDs:** CC-P8-A6-002
**Phases:** CC
The `version: 1` literal type is intended for schema migrations, but `JSON.parse()` returns `number`, not literal `1`. If a file has `"version": 2`, TypeScript won't catch it at compile time. Same issue in `TransfersFile`, `GovernanceRequestsFile`, `TeamsFile`, `MeetingsFile`.

**Fix:** Add runtime version check after parsing: `if (parsed.version !== 1) throw new Error(...)`.

### [SF-026] localStorage reads in useState initializers not wrapped in try/catch (footerTab + loggingEnabled)
**File:** components/TerminalView.tsx:72, 78-79
**Original IDs:** CC-P8-A7-001, CC-P8-A7-002
**Phases:** CC
**Also identified by:** CC-P8-A7-002 (same file, adjacent lines, same root cause)
The `footerTab` (line 72) and `loggingEnabled` (line 78) useState initializers call `localStorage.getItem()` without try/catch. The `notesCollapsed` initializer a few lines above at line 57 wraps the identical pattern in try/catch with comment "SF-018". This inconsistency means both initializers will throw in private browsing mode on Safari.

**Fix:** Wrap each `localStorage.getItem` call in try/catch, returning defaults on failure.

### [SF-027] localStorage.setItem calls not wrapped in try/catch (collapsed, logging, footer tab)
**File:** components/TerminalView.tsx:577, 582, 586
**Original IDs:** CC-P8-A7-003
**Phases:** CC
Three useEffect hooks write to localStorage without try/catch. Other write effects in the same file at lines 543-548 and 551-557 DO use try/catch.

**Fix:** Wrap each `localStorage.setItem` call in try/catch.

### [SF-028] N+1 fetch pattern for team task/document counts on teams page
**File:** app/teams/page.tsx:37-51
**Original IDs:** CC-P8-A7-004
**Phases:** CC
The teams page fetches all teams, then for each team performs two additional fetches for counts. Creates 2N+1 HTTP requests. Comment acknowledges this as Phase 1 known limitation.

**Fix:** Implement the Phase 2 `/api/teams/[id]/stats` endpoint.

### [SF-029] Rate limiter in manager route uses non-atomic check-then-record pattern
**File:** app/api/governance/manager/route.ts:24-37
**Original IDs:** CC-P8-A1-002
**Phases:** CC
The manager route uses separate `checkRateLimit` + `recordFailure`, while cross-host governance has migrated to atomic `checkAndRecordAttempt`. The password route has the same issue.

**Fix:** Replace with `checkAndRecordAttempt` + `resetRateLimit`, matching `cross-host-governance-service.ts`.

### [SF-030] GET transfers endpoint does not validate `teamId` or `agentId` query parameters
**File:** app/api/governance/transfers/route.ts:17-18
**Original IDs:** CC-P8-A1-003
**Phases:** CC
The GET handler validates `status` but not `teamId` or `agentId`. Should be validated as UUIDs for consistency.

**Fix:** Add `isValidUuid` checks for both parameters.

### [SF-031] Duplicate password business logic between route and service layer
**File:** services/governance-service.ts:138-139
**Original IDs:** CC-P8-A1-004
**Phases:** CC
Same password-set logic exists in `password/route.ts` (direct route) and `governance-service.ts:setGovernancePassword` (service). Maintenance risk.

**Fix:** Delegate all logic from `password/route.ts` to `governance-service.ts:setGovernancePassword`.

### [SF-032] Missing `password` type validation in reject route's local path
**File:** app/api/v1/governance/requests/[id]/reject/route.ts:76
**Original IDs:** CC-P8-A1-005
**Phases:** CC
The local rejection path checks `body.password` is truthy but does not validate it is a string. Compare to `requests/route.ts:93` which explicitly checks `typeof body.password !== 'string'`.

**Fix:** Add type check: `typeof body.password !== 'string'`.

### [SF-033] Reachable agents cache duplicated between route and service, service cache unbounded
**File:** app/api/governance/reachable/route.ts:12-13, services/governance-service.ts:152, :188-194
**Original IDs:** CC-P8-A1-006, CC-P8-A1-007
**Phases:** CC
**Also identified by:** CC-P8-A1-007 (same cache pair, the unbounded size issue is automatically resolved by consolidating caches)
The reachable route maintains its own in-memory cache while `governance-service.ts` has an independent `reachableCache`. The route cache has a 1000-entry size bound but the service cache does not. Two caches for the same data, one unbounded.

**Fix:** Delegate the route to `governance-service.ts:getReachableAgents` and remove the duplicate cache.

### [SF-034] `validateApiKey` timing side-channel via early-exit find()
**File:** lib/amp-auth.ts:208-216
**Original IDs:** CC-P8-A5-003
**Phases:** CC
While `timingSafeEqual` is used for hash comparison, the `find()` loop exits early on the first match. An attacker can measure response time to determine position. Comment acknowledges this as "Phase 1 acceptable."

**Fix:** Iterate all keys, collect the match, then return it.

### [SF-035] `acquireIndexSlot` has no timeout -- queued items wait forever
**File:** lib/index-delta.ts:44-54
**Original IDs:** CC-P8-A5-004
**Phases:** CC
The `acquireIndexSlot` Promise only resolves when the slot is released. If `releaseIndexSlot` is never called, queued Promises wait indefinitely.

**Fix:** Add a timeout (e.g., 60 seconds), similar to `file-lock.ts:48-75`.

### [SF-036] `getMessageStats` can increment undefined priority keys
**File:** lib/messageQueue.ts:926-928
**Original IDs:** CC-P8-A5-005
**Phases:** CC
`stats.byPriority[m.priority]++` would produce `NaN` if priority is not in the predefined set.

**Fix:** Guard with: `if (stats.byPriority[m.priority] !== undefined) stats.byPriority[m.priority]++`.

### [SF-037] `loadApiKeys` returns cached mutable array -- external mutations corrupt cache
**File:** lib/amp-auth.ts:52-75
**Original IDs:** CC-P8-A5-006
**Phases:** CC
`loadApiKeys()` returns `_apiKeysCache` directly. If `revokeApiKey` mutates an element then `saveApiKeys` fails, the in-memory cache is mutated but disk is not updated.

**Fix:** Return a defensive copy or mutate only after successful save.

### [SF-038] `createTeam` chiefOfStaffId may be set to `undefined` instead of `null`
**File:** lib/team-registry.ts:279-281
**Original IDs:** CC-P8-A5-007
**Phases:** CC
When building `newTeam`, if `chiefOfStaffId` is not provided, it becomes `undefined`. The codebase uses `null` for "no COS".

**Fix:** Default to `null`: `data.chiefOfStaffId ?? null`.

### [SF-039] `broadcastStatusUpdate` does not clean up dead WebSocket subscribers
**File:** services/shared-state-bridge.mjs:43-47
**Original IDs:** CC-P8-A9-002
**Phases:** CC
Dead/closed connections are never removed from the Set during broadcast. Minor memory leak over time.

**Fix:** Add cleanup of non-OPEN sockets in the broadcast loop.

### [SF-040] Companion WebSocket event listener leak when multiple companion clients connect
**File:** server.mjs:659-698
**Original IDs:** CC-P8-A9-003
**Phases:** CC
Each companion client registers a `voice:speak` listener, but cleanup only triggers when the last client disconnects. Earlier clients' listeners are never removed.

**Fix:** Move listener cleanup outside the `size === 0` check so each client removes its own listener on close.

### [SF-041] `ptyProcess.resume()` called even if process has already exited
**File:** server.mjs:1014-1016
**Original IDs:** CC-P8-A9-004
**Phases:** CC
In the PTY `onData` handler, `resume()` is called in `.finally()` but the PTY may have exited during the backpressure cycle.

**Fix:** Wrap `resume()` in try-catch: `try { ptyProcess.resume() } catch { /* PTY already exited */ }`.

### [SF-042] `update-aimaestro.sh` always fetches from `origin/main` even when on a different branch
**File:** update-aimaestro.sh:149-151
**Original IDs:** CC-P8-A9-005
**Phases:** CC
The script hardcodes `git fetch origin main` and `git pull origin main`. If on a feature branch, this pulls `main` into the current branch.

**Fix:** Check that the current branch is `main` before proceeding, or warn the user.

### [SF-043] `bump-version.sh` regex replacement may fail for patterns with special sed characters
**File:** scripts/bump-version.sh:114-116
**Original IDs:** CC-P8-A9-006
**Phases:** CC
The `update_file` function uses bash string substitution for regex and sed delimiter `|`. If replacement contains `|`, sed breaks. Minor fragility.

**Fix:** Validate version strings cannot contain `|`, or use a different delimiter.

### [SF-044] `amp-inbox.sh` base64 decoding not portable across macOS and Linux
**File:** plugin/plugins/ai-maestro/scripts/amp-inbox.sh:132
**Original IDs:** CC-P8-A9-007
**Phases:** CC
Script uses `base64 -d` (GNU convention). On older macOS, `-D` is the correct flag. Modern macOS supports `-d` as alias.

**Fix:** Use portable decode: `base64 -d 2>/dev/null || base64 -D`, or use jq's `@base64d`.

### [SF-045] `parseConversationFile` leaks user-controlled file path in error message
**File:** services/config-service.ts:625
**Original IDs:** CC-P8-A4-004
**Phases:** CC
The error message includes the user-provided `conversationFile` value verbatim, leaking filesystem path structure.

**Fix:** Return a generic error: `error: 'Conversation file not found'`.

### [SF-046] `controlPlayback` uses `parseInt(sessionId, 10)` on a non-numeric string field
**File:** services/agents-playback-service.ts:98
**Original IDs:** CC-P8-A4-005
**Phases:** CC
If `sessionId` is a UUID, `parseInt` returns `NaN`, and the `some()` check never matches, always returning 404.

**Fix:** Clarify whether `sessionId` is an index or ID. If index, validate `parseInt` doesn't produce `NaN`.

### [SF-047] `agents-chat-service` reads entire JSONL file into memory without size limit
**File:** services/agents-chat-service.ts:92
**Original IDs:** CC-P8-A4-006
**Phases:** CC
`getConversationMessages` uses `fs.readFileSync` on conversation files that can grow to 50MB+, potentially causing OOM.

**Fix:** Use async `fs.promises.readFile` or streaming, and enforce a maximum file size check.

### [SF-048] `getConversationMessages` uses synchronous file I/O in async function
**File:** services/agents-chat-service.ts:68-75
**Original IDs:** CC-P8-A4-007
**Phases:** CC
Uses `fs.readdirSync` and `fs.statSync` in an async function, blocking the event loop.

**Fix:** Replace with `fs.promises.readdir` and `fs.promises.stat`.

### [SF-049] `updateExistingMeeting` casts `status` to `any` without validation
**File:** services/messages-service.ts:537
**Original IDs:** CC-P8-A4-008
**Phases:** CC
Meeting status field cast `as any` bypasses type checking entirely.

**Fix:** Validate against allowed values and remove `as any` cast.

### [SF-050] `agents-docker-service` silently swallows agent registry errors
**File:** services/agents-docker-service.ts:238-239
**Original IDs:** CC-P8-A4-009
**Phases:** CC
If agent registry creation fails after Docker container start, the error is logged but success is returned. Creates orphaned container.

**Fix:** Return an error/warning status, or clean up the container on registry failure.

### [SF-051] `agents-docker-service` exposes `githubToken` as Docker environment variable in plain text
**File:** services/agents-docker-service.ts:183-184
**Original IDs:** CC-P8-A4-010
**Phases:** CC
GitHub token passed via `docker run -e GITHUB_TOKEN=...`, visible in `docker inspect` and `/proc/[pid]/environ`.

**Fix:** Use Docker secrets or a mounted `.env` file.

### [SF-052] Claim: "169 new tests" -- actual count is 199 across the 9 listed files
**File:** CHANGELOG.md line 19
**Original IDs:** CV-P8-002
**Phases:** CV
The CHANGELOG claims 169 tests across 9 files, but the actual count is 199. Additionally, a 10th file (agent-config-governance-extended.test.ts with 56 tests) exists but is unlisted. Count is higher than claimed -- CHANGELOG is stale.

**Fix:** Update CHANGELOG to reflect 199 tests across 9 files (or 255 across 10).

### [SF-053] Claim: "agentHostMap" listed as functional feature but is only a type stub
**File:** types/team.ts:30-34, CHANGELOG.md line 16
**Original IDs:** CV-P8-003
**Phases:** CV
The `agentHostMap` field has a `@planned` JSDoc annotation stating "type stub only, not yet populated or consumed anywhere." No code reads or writes it. Flagged in passes P5, P6, P7 with no correction.

**Fix:** Annotate CHANGELOG entry as "@planned" or remove from "Added" section.

### [SF-054] Claim: "56 new tests" -- actual count is 73
**File:** Commit 9e4e2cf Step 10
**Original IDs:** CV-P8-004
**Phases:** CV
Commit claims 56 new tests but agent-config-governance.test.ts (17) + agent-config-governance-extended.test.ts (56) = 73 tests.

**Fix:** Minor discrepancy -- update commit documentation if corrected.

---

## Nits & Suggestions

### [NT-001] agent-auth.test.ts: Unused `beforeEach` import from vitest
**File:** tests/agent-auth.test.ts:1
**Original IDs:** CC-P8-A8-005
**Phases:** CC
Dead import -- no `beforeEach()` call exists in the file.

**Fix:** Remove `beforeEach` from the import destructuring.

### [NT-002] transfer-registry.test.ts uses fake timers but other timestamp tests do not
**File:** tests/transfer-registry.test.ts:78-84
**Original IDs:** CC-P8-A8-006
**Phases:** CC
Inconsistent test style -- this test uses `vi.useFakeTimers()` for deterministic timestamps, others only check `.toBeDefined()`.

**Fix:** No action required. Observation about test style consistency.

### [NT-003] governance-peers.test.ts: Mock pattern uses both named and default fs exports undocumented
**File:** tests/governance-peers.test.ts
**Original IDs:** CC-P8-A8-007
**Phases:** CC
Dual export mock pattern works but is not documented, unlike agent-registry.test.ts's SF-032 comment.

**Fix:** Add a brief comment explaining the dual export pattern.

### [NT-004] team-api.test.ts: vi.spyOn on dynamic import is fragile but correct
**File:** tests/team-api.test.ts:207-225
**Original IDs:** CC-P8-A8-008
**Phases:** CC
Uses `vi.spyOn(await import(...), 'createTeam')`. Valid but fragile if vitest module resolution changes.

**Fix:** No action required. Consider extracting to a reusable utility.

### [NT-005] service-mocks.ts: createSharedStateMock includes potentially unused fields
**File:** tests/test-utils/service-mocks.ts:51-60
**Original IDs:** CC-P8-A8-009
**Phases:** CC
Extra fields (`statusSubscribers`, `terminalSessions`, `companionClients`) may be needed for type compatibility.

**Fix:** Verify if required for TypeScript type compatibility. If so, add a comment.

### [NT-006] Unused requestingAgentId passed to notifyTeamAgents
**File:** app/api/teams/notify/route.ts:22
**Original IDs:** CC-P8-A2-005
**Phases:** CC
`requestingAgentId` is spread into params but `NotifyTeamParams` does not define it.

**Fix:** Remove the spread, or add to `NotifyTeamParams` for audit logging.

### [NT-007] Deprecated `recordFailure` alias used instead of `recordAttempt`
**File:** app/api/teams/[id]/chief-of-staff/route.ts:5,42
**Original IDs:** CC-P8-A2-006
**Phases:** CC
Uses deprecated alias `recordFailure` for `recordAttempt`.

**Fix:** Change import and usage to `recordAttempt`.

### [NT-008] Missing `export const dynamic = 'force-dynamic'` on chief-of-staff, notify routes
**File:** app/api/teams/[id]/chief-of-staff/route.ts, app/api/teams/notify/route.ts
**Original IDs:** CC-P8-A2-007
**Phases:** CC
POST-only routes, so not strictly required. For consistency with project convention.

**Fix:** Consider adding for consistency.

### [NT-009] `path` import used only for `path.isAbsolute` in sessions/create
**File:** app/api/sessions/create/route.ts:2
**Original IDs:** CC-P8-A3-008
**Phases:** CC
Minor note -- `path.isAbsolute` is more correct than `startsWith('/')`.

**Fix:** No change needed.

### [NT-010] Unused import type `NextRequest` in v1/health and v1/info routes
**File:** app/api/v1/health/route.ts:10, app/api/v1/info/route.ts:10
**Original IDs:** CC-P8-A3-009
**Phases:** CC
`NextRequest` imported but `_request` parameter never accessed for Next.js-specific features.

**Fix:** Can simplify to `Request` or remove parameter.

### [NT-011] Deprecation logs on every request in sessions/[id]/* routes
**File:** app/api/sessions/[id]/command/route.ts:10-12, sessions/[id]/rename/route.ts:12-14, sessions/[id]/route.ts:12-14
**Original IDs:** CC-P8-A3-011
**Phases:** CC
`logDeprecation()` called on every request, flooding logs in high-traffic scenarios.

**Fix:** Use a module-level `let warned = false` guard.

### [NT-012] `body` typed as `any` in docs and graph/code POST handlers
**File:** app/api/agents/[id]/docs/route.ts:61, app/api/agents/[id]/graph/code/route.ts:51
**Original IDs:** CC-P8-A0-012
**Phases:** CC
Using `any` suppresses type checking at the route level.

**Fix:** Consider using `Record<string, unknown>` or a specific request type.

### [NT-013] Unused `request` parameter not prefixed with underscore in several handlers
**File:** Multiple files (e.g., database/route.ts:30-31)
**Original IDs:** CC-P8-A0-013
**Phases:** CC
Inconsistent naming convention for unused parameters.

**Fix:** Prefix unused parameters with `_`.

### [NT-014] `import` route uses `result.status || 500` fallback uniquely
**File:** app/api/agents/import/route.ts:34
**Original IDs:** CC-P8-A0-014
**Phases:** CC
Only this route has a fallback for `result.status`. Minor inconsistency.

**Fix:** Standardize across all routes.

### [NT-015] parseSessionName does not guard against empty string input
**File:** types/agent.ts:93-99
**Original IDs:** CC-P8-A6-003
**Phases:** CC
`parseSessionName("")` returns `{ agentName: "", index: 0 }`. Low risk since tmux never produces empty names.

**Fix:** Add runtime guard or document precondition.

### [NT-016] RESTORE_MEETING action passes redundant `teamId` alongside `meeting`
**File:** types/team.ts:112
**Original IDs:** CC-P8-A6-004
**Phases:** CC
The action includes both `meeting: Meeting` (which has `teamId`) and separate `teamId`. Reducer only reads `action.meeting`.

**Fix:** Remove redundant `teamId` from the action type.

### [NT-017] Deprecated `type` field on Host interface lacks runtime migration
**File:** types/host.ts:55-58
**Original IDs:** CC-P8-A6-005
**Phases:** CC
Marked DEPRECATED but no migration strips it from loaded configs.

**Fix:** Schedule removal or add migration code.

### [NT-018] `EmailTool` has deprecated single-address fields without migration plan
**File:** types/agent.ts:350-354
**Original IDs:** CC-P8-A6-006
**Phases:** CC
`address` and `provider` fields marked `@deprecated` with no removal timeline.

**Fix:** Track removal as a backlog task.

### [NT-019] useWebSocket reconnection attempts may not reset on `hostId`/`socketPath` change
**File:** hooks/useWebSocket.ts:203-214
**Original IDs:** CC-P8-A7-005
**Phases:** CC
The useEffect deps exclude `hostId` and `socketPath` (eslint-disable). In practice these rarely change without `sessionId` also changing.

**Fix:** Consider adding to dependency array, or document exclusion.

### [NT-020] SkillPicker passes no-op `onSkillsFound` to RepoScanner
**File:** components/plugin-builder/SkillPicker.tsx:255
**Original IDs:** CC-P8-A7-006
**Phases:** CC
The callback result is ignored.

**Fix:** Make `onSkillsFound` optional in `RepoScannerProps`.

### [NT-021] AgentSkillEditor `canApprove` hardcoded to `true`
**File:** components/marketplace/AgentSkillEditor.tsx:82
**Original IDs:** CC-P8-A7-007
**Phases:** CC
Phase 1 localhost bypass. Phase 2 TODO to wire to governance role checks.

**Fix:** Phase 2: Wire to actual role checks.

### [NT-022] password/route.ts: combined validation condition is hard to read
**File:** app/api/governance/password/route.ts:13
**Original IDs:** CC-P8-A1-008
**Phases:** CC
The `!password || typeof password !== 'string' || password.length < 6` logic is correct but dense.

**Fix:** No code change needed. Consider splitting for readability.

### [NT-023] Missing `dynamic = 'force-dynamic'` on manager and password routes
**File:** app/api/governance/manager/route.ts, app/api/governance/password/route.ts
**Original IDs:** CC-P8-A1-009
**Phases:** CC
POST-only routes, so low risk. For consistency with other governance routes.

**Fix:** Add `export const dynamic = 'force-dynamic'` for consistency.

### [NT-024] Inconsistent error response formatting for agentId between service and route
**File:** services/governance-service.ts:95 vs app/api/governance/manager/route.ts:53
**Original IDs:** CC-P8-A1-010
**Phases:** CC
One says `Agent ${agentId} not found` (no quotes), other says `Agent '${agentId}' not found` (with quotes).

**Fix:** Unify error message format.

### [NT-025] Sync route POST does not validate `body.type` against known sync message types
**File:** app/api/v1/governance/sync/route.ts:24
**Original IDs:** CC-P8-A1-011
**Phases:** CC
Validates `body.type` is present but not that it is a known type.

**Fix:** Add validation against known sync message types.

### [NT-026] `fromVerified` double nullish coalescing is misleading dead code
**File:** lib/messageQueue.ts:267
**Original IDs:** CC-P8-A5-002, CC-P8-A5-008
**Phases:** CC
**Also identified by:** CC-P8-A5-008 (same finding, same line, same issue)
The trailing `?? false` is unreachable because `Boolean()` always returns a boolean.

**Fix:** Remove `?? false`.

### [NT-027] Notification service typing into non-shell programs is a known design limitation
**File:** lib/notification-service.ts:52-59
**Original IDs:** CC-P8-A5-001, CC-P8-A5-009
**Phases:** CC
**Also identified by:** CC-P8-A5-009 (same finding, same line, same issue)
`sendTmuxNotification` sends echo as literal keystrokes. If target pane runs vim/REPL, keystrokes are injected as input. Code comments acknowledge this.

**Fix:** Consider using tmux `display-message` instead of `send-keys`.

### [NT-028] `_agentCacheSweepInterval` exported only for test cleanup
**File:** lib/messageQueue.ts:496-516
**Original IDs:** CC-P8-A5-010
**Phases:** CC
Module API cluttered by test-only export. TODO in code to wire into shutdown handler.

**Fix:** Wire into graceful shutdown handler, or keep for test use.

### [NT-029] `loadGovernance` returns defaults on ANY read error, not just corruption
**File:** lib/governance.ts:43-59
**Original IDs:** CC-P8-A5-011
**Phases:** CC
EACCES permission denied is silently masked. Acceptable for Phase 1.

**Fix:** Phase 2: Distinguish ENOENT from other errors.

### [NT-030] Missing file reference: `lib/tmux-discovery.ts` does not exist
**File:** lib/tmux-discovery.ts
**Original IDs:** CC-P8-A5-012
**Phases:** CC
Stale reference in audit file list. Functionality lives in `lib/agent-runtime.ts`.

**Fix:** Remove from domain file list.

### [NT-031] `server.mjs` imports `.ts` files directly, relying on runtime transpilation
**File:** server.mjs:583,651,684,856,872,1045,1224
**Original IDs:** CC-P8-A9-008
**Phases:** CC
Hidden dependency on tsx/Next.js transpiler. Would fail with plain `node`.

**Fix:** Document as a constraint.

### [NT-032] `start-with-ssh.sh` uses `exec tsx` without checking for tsx existence
**File:** scripts/start-with-ssh.sh:30
**Original IDs:** CC-P8-A9-009
**Phases:** CC
If `tsx` is not installed, cryptic "command not found" error after SSH setup.

**Fix:** Add: `command -v tsx &>/dev/null || { echo "Error: tsx not found"; exit 1; }`.

### [NT-033] `install-messaging.sh` uses `local` keyword inside while loop body
**File:** install-messaging.sh:273-274
**Original IDs:** CC-P8-A9-010
**Phases:** CC
Re-declares `local` variables on each iteration. Harmless but unconventional.

**Fix:** Move `local` declarations to top of function.

### [NT-034] `server.mjs` status WebSocket initial fetch uses loopback HTTP
**File:** server.mjs:600
**Original IDs:** CC-P8-A9-011
**Phases:** CC
Creates loopback HTTP request to self. Unnecessary overhead, can fail during startup.

**Fix:** Call the handler function directly.

### [NT-035] `remote-install.sh` cleanup trap removes partial installation silently in non-interactive mode
**File:** scripts/remote-install.sh:166-169
**Original IDs:** CC-P8-A9-012
**Phases:** CC
Existing guards are adequate. Documentation note.

**Fix:** Existing guards sufficient.

### [NT-036] Extensive use of `ServiceResult<any>` throughout service files
**File:** Multiple files (agents-docs-service.ts, agents-memory-service.ts, etc.)
**Original IDs:** CC-P8-A4-011
**Phases:** CC
Reduces TypeScript's ability to catch type errors. Known TODO (NT-031 in code).

**Fix:** Define specific result interfaces.

### [NT-037] `agents-directory-service.ts` returns status 500 with `data` instead of `error` on failure
**File:** services/agents-directory-service.ts:82
**Original IDs:** CC-P8-A4-012
**Phases:** CC
Inconsistent with other service methods that use `{ error: '...', status: 500 }`.

**Fix:** Return `{ error: 'Failed to lookup agent', status: 500 }`.

### [NT-038] `parseConversationFile` logs every line parse error at ERROR level
**File:** services/config-service.ts:730-731
**Original IDs:** CC-P8-A4-013
**Phases:** CC
Malformed JSONL lines are common. Logging each at ERROR creates noise. Also logs raw line content (sensitive).

**Fix:** Use `console.warn` or count errors and log summary. Remove raw line content.

### [NT-039] `shared-state-bridge.mjs` duplicates logic from `shared-state.ts`
**File:** services/shared-state-bridge.mjs:1-48
**Original IDs:** CC-P8-A4-014
**Phases:** CC
Intentional ESM bridge but lacks TypeScript type safety. Divergence risk.

**Fix:** Add documentation noting both files must stay in sync.

### [NT-040] `agents-playback-service.ts` uses `isNaN(value)` instead of `Number.isNaN(value)`
**File:** services/agents-playback-service.ts:94
**Original IDs:** CC-P8-A4-015
**Phases:** CC
Global `isNaN` coerces argument. `Number.isNaN` is more precise.

**Fix:** Use `Number.isNaN(value)` or `typeof value !== 'number'`.

---

## Deduplication Log

| Final ID | Original IDs | Duplicates Removed | Reason |
|----------|-------------|-------------------|--------|
| MF-001 | CC-P8-A3-001 | 0 | Unique finding |
| MF-002 | CC-P8-A0-001 | 0 | Unique finding |
| MF-003 | CC-P8-A0-002 | 0 | Unique finding |
| MF-004 | CC-P8-A1-001 | 0 | Unique finding (different files from SF-009/010/011 despite same pattern) |
| MF-005 | CC-P8-A4-001 | 0 | Unique finding (different file from MF-006) |
| MF-006 | CC-P8-A4-002 | 0 | Unique finding (different file from MF-005) |
| MF-007 | CC-P8-A4-003 | 0 | Unique finding |
| MF-008 | CV-P8-001 | 0 | Unique finding |
| SF-001 | CC-P8-A8-001 | 0 | Unique finding |
| SF-002 | CC-P8-A8-002 | 0 | Unique finding |
| SF-003 | CC-P8-A8-003 | 0 | Unique finding |
| SF-004 | CC-P8-A8-004 | 0 | Unique finding |
| SF-005 | CC-P8-A2-001 | 0 | Unique finding (different file from SF-006) |
| SF-006 | CC-P8-A2-002 | 0 | Unique finding (different file from SF-005) |
| SF-007 | CC-P8-A2-003 | 0 | Unique finding (different file from SF-008) |
| SF-008 | CC-P8-A2-004 | 0 | Unique finding (different file from SF-007) |
| SF-009 | CC-P8-A3-002 | 0 | Unique finding |
| SF-010 | CC-P8-A3-003 | 0 | Unique finding |
| SF-011 | CC-P8-A3-004 | 0 | Unique finding |
| SF-012 | CC-P8-A3-005 | 0 | Unique finding |
| SF-013 | CC-P8-A3-006 | 0 | Unique finding |
| SF-014 | CC-P8-A3-007 | 0 | Unique finding |
| SF-015 | CC-P8-A0-003 | 0 | Unique finding |
| SF-016 | CC-P8-A0-004 | 0 | Unique finding |
| SF-017 | CC-P8-A0-005 | 0 | Unique finding |
| SF-018 | CC-P8-A0-006 | 0 | Unique finding |
| SF-019 | CC-P8-A0-007 | 0 | Unique finding |
| SF-020 | CC-P8-A0-008 | 0 | Unique finding |
| SF-021 | CC-P8-A0-009 | 0 | Unique finding |
| SF-022 | CC-P8-A0-010 | 0 | Unique finding |
| SF-023 | CC-P8-A0-011 | 0 | Unique finding |
| SF-024 | CC-P8-A6-001 | 0 | Unique finding |
| SF-025 | CC-P8-A6-002 | 0 | Unique finding |
| SF-026 | CC-P8-A7-001, CC-P8-A7-002 | 1 | Same file (TerminalView.tsx), adjacent lines (72 vs 78), same root cause (localStorage read missing try/catch) |
| SF-027 | CC-P8-A7-003 | 0 | Unique finding |
| SF-028 | CC-P8-A7-004 | 0 | Unique finding |
| SF-029 | CC-P8-A1-002 | 0 | Unique finding |
| SF-030 | CC-P8-A1-003 | 0 | Unique finding |
| SF-031 | CC-P8-A1-004 | 0 | Unique finding |
| SF-032 | CC-P8-A1-005 | 0 | Unique finding |
| SF-033 | CC-P8-A1-006, CC-P8-A1-007 | 1 | Same caches (reachable/route.ts + governance-service.ts), overlapping concern: consolidating duplicate caches (A1-006) automatically resolves unbounded size (A1-007) |
| SF-034 | CC-P8-A5-003 | 0 | Unique finding |
| SF-035 | CC-P8-A5-004 | 0 | Unique finding |
| SF-036 | CC-P8-A5-005 | 0 | Unique finding |
| SF-037 | CC-P8-A5-006 | 0 | Unique finding |
| SF-038 | CC-P8-A5-007 | 0 | Unique finding |
| SF-039 | CC-P8-A9-002 | 0 | Unique finding |
| SF-040 | CC-P8-A9-003 | 0 | Unique finding |
| SF-041 | CC-P8-A9-004 | 0 | Unique finding |
| SF-042 | CC-P8-A9-005 | 0 | Unique finding |
| SF-043 | CC-P8-A9-006 | 0 | Unique finding |
| SF-044 | CC-P8-A9-007 | 0 | Unique finding |
| SF-045 | CC-P8-A4-004 | 0 | Unique finding |
| SF-046 | CC-P8-A4-005 | 0 | Unique finding |
| SF-047 | CC-P8-A4-006 | 0 | Unique finding |
| SF-048 | CC-P8-A4-007 | 0 | Unique finding |
| SF-049 | CC-P8-A4-008 | 0 | Unique finding |
| SF-050 | CC-P8-A4-009 | 0 | Unique finding |
| SF-051 | CC-P8-A4-010 | 0 | Unique finding |
| SF-052 | CV-P8-002 | 0 | Unique finding |
| SF-053 | CV-P8-003 | 0 | Unique finding |
| SF-054 | CV-P8-004 | 0 | Unique finding |
| NT-001 | CC-P8-A8-005 | 0 | Unique finding |
| NT-002 | CC-P8-A8-006 | 0 | Unique finding |
| NT-003 | CC-P8-A8-007 | 0 | Unique finding |
| NT-004 | CC-P8-A8-008 | 0 | Unique finding |
| NT-005 | CC-P8-A8-009 | 0 | Unique finding |
| NT-006 | CC-P8-A2-005 | 0 | Unique finding |
| NT-007 | CC-P8-A2-006 | 0 | Unique finding |
| NT-008 | CC-P8-A2-007 | 0 | Unique finding |
| NT-009 | CC-P8-A3-008 | 0 | Unique finding |
| NT-010 | CC-P8-A3-009 | 0 | Unique finding |
| NT-011 | CC-P8-A3-011 | 0 | Unique finding |
| NT-012 | CC-P8-A0-012 | 0 | Unique finding |
| NT-013 | CC-P8-A0-013 | 0 | Unique finding |
| NT-014 | CC-P8-A0-014 | 0 | Unique finding |
| NT-015 | CC-P8-A6-003 | 0 | Unique finding |
| NT-016 | CC-P8-A6-004 | 0 | Unique finding |
| NT-017 | CC-P8-A6-005 | 0 | Unique finding |
| NT-018 | CC-P8-A6-006 | 0 | Unique finding |
| NT-019 | CC-P8-A7-005 | 0 | Unique finding |
| NT-020 | CC-P8-A7-006 | 0 | Unique finding |
| NT-021 | CC-P8-A7-007 | 0 | Unique finding |
| NT-022 | CC-P8-A1-008 | 0 | Unique finding |
| NT-023 | CC-P8-A1-009 | 0 | Unique finding |
| NT-024 | CC-P8-A1-010 | 0 | Unique finding |
| NT-025 | CC-P8-A1-011 | 0 | Unique finding |
| NT-026 | CC-P8-A5-002, CC-P8-A5-008 | 1 | Same file+line (messageQueue.ts:267), same issue (double nullish coalescing). Originally MUST-FIX then retracted to NIT, re-reported in NIT section |
| NT-027 | CC-P8-A5-001, CC-P8-A5-009 | 1 | Same file+line (notification-service.ts:59), same issue (tmux send-keys limitation). Originally MUST-FIX then retracted to NIT, re-reported in NIT section |
| NT-028 | CC-P8-A5-010 | 0 | Unique finding |
| NT-029 | CC-P8-A5-011 | 0 | Unique finding |
| NT-030 | CC-P8-A5-012 | 0 | Unique finding |
| NT-031 | CC-P8-A9-008 | 0 | Unique finding |
| NT-032 | CC-P8-A9-009 | 0 | Unique finding |
| NT-033 | CC-P8-A9-010 | 0 | Unique finding |
| NT-034 | CC-P8-A9-011 | 0 | Unique finding |
| NT-035 | CC-P8-A9-012 | 0 | Unique finding |
| NT-036 | CC-P8-A4-011 | 0 | Unique finding |
| NT-037 | CC-P8-A4-012 | 0 | Unique finding |
| NT-038 | CC-P8-A4-013 | 0 | Unique finding |
| NT-039 | CC-P8-A4-014 | 0 | Unique finding |
| NT-040 | CC-P8-A4-015 | 0 | Unique finding |
| -- | CC-P8-A9-001 | RETRACTED | Finding self-retracted after deeper analysis: portable_sed works correctly |
| -- | CC-P8-A3-010 | RETRACTED | Finding self-corrected: AMPError import is actually used correctly |
| -- | CV-P8-005 | RESOLVED | Config/deploy endpoint confirmed present in headless router at line 861 |

## Retracted/Resolved Findings (Not Counted)

| Original ID | Reason |
|-------------|--------|
| CC-P8-A9-001 | `portable_sed` function analyzed and found correct on re-tracing |
| CC-P8-A3-010 | Self-corrected during analysis -- import is used correctly |
| CV-P8-005 | Config/deploy endpoint confirmed present at headless-router.ts:861 |

---

## Self-Verification

- [x] I parsed ALL findings from the intermediate report (none skipped or missed)
- [x] I grouped findings by file path before comparing
- [x] For each potential duplicate pair, I checked ALL THREE conditions:
      (1) same file path + (2) overlapping lines within 5 + (3) same root cause
- [x] I did NOT merge findings at the same line that describe DIFFERENT bugs
- [x] I did NOT merge findings from different files even if they describe the same pattern
- [x] For merged findings: I kept the HIGHEST severity (A5-001/A5-002 moved from MUST-FIX to NIT per their own retraction; A7-001+A7-002 both SHOULD-FIX; A1-006+A1-007 both SHOULD-FIX)
- [x] For merged findings: I preserved ALL original IDs in "Also identified by" annotation
- [x] For merged findings: I noted ALL source phases (CC, CV, SR)
- [x] My final IDs use sequential numbering: MF-001 through MF-008, SF-001 through SF-054, NT-001 through NT-040
- [x] My deduplication log has an entry for EVERY final finding (including unique ones) plus retracted entries
- [x] Each dedup log entry includes the merge reasoning
- [x] My verdict follows the rules: MUST-FIX=8 > 0 -> REQUEST CHANGES
- [x] Math check: 109 raw - 3 retracted = 106 valid - 4 duplicates merged = 102 final (8+54+40=102)
- [x] The source reports section is listed below
- [x] My return message to the orchestrator is exactly 1-2 lines

---

## Source Reports

- `epcp-correctness-P8-R57d244f7-1ad1f5f8-7387-4abe-bd35-5d00bc62c768.md`
- `epcp-correctness-P8-R57d244f7-42ea92e3-72c8-4821-ab5a-df63082fc80e.md`
- `epcp-correctness-P8-R57d244f7-9513d8b0-d2b4-47c2-9e31-2beefa11332d.md`
- `epcp-correctness-P8-R57d244f7-9bd2a50f-4ceb-4c1a-992a-7583172ec38b.md`
- `epcp-correctness-P8-R57d244f7-9c2ed7e0-b82e-44d6-9cee-21a6d97d0674.md`
- `epcp-correctness-P8-R57d244f7-b0a5930d-3352-451e-9080-0b76d606b248.md`
- `epcp-correctness-P8-R57d244f7-ebe28e6f-732e-4d47-b907-89c3ca1cd350.md`
- `epcp-correctness-P8-R57d244f7-f20eed6a-74cf-4a9e-acf9-bb9c6d3a1563.md`
- `epcp-correctness-P8-R57d244f7-f50ce148-d927-4c71-aa56-cde8ad13838f.md`
- `epcp-correctness-P8-R57d244f7-fc06e1a7-1f77-4b7f-a576-a6db2502c49b.md`
- `epcp-claims-P8-R57d244f7-a4e5e1b4-1f08-4c99-9291-ef7fe92886d0.md`
- `epcp-review-P8-R57d244f7-6cbeaa05-e4b5-4ec1-9947-1c1a3ab342fc.md`

# EPCP Final PR Review Report

**Generated:** 2026-02-26
**Pass:** 9
**Run ID:** 1ebfebc5
**Pipeline:** Code Correctness -> Claim Verification -> Skeptical Review
**Dedup:** 135 raw findings -> 119 unique findings (16 removed: 8 duplicates merged, 5 retracted as false positives/self-resolving, 3 excluded as "no action needed" by reviewers)

---

## Summary

| Severity | Count |
|----------|-------|
| **MUST-FIX** | 18 |
| **SHOULD-FIX** | 55 |
| **NIT** | 46 |
| **Total findings** | 119 |

**Verdict: REQUEST CHANGES -- 18 must-fix issue(s) found.**

---

## MUST-FIX Issues

### [MF-001] `sendCommand` returns both `data` and `error` in same response (ambiguous contract)
**File:** services/sessions-service.ts:783-787
**Original IDs:** CC-P9-A4-001
**Phases:** CC
When `sendCommand` detects the session is not idle, it returns an object with BOTH `data` (containing `success: false`, `idle: false`, etc.) AND `error: 'Session is not idle'`, AND `status: 409`. The `ServiceResult<T>` type contract is `{ data, status }` OR `{ error, status }`, not both. The same issue occurs in `sendAgentSessionCommand` at line 1185-1194. Callers like `sendServiceResult` in headless-router may behave unpredictably since they check `result.error` first -- so the `data` field is silently discarded.
**Fix:** Return either `{ error, status }` or `{ data, status }`, not both. If the intent is to provide structured idle info alongside the error, put it in the error message or make a dedicated error shape. The same pattern at agents-core-service.ts:1185-1194 needs the same fix.

### [MF-002] Cloud agent rename is not atomic -- crash between write and unlink loses data
**File:** services/sessions-service.ts:730-735
**Original IDs:** CC-P9-A4-002
**Phases:** CC
The `renameSession` function for cloud agents writes the new file, then unlinks the old file. If the process crashes between `writeFileSync` and `unlinkSync`, both files exist with potentially inconsistent state. This is not atomic.
**Fix:** Use the write-to-temp-then-rename pattern: write to a `.tmp` file first, then `fs.renameSync` the temp to the new path (atomic on same filesystem), then `fs.unlinkSync` the old path. If the rename fails, clean up the temp file.

### [MF-003] `createSession` has conflicting agentId assignment (last write wins)
**File:** services/sessions-service.ts:620-627
**Original IDs:** CC-P9-A4-003
**Phases:** CC
In `persistSession`, the `agentId` is spread twice: first from the function parameter (`params.agentId`), then from the registered agent result (`registeredAgent.id`). If both are truthy, the second one silently overwrites the first via object spread order.
**Fix:** Choose one source of truth for `agentId`. If `registeredAgent.id` is always preferred, remove the `...(agentId && { agentId })` spread.

### [MF-004] SSRF bypass: health check proceeds when hostUrl is empty string
**File:** app/api/hosts/health/route.ts:18,58
**Original IDs:** CC-P9-A3-001
**Phases:** CC
When `url` query parameter is missing, `hostUrl` defaults to `''` (empty string). The SSRF allowlist check on line 18 is guarded by `if (hostUrl)`, so an empty string skips the entire allowlist validation. Control then falls through to `checkRemoteHealth('')` on line 58.
**Fix:** Return 400 immediately when `hostUrl` is empty/missing.

### [MF-005] SSRF allowlist bypass via bare alias hostname match with attacker-controlled port
**File:** app/api/hosts/health/route.ts:45
**Original IDs:** CC-P9-A3-002
**Phases:** CC
When a host alias is a bare hostname/IP (not a URL), the code falls back to a direct hostname string comparison that ignores the port entirely. An attacker can request `?url=http://myhost:6379/` to reach Redis on that host, bypassing the origin-based port check.
**Fix:** Either remove the bare hostname fallback entirely (forcing aliases to always be full URLs), or also validate that the requested port matches the known host's configured port.

### [MF-006] Missing error guard in GET /api/organization allows undefined result.status
**File:** app/api/organization/route.ts:10
**Original IDs:** CC-P9-A3-003
**Phases:** CC
The GET handler returns `NextResponse.json(result.data, { status: result.status })` without checking `result.error` first. If `getOrganization()` returns an error result, `result.data` could be `undefined` and no `{ error: ... }` wrapper is returned.
**Fix:** Add the standard error guard checking `result.error` first.

### [MF-007] hosts-config.ts has its own lock implementation, duplicating and diverging from file-lock.ts
**File:** lib/hosts-config.ts:20-79
**Original IDs:** CC-P9-A5-001
**Phases:** CC
`hosts-config.ts` implements its own `acquireLock`/`releaseLock`/`withLock` functions with different behavior than the canonical `file-lock.ts`. Critically, in `acquireLock()`, the `findIndex` comparison uses function reference equality on a wrapper function vs the raw resolve, meaning timed-out waiters will never be removed from the queue, causing a memory leak and phantom lock grants.
**Fix:** The `findIndex` on timeout should compare by a stable identifier, or migrate to using the canonical `withLock` from `file-lock.ts`.

### [MF-008] index-delta.ts acquireIndexSlot references `entry` before it is declared
**File:** lib/index-delta.ts:51
**Original IDs:** CC-P9-A5-002
**Phases:** CC
In `acquireIndexSlot()`, the timeout callback on line 51 references `entry.timestamp`, but `entry` is declared on line 56 (5 lines later). JavaScript hoists `const` declarations but does not initialize them, creating a temporal dead zone hazard.
**Fix:** Move the `entry` declaration before the `setTimeout` call, or capture `entry.timestamp` in a separate variable declared before the timeout.

### [MF-009] amp-auth.ts validateApiKey mutates shared cache without lock protection
**File:** lib/amp-auth.ts:216-246
**Original IDs:** CC-P9-A5-003
**Phases:** CC
`validateApiKey()` mutates `record.last_used_at` in-place and calls `saveApiKeys(keys)` which writes ALL keys to disk. If two concurrent HTTP requests both hit the debounce window simultaneously, both will call `saveApiKeys` with the same array reference -- the second write will overwrite the first. If `createApiKey` or `revokeApiKey` runs between the two saves, the second `saveApiKeys` call will overwrite the newly created/revoked key.
**Fix:** Either wrap the `saveApiKeys` call inside `withLock('amp-api-keys')`, or schedule the lastUsed write via a debounced async callback that acquires the lock.

### [MF-010] `portable_sed` in remote-install.sh passes file path twice to sed, breaking on edge cases
**File:** scripts/remote-install.sh:327-330
**Original IDs:** CC-P9-A9-001, CC-P9-A9-007
**Phases:** CC
**Also identified by:** CC-P9-A9-007 (which explicitly states "Same root cause as CC-P9-A9-001")
The `portable_sed` function extracts the last argument as the file, then passes ALL arguments (including the file) to `sed -i.bak`. The function is semantically wrong: it should separate the sed arguments from the file path. Works by coincidence in current call sites but is fragile.
**Fix:** Separate the file arg explicitly: extract args without the last element.

### [MF-011] `server.mjs` crash log directory may not exist on first uncaught exception
**File:** server.mjs:36-43
**Original IDs:** CC-P9-A9-002
**Phases:** CC
The `uncaughtException` handler writes to `logs/crash.log`. The logs directory creation happens much later (line 366-368), AFTER the global error handlers are registered (lines 31-70). If an uncaught exception is thrown during module import resolution, the logs directory doesn't exist yet and `fs.appendFileSync` will throw (swallowed by try/catch), silently losing the crash log.
**Fix:** Move the `logsDir` creation block to before the global error handlers, or create the directory lazily inside the error handler before writing.

### [MF-012] MeetingRoom: duplicate team creation race between createMeetingRecord and teamId resolution effects
**File:** components/team-meeting/MeetingRoom.tsx:264-321 and :367-391
**Original IDs:** CC-P9-A6-001
**Phases:** CC
Two independent `useEffect` blocks both attempt to create a team via `POST /api/teams` when `teamId` is null and the meeting is active. Both check for an existing team by name and create one if missing. Since they run independently on the same render cycle, a race condition creates duplicate teams.
**Fix:** Consolidate team creation into a single effect, or add a `creatingTeamRef` guard.

### [MF-013] MeetingRoom: handleStartMeeting dispatches SET_TEAM_NAME but reads stale state.teamName
**File:** components/team-meeting/MeetingRoom.tsx:447-463
**Original IDs:** CC-P9-A6-002
**Phases:** CC
`handleStartMeeting` dispatches `SET_TEAM_NAME` with a generated name, then immediately uses `state.teamName` in the AMP notification. React state updates from `dispatch` are batched and not yet applied, so the notification sends "Unnamed Team" instead of the generated name.
**Fix:** Capture the generated name in a local variable and use it consistently.

### [MF-014] MessageCenter: compose sends message but never clears composePriority/composeType state
**File:** components/MessageCenter.tsx
**Original IDs:** CC-P9-A6-003
**Phases:** CC
When the user sends a composed message and then composes another, the priority and type selections from the previous message persist. The send handler clears `composeTo`, `composeSubject`, `composeBody`, but does not reset `composePriority` and `composeType`.
**Fix:** Add `setComposePriority('normal')` and `setComposeType('request')` after clearing the other compose fields.

### [MF-015] Missing try-catch in directory/lookup route allows unhandled exceptions
**File:** app/api/agents/directory/lookup/[name]/route.ts:18
**Original IDs:** CC-P9-A0-001
**Phases:** CC
The `GET` handler does not have an outer try-catch. If `lookupAgentByDirectoryName()` throws, the request will fail with an unhandled exception and no 500 response will be sent.
**Fix:** Wrap the handler body in a try-catch block returning a 500 JSON error.

### [MF-016] Missing try-catch in directory GET route allows unhandled exceptions
**File:** app/api/agents/directory/route.ts:14
**Original IDs:** CC-P9-A0-002
**Phases:** CC
The `GET` handler for `/api/agents/directory` lacks a try-catch. If `getDirectory()` throws, the request crashes with no JSON error response.
**Fix:** Add outer try-catch returning `{ error: 'Internal server error' }` with status 500.

### [MF-017] Missing try-catch in directory/sync POST route allows unhandled exceptions
**File:** app/api/agents/directory/sync/route.ts:13
**Original IDs:** CC-P9-A0-003
**Phases:** CC
The `POST` handler for `/api/agents/directory/sync` lacks a try-catch.
**Fix:** Add outer try-catch returning `{ error: 'Internal server error' }` with status 500.

### [MF-018] Missing try-catch in startup POST and GET routes, and normalize-hosts GET and POST routes
**File:** app/api/agents/startup/route.ts:10-36, app/api/agents/normalize-hosts/route.ts:16-30
**Original IDs:** CC-P9-A0-004, CC-P9-A0-005
**Phases:** CC
**Also identified by:** CC-P9-A0-005 (same pattern, different route)
Both startup and normalize-hosts route handlers (4 total: 2 POST, 2 GET) lack try-catch. If service functions throw, the requests will crash without JSON error responses.
**Fix:** Add outer try-catch to all four handlers.

---

## SHOULD-FIX Issues

### [SF-001] notify/route.ts passes raw unvalidated body directly to service function
**File:** app/api/teams/notify/route.ts:26
**Original IDs:** CC-P9-A2-001
**Phases:** CC
The `POST` handler passes the raw `body` object directly to `notifyTeamAgents(body)` without any field whitelisting or input validation, inconsistent with the defense-in-depth pattern used in other routes.
**Fix:** Whitelist only `agentIds` and `teamName` from `body` before passing to service.

### [SF-002] documents/route.ts POST passes raw body with spread, no field whitelisting
**File:** app/api/teams/[id]/documents/route.ts:58
**Original IDs:** CC-P9-A2-002
**Phases:** CC
The POST handler passes `{ ...body, requestingAgentId }` directly to `createTeamDocument`. Similarly, `documents/[docId]/route.ts` PUT handler does the same.
**Fix:** Whitelist only `CreateDocumentParams` / `UpdateDocumentParams` fields at the route level.

### [SF-003] notify/route.ts does not validate agentIds array elements are UUIDs
**File:** app/api/teams/notify/route.ts:26
**Original IDs:** CC-P9-A2-003
**Phases:** CC
Neither the route nor the service validates that each element of `agentIds` is a valid UUID. Non-string or malformed values could be passed to downstream tmux commands.
**Fix:** Add element-level validation at the route or service layer.

### [SF-004] `any[]` return types in service functions weaken type safety
**File:** services/teams-service.ts:120,300,468,623
**Original IDs:** CC-P9-A2-004
**Phases:** CC
Multiple service functions use `any[]` in their return types, eliminating TypeScript's ability to catch shape mismatches.
**Fix:** Replace `any[]` with concrete types: `Team[]`, `Task[]`, `TeamDocument[]`.

### [SF-005] `assertValidServiceResult` is defined but never called
**File:** types/service.ts:27
**Original IDs:** CC-P9-A7-001, CC-P9-A2-007
**Phases:** CC
**Also identified by:** CC-P9-A2-007 (NIT, same finding about assertValidServiceResult never being used)
The `assertValidServiceResult()` runtime guard was introduced for defense-in-depth but is never imported or called by any route handler or service. Multiple route handlers use the unsafe `result.data ?? {}` pattern without checking `result.error` first.
**Fix:** Either add `assertValidServiceResult(result, 'contextName')` calls in route handlers, or document that this guard is deferred to Phase 2.

### [SF-006] Multiple headless-router handlers pass raw `query` object as `any`
**File:** services/headless-router.ts:1182, 1188-1189
**Original IDs:** CC-P9-A4-004
**Phases:** CC
Multiple handlers pass the raw `query` object directly to service functions cast as `any`, bypassing type checking.
**Fix:** Extract and validate specific query parameters before passing to service functions.

### [SF-007] `readJsonBody` resolves `{}` for empty body -- callers don't distinguish empty from valid
**File:** services/headless-router.ts:330
**Original IDs:** CC-P9-A4-005
**Phases:** CC
When the request body is empty, `readJsonBody` returns `{}`. Service functions that expect specific fields will see `undefined` without knowing whether the client sent an empty body vs. `{}`.
**Fix:** Return `null` for empty bodies, or throw an error for POST/PUT/PATCH methods that require a body.

### [SF-008] `resolveStartCommand` has unreachable `'claude code'` match
**File:** services/agents-core-service.ts:186
**Original IDs:** CC-P9-A4-006
**Phases:** CC
The condition `program.includes('claude') || program.includes('claude code')` has the second disjunct as unreachable.
**Fix:** Remove the redundant `|| program.includes('claude code')` clause.

### [SF-009] Duplicate program resolution logic -- `createSession` and `resolveStartCommand` diverge
**File:** services/sessions-service.ts:653-661 vs services/agents-core-service.ts:185-199
**Original IDs:** CC-P9-A4-007
**Phases:** CC
`createSession` includes `openclaw`, while `resolveStartCommand` does NOT. Waking a hibernated agent with program `'openclaw'` falls through to `'claude'` default.
**Fix:** Consolidate program resolution into a single shared function.

### [SF-010] headless-router GET /api/teams/[id]/tasks/[taskId] returns 405 instead of routing to service
**File:** services/headless-router.ts:1498-1501
**Original IDs:** CC-P9-A4-008
**Phases:** CC
The headless router returns 405 (Method Not Allowed) for a GET single task endpoint. 405 implies the resource exists but the method isn't allowed.
**Fix:** Either implement the GET handler or remove the route entirely so it falls through to 404.

### [SF-011] `proxyHealthCheck` SSRF protection can be bypassed via DNS rebinding
**File:** services/agents-core-service.ts:1633-1669
**Original IDs:** CC-P9-A4-009
**Phases:** CC
The health check proxy validates hostname against private IP patterns, but hostname resolution happens later in `fetch()`. A DNS rebinding attack could make a hostname resolve to a private IP after validation.
**Fix:** For Phase 2, resolve the hostname to an IP first. For now, document the accepted risk more prominently.

### [SF-012] `wakeAgent` sends single-quoted shell exports via `sendKeys` -- fragile for non-POSIX shells
**File:** services/agents-core-service.ts:1387-1418
**Original IDs:** CC-P9-A4-010
**Phases:** CC
The wake agent flow builds shell export commands with single-quote escaping and sends via `sendKeys`, while `createSession` uses `runtime.setEnvironment` (shell-independent).
**Fix:** Use `runtime.setEnvironment` consistently.

### [SF-013] `listPendingMessages` parseInt without validation
**File:** services/headless-router.ts:1149
**Original IDs:** CC-P9-A4-011
**Phases:** CC
The headless router uses `parseInt(query.limit)` without checking if the result is NaN or negative.
**Fix:** Validate the parsed integer.

### [SF-014] `as` cast bypasses runtime validation for PluginPushConfig
**File:** app/api/plugin-builder/push/route.ts:62
**Original IDs:** CC-P9-A3-004
**Phases:** CC
After validating individual fields, the code performs an unsafe `as PluginPushConfig` cast. The `sources` array elements are never validated. `manifest.plugin` internal structure is not validated.
**Fix:** Add validation for `manifest.sources` elements and `manifest.plugin` internal structure.

### [SF-015] Operator precedence issue with `?? {} as AMPHealthResponse`
**File:** app/api/v1/health/route.ts:22
**Original IDs:** CC-P9-A3-005
**Phases:** CC
Returns an empty `{}` typed as `AMPHealthResponse` when `result.data` is null, violating the API contract. Same pattern in `app/api/v1/info/route.ts:22`.
**Fix:** Throw an error instead of returning an empty object if `result.data` is unexpectedly null.

### [SF-016] No input validation for `conversationFile` in parse endpoint
**File:** app/api/conversations/parse/route.ts:14
**Original IDs:** CC-P9-A3-006
**Phases:** CC
The `conversationFile` value is passed directly to `parseConversationFile()` without type or path traversal validation.
**Fix:** Add type validation and consider path traversal checks.

### [SF-017] No validation on `encodedFile` parameter in conversation messages endpoint
**File:** app/api/conversations/[file]/messages/route.ts:12
**Original IDs:** CC-P9-A3-007
**Phases:** CC
The `encodedFile` path parameter is passed directly to `getConversationMessages()` without validation, potential path traversal vector.
**Fix:** Validate `encodedFile` does not contain path traversal sequences.

### [SF-018] `checkIdleStatus` exceptions not wrapped in service result pattern
**File:** app/api/sessions/[id]/command/route.ts:75
**Original IDs:** CC-P9-A3-008
**Phases:** CC
The GET handler calls `checkIdleStatus` directly and wraps the result in `{ success: true, ...data }`, inconsistent with the POST handler's use of the service result pattern.
**Fix:** Update `checkIdleStatus` to return a service result.

### [SF-019] Missing `dynamic = 'force-dynamic'` on multiple routes that read runtime state
**File:** Multiple files (conversations, domains, webhooks, messages, teams sub-routes, agents sub-routes)
**Original IDs:** CC-P9-A3-009, CC-P9-A2-006, CC-P9-A0-014
**Phases:** CC
**Also identified by:** CC-P9-A2-006 (NIT, teams sub-routes), CC-P9-A0-014 (NIT, agents sub-routes)
Multiple routes read from runtime filesystem state but do not export `dynamic = 'force-dynamic'`. Affected routes include conversations/parse, conversations/[file]/messages, domains/*, webhooks/*, messages/*, teams/[id]/* sub-routes, agents/startup, agents/normalize-hosts, agents/import, and others. Routes with dynamic path segments are automatically dynamic in Next.js App Router, so this is mainly a consistency issue for routes without path params.
**Fix:** Add `export const dynamic = 'force-dynamic'` to all routes that read from runtime state.

### [SF-020] Session `name` not validated for tmux naming constraints in create endpoint
**File:** app/api/sessions/create/route.ts:17-26
**Original IDs:** CC-P9-A3-010
**Phases:** CC
The session creation endpoint does not validate `body.name` against tmux session naming constraints (`^[a-zA-Z0-9_-]+$`).
**Fix:** Add name validation regex check.

### [SF-021] `newName` not validated in session rename endpoint
**File:** app/api/sessions/[id]/rename/route.ts:32
**Original IDs:** CC-P9-A3-011
**Phases:** CC
The rename endpoint extracts `newName` without validating it exists or matches tmux naming constraints.
**Fix:** Validate `newName` is a string matching `^[a-zA-Z0-9_-]+$`.

### [SF-022] `body.command` not validated before passing to tmux send-keys
**File:** app/api/sessions/[id]/command/route.ts:34
**Original IDs:** CC-P9-A3-012
**Phases:** CC
The command endpoint passes `body.command` directly to `sendCommand()` without checking it is a non-empty string.
**Fix:** Add basic type validation.

### [SF-023] messageQueue.ts deleteMessage has no lock protection for concurrent access
**File:** lib/messageQueue.ts:857-868
**Original IDs:** CC-P9-A5-004
**Phases:** CC
`deleteMessage()` has no lock, unlike `markMessageAsRead` and `archiveMessage` which both use `withLock`. The delete could race with concurrent writes.
**Fix:** Wrap the unlink in `withLock('msg-${messageId}')`.

### [SF-024] governance-peers.ts savePeerGovernance has no file lock
**File:** lib/governance-peers.ts:60-68
**Original IDs:** CC-P9-A5-005
**Phases:** CC
`savePeerGovernance()` writes without any lock. Two simultaneous governance sync messages from the same peer could cause last-writer-wins data loss.
**Fix:** Use `withLock('governance-peers-' + hostId)`.

### [SF-025] message-filter.ts passes alias strings where UUIDs are expected
**File:** lib/message-send.ts:180-187
**Original IDs:** CC-P9-A5-006
**Phases:** CC
When the recipient cannot be resolved to an agent, the code uses `toResolved.alias` as `recipientAgentId` for the message filter, but `agentIds` in teams contain UUIDs, not aliases. The closed-team check always passes for unresolved recipients.
**Fix:** When the recipient is known only by alias, apply stricter rules or attempt resolution within the filter.

### [SF-026] hosts-config.ts caches hosts but never invalidates on file change
**File:** lib/hosts-config.ts:236
**Original IDs:** CC-P9-A5-007
**Phases:** CC
`cachedHosts` is set once and never invalidated unless modified through the module's own API. External edits to `~/.aimaestro/hosts.json` will be stale for the lifetime of the process.
**Fix:** Add mtime-based cache invalidation or a TTL.

### [SF-027] amp-auth.ts _loadApiKeysRaw returns empty array on error without caching it
**File:** lib/amp-auth.ts:73-76
**Original IDs:** CC-P9-A5-008
**Phases:** CC
When `_loadApiKeysRaw()` catches a JSON parse or read error, it returns `[]` but does not cache it, causing repeated I/O and error log spam on every subsequent call.
**Fix:** On error, set `_apiKeysCache = []` and `_apiKeysCacheTimestamp = now`.

### [SF-028] task-registry.ts saveTasks returns false on error but callers don't check
**File:** lib/task-registry.ts:49-63 and 131
**Original IDs:** CC-P9-A5-009
**Phases:** CC
`saveTasks()` returns `false` on error instead of throwing. All callers inside `withLock` ignore the return value, so the caller returns the task as if it was successfully persisted.
**Fix:** Either have `saveTasks` throw on error, or check the return value in callers.

### [SF-029] document-registry.ts saveDocuments returns false on error but callers ignore it
**File:** lib/document-registry.ts:48-62 and 92
**Original IDs:** CC-P9-A5-010
**Phases:** CC
Same issue as SF-028 but for `saveDocuments()`.
**Fix:** Either throw on failure or check the return value in callers.

### [SF-030] messageQueue.ts getSentCount loads ALL messages just to count them
**File:** lib/messageQueue.ts:731-734
**Original IDs:** CC-P9-A5-011
**Phases:** CC
`getSentCount()` calls `listSentMessages()` which reads, parses, and converts every message file. Similarly `getUnreadCount()` loads all inbox messages. Very inefficient when only a count is needed.
**Fix:** Add an optimized count function that walks the directory and counts JSON files without parsing.

### [SF-031] use-governance-hook.test.ts tests standalone replicas, not actual hook code
**File:** tests/use-governance-hook.test.ts:41-112
**Original IDs:** CC-P9-A8-001
**Phases:** CC
The test file explicitly tests standalone function replicas of the `useGovernance` hook's callbacks, NOT the actual hook. If the actual hook drifts from these replicas, the tests will still pass but the real code will be wrong.
**Fix:** Add `@testing-library/react` to test dependencies so the actual React hook can be rendered and tested.

### [SF-032] governance-peers.test.ts dual-export mock pattern creates inconsistent function references
**File:** tests/governance-peers.test.ts:33-88
**Original IDs:** CC-P9-A8-002
**Phases:** CC
The fs mock exports named functions AND a `default` object with duplicated but separate `vi.fn()` instances.
**Fix:** Use shared function references between named and default exports.

### [SF-033] agent-config-governance-extended.test.ts does not restore globalThis.fetch on error paths
**File:** tests/agent-config-governance-extended.test.ts:380-447
**Original IDs:** CC-P9-A8-003
**Phases:** CC
`globalThis.fetch` is overridden in `beforeEach` and restored in `afterEach`. Pattern is fragile.
**Fix:** Use `vi.stubGlobal('fetch', ...)` which integrates with Vitest's mock lifecycle.

### [SF-034] task-registry.test.ts empty string assigneeAgentId test relies on implementation detail
**File:** tests/task-registry.test.ts:241-245
**Original IDs:** CC-P9-A8-004
**Phases:** CC
Test verifies `createTask` with `assigneeAgentId: ''` preserves the empty string because "Source uses `??` not `||`". Tests an implementation detail rather than a documented API contract.
**Fix:** If empty-string assigneeAgentId is not valid, normalize it to `null` using `||`.

### [SF-035] `bump-version.sh` regex replacement on `ai-index.html` may match wrong patterns
**File:** scripts/bump-version.sh:190-202
**Original IDs:** CC-P9-A9-003
**Phases:** CC
The regex `[A-Za-z]* [0-9]*` uses `*` (zero or more) meaning it could match zero characters.
**Fix:** Use `[A-Za-z][A-Za-z]* [0-9][0-9]*` to ensure at least one character of each.

### [SF-036] `update-aimaestro.sh` git status --porcelain may include untracked files causing false "uncommitted changes" warning
**File:** update-aimaestro.sh:119
**Original IDs:** CC-P9-A9-004
**Phases:** CC
`git status --porcelain` includes untracked files, triggering false warnings.
**Fix:** Use `git status --porcelain --untracked-files=no` or `git stash push -u`.

### [SF-037] `amp-send.sh` sign_message trap collision with set -e
**File:** plugin/plugins/ai-maestro/scripts/amp-helper.sh:737-739
**Original IDs:** CC-P9-A9-005
**Phases:** CC
The `sign_message` function uses `trap RETURN` for cleanup but on older bash with `set -e` this may not fire on function exit due to command failure.
**Fix:** Use explicit cleanup in all code paths rather than relying on `trap RETURN`.

### [SF-038] `server.mjs` WebSocket message handler registers duplicate event listeners on retry
**File:** server.mjs:486-540
**Original IDs:** CC-P9-A9-006
**Phases:** CC
In `handleRemoteWorker`, when retrying a connection, NEW listeners are added to `clientWs` without removing old ones, causing duplicate handlers and a resource leak.
**Fix:** Register clientWs event listeners once outside `attemptConnection`, or remove old listeners before adding new ones on retry.

### [SF-039] `amp-inbox.sh` processes empty `$MESSAGES` JSON as having `COUNT` of `null`
**File:** plugin/plugins/ai-maestro/scripts/amp-inbox.sh:89-93
**Original IDs:** CC-P9-A9-008
**Phases:** CC
If `list_inbox` returns empty output (due to jq error), COUNT could be `null`, causing bash arithmetic error.
**Fix:** Add a fallback: `COUNT=$(echo "$MESSAGES" | jq 'length' 2>/dev/null || echo "0")`.

### [SF-040] useGovernance: refresh() fire-and-forget cannot be cancelled on unmount
**File:** hooks/useGovernance.ts:170-215
**Original IDs:** CC-P9-A6-004
**Phases:** CC
After each mutation, `refresh()` is called without an AbortSignal. The fetch continues to completion even if the component unmounts.
**Fix:** Consider passing the existing AbortController's signal.

### [SF-041] useWebSocket: connect/disconnect not in useEffect dependency array (suppressed eslint)
**File:** hooks/useWebSocket.ts:202-216
**Original IDs:** CC-P9-A6-005
**Phases:** CC
The `useEffect` calls `connect`/`disconnect` but they are not in the dependency array. Currently safe due to stable `useCallback` memoization but fragile.
**Fix:** Add `connect` and `disconnect` to the dependency array (they are stable).

### [SF-042] MeetingRoom: AGENT_JOINED can add duplicate agentIds to joinedAgentIds array
**File:** components/team-meeting/MeetingRoom.tsx:99-103
**Original IDs:** CC-P9-A6-006
**Phases:** CC
The reducer blindly appends agentId without dedup check.
**Fix:** Add `state.joinedAgentIds.includes(action.agentId)` guard.

### [SF-043] TerminalView: localStorage writes for notes/collapsed lack try/catch
**File:** components/TerminalView.tsx
**Original IDs:** CC-P9-A6-007
**Phases:** CC
localStorage writes may throw in private browsing mode or when storage is full.
**Fix:** Wrap all `localStorage.setItem()` calls in try/catch blocks.

### [SF-044] useTeam: optimistic update validKeys mismatch with function signature
**File:** hooks/useTeam.ts:61-65
**Original IDs:** CC-P9-A6-008
**Phases:** CC
`validKeys` includes `'type'`, `'chiefOfStaffId'`, `'managerId'` but the function signature only accepts `{ name?, description?, agentIds?, instructions? }`.
**Fix:** Either remove extra keys from `validKeys` or expand the function signature.

### [SF-045] RoleAssignmentDialog: COS assignment to new teams is sequential, not parallel
**File:** components/governance/RoleAssignmentDialog.tsx:232-235
**Original IDs:** CC-P9-A6-009
**Phases:** CC
Uses sequential `for...of` loop instead of `Promise.allSettled` (which is used for COS removal). Slow for multiple teams, partial failure risk.
**Fix:** Use `Promise.allSettled` for parallel execution.

### [SF-046] email-index GET route lacks try-catch
**File:** app/api/agents/email-index/route.ts:15-29
**Original IDs:** CC-P9-A0-006
**Phases:** CC
The GET handler used by external gateways lacks try-catch. An unhandled throw returns a non-JSON error.
**Fix:** Add outer try-catch returning JSON 500 error.

### [SF-047] `address` path parameter in AMP/email address routes not validated
**File:** app/api/agents/[id]/amp/addresses/[address]/route.ts:18, app/api/agents/[id]/email/addresses/[address]/route.ts:18
**Original IDs:** CC-P9-A0-007
**Phases:** CC
The `[address]` path parameter is passed directly to service functions without format validation, while `[id]` is validated as UUID.
**Fix:** Add basic format validation (e.g., email format regex).

### [SF-048] `messageId` path parameter not validated
**File:** app/api/agents/[id]/messages/[messageId]/route.ts:22,57,89,117
**Original IDs:** CC-P9-A0-008
**Phases:** CC
The `messageId` path parameter is never validated in GET, PATCH, DELETE, and POST handlers, potential path traversal vector.
**Fix:** Validate `messageId` with `isValidUuid()`.

### [SF-049] Inconsistent error response shapes across routes
**File:** Multiple files
**Original IDs:** CC-P9-A0-009, CC-P9-A3-013
**Phases:** CC
**Also identified by:** CC-P9-A3-013 (NIT, same root cause about inconsistent error shapes)
Error responses have inconsistent shapes: some use `{ error }`, some `{ success: false, error }`, some `{ success: false, status: 'failed', error }`.
**Fix:** Standardize error response shape. Acknowledged as Phase 2 work (SF-054).

### [SF-050] `bm25Weight` and `semanticWeight` in search route not NaN-guarded
**File:** app/api/agents/[id]/search/route.ts:56-57
**Original IDs:** CC-P9-A0-010
**Phases:** CC
Unlike `limit`, `minScore`, `startTs`, and `endTs` which have NaN guards, `bm25Weight` and `semanticWeight` do not.
**Fix:** Add NaN guards consistent with other parameters.

### [SF-051] `by-name` route has no input validation on the `name` parameter
**File:** app/api/agents/by-name/[name]/route.ts:13
**Original IDs:** CC-P9-A0-011
**Phases:** CC
The `name` path parameter is passed directly to `lookupAgentByName()` without any validation.
**Fix:** Add format validation `^[a-zA-Z0-9_-]+$`.

### [SF-052] `metadata` PATCH passes raw user JSON directly to updateAgent as metadata
**File:** app/api/agents/[id]/metadata/route.ts:50-54
**Original IDs:** CC-P9-A0-012
**Phases:** CC
No schema validation, no depth limit, and no size limit on the metadata JSON body.
**Fix:** Add basic validation: check that metadata is a plain object with reasonable depth/size.

### [SF-053] Transfer resolve: `auth.status` may use `||` instead of `??`
**File:** app/api/governance/transfers/[id]/resolve/route.ts:32
**Original IDs:** CC-P9-A1-002
**Phases:** CC
`auth.status || 401` would fall through to 401 if `status` is 0 (falsy). Using `??` would be more precise.
**Fix:** Replace `auth.status || 401` with `auth.status ?? 401` in both locations.

### [SF-054] Governance GET route: `getAgent()?.name || null` may hide empty-string names
**File:** app/api/governance/route.ts:12
**Original IDs:** CC-P9-A1-003
**Phases:** CC
`||` coalesces empty string to `null`. GET route does not fall back to alias, while POST route does. Inconsistent behavior.
**Fix:** Use the same fallback pattern in both routes.

### [SF-055] Governance requests POST: Missing top-level try/catch for local submission path
**File:** app/api/v1/governance/requests/route.ts:89-128
**Original IDs:** CC-P9-A1-004
**Phases:** CC
The local submission path's validation code runs outside any try/catch, while the remote receive path is correctly wrapped.
**Fix:** Wrap the entire local submission path in a try/catch.

### [SF-056] Transfer resolve: `fromTeam!` and `toTeam!` non-null assertions after conditional checks
**File:** app/api/governance/transfers/[id]/resolve/route.ts:113,146
**Original IDs:** CC-P9-A1-005
**Phases:** CC
Non-null assertions are logically safe now but fragile if refactored.
**Fix:** Narrow the types earlier to avoid non-null assertions.

### [SF-057] Governance requests GET: Reflected query parameter in error message (minor XSS vector)
**File:** app/api/v1/governance/requests/route.ts:138,146
**Original IDs:** CC-P9-A1-006
**Phases:** CC
User-provided query parameters reflected in JSON error responses could become attack vectors if rendered in HTML.
**Fix:** Sanitize/truncate reflected values. Low priority given Phase 1 localhost context.

### [SF-058] Claim: "Deprecated alias: recordFailure -> recordAttempt" -- rename incomplete
**File:** lib/rate-limit.ts:73-74, services/headless-router.ts:245, services/governance-service.ts:499,530
**Original IDs:** CV-001, CC-P9-A5-014
**Phases:** CV, CC
**Also identified by:** CC-P9-A5-014 (NIT, about the deprecated alias export)
The old name `recordFailure` is still directly imported and used in 3 production files (headless-router.ts, governance-service.ts). The deprecated alias works at runtime but the rename is incomplete.
**Fix:** Update all callers to use `recordAttempt` and remove the deprecated alias.

### [SF-059] Claim: "canApprove logic: check isManager || isCos in AgentSkillEditor" -- only commented TODO
**File:** components/marketplace/AgentSkillEditor.tsx:85
**Original IDs:** CV-002
**Phases:** CV
`canApprove` is hardcoded to `true`. The claimed `isManager || isCos` check is only in a comment, not actual code. The commit message implies the check was implemented.
**Fix:** Document that canApprove is intentionally `true` for Phase 1 localhost mode. Update commit message or PR notes to clarify.

### [SF-060] Claim: "ToxicSkills scan: add warning when scanner unavailable" -- warning exists but scanner unimplemented
**File:** services/agents-config-deploy-service.ts:162-167
**Original IDs:** CV-003
**Phases:** CV
The warning IS correctly implemented. However, the underlying scanner (`lib/toxic-skills.ts`) does not exist yet. Skills are deployed without any content scanning. The MUST-FIX severity in the original claim is overstated since the fix (adding a warning) IS implemented.
**Fix:** No immediate action needed for the warning. The missing scanner is a known Phase 2 gap.

---

## Nits & Suggestions

### [NT-001] chief-of-staff/route.ts error handler exposes internal error messages
**File:** app/api/teams/[id]/chief-of-staff/route.ts:110
**Original IDs:** CC-P9-A2-005
**Phases:** CC
The catch-all returns `error.message` directly to the client, potentially leaking implementation details.
**Fix:** For Phase 2 remote access, replace with a generic error message.

### [NT-002] ServiceResult data+error ambiguity documented but not enforced at type level
**File:** types/service.ts:10-20
**Original IDs:** CC-P9-A7-002
**Phases:** CC
The current type permits `{ data: T, error: string, status: 200 }` which callers must guard against manually.
**Fix:** Phase 2 discriminated union refactor as already planned.

### [NT-003] Deprecated fields in Agent/AgentSummary lack JSDoc @deprecated annotation
**File:** types/agent.ts:198-199, 472-475
**Original IDs:** CC-P9-A7-003
**Phases:** CC
Several deprecated fields use `// DEPRECATED:` comments instead of proper `/** @deprecated */` JSDoc annotations. TypeScript IDEs only show strikethrough for JSDoc tags.
**Fix:** Change to `/** @deprecated ... */` JSDoc annotations.

### [NT-004] Host.type deprecated field lacks removal timeline
**File:** types/host.ts:55-58
**Original IDs:** CC-P9-A7-004
**Phases:** CC
Unlike other deprecated fields (which have "Removal: Phase 2"), no target removal version specified.
**Fix:** Add a removal target and convert to `/** @deprecated */` JSDoc.

### [NT-005] GovernanceSyncMessage.payload typed as Record<string, unknown> loses type safety
**File:** types/governance.ts:77
**Original IDs:** CC-P9-A7-005
**Phases:** CC
The consuming code manually casts with `as`, bypassing type checking.
**Fix:** Consider a discriminated union for the payload based on `type`.

### [NT-006] Inconsistent `ServiceResult` re-export pattern
**File:** Multiple service files
**Original IDs:** CC-P9-A4-012
**Phases:** CC
Multiple service files independently import and re-export `ServiceResult`, creating redundant export paths.
**Fix:** Import directly from `@/types/service`.

### [NT-007] `sendServiceResult` hardcodes `result.data` check without discriminated union
**File:** services/headless-router.ts (sendServiceResult helper)
**Original IDs:** CC-P9-A4-013
**Phases:** CC
Since `ServiceResult<T>` has no discriminant property, TypeScript cannot narrow safely.
**Fix:** Add a discriminant to `ServiceResult` or use a tagged union.

### [NT-008] `queryCodeGraph` `depth` parameter accepted but not used in `focus` action
**File:** services/agents-graph-service.ts:861, 1022-1126
**Original IDs:** CC-P9-A4-014
**Phases:** CC
The `depth` parameter is logged and returned but never used for multi-hop traversal.
**Fix:** Either implement multi-hop traversal or remove the `depth` parameter.

### [NT-009] `getAgent(auth.agentId!)` uses non-null assertion
**File:** services/amp-service.ts:1631
**Original IDs:** CC-P9-A4-015
**Phases:** CC
Using `!` bypasses TypeScript's null safety.
**Fix:** Add a guard before the call.

### [NT-010] Synchronous file reads at module load time in sessions-service
**File:** services/sessions-service.ts
**Original IDs:** CC-P9-A4-016
**Phases:** CC
The module reads `package.json` synchronously at import time, blocking the event loop during initialization.
**Fix:** Lazy-load on first use or cache after async read during startup.

### [NT-011] `AMPError` type cast on inline error objects may not match actual type shape
**File:** app/api/v1/messages/pending/route.ts:26,42,63
**Original IDs:** CC-P9-A3-015
**Phases:** CC
Inline error objects cast with `as AMPError` duplicate the error string in both `error` and `message` fields.
**Fix:** Consider a helper function like `toAMPError(result)`.

### [NT-012] Module-level mutable state for deprecation warnings shared across requests
**File:** app/api/sessions/[id]/command/route.ts:11, app/api/sessions/[id]/rename/route.ts:13, app/api/sessions/[id]/route.ts:13
**Original IDs:** CC-P9-A3-016
**Phases:** CC
Three deprecated route files use `let _deprecationWarned = false`. In serverless, the "warn once" guarantee is only per-cold-start. Working as designed for localhost.
**Fix:** No action needed.

### [NT-013] GET handler in sessions/restore does not use service result pattern
**File:** app/api/sessions/restore/route.ts:10-11
**Original IDs:** CC-P9-A3-017
**Phases:** CC
Returns `NextResponse.json(result)` directly instead of using the standard `result.data / result.error` decomposition.
**Fix:** Standardize `listRestorableSessions()` to return the service result pattern.

### [NT-014] governance.ts: `isChiefOfStaffAnywhere`, `getClosedTeamForAgent`, `getClosedTeamsForAgent` lack agentId guard
**File:** lib/governance.ts:162-184
**Original IDs:** CC-P9-A5-012, CC-P9-A5-013
**Phases:** CC
**Also identified by:** CC-P9-A5-013 (same pattern, adjacent functions in same file)
Unlike `isManager()` and `isChiefOfStaff()` which guard against falsy agentId, these three functions do not. Functionally safe but inconsistent.
**Fix:** Add `if (!agentId) return false/null/[]` for consistency.

### [NT-015] hosts-config.ts isSelf has redundant toLowerCase on selfId
**File:** lib/hosts-config.ts:182
**Original IDs:** CC-P9-A5-017
**Phases:** CC
`getSelfHostId()` already returns lowercase. The `.toLowerCase()` call is redundant.
**Fix:** Change to `hostIdLower === selfId`.

### [NT-016] Inconsistent mock pattern for `withLock` across test files
**File:** Multiple test files
**Original IDs:** CC-P9-A8-005
**Phases:** CC
Some use `() => unknown`, others `() => any` for the withLock mock callback type.
**Fix:** Standardize on `() => unknown`.

### [NT-017] test-utils/service-mocks.ts MockTeamValidationException pattern is duplicated
**File:** tests/test-utils/service-mocks.ts, tests/transfer-resolve-route.test.ts:23-29
**Original IDs:** CC-P9-A8-008
**Phases:** CC
The `MockTeamValidationException` class is defined in both files.
**Fix:** Import the shared version from test-utils.

### [NT-018] `install-messaging.sh` box-drawing lines are misaligned
**File:** install-messaging.sh:65-71
**Original IDs:** CC-P9-A9-009
**Phases:** CC
Box-drawing character widths are inconsistent.
**Fix:** Verify all lines have consistent character counts.

### [NT-019] `start-with-ssh.sh` does not check if SSH_AUTH_SOCK is set
**File:** scripts/start-with-ssh.sh:10
**Original IDs:** CC-P9-A9-010
**Phases:** CC
An unset `SSH_AUTH_SOCK` evaluates to empty string giving a misleading message.
**Fix:** Add explicit check for unset variable.

### [NT-020] `bump-version.sh` double-counts FILES_UPDATED
**File:** scripts/bump-version.sh:135-138
**Original IDs:** CC-P9-A9-011
**Phases:** CC
`FILES_UPDATED` counts replacements not unique files. The final message "Updated N files" is misleading.
**Fix:** Count unique files or change message wording.

### [NT-021] TerminalView: initializeTerminal effect suppresses exhaustive-deps with implicit session.id dependency
**File:** components/TerminalView.tsx:296-360
**Original IDs:** CC-P9-A6-010
**Phases:** CC
The effect has `[]` as deps with eslint-disable but references `session.id`. Correct in tab architecture but could benefit from a comment.
**Fix:** Add a comment explaining session.id stability.

### [NT-022] RoleBadge: switch default case uses String(role) which may hide type narrowing bugs
**File:** components/governance/RoleBadge.tsx
**Original IDs:** CC-P9-A6-012
**Phases:** CC
The default case swallows new enum values instead of producing compile-time errors.
**Fix:** Consider using a `never` type assertion in the default case.

### [NT-023] TeamsPage: validateTeamName sets state on every keystroke
**File:** app/teams/page.tsx:88-129
**Original IDs:** CC-P9-A6-013
**Phases:** CC
Runs validation on every keystroke. Fine for current scale.
**Fix:** Consider debouncing if reservedNames grows large.

### [NT-024] useTerminal: debounce utility casts return type unsafely
**File:** hooks/useTerminal.ts:18-24
**Original IDs:** CC-P9-A6-014
**Phases:** CC
The `as T` cast is technically unsound but safe given the `=> void` constraint.
**Fix:** No change needed unless constraint is relaxed.

### [NT-025] TeamOverviewSection: unnecessary effect runs when description toggles between undefined and ''
**File:** components/teams/TeamOverviewSection.tsx:27-31
**Original IDs:** CC-P9-A6-015
**Phases:** CC
The dependency array triggers on description changes between `undefined` and `''`. Correct behavior, no fix needed.
**Fix:** No functional change needed.

### [NT-026] `_request` parameter typed differently across routes (Request vs NextRequest)
**File:** Multiple route files
**Original IDs:** CC-P9-A0-013
**Phases:** CC
Some routes type unused first parameter as `Request` (native) while others use `NextRequest`.
**Fix:** Standardize on `NextRequest`.

### [NT-027] URL parsing method inconsistency (request.nextUrl vs new URL)
**File:** Multiple route files
**Original IDs:** CC-P9-A0-015
**Phases:** CC
Some routes use `request.nextUrl.searchParams`, others use `new URL(request.url).searchParams`.
**Fix:** Standardize based on handler type.

### [NT-028] Manager route: `agent.name || agent.alias` uses `||` instead of `??`
**File:** app/api/governance/manager/route.ts:60
**Original IDs:** CC-P9-A1-007
**Phases:** CC
Using `||` skips empty string names. Using `??` would only skip null/undefined.
**Fix:** Use `agent.name ?? agent.alias`.

### [NT-029] Transfer resolve: `saveError` caught but not logged
**File:** app/api/governance/transfers/[id]/resolve/route.ts:159
**Original IDs:** CC-P9-A1-008
**Phases:** CC
The `catch (saveError)` block does not log `saveError`, making disk/permission issues harder to diagnose.
**Fix:** Add `console.error('[TransferResolve] saveTeams failed:', saveError)`.

### [NT-030] Trust DELETE route: Missing `export const dynamic = 'force-dynamic'`
**File:** app/api/governance/trust/[hostId]/route.ts
**Original IDs:** CC-P9-A1-009
**Phases:** CC
Inconsistent with parent `trust/route.ts` which has it.
**Fix:** Add the directive for consistency.

### [NT-031] Governance sync POST: `validSyncTypes` should be module-scope Set for consistency
**File:** app/api/v1/governance/sync/route.ts:32
**Original IDs:** CC-P9-A1-010, CC-P9-A1-011
**Phases:** CC
**Also identified by:** CC-P9-A1-011 (same line, same variable, module scope aspect)
Declared inside function body as mutable array instead of module-scope Set. Other routes use Set + `.has()`.
**Fix:** Change to `const VALID_SYNC_TYPES = new Set([...])` at module scope.

---

## Deduplication Log

| Final ID | Original IDs | Duplicates Removed | Reason |
|----------|-------------|-------------------|--------|
| MF-001 | CC-P9-A4-001 | 0 | Unique finding |
| MF-002 | CC-P9-A4-002 | 0 | Unique finding |
| MF-003 | CC-P9-A4-003 | 0 | Unique finding |
| MF-004 | CC-P9-A3-001 | 0 | Unique finding |
| MF-005 | CC-P9-A3-002 | 0 | Unique finding -- same file as MF-004 but different line, different bug (empty string bypass vs port bypass) |
| MF-006 | CC-P9-A3-003 | 0 | Unique finding |
| MF-007 | CC-P9-A5-001 | 0 | Unique finding |
| MF-008 | CC-P9-A5-002 | 0 | Unique finding |
| MF-009 | CC-P9-A5-003 | 0 | Unique finding |
| MF-010 | CC-P9-A9-001, CC-P9-A9-007 | 1 | Same file (remote-install.sh:327-330), same function portable_sed, same root cause. CC-P9-A9-007 explicitly states "Same root cause as CC-P9-A9-001" |
| MF-011 | CC-P9-A9-002 | 0 | Unique finding |
| MF-012 | CC-P9-A6-001 | 0 | Unique finding |
| MF-013 | CC-P9-A6-002 | 0 | Unique finding -- same file as MF-012 but different lines, different bug (team race vs stale state) |
| MF-014 | CC-P9-A6-003 | 0 | Unique finding |
| MF-015 | CC-P9-A0-001 | 0 | Unique finding |
| MF-016 | CC-P9-A0-002 | 0 | Unique finding |
| MF-017 | CC-P9-A0-003 | 0 | Unique finding |
| MF-018 | CC-P9-A0-004, CC-P9-A0-005 | 1 | Same pattern (missing try-catch), same root cause, adjacent routes in same category. Merged into one finding covering both startup and normalize-hosts |
| SF-001 | CC-P9-A2-001 | 0 | Unique finding |
| SF-002 | CC-P9-A2-002 | 0 | Unique finding |
| SF-003 | CC-P9-A2-003 | 0 | Unique finding -- same file as SF-001 but different bug (field whitelisting vs element validation) |
| SF-004 | CC-P9-A2-004 | 0 | Unique finding |
| SF-005 | CC-P9-A7-001, CC-P9-A2-007 | 1 | Same file (types/service.ts:27), same function (assertValidServiceResult), same root cause (never called). Severity upgraded from NIT to SHOULD-FIX |
| SF-006 | CC-P9-A4-004 | 0 | Unique finding |
| SF-007 | CC-P9-A4-005 | 0 | Unique finding |
| SF-008 | CC-P9-A4-006 | 0 | Unique finding |
| SF-009 | CC-P9-A4-007 | 0 | Unique finding |
| SF-010 | CC-P9-A4-008 | 0 | Unique finding |
| SF-011 | CC-P9-A4-009 | 0 | Unique finding |
| SF-012 | CC-P9-A4-010 | 0 | Unique finding |
| SF-013 | CC-P9-A4-011 | 0 | Unique finding |
| SF-014 | CC-P9-A3-004 | 0 | Unique finding |
| SF-015 | CC-P9-A3-005 | 0 | Unique finding |
| SF-016 | CC-P9-A3-006 | 0 | Unique finding |
| SF-017 | CC-P9-A3-007 | 0 | Unique finding |
| SF-018 | CC-P9-A3-008 | 0 | Unique finding |
| SF-019 | CC-P9-A3-009, CC-P9-A2-006, CC-P9-A0-014 | 2 | Same root cause (missing force-dynamic export on routes reading runtime state). Different file sets but same pattern. Merged into one comprehensive finding |
| SF-020 | CC-P9-A3-010 | 0 | Unique finding |
| SF-021 | CC-P9-A3-011 | 0 | Unique finding |
| SF-022 | CC-P9-A3-012 | 0 | Unique finding |
| SF-023 | CC-P9-A5-004 | 0 | Unique finding |
| SF-024 | CC-P9-A5-005 | 0 | Unique finding |
| SF-025 | CC-P9-A5-006 | 0 | Unique finding |
| SF-026 | CC-P9-A5-007 | 0 | Unique finding |
| SF-027 | CC-P9-A5-008 | 0 | Unique finding |
| SF-028 | CC-P9-A5-009 | 0 | Unique finding -- same pattern as SF-029 but different file (task-registry.ts vs document-registry.ts) |
| SF-029 | CC-P9-A5-010 | 0 | Unique finding -- same pattern as SF-028 but different file |
| SF-030 | CC-P9-A5-011 | 0 | Unique finding |
| SF-031 | CC-P9-A8-001 | 0 | Unique finding |
| SF-032 | CC-P9-A8-002 | 0 | Unique finding |
| SF-033 | CC-P9-A8-003 | 0 | Unique finding |
| SF-034 | CC-P9-A8-004 | 0 | Unique finding |
| SF-035 | CC-P9-A9-003 | 0 | Unique finding |
| SF-036 | CC-P9-A9-004 | 0 | Unique finding |
| SF-037 | CC-P9-A9-005 | 0 | Unique finding |
| SF-038 | CC-P9-A9-006 | 0 | Unique finding |
| SF-039 | CC-P9-A9-008 | 0 | Unique finding |
| SF-040 | CC-P9-A6-004 | 0 | Unique finding |
| SF-041 | CC-P9-A6-005 | 0 | Unique finding |
| SF-042 | CC-P9-A6-006 | 0 | Unique finding |
| SF-043 | CC-P9-A6-007 | 0 | Unique finding |
| SF-044 | CC-P9-A6-008 | 0 | Unique finding |
| SF-045 | CC-P9-A6-009 | 0 | Unique finding |
| SF-046 | CC-P9-A0-006 | 0 | Unique finding |
| SF-047 | CC-P9-A0-007 | 0 | Unique finding |
| SF-048 | CC-P9-A0-008 | 0 | Unique finding |
| SF-049 | CC-P9-A0-009, CC-P9-A3-013 | 1 | Same root cause (inconsistent error response shapes). CC-P9-A3-013 is NIT, CC-P9-A0-009 is SHOULD-FIX. Merged at SHOULD-FIX |
| SF-050 | CC-P9-A0-010 | 0 | Unique finding |
| SF-051 | CC-P9-A0-011 | 0 | Unique finding |
| SF-052 | CC-P9-A0-012 | 0 | Unique finding |
| SF-053 | CC-P9-A1-002 | 0 | Unique finding |
| SF-054 | CC-P9-A1-003 | 0 | Unique finding |
| SF-055 | CC-P9-A1-004 | 0 | Unique finding |
| SF-056 | CC-P9-A1-005 | 0 | Unique finding |
| SF-057 | CC-P9-A1-006 | 0 | Unique finding |
| SF-058 | CV-001, CC-P9-A5-014 | 1 | Same root cause (incomplete recordFailure->recordAttempt rename). CV-001 covers callers, CC-P9-A5-014 covers the deprecated export. Merged at SHOULD-FIX |
| SF-059 | CV-002 | 0 | Unique finding |
| SF-060 | CV-003 | 0 | Unique finding |
| NT-001 | CC-P9-A2-005 | 0 | Unique finding |
| NT-002 | CC-P9-A7-002 | 0 | Unique finding |
| NT-003 | CC-P9-A7-003 | 0 | Unique finding |
| NT-004 | CC-P9-A7-004 | 0 | Unique finding |
| NT-005 | CC-P9-A7-005 | 0 | Unique finding |
| NT-006 | CC-P9-A4-012 | 0 | Unique finding |
| NT-007 | CC-P9-A4-013 | 0 | Unique finding |
| NT-008 | CC-P9-A4-014 | 0 | Unique finding |
| NT-009 | CC-P9-A4-015 | 0 | Unique finding |
| NT-010 | CC-P9-A4-016 | 0 | Unique finding |
| NT-011 | CC-P9-A3-015 | 0 | Unique finding |
| NT-012 | CC-P9-A3-016 | 0 | Unique finding |
| NT-013 | CC-P9-A3-017 | 0 | Unique finding |
| NT-014 | CC-P9-A5-012, CC-P9-A5-013 | 1 | Same file (governance.ts), adjacent lines (162-184), same root cause (missing agentId guard). Different functions but identical pattern |
| NT-015 | CC-P9-A5-017 | 0 | Unique finding |
| NT-016 | CC-P9-A8-005 | 0 | Unique finding |
| NT-017 | CC-P9-A8-008 | 0 | Unique finding |
| NT-018 | CC-P9-A9-009 | 0 | Unique finding |
| NT-019 | CC-P9-A9-010 | 0 | Unique finding |
| NT-020 | CC-P9-A9-011 | 0 | Unique finding |
| NT-021 | CC-P9-A6-010 | 0 | Unique finding |
| NT-022 | CC-P9-A6-012 | 0 | Unique finding |
| NT-023 | CC-P9-A6-013 | 0 | Unique finding |
| NT-024 | CC-P9-A6-014 | 0 | Unique finding |
| NT-025 | CC-P9-A6-015 | 0 | Unique finding |
| NT-026 | CC-P9-A0-013 | 0 | Unique finding |
| NT-027 | CC-P9-A0-015 | 0 | Unique finding |
| NT-028 | CC-P9-A1-007 | 0 | Unique finding |
| NT-029 | CC-P9-A1-008 | 0 | Unique finding |
| NT-030 | CC-P9-A1-009 | 0 | Unique finding |
| NT-031 | CC-P9-A1-010, CC-P9-A1-011 | 1 | Same file (sync/route.ts), same line (32), same variable (validSyncTypes). Two aspects of the same improvement (Set type + module scope) |

### Retracted/Excluded Findings

| Original ID | Reason |
|-------------|--------|
| CC-P9-A1-001 | RETRACTED by reviewer: "After full trace, the lock acquisition and release pattern is correct" |
| CC-P9-A2-005 (first, line 1538) | RETRACTED by reviewer: "teams/route.ts does have `dynamic = 'force-dynamic'`" |
| CC-P9-A3-014 | RETRACTED by reviewer: "path is used" |
| CC-P9-A5-015 | Excluded: reviewer concluded "No action needed" (UUID regex case insensitivity is correct per RFC 4122) |
| CC-P9-A5-016 | Excluded: reviewer concluded "No action needed" (naming convention is fine) |
| CC-P9-A8-006 | Excluded: reviewer concluded "No action needed if the source module uses crypto.randomUUID()" |
| CC-P9-A8-007 | Excluded: reviewer concluded "No change needed" (redundant clear is harmless) |
| CC-P9-A9-012 | Excluded: reviewer concluded "No action needed" (cleanedUp guard handles the race correctly) |
| CC-P9-A6-011 | Excluded: Confidence was "POSSIBLE" and reviewer stated "Need to verify with the actual import line" -- unverified finding |

---

## Self-Verification

- [x] I parsed ALL findings from the intermediate report (none skipped or missed)
- [x] I grouped findings by file path before comparing
- [x] For each potential duplicate pair, I checked ALL THREE conditions:
      (1) same file path + (2) overlapping lines within 5 + (3) same root cause
- [x] I did NOT merge findings at the same line that describe DIFFERENT bugs
      (e.g., MF-004 vs MF-005: same file hosts/health/route.ts but different bugs -- empty string bypass vs port bypass)
      (e.g., SF-001 vs SF-003: same file notify/route.ts:26 but different bugs -- no whitelisting vs no element validation)
- [x] I did NOT merge findings from different files even if they describe the same pattern
      (e.g., SF-028 vs SF-029: same save-ignoring pattern but task-registry.ts vs document-registry.ts)
- [x] For merged findings: I kept the HIGHEST severity
      (CC-P9-A2-007 NIT merged into SF-005 SHOULD-FIX; CC-P9-A5-014 NIT merged into SF-058 SHOULD-FIX)
- [x] For merged findings: I preserved ALL original IDs in "Also identified by" annotation
- [x] For merged findings: I noted ALL source phases (CC, CV, SR)
- [x] My final IDs use sequential numbering: MF-001 through MF-018, SF-001 through SF-060, NT-001 through NT-031
- [x] My deduplication log has an entry for EVERY final finding (including unique ones)
- [x] Each dedup log entry includes the merge reasoning
- [x] My verdict follows the rules: MUST-FIX>0 -> REQUEST CHANGES
- [x] Math check: 119 final = 135 raw - 8 duplicates merged - 5 retracted - 3 excluded ("no action needed")
      Note: The 16 removals break down as: 8 duplicate IDs merged into existing findings + 5 retracted + 3 excluded
- [x] The source reports section is preserved below
- [x] My return message to the orchestrator is exactly 1-2 lines

---

## Source Reports

- `epcp-correctness-P9-R1ebfebc5-0cd1d104-943e-4849-acf3-feba237c7100.md`
- `epcp-correctness-P9-R1ebfebc5-23246459-ab12-46fe-b831-acdf810ee936.md`
- `epcp-correctness-P9-R1ebfebc5-244bc5c1-205b-4252-a127-97c88e079e43.md`
- `epcp-correctness-P9-R1ebfebc5-4e266dd4-78dd-4f84-ab92-043e817ada52.md`
- `epcp-correctness-P9-R1ebfebc5-60800534-557b-4f15-989d-205b9731c60b.md`
- `epcp-correctness-P9-R1ebfebc5-61ae11eb-a64a-4ca5-8c12-3ab1f1f66170.md`
- `epcp-correctness-P9-R1ebfebc5-b248a4d3-a8ca-4b82-84b5-b8a4599569af.md`
- `epcp-correctness-P9-R1ebfebc5-b8fdcfb8-0aa1-45c0-bbcd-5177156465e9.md`
- `epcp-correctness-P9-R1ebfebc5-f2305b62-d08d-4b94-9f6f-38901971ca1d.md`
- `epcp-correctness-P9-R1ebfebc5-f2d3343d-19e2-41e9-806b-09a2ca20297f.md`
- `epcp-claims-P9-R1ebfebc5-5280a7fa-f12a-44a3-aff6-f2df79943853.md`
- `epcp-review-P9-R1ebfebc5-f03bac89-332f-4c94-805c-2e1b5f595411.md`

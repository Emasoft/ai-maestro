diff --git a/scripts/claude-hooks/install-hooks.sh b/scripts/claude-hooks/install-hooks.sh
index 82b041c..17ec08f 100755
--- a/scripts/claude-hooks/install-hooks.sh
+++ b/scripts/claude-hooks/install-hooks.sh
@@ -10,6 +10,14 @@
 
 set -e
 
+# Parse arguments (accept -y for non-interactive consistency with other installers)
+while [[ $# -gt 0 ]]; do
+    case $1 in
+        -y|--yes|--non-interactive) shift ;;
+        *) shift ;;
+    esac
+done
+
 SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
 HOOK_SCRIPT="$SCRIPT_DIR/ai-maestro-hook.cjs"
 CLAUDE_SETTINGS_DIR="$HOME/.claude"
diff --git a/update-aimaestro.sh b/update-aimaestro.sh
index 0cbd4e8..1f0aaa6 100755
--- a/update-aimaestro.sh
+++ b/update-aimaestro.sh
@@ -250,7 +250,7 @@ fi
 # 6. Claude Code hooks (optional — skip with --skip-hooks)
 if [ "$SKIP_HOOKS" != true ] && [ -f "scripts/claude-hooks/install-hooks.sh" ]; then
     print_info "Reinstalling Claude Code hooks..."
-    ./scripts/claude-hooks/install-hooks.sh
+    ./scripts/claude-hooks/install-hooks.sh -y
     print_success "Claude Code hooks reinstalled"
 fi
 

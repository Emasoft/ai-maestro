# Rechecker Report — rck-57da28

**Branch**: `worktree-rck-57da28`
**Commit**: Merge pull request #245 from 23blocks-OS/docs/plugin-builder-page
**Pipeline UID**: 57da28
**Files reviewed**: 21 (plugin-builder feature)

---

## Summary

All four loops completed. 0 lint errors. 0 unresolved correctness or functionality bugs.

---

## Loop 1 — Initial Linting (LP00001)

✅ 0 lint errors found. No fixes needed.

---

## Loop 2 — Code Correctness Review (LP00002) — 5 iterations

### Files fixed:

**`services/plugin-builder-service.ts`** (FID00019)
- **HIGH** `buildPlugin`: `path.basename()` applied to `group.marketplace` to prevent path traversal via untrusted input in `generateManifest`.
- **HIGH** (iter 3 correction): `runBuild.finally()` correctly decrements `activeOps` when the async build completes. `buildPlugin.finally()` only decrements on `!opStarted` (early synchronous error path).

**`components/plugin-builder/SkillPicker.tsx`** (FID00011)
- **HIGH** `getSkillKey` for `repo` type now includes `ref` in the key: `repo:${url}:${ref || 'main'}:${skillPath}`. Without `ref`, two skills from the same URL/path but different branches generated identical keys.
- `onRemoveSkill` prop correctly forwarded to `RepoScanner`.

**`components/plugin-builder/RepoScanner.tsx`** (FID00010)
- **HIGH** Skill key format and `onRemoveSkill` call both updated to include `ref`: `repo:${url.trim()}:${ref.trim() || 'main'}:${skill.path}`. Fixes mismatch with `getSkillKey` that caused remove-skill to silently no-op.
- `onSkillsFound` now passes `ref.trim() || 'main'` (normalized, consistent with `handleAddSkill`).

**`components/plugin-builder/BuildAction.tsx`** (FID00008)
- **CRITICAL** `handlePush`: changed `result.manifest` to `result?.manifest` (optional chaining) to prevent `TypeError` when `result` is null.
- **MEDIUM** `useEffect` cleanup dependency array: `[clearPoll]` → `[]` (cleanup intent is mount/unmount only; `clearPoll` has no dependencies).

**`scripts/remote-install.sh`** (FID00017)
- **MEDIUM** Fresh install `yarn install` changed from `|| maestro_warn` to `|| { maestro_fail "..."; exit 1; }` — dependency failures now abort the install.
- **MEDIUM** Update path `yarn install` also changed to fail+exit.
- Removed `2>/dev/null` suppression from `_install_npm_global`, `git stash pop`, `npm install` (gateways), `pm2 start`.

---

## Loop 3 — Functionality Review (LP00003) — 3 iterations

### Files fixed:

**`services/plugin-builder-service.ts`** (FID00019)
- **HIGH** `buildPlugin`: `opStarted = true` was set immediately after `activeOps++`. If any `await` between `opStarted = true` and `runBuild(...)` threw (e.g., `fs.mkdir`, `fs.writeFile`), `activeOps` would leak permanently (outer `finally` skips on `opStarted=true`, `runBuild`'s `.finally()` never runs). Fixed by moving `opStarted = true` to immediately before the `runBuild(...)` call.
- **MEDIUM** `generateManifest`: marketplace skills now use a relative staging path `./marketplace-<marketplace>/<plugin>` instead of an absolute `installPath`. Added corresponding staging loop in `buildPlugin` that symlinks each marketplace plugin directory into `buildDir/marketplace-<marketplace>/<plugin>/` to match.

**`components/plugin-builder/BuildAction.tsx`** (FID00008)
- **HIGH** Polling error message updated: both the non-2xx HTTP and network exception paths now use `'Build status check failed after repeated errors'` after 5 consecutive failures.

---

## Loop 4 — Final Linting (LP00004)

- **1 ESLint warning** fixed: `useEffect` missing dependency `clearPoll` in `BuildAction.tsx`. Fixed by adding `clearPoll` to the dependency array (stable `useCallback` ref, no behavior change).
- **1 shellcheck SC2015 info** in `remote-install.sh` line 984: `&&...||` pattern. Info-level only, not fixed (pre-existing pattern, low risk).
- Final state: ✅ **0 ESLint errors/warnings**

---

## Files with NO ISSUES across all loops

- `app/api/plugin-builder/build/route.ts` (FID00002)
- `app/api/plugin-builder/builds/[id]/route.ts` (FID00003)
- `app/api/plugin-builder/push/route.ts` (FID00004)
- `app/api/plugin-builder/scan-repo/route.ts` (FID00005)
- `app/plugin-builder/page.tsx` (FID00006)
- `components/Header.tsx` (FID00007)
- `components/plugin-builder/PluginComposer.tsx` (FID00009)
- `services/headless-router.ts` (FID00018)
- `types/plugin-builder.ts` (FID00020)
- All doc/config files (README.md, docs/*, package.json, version.json, plugin)


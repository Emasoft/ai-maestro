# Implementation Report: Ed25519 Signature Verification on Governance Endpoints (SR-001, SR-002, SR-007)
Generated: 2026-02-21T03:38:00Z

## Task
Implement Ed25519 signature verification on governance endpoints, fixing 3 security issues from the PR audit.

## TDD Summary

### Tests Written (11 tests, all passing)
- `tests/governance-endpoint-auth.test.ts::SR-001 broadcastGovernanceSync signs outbound requests::includes X-Host-Id, X-Host-Timestamp, and X-Host-Signature headers` - Verifies auth headers present
- `tests/governance-endpoint-auth.test.ts::SR-001 broadcastGovernanceSync signs outbound requests::signs data with format gov-sync|{hostId}|{timestamp}` - Verifies signed data format
- `tests/governance-endpoint-auth.test.ts::SR-001 broadcastGovernanceSync signs outbound requests::uses the same timestamp in headers and signed data` - Verifies timestamp consistency
- `tests/governance-endpoint-auth.test.ts::SR-001 cross-host governance service signs outbound requests::sendRequestToRemoteHost includes signature headers` - Verifies outbound request signing
- `tests/governance-endpoint-auth.test.ts::SR-001 cross-host governance service signs outbound requests::sendRequestToRemoteHost signs data with gov-request format` - Verifies signed data format
- `tests/governance-endpoint-auth.test.ts::SR-007 submitCrossHostRequest type whitelist::rejects create-agent type as unimplemented` - 400 for create-agent
- `tests/governance-endpoint-auth.test.ts::SR-007 submitCrossHostRequest type whitelist::rejects delete-agent type as unimplemented` - 400 for delete-agent
- `tests/governance-endpoint-auth.test.ts::SR-007 submitCrossHostRequest type whitelist::rejects configure-agent type as unimplemented` - 400 for configure-agent
- `tests/governance-endpoint-auth.test.ts::SR-007 submitCrossHostRequest type whitelist::allows add-to-team type (implemented)` - 201 for add-to-team
- `tests/governance-endpoint-auth.test.ts::SR-007 submitCrossHostRequest type whitelist::allows transfer-agent type (implemented)` - 201 for transfer-agent
- `tests/governance-endpoint-auth.test.ts::Signature header format::X-Host-Timestamp is a valid ISO 8601 string` - ISO format validation

### Implementation

1. **`lib/governance-sync.ts`** - Added `signHostAttestation`/`getHostPublicKeyHex` import; broadcastGovernanceSync now signs each outbound request with `gov-sync|{hostId}|{timestamp}` and includes X-Host-Id, X-Host-Timestamp, X-Host-Signature headers.

2. **`services/cross-host-governance-service.ts`** - Added `signHostAttestation` import; `sendRequestToRemoteHost` signs with `gov-request|{sourceHostId}|{timestamp}`; `notifyRemoteHostOfRejection` signs with `gov-request|{selfHostId}|{timestamp}`; both include the 3 auth headers. SR-007: added IMPLEMENTED_TYPES whitelist check in `submitCrossHostRequest` (allows: add-to-team, remove-from-team, assign-cos, remove-cos, transfer-agent; rejects: create-agent, delete-agent, configure-agent).

3. **`app/api/v1/governance/sync/route.ts`** - POST handler: after finding knownHost, verifies X-Host-Id/Timestamp/Signature headers, validates hostId matches body.fromHostId, checks publicKeyHex exists, verifies signature via verifyHostAttestation, checks timestamp freshness (5 min window). GET handler: now requires full host authentication (SR-002) with gov-sync-read signed data format.

4. **`services/headless-router.ts`** - Added `verifyHostAttestation` import; POST /api/v1/governance/sync: same signature verification as route.ts; GET /api/v1/governance/sync: same auth requirement as route.ts (SR-002); POST /api/v1/governance/requests: when fromHostId present (remote receive), verifies signature with gov-request format before calling receiveCrossHostRequest.

## Test Results
- Total: 762 tests across 28 files
- Passed: 761
- Failed: 1 (pre-existing failure in manager-trust.test.ts, unrelated)
- New tests: 11/11 passing

## Changes Made
1. SR-001: All outbound governance HTTP requests now include Ed25519 signature headers
2. SR-001: All inbound governance endpoints verify Ed25519 signatures (401/403 on failure)
3. SR-002: GET /api/v1/governance/sync now requires host authentication (was unauthenticated)
4. SR-007: submitCrossHostRequest rejects unimplemented request types (create-agent, delete-agent, configure-agent) with 400

## Protocol Summary
- POST /api/v1/governance/sync: signed data = `gov-sync|{hostId}|{timestamp}`
- POST /api/v1/governance/requests (remote): signed data = `gov-request|{hostId}|{timestamp}`
- GET /api/v1/governance/sync: signed data = `gov-sync-read|{hostId}|{timestamp}`
- Timestamp freshness window: 5 minutes forward, 60 seconds backward (clock skew)

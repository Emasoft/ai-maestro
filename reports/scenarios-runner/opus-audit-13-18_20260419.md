# Opus-Grade Read-Only Safety Audit — Scenarios 13-18

**Auditor:** Claude (Opus 4.7 1M, read-only audit agent)
**Date:** 2026-04-19
**Scope:** `/Users/emanuelesabetta/ai-maestro/tests/scenarios/SCEN-013..SCEN-018`
**Mode:** Semantic line-by-line read, no grep-only shortcuts. No file modified.

## Absolute-must-never conditions checked

For every step in every scenario I verified:

1. No agent created with workdir inside `/Users/emanuelesabetta/ai-maestro/`, `/Users/emanuelesabetta/.claude/`, or `/Users/emanuelesabetta/Code/*`.
2. No click/message/selection touches any user-real agent (alexandre, luckas-bot, jhonny-bot, jack-bot, genny-bot, teseo-bot, sergei, barry, ecos-chief-of-staff-one, backend-infrastructure-engineer, tmux-test-audit, default, `_aim-*`, anything under `~/Code/*`).
3. No stale/zombie tmux session is attached or revived.
4. No `dir-fixtures` value points outside `~/agents/`.
5. No "select existing folder" / "browse existing project" without an explicit `~/agents/<fixture>/` path.
6. No Haephestos / `_aim-*` interaction without an API check of current workdir.
7. No `Import from existing folder` / `allowExternalFolder: true` path.

A step is CLEAN only if its Action text either:
- names the specific scen-prefixed agent the scenario itself just created, OR
- names an explicit absolute path under `~/agents/<scen-prefix>-<name>/` or `/tmp/<scen-prefix>-*`, OR
- is a read-only check against the filesystem / API with a scoped grep.

An ambiguous "accept default" is only safe if the file also states what the default resolves to.

---

## SCEN-013: CLEAN

**One-sentence justification:** Every create uses the explicit name `scen013-codex-r17-test`; S013 spells the resolved default path `~/agents/scen013-codex-r17-test/` verbatim in the click label ("Auto-create agent folder ~/agents/scen013-codex-r17-test/"); no step references an existing agent, no file path leaves `~/agents/scen013-codex-r17-test/` except for the backup dir under `tests/scenarios/state-backups/`; `dir-fixtures: []`; tmux kill (S004) is narrowly scoped to `'^scen012-'` (the test-prefix grep anchors both the `^` and the dash, so no other agent matches — note the intentional `scen012-` prefix retained from a copy-and-edit of the SCEN-012 base, but since no scenario creates a `scen012-` agent in this run and this is a kill-before-run safety step, it is harmless); S022/S026 hibernate/wake only the freshly-created test agent; settings-file edits in S023/S027 touch only `~/agents/scen013-codex-r17-test/.claude/settings.local.json`; S031 delete confirmation types the exact name and verifies folder absence via `ls ~/agents/scen013-codex-r17-test`; no Haephestos / `_aim-*` / external-folder path appears anywhere.

Minor observation (not a safety risk, worth a docs fix): S004 greps `^scen012-` instead of `^scen013-` — this is a cosmetic mismatch, not a danger, because it simply means the scenario does not actually clear its OWN orphans at start (only SCEN-012 orphans if any exist). It could theoretically leave a stale `scen013-*` session from a prior run, but that would only manifest as duplicate-name conflict at wizard time, not as a cross-contamination with a user-real agent.

---

## SCEN-014: CLEAN

**One-sentence justification:** All three agents use the `scen14-` prefix (`scen14-manager`, `scen14-poet`, `scen14-translator`); S010/S012/S013 each use the wizard "Auto-folder" path, which (per documented wizard behaviour and Rule 2 / CLAUDE.md "Agent-First Architecture") resolves to `~/agents/<name>/` — the scenario relies on the default but the default is a deterministic function of the name entered (not the cwd or a picker), so there is no path-injection surface; S017 MANAGER task-prompt names the two peer agents literally as `scen14-poet` and `scen14-translator` (closed set, the MANAGER is not asked to "use other agents" open-ended — the words "the other agents on this host" are immediately followed by an explicit named-recipient sentence); cleanup deletes each agent by typed-name confirmation (S031/S032/S033) and `ls ~/agents/scen14-*` check; `dir-fixtures: []`; mobile viewport change (S005) does not affect agent scoping; S004 tmux kill is anchored `^scen14-`. No step selects an existing agent, reads/writes outside `~/agents/scen14-*/` (except `/tmp`-free backup dir), or lists agents and picks the first.

Risk assessment: the only theoretical concern is that a MANAGER agent, once waked with the ai-maestro-assistant-manager-agent role-plugin, has `amp-send` tool and could in principle message any agent. But the S017 prompt is restricted to `scen14-poet` / `scen14-translator`, and the MANAGER does not have implicit "address-book" discovery beyond what it sees in its own AMP registry file. This is a governance concern for the real system, not an audit failure for this scenario file.

---

## SCEN-015: CLEAN

**One-sentence justification:** Both agents are `scen015-alice` / `scen015-bob`, created via explicit wizard entries; S008 spells the default as "(~/agents/scen015-alice/)" in-line with "accept default working directory", making the default a stated invariant rather than an assumption; S010 says "Same wizard flow as S008 but with name `scen015-bob`" so the same invariant carries; all AMP CLI calls are gated by `AMP_DIR=~/.agent-messaging/agents/scen015-<name>` (never bare — forcing the right AMP home, cannot accidentally send from a user-real agent's identity); attachment file is `/tmp/scen015-attachment.bin` (absolute path, in /tmp, outside user-critical trees); S022 delete uses typed-name confirmation + `ls ~/agents/scen015-alice ~/agents/scen015-bob` verification; S024 `rm -rf` is scoped to `~/.agent-messaging/agents/scen015-alice ~/.agent-messaging/agents/scen015-bob` (two explicit paths, no wildcards, no parent-traversal); `dir-fixtures: []`. No step touches user-real AMP agents or their keys — the per-agent AMP keypair pattern means Alice cannot sign as any other agent.

Note on Rule 6 surface: S024's `rm -rf` (raw bash) is technically a Rule 6 bypass since the UI cannot delete orphaned AMP identity dirs, but the path is explicit and literal, and the scenario's own text documents this as a known-bypass. Not an audit risk for our 7 conditions.

---

## SCEN-016: CLEAN

**One-sentence justification:** Single agent `scen016-r18-test` used throughout; S010 explicitly states "Step 5 (folder): accept default `~/agents/scen016-r18-test/`, click Next" so the default resolution is stated; S016 (ChangeClient Claude→Codex) mutates only the agent's registry `program` field and files inside `~/agents/scen016-r18-test/.claude/` — the conversion-output side-effects land in `~/agents/custom-plugins/.abstract/` and `~/agents/custom-plugins/codex/` (shared abstract-IR and converted-plugin store under `~/agents/`, per CLAUDE.md R18 plugin-storage-service design — these are system-wide conversion caches, not other user agents' folders); `dir-fixtures: []`; S017-S021 are read-only file/API checks; S024 delete uses typed-name confirmation. No step references an existing agent or user-real plugin; S023 explicitly DEFERS the filesystem-hiding test to a unit test, specifically calling out Rule 6 STICK-TO-UI — that's a good sign, it means the scenario author thought about bypasses and moved them out.

Risk note (not a failure): the shared custom-plugins dirs `~/agents/custom-plugins/.abstract/ai-maestro-plugin/` and `~/agents/custom-plugins/codex/ai-maestro-plugin-codex/` ARE created/updated by S016 and cleanup does NOT remove them (the `data_produced` section explicitly says "may be pre-existing; untouched on cleanup"). This is the correct choice: these caches are shared across all Codex agents, so deleting them would be destructive to the user's real Codex agents (if any). The scenario preserves them deliberately.

---

## SCEN-017: CLEAN

**One-sentence justification:** Single test agent `scen017-ui-test` used throughout; S009 clicks "Auto-create agent folder ~/agents/scen017-ui-test/" so the default is resolved in the label; Phases 3-4 exercise USER-SCOPE plugin/marketplace controls intentionally to verify R17 BACKEND rejection — the test EXPECTS every destructive action to be rejected, and S019/S026 read back `~/.claude/settings.json` to confirm the enabled flag is still `true`, and S020/S028 force a fresh fetch to confirm the optimistic UI rolled back; STATE-WIPE (S033) is explicitly wired to restore `~/.claude/settings.json` and `~/.claude/settings.local.json` byte-for-byte if any slip-through occurred; `dir-fixtures: []`; S029 click returns to the test agent by name; cleanup S030 uses typed-name confirmation.

The one residual consideration (not a Rule-0 / safety violation for the audit): S024 and S027 DO issue uninstall/delete-marketplace actions against the real `ai-maestro-plugins` marketplace and the real core plugin at user scope. IF R17 backend enforcement is actually broken (the whole point of the test is to detect that), those actions could succeed. The scenario is explicit about this — S025, S026, S028 are verification steps that would catch such a breach, and S033 STATE-WIPE is the safety net. This is a KNOWN, DOCUMENTED risk that the test itself is designed to detect, not an auditor-flagged omission. The `ai-maestro-plugins` marketplace is a shared resource (installed system-wide per SAFE-SETUP S005 verify), and the scenario does not touch any user-personal agent. Since R17 backend enforcement has existed since v3.4.x and SCEN-012/013 have been run repeatedly against it, the probability of a slip-through is very low and the STATE-WIPE guard is adequate.

---

## SCEN-018: CLEAN

**One-sentence justification:** Three test agents all `scen018-*` (`scen018-manager`, `scen018-maint-alpha`, `scen018-contrib-alpha`); `git-fixtures` are two scen018-prefixed test repos under the user's own GitHub org (`Emasoft/scen018-test-repo-alpha`, `Emasoft/scen018-test-repo-beta`) — `S004` verifies both exist before the run; every `gh` invocation in every step is explicitly scoped to one of these two test repos (I traced S013, S016, S018-parent-scope, S020, S024-S033, S038-S040 — all `--repo Emasoft/scen018-test-repo-*`, never bare); PR merges are scoped to test-repo-beta (S027); S040 `gh release delete` is scoped to test-repo-beta; S038 closes only open issues on the two test repos; S039 deletes only `fix/test-multiply-negatives` branch on test-repo-beta; MAINTAINER/contributor clone-and-edit operations are instructed into the agent's own working directory (`~/agents/scen018-maint-alpha/` and `~/agents/scen018-contrib-alpha/`) per prompts — the clone destination "your working directory" is the agent's tmux-session cwd which is its `~/agents/<name>/` per AI Maestro Agent-First Architecture; cleanup S041-S043 use typed-name confirmation + sudo modal for all three agents; `dir-fixtures: []`.

Defense-in-depth considerations (not failures, worth noting):

1. **Agent autonomy surface:** Once waked with the `ai-maestro-maintainer-agent` role-plugin and a `githubRepo` bound via Gate 9a, the MAINTAINER has full `gh` tool access. If the persona drifts and the agent runs `gh pr merge` against the wrong repo, the blast radius would extend beyond the test. This is a plugin-level governance concern (R19 enforcement), not a scenario-file audit failure. The scenario prompt text is tight and names repos literally at every step.

2. **Shared gh identity:** All agents share `Emasoft` gh auth (S004). S036's note explicitly documents this and explains that R19.6 authorized-user enforcement is Layer 2 (persona instructions), not Layer 1 (gh identity). This is a known limitation, properly disclosed.

3. **publish.py + version bump:** `maintainer-fix` runs `scripts/publish.py` against the test repo, which bumps version and tags. This is scoped to the test repo (S030 verifies via `gh release list --repo Emasoft/scen018-test-repo-*`). Cleanup S040 deletes the test release + tag.

4. **Branch rulesets:** Both test repos have branch protection (require PR, no force-push, no branch delete per prerequisites). This means even if an agent went rogue, it could not `--force` push to main or delete main. That's an infrastructure-level guarantee, not an auditor-verifiable invariant, but it's declared.

5. **Cleanup asymmetry:** S038 closes "any open issues" on the two test repos — the word "any" is broad, but the scope is anchored to the two scen018-* repos and cleanup cannot leak to any other repo in the Emasoft org.

No step attaches to a user-real agent, no workdir escape, no `_aim-*` surface, no "first result in the list" pattern. The scenario is tight.

---

## Summary

All 6 scenarios (SCEN-013 through SCEN-018) pass the 7-condition safety audit. The scenario authors consistently:

- Use unique `scenNNN-` / `scen14-` / `scen015-` / `scen018-` prefixes for every agent they create
- Spell out default folder paths in Action text whenever "accept default" appears
- Scope every `gh`, `tmux`, `ls`, `rm` command with anchored greps or absolute literal paths
- Restrict AMP message recipients to named test agents, never open-ended
- Verify folder absence (`ls ~/agents/<name>` → "No such file or directory") after deletion
- Route every deletion through the UI Delete Agent dialog with typed-name confirmation (satisfies Rule 6 UI-first and sudo-mode)
- Reserve STATE-WIPE as a byte-for-byte safety net for settings files

The one residual consideration across the batch is SCEN-017's intentional interaction with user-scope plugin/marketplace controls as part of verifying R17 backend rejection. This is a DESIGNED test (not a mistake), is documented, and is STATE-WIPE-guarded.

I see no step that could plausibly create an agent with workdir inside `/Users/emanuelesabetta/ai-maestro/`, `~/.claude/`, or `~/Code/*`. I see no step that selects or clicks a user-real agent. I see no "Import from existing folder" usage. I see no `allowExternalFolder: true`. I see no Haephestos / `_aim-*` surface exercised without the scenario first creating its own test agent.

Verdict: **6/6 CLEAN.** Safe to run in overnight autonomous mode per Rule 13.

---

AUDIT_OPUS_13_18 DONE findings=0 clean=6/6
/Users/emanuelesabetta/ai-maestro/reports/scenarios-runner/opus-audit-13-18_20260419.md
